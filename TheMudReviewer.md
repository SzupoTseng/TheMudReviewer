# 《泥巴考古學》
### ——從一個 APK 到三十七年的驅動血脈 · The MUD Reviewer

![泥巴考古學 — 封面海報](TheMudReviewer.png)

> 全書合併版。本書為教育與研究用途：把一套沒有規格書的 MUD 協議還原出來、再據以重寫一個客戶端，並完整記錄七次交付了不能用的東西。客戶端為淨室實作，未複製任何原始碼或資源。


---

## 目錄

- 序言
- 導讀 · 證據等級的判讀學

**第一篇　逆向工程**

- 逆向 · 從一個APK開始（001–003）

**第二篇　協議解剖**

- 協議 · 三層結構（004–005）
- 協議 · 樣式方言與握手（006–008）

**第三篇　方言與相容性**

- 方言 · 三個世代與撞號（009–011）

**第四篇　客戶端架構**

- 架構 · 分層與雙形態（012–014）

**第五篇　工程實務**

- 工程 · 七次事故（015–017）

**第六篇　實作規格**

- 規格 · 模組介面（018–020）
- 規格 · 狀態與事件（021–023）
- 規格 · UI元件與偏好（024–025）
- 規格 · DOM與後端（026–028）

**第七篇　交付**

- 交付 · 體積與WebAssembly（029–032）

**第八篇　源流：連線的另一端是什麼**

- 源流 · 從LPMud到FluffOS（033–035）
- 驅動 · FluffOS的內部構造（036–039）
- 移植 · 同一個分頁裡的driver（040–042）
- 接縫 · LPC那一端怎麼生出協議（043–046）

**第九篇　引擎、mudlib 與客戶端：三層各自的架構**

- 引擎 · MudOS帶來的能力面（047–049）
- mudlib · 骨架與機制的架構（050–053）
- 客戶端 · MUD客戶端與擴充協議（054–057）

**第十篇　語言、安全與編譯：把 LPC 這一層講完**

- 語言 · LPC的型別安全與編譯（058–062）
- 分歧 · 五支driver的技術對照（063–065）

**第十一篇　深入各家設計：四支 driver 的解剖**

- 深入 · 各家設計的解剖（066–070）

**第十二篇　英文線：MudOS 的社群史**

- 英文線 · MudOS的社群史（071–074）

**第十三篇　周邊：工具鏈、標準庫與參考實作**

- 周邊 · 工具鏈標準庫與參考實作（075–078）

**第十四篇　地層學：ZJMUD 協議自己的歷史**

- 地層 · ZJMUD協議自己的歷史（079–082）
- 附錄 A　opcode 總索引
- 附錄 B　檔案地圖與接手指南
- 附錄 C　1:1 還原檢查表
- 附錄 D　MUD 家族年表：1978 – 2026
- 附錄 E　FluffOS / LPC 技術速查
- 附錄 F　17 份 mudlib：血緣、落差與實測數據
- 附錄 G　全書單元索引 ＋ 引用結構
- 附錄 H　ZJMUD 協議規格與客戶端系統分析
- 附錄 I　外部文獻與交叉佐證


---



## 序　當一份協議只剩下它的影子

二〇二二年十月，一個 1.1 MB 的 Android 安裝檔被上傳到某個開源倉庫，沒有文件、沒有註解、沒有一行說明。

四年後，它成了唯一一份還能證明「這個協議長什麼樣」的證據。

寫這本書的起點，就是這麼一個問題：**當一套協議的規格書已經不存在，你要怎麼把它重新寫回來？**

不是猜。不是「看起來像就實作」。是拿出證據，把每一個位元組的意義釘死在紙上，讓後來的人可以照著它，從零寫出一個能真正跑起來的客戶端。

這本書記錄的，就是那段把影子還原成骨架的過程——以及**還原之後，用它重寫一個客戶端時，撞上的每一堵牆**。

> 💡 全書的核心命題
> **逆向工程的盡頭，不是「我看懂了」，而是「我能證明它，而且我的實作經得起真機打臉」。**

### 這本書在講什麼

一個名為 **ZJMUD** 的中文文字遊戲協議，和一個從零重寫的客戶端。

原本的客戶端是 2013 年代的 Android App。原本的伺服器是一套 LPC 寫的 FluffOS mudlib。兩者之間跑著一套**沒有任何公開規格**的私有協議——它不是純 telnet，而是在 UTF-8 逐行串流上疊了一層 `ESC` + 三碼的行級 opcode，行內再用一套 ANSI 的擴充方言做樣式。

我們要做的事很單純：**把協議還原出來，然後不碰原始碼、重寫一個 Web 客戶端**。

「不碰原始碼」不是潔癖，是必要條件。原專案明載「開源代碼嚴禁出售」，唯一乾淨的路徑是**淨室（clean-room）重寫**——只依據對外部行為的觀察整理出互通性規格，再據此獨立實作。協議格式本身是互通所必需的事實資訊，這條界線必須畫得很清楚。

### 三個面向：你在讀哪一種「還原」

這本書的每一章，都可以從三個角度來讀。它們是三種不同的功夫，混在一起會出事：

| 面向 | 符號 | 它回答的問題 | 典型章節 |
|------|------|------------|---------|
| **逆向** | 🔬 | 這個位元組到底是什麼意思？我憑什麼這樣說？ | 第一篇、第二篇 |
| **架構** | 🏛 | 知道了協議，要怎麼把它變成一個不會腐爛的程式？ | 第四篇 |
| **工程** | 🔧 | 為什麼測試全綠、卻在真機上一按就死？ | 第五篇 |

搞混這三者，是這個專案吃過最大的虧：**協議層 100% 正確、Rust 編譯零錯誤、九十八條測試全綠，交到使用者手上，按下「連線」毫無反應**——連續四次。

那四次的完整解剖，在第五篇。它們是這本書最有價值的部分。

### 證據等級：這本書最重要的一條規矩

書裡每一個關於協議的斷言，都標了它的**證據等級**。這不是形式，是這個專案能收斂的唯一原因：

| 等級 | 意義 | 可信度 |
|------|------|--------|
| 🟢 **實機封包** | 真的連上伺服器抓到的位元組 | 最高。與它衝突的一律作廢 |
| 🟡 **伺服器原始碼** | LPC 的 `#define` 與實際發送點 | 高。但「定義了」不等於「用得到」 |
| 🟠 **客戶端反編譯** | 從 APK 還原出的 Java | 中。它只證明客戶端「會怎麼解讀」 |
| 🔴 **推論** | 由結構相似性推得 | 最低。必須標註，且優先安排驗證 |

**衝突時的順序永遠是：實機封包 > 伺服器原始碼 > 客戶端反編譯 > 推論。**

這條規矩救過命。有兩處我照著客戶端反編譯寫得很篤定，後來被伺服器原始碼直接推翻；還有一處三方都同意、卻在真機上發現多了六組 telnet 協商位元組——那六組差點讓整個連線在第一個位元組就死掉。

### 一點立場說明

書裡所有的「老手視角」，都出自一個**中性的資深工程師**角度——不綁定任何公司、任何職級。我們在乎的不是誰說了什麼，而是**一個踩過夠多坑的人，會在哪裡先停下來看一眼**。

也不美化過程。這個專案有七次事故，每一次都寫進書裡，包括那次「我把編譯成功當成能用，直接丟給使用者當測試員」的判斷失誤。**逆向工程的書如果只寫成功的推導，那是小說不是筆記。**

### 後來多出來的那一半

本書原本寫到第七篇就結束了——協議還原完、客戶端重寫完、交付出去，故事該收了。

**是「伺服器搬進瀏覽器分頁」這件事把它拉長的。**

FluffOS 發了官方的 WebAssembly build，於是整台 driver 可以跑在網頁裡。本專案接上它、收了 17 份 mudlib，然後發現一件事：**要讓一份 1990 年代的封存在 2026 年的引擎上跑起來，光懂協議完全不夠**。你得知道那台引擎是什麼、那份 mudlib 是怎麼組起來的、以及它們各自對彼此做了什麼假設。

所以有了後面七篇。它們一層一層往下：

| 篇 | 問題 |
|---|------|
| **第八篇** | **連線的另一端是什麼？**——LPMud 的 driver／mudlib 分家、MudOS 的分支、FluffOS 的相容性承諾、driver 的內部構造與 WASM 移植 |
| **第九篇** | **引擎／mudlib／客戶端各自把知識放在哪一層？**——能力面、mudlib 骨架與遊戲機制、客戶端三十年沒變的架構與四代擴充協議 |
| **第十篇** | **這個設計是本質，還是某人在 1992 年的一個決定？**——LPC 的型別／修飾詞／繼承／安全模型，以及五支 driver 的技術對照 |
| **第十一篇** | **driver 該保留多少決定權？**——LDMud、DGD、CD 各自的解剖與四種擴充點的綜合 |
| **第十二篇** | **我們用的這台 driver，中間經過了哪些人的手？**——TMI、三份 mudlib 的競賽、MudOS 的三次死而復生、FluffOS 的建國宣言 |
| **第十三篇** | **有人在做同一件事，規模大十倍——我的結論站得住嗎？**——工具鏈、標準庫，以及與官方參考站的逐項對照 |
| **第十四篇** | **沒有版本歷史的東西，怎麼斷代？**——把前面的方法用回 ZJMUD 協議自己身上 |

這七篇和前七篇的關係，是**同一個問題的另一半**：前七篇問「這個位元組是什麼意思」，後七篇問「**為什麼它會長成這個樣子**」。而答案幾乎都不在協議裡，在它上游的那三十年。

其中有兩段是本書最意外的收穫：

- 本專案用檔名集合量出 17 份封存的血緣圖，**做完之後才去查史料**，結果量測、公開記載、與檔案裡三十三年沒改過的作者署名，三條線指向同一張圖（附錄 D.3）。
- 而第十四篇回頭挖 ZJMUD 自己時發現：**三個世代、三種握手格式、116 個 opcode、四個撞號——版本號從頭到尾都是 `1.0`。**

**逆向工程偶爾會給你這種東西——當你量得夠仔細，歷史會自己浮出來。**

> 全書共 **14 篇、82 個單元、7 個附錄**。
> 想先看地圖的，翻**附錄 G**（一頁列完所有單元，外加本書自己的引用結構量測）。

### 最後

技術會過時，這套協議可能三年後就沒人用了。但這本書真正想留下的，不是一份會過期的 opcode 表，而是一種**面對黑箱時，怎麼把猜測一步步逼成證據的方法**。

翻過這一頁，我們從那個 1.1 MB 的安裝檔開始——**以及為什麼另外兩個 4.5 MB 的版本，看一眼就該放棄。**

---



## 導讀　三個面向的判讀學

在翻開任何一章之前，先花五分鐘搞清楚一件事：**你手上的每一條「協議知識」，可信度都不一樣。**

這是逆向工程最容易被跳過、也最致命的一課。多數人失敗不是因為看不懂組合語言，而是因為**把「我推論出來的」和「我驗證過的」混在同一張表裡**，然後在三個月後的某個深夜，被自己三個月前的樂觀judge打臉。

---

### 一、三個面向，三種功夫

| 面向 | 符號 | 核心問題 | 失敗的樣子 |
|------|------|---------|-----------|
| **逆向** | 🔬 | 這個位元組憑什麼是這個意思？ | 「看起來像」就寫進規格，半年後全盤崩潰 |
| **架構** | 🏛 | 怎麼讓協議知識不汙染 UI？ | 解析器直接操作畫面，改一行協議要動十個檔案 |
| **工程** | 🔧 | 怎麼證明它真的能跑？ | 測試全綠、真機全掛 |

三者的難度曲線完全不同。**逆向的難點在「證據」，架構的難點在「邊界」，工程的難點在「你以為你測了」。**

這個專案在逆向上做得還行，在架構上算乾淨，但在工程上摔得最重——連續四次把「不能用」的東西交出去。原因後面會講到。

---

### 二、證據等級：本書的度量衡

每一條協議斷言，都必須能回答一個問題：**你怎麼知道？**

#### 🟢 實機封包（最高）

真的建立連線、真的抓到位元組。這是唯一能推翻其他所有來源的證據。

```
ff fd 18 ff fd 1f ff fd 27 ff fb 56 ff fb 46 ff fb 2a
0d 0a 76 65 72 31 2e 30 3a ...
```

這 42 個位元組是本專案最貴的一份證據——它一口氣推翻了「這協議沒有 telnet 協商」這個寫在文件裡的斷言，也解釋了為什麼一個用嚴格 UTF-8 解碼的讀取器會在連線的第一毫秒就死掉。

**它的限制**：你只能看到伺服器「這次」送了什麼。沒觸發的功能，你看不到。

#### 🟡 伺服器原始碼（高）

LPC 的 `#define` 與 `.c` 裡的實際發送點。

它比反編譯強，因為它是**作者親手寫的意圖**，而且常常帶中文註解。但有一個巨大的陷阱：

> **「定義了」不等於「用得到」。**

本專案掃過的一份 header 定義了 **102 個 opcode**，實際在 `.c` 裡被使用的只有 **16 個**——其餘 86 個是從別處複製過來的，從未出現在線路上。如果只看 header 就開工，會為了七十幾個永遠不會出現的 opcode 寫一堆處理器。

**判讀規則**：看 header 知道「可能有什麼」，看 `.c` 才知道「真的有什麼」。

#### 🟠 客戶端反編譯（中）

從 APK 還原出的 Java。

它證明的是**「客戶端會怎麼解讀」**，不是「伺服器會怎麼送」。這兩件事有時不一致——本專案就抓到一段客戶端裡的 `ESC006` 分支寫了兩次，第二次因為 if/else 鏈的順序**永遠不可能被執行**。那是一段死碼，照著它實作只會浪費時間。

**它的獨門價值**：反編譯能看到伺服器原始碼看不到的東西——**客戶端自己的行為**。例如「收到某個字串就自動回送 `l`」這種客戶端主動邏輯，只有反編譯看得到。

#### 🔴 推論（最低，但不可避免）

由結構相似性推得。例如「這個 payload 長得像那個已知格式，所以應該也是同一套解析」。

**推論不是罪，隱藏推論才是。** 本書所有推論都明確標註，並且優先安排驗證。第二篇有兩處被標為「未驗證」的欄位，後來一處被伺服器原始碼證實、一處被證偽——證偽的那次，如果沒標，就是一個潛伏半年的 bug。

---

### 二之二、後來擴充的三種來源（第八、九篇）

前四級是為了**還原一套私有協議**而定的。第八、九篇把鏡頭轉向伺服器那一端（driver、mudlib、客戶端史），出現了三種原本的分級涵蓋不到的來源。它們的位階如下：

#### 🟡 上游官方文件與原始碼

FluffOS 的 `docs/`、`src/`。它與「伺服器原始碼」同級（🟡），理由一樣——**它是作者親手寫的意圖**，而且同一個陷阱也適用：

> **文件寫了不等於這台 driver 編了。**

§048 整節在講這件事：能力面是**編譯期**決定的，所以「官方文件說有這個 efun」與「你手上這台有」是兩回事。**對照 header 與 `.c` 的規矩，在這裡變成對照文件與套件矩陣。**

#### 🟡 一手史料（同時代的原始文獻）

`Credits.LPmud`（1989）、`History.MudOS`（1992 年的 Usenet 貼文）、TMI-2 的檔頭署名。

它們的可信度來自一個性質：**寫的時候還不知道後來會怎樣**。1992 年那篇貼文不是回憶錄，是當事人在罵戰中途寫的辯護文——**它有立場，但沒有事後諸葛的空間**。

**判讀規則**：一手史料證明「當時有人這樣說」，不證明「事實如此」。所以 §034 引它時，引的是**可查證的技術細節**（版號怎麼爛掉、哪些子系統重寫了），而不是它對動機的自述。

#### 🟢 可重跑的量測

§050 的 Jaccard 血緣矩陣、附錄 F 的開機測試、§055 的實測位元組。

它與「實機封包」同級（🟢），因為它有實機封包唯一重要的那個性質：**別人可以重跑，並得到相同結果**。附錄 F 因此附上了每一張表的重跑指令。

**但它有一個實機封包沒有的弱點**：量測有**方法**，而方法有偏差。所以本書的規矩是——

> **每一張量測表都必須寫明「它會漏掉什麼」。**

附錄 F 的 opcode 分佈表附了兩條已知偏差；§050 的血緣量測寫明它量的是路徑而非內容；§005 的 opcode 頻率統計在 §052 發現漏算之後，補上了那條說明。**一個標了偏差的量測是證據，沒標的是宣傳。**

#### 擴充後的完整順序

```
🟢 實機封包 ≈ 🟢 可重跑量測（需附方法與偏差）
      >  🟡 伺服器原始碼 ≈ 🟡 上游文件 ≈ 🟡 一手史料
            >  🟠 客戶端反編譯  >  🔴 推論
```

**同級之間衝突怎麼辦？** 本書遇過一次，處理方式記在附錄 D.3：史料說「俠客行 1996 年外流導致大量分支」，量測看到「16 份 lib 彼此 0.44–0.90」，檔頭寫著 `Creator : ken@XAJH`——**三條獨立的線指向同一張圖，才算釘死**。如果只有其中一條，本書會標成推論。

---

### 三、每一節的固定骨架

為了讓不同性質的主題可以被橫向比較，本書每個「單元」都用同一套結構：

> **標籤 → 證據等級 → 起源 → 技術核心 → 解決的痛點 → 踩過的坑 → 優點／罩門 → 對照表 → 效益**，最後收在一句「君之一席話」與一段「🔍 老手視角」。

其中兩欄是本書的偏心：

- **證據等級**不是裝飾。它讓你在三個月後回頭看時，知道哪些結論可以直接信、哪些要重驗。
- **踩過的坑**寫的是**真的發生過的事**，包含當下的錯誤判斷。不美化。

---

### 四、給不同讀者的路徑

**想學逆向的**：第一篇 → 第二篇 → 附錄 A。重點在「怎麼把猜測逼成證據」。

**要接手這個專案的**：導讀 → 第四篇 → 第五篇 → 附錄 B。重點在架構邊界與已知的坑。

**要寫自己的 MUD 客戶端的**：第二篇 → 第三篇。那是完整的協議規格。

**只想看教訓的**：直接翻第五篇。七次事故，七種完全不同的死法。

---

### 五、關於數字

書裡所有的行數、opcode 數量、測試數量，都是**撰寫當下實際數出來的**，不是估計：

| 項目 | 數量 |
|------|------|
| 產品程式碼 | 約 6,613 行（前端 JS 4,428、Rust 455、橋接 191、HTML/CSS 1,370、WASM 綁定 524） |
| 測試程式碼 | 約 2,756 行 |
| 測試數量 | **202 條**（12 個檔案，全過） |
| 核心 opcode | 27 個（14 個 mudlib 共用） |
| 擴充 opcode | 89 個（跨三個方言，其中 4 個撞號） |
| 實機驗證的封包 | 53 行 |

**測試程式碼佔產品程式碼的 42%**——這個比例不是刻意追求的，是被七次事故逼出來的。

> 數字會長大：本書初稿時是 4,977 行產品碼／177 條測試，第八、九篇寫成時已是 6,613 行／202 條。
> **書裡所有數字都標了「撰寫當下」，並且附上重數的方法**（附錄 B、附錄 F.6）——
> 一個數字如果沒有辦法重新量一次，它遲早會變成謊言。

> 💡 君之一席話
> **在黑箱面前，最危險的不是無知，而是「我覺得我知道」——證據等級這件事，本質上是替未來的自己標好哪些話不能信。**

> 🔍 老手視角──真正的門道
> 資深工程師看逆向專案，第一眼不看「解出了多少」，而看**「有沒有可重跑的驗證管道」**。一個能一鍵連上真機、抓封包、跑過解析器並報告失敗數的探針，價值遠高於一份漂亮但靜態的規格書——因為協議會變、伺服器會改版，而規格書不會自己告訴你它過期了。本專案最終把 53 行實機封包固化成回歸測試，正是為了讓「協議變了」這件事在 CI 就爆出來，而不是在使用者面前。可落地的做法：任何逆向專案，**第一天就先寫探針，再寫規格**——先有量尺，再談測量。

---



# 第一篇　逆向工程

## 001　選對標的 — 三個 APK 裡，只有一個值得拆

**標籤**：`#逆向` `#APK` `#multidex` `#選型` `#成本控制`
**證據等級**：🟢 實機（檔案本身）
**工具**：apktool 2.7.0、jadx 1.5.1

**起源**：手上有三個安裝檔，檔名看不出差別：`UTF-8版客户端_sign.apk`（1.1 MB）、`newzjclient.apk`（4.3 MB）、`zjmudclient.apk`（4.5 MB）。直覺會想「拆最大的，功能最完整」。這個直覺會讓你多花一整天。

**技術核心**：判斷標的優劣，不需要先反編譯，**看 dex 結構就夠了**。

```
UTF-8版客户端_sign.apk   classes.dex 195 KB          132 個檔案
newzjclient.apk          classes.dex ~ classes26.dex  277 個檔案
zjmudclient.apk          classes.dex ~ classes26.dex  294 個檔案
```

單一 dex 對上 **26+ 個 multidex**。Android 的 dex 有 65,536 個方法引用的上限，一旦超過就得拆成 multidex。一個純粹的 MUD 文字客戶端**不可能**寫到需要 26 個 dex——那些額外的體積幾乎必然是廣告 SDK、推播、統計、熱更新框架，或是加固殼。

拆開後印證了這個判斷：那份 1.1 MB 的版本只有 **130 個 smali 類別**，全部在 `com.zwyouto.*` 一個命名空間下，四個 Activity 加一個 Application 類。**沒有一行是無關的。**

**解決的痛點**：逆向的成本幾乎全花在「讀懂無關的東西」。選錯標的，你會在廣告 SDK 的混淆代碼裡耗掉兩天，才發現真正的協議邏輯只有三百行。

**踩過的坑**：本地備好的 jadx 是 **GUI 專用的 exe**，沒有 CLI，無法批次反編譯。改從 GitHub 抓 CLI 版時，第一次下載在 94 MB 處逾時中斷——因為預期它「應該只有 40 MB」，一度懷疑抓錯檔案。實際用 `curl -sIL` 查 `content-length` 才確認：**這個版本本來就是 116 MB**。

> 憑感覺判斷「檔案應該多大」，比不上一行 HEAD 請求。

**優點 / 罩門**：apktool 給出可讀的資源與 layout XML，是 UI 規格的最佳來源；jadx 給出接近原始碼的 Java，是邏輯的最佳來源。**兩個都要用，缺一不可**——apktool 不還原邏輯，jadx 不還原資源。罩門是兩者都會在混淆過的程式碼上失效，而這正是為什麼「選一個沒混淆的標的」如此關鍵。

**工具對照**：

| 對手 | 定位 | 相對優勢 | 相對劣勢 |
|------|------|---------|---------|
| apktool | 資源與 smali 還原 | layout/values/manifest 完整可讀，是 UI 規格聖經 | 只給 smali，邏輯難讀 |
| jadx | dex → Java 反編譯 | 輸出接近原始碼，控制流清楚 | 不還原資源；遇混淆會產生大量 `a.b.c` |
| Ghidra | 通用逆向平台 | 支援 DEX/Dalvik，能做交叉引用分析 | 對純 Java 專案殺雞用牛刀 |

**效益**：正確的標的選擇，讓整個協議還原工作從「預估兩三天」壓縮到半天。

> 💡 君之一席話
> **逆向工程的第一個技術決策，發生在你打開反編譯器之前——選錯標的，之後每一分鐘都在替別人的廣告 SDK 加班。**

> 🔍 老手視角──真正的門道
> 老手拿到多個版本的 APK，會先做三件不花錢的事：比 dex 數量、比 `AndroidManifest.xml` 的權限清單、比 `assets/` 的檔名。本專案那份乾淨版申請了 **27 項權限**（相機、定位、通話、開機自啟…），對一個文字遊戲來說絕大多數用不到——這既是「原作者習慣性複製權限模板」的證據，也提醒你：**權限清單是最快看穿一個 App 真實企圖的地方**。真正的門道在於，逆向的目標不是「還原全部」，而是「還原到夠寫出可互通的實作為止」——分得清這條線，專案才會收斂。

---

## 002　字串常數 — 三十分鐘看穿一套協議的骨架

**標籤**：`#逆向` `#字串分析` `#協議發現` `#smali` `#快速偵察`
**證據等級**：🟠 客戶端反編譯

**起源**：反編譯完成後，最快理解一個未知協議的方法不是逐行讀邏輯，而是**把所有字串常數倒出來排序**。程式碼會騙人（死碼、廢棄分支），字串不會——它們是協議在二進位裡留下的指紋。

**技術核心**：一行指令從 130 個 smali 檔抽出 431 個唯一字串：

```bash
grep -rhoP 'const-string[^,]*, "\K[^"]{2,}' smali/ | sort -u
```

其中三組立刻讓整套協議的形狀浮現：

**第一組——行級 opcode。** 三十幾個 `` 開頭、後接三位數字的字串：

```
000  001  002 … 024
045  100  900  903  905  913
997  998  999
```

看到這組的瞬間就知道：這不是純 telnet 終端機，而是**在文字串流上疊了一層行級指令**。而且編號有規律——`9xx` 明顯是 `0xx` 的反向操作（`903` 刪出口對 `003` 建出口，`905` 刪物件對 `005` 建物件）。

**第二組——欄位標記。** `$zj#`、`$z2#`、`$br#`、`$dh#`、`$sock#`、`$N`、`$obj#`、`$exp#`、`$god#`、`$txt#`。

這組告訴你 payload 不是自由文字，而是**有結構的記錄**，用字串當分隔符而不是二進位長度前綴——典型的「從 telnet 時代長出來」的設計。

**第三組——ANSI 擴充。** 除了標準的 `[0m`、`[31m`、`[1;33m`，還混著 `[f#`、`[b#`、`[s`、`[u`、`[9m`、`[2J`。

前兩個是自訂前景／背景色，後面幾個更有趣：`[s` 和 `[u` **以 `]` 收尾而不是 `m`**——那是這套方言自己加的東西，也是後來咬人最狠的一個細節。

**解決的痛點**：面對未知協議時，「從哪裡開始讀」本身就是最大的成本。字串常數把搜尋空間從「130 個類別、上萬行程式碼」壓縮到「431 行文字」。

**踩過的坑**：字串傾印會同時撈出一批**假線索**——HTTP 端點寫著 `http://127.0.0.1/mobi/login.php`（佔位值，不是真實伺服器）、一個八字元的簽章 salt 直接明文躺在裡面、還有 `https://baidu.com` 這種顯然是佔位的語音下載網址。**別急著把它們寫進規格**，先確認它們在 `.c` 或實機上真的出現過。

**優點 / 罩門**：字串分析的優點是快、幾乎零成本、能一眼看出協議家族。罩門是它**只給你名詞，不給你文法**——你知道有 `$zj#` 這個分隔符，但不知道哪個 opcode 用它、切幾層。那要靠下一步。

**方法對照**：

| 方法 | 適用階段 | 產出 | 侷限 |
|------|---------|------|------|
| 字串常數傾印 | 偵察 | 協議詞彙表、opcode 清單 | 沒有文法與語意 |
| 分派器反編譯 | 解剖 | opcode → 行為的完整對應 | 需要讀懂控制流 |
| 實機抓包 | 驗證 | 真實 payload 樣本 | 只涵蓋觸發到的功能 |

**效益**：三十分鐘內建立起整套協議的心智模型，讓後續每一次閱讀原始碼都有地圖可循。

> 💡 君之一席話
> **程式碼會有死碼，字串不會說謊——當你不知道從哪裡下手，就先把所有字面量倒出來排序，協議的骨架常常就浮在那裡。**

> 🔍 老手視角──真正的門道
> 這一步真正的價值不在「找到 opcode」，而在**建立分類直覺**。看到 `903` 對 `003`、`905` 對 `005`，老手立刻會問：「那 `913` 對什麼？」——答案是「清空全部出口」，一個沒有對應正向操作的碼。這種**編號空隙**往往指向兩件事之一：某個功能被移除了，或某個功能還沒實作。本專案後來在另一份 header 裡發現 `1085` 這個**四字元 opcode**，正是因為早就對「編號應該長什麼樣」有了預期，才會在它出現時立刻警覺。可落地的提醒：逆向時**把編號表畫出來，盯著空格看**，空格比填滿的格子資訊量更大。

---

## 003　三方交叉驗證 — 這個專案能收斂的唯一原因

**標籤**：`#方法論` `#交叉驗證` `#證據等級` `#淨室` `#規格書`
**證據等級**：🟢🟡🟠 三方並用

**起源**：協議還原做到一半，出現一個典型困境：客戶端反編譯說 A，但沒有任何東西能證明伺服器真的送 A。這時候有兩條路——賭下去，或者去找第二個證據源。

**技術核心**：本專案最終建立了**三個獨立的證據源**，並明確定義它們的優先順序：

```
🟢 實機封包        真的連上伺服器抓到的位元組
     ↑ 推翻
🟡 伺服器原始碼    LPC 的 #define 與 .c 的實際發送點
     ↑ 推翻
🟠 客戶端反編譯    APK 還原出的 Java
     ↑ 推翻
🔴 推論            由結構相似性推得
```

**三者互補的方式很清楚**：

- 反編譯告訴你**客戶端會怎麼解讀**（含客戶端自己的主動行為，這是另外兩者看不到的）
- 伺服器原始碼告訴你**伺服器會怎麼送**（含中文註解說明意圖，資訊密度最高）
- 實機封包告訴你**線路上到底跑什麼**（唯一能推翻其他兩者的裁判）

**解決的痛點**：單一證據源的逆向，錯誤會安靜地潛伏。你不會知道自己錯了，直到某個使用者在半年後回報一個無法重現的 bug。

**踩過的坑**：這套方法**當場抓到兩個已經寫進規格書的錯誤**——

**第一個：`[u` 少了一個冒號。**
反編譯裡的取值是 `substring(3, indexOf("]"))`。我當時讀成「跳過 `[`、`u`，再跳一個字元」，寫成 `[u<url>]`。伺服器原始碼一翻，巨集寫得清清楚楚：

```c
#define ZJURL(w)   ESA + "[u:" + w + "]"
```

**是 `[u:` 有冒號**，索引 3 正好跳過 `[`、`u`、`:` 三個字元。我的實作會把 URL 解成 `:cmds:look`，**所有可點擊連結全部失效**——而且不會報錯，只是點了沒反應。

**第二個：`$N` 根本不屬於那個 opcode。**
我把 `ESC001`（文字輸入面板）的指令樣板寫成用 `$N` 替換數量。伺服器原始碼裡 147 處 `INPUTTXT` 用例，**全部使用 `$txt#`**：

```c
INPUTTXT("请输入想设定的【中文名字 英文名字】：", "name $txt#")
```

而且輸入是**自由文字**（伺服器拿它接角色名、四項屬性數列、聊天內容），不是數字。我不只用錯了佔位符，連輸入框的型別都做錯了。

`$N` 確實存在，但它屬於**另一個** opcode（`ESC010` 對話框）。兩個機制長得像，但不能混用。

**優點 / 罩門**：三方交叉的優點是**錯誤會自己浮出來**，而且能標出可信度。罩門是成本——你得同時有 APK、伺服器原始碼和一台能跑的伺服器。少一個，方法就降級。

**證據源對照**：

| 證據源 | 看得到 | 看不到 | 典型陷阱 |
|--------|--------|--------|---------|
| 客戶端反編譯 | 客戶端的解讀邏輯與主動行為 | 伺服器實際送什麼 | 死碼；索引差一 |
| 伺服器原始碼 | 發送意圖、中文註解、巨集定義 | 客戶端怎麼反應 | **定義了 ≠ 用得到** |
| 實機封包 | 線路上的真實位元組 | 沒觸發到的功能 | 只有一次會話的樣本偏差 |

**效益**：規格書從「大概是這樣」升級成「每一條都指得出出處」，而且**兩個潛伏的致命 bug 在寫進程式碼之前就被攔下**。

> 💡 君之一席話
> **一個證據源給你答案，兩個給你懷疑，三個才給你確定——逆向工程裡最貴的不是時間，是那個你以為對了卻沒人反駁的結論。**

> 🔍 老手視角──真正的門道
> 這裡最容易被忽略的一點是：**三方衝突時，別急著選一邊，先問「為什麼會不一致」**。本專案有一處 `ESC003` 出口格式，官方文件寫成「出口名字:方向」，而反編譯與實機封包都顯示是「方向:名字」。答案不是誰錯了——是文件作者用「出口名字」這個詞去指涉方向關鍵字，措辭鬆散而已。如果當時盲從文件把欄位對調，八個方向鍵會全部失效。**不一致本身就是資訊**，它常常指向命名習慣、版本演進或文件過期，而不是單純的對錯。可落地的做法：規格書裡替每個有爭議的欄位保留一行「衝突紀錄」，寫明誰說了什麼、你採信誰、為什麼——半年後回頭看，這行字比結論本身更有價值。

---



# 第二篇　協議解剖

## 004　三層結構 — 一句話說完 ZJMUD 是什麼

**標籤**：`#協議` `#分層` `#telnet` `#ANSI` `#行級opcode`
**證據等級**：🟢 實機封包 ＋ 🟡 伺服器原始碼

**起源**：所有中文 MUD 客戶端都面對同一個矛盾——**伺服器只會吐純文字，但手機螢幕需要按鈕、血條、面板**。ZJMUD 的解法不是換一套二進位協議，而是**在文字串流上疊三層標記**，讓舊的 telnet 客戶端還能用，新的圖形客戶端也能解析。

**技術核心**：

```
┌──────────────────────────────────────────────────────┐
│ TCP，連線瞬間有 telnet IAC 協商（必須剝除並拒絕）        │
│  └─ UTF-8（埠 5001；埠 5003 是 GBK），以 \n 逐行分幀    │
│      └─ 行首 4 字元 opcode：ESC + "003"                │
│          └─ payload：用 $zj# 分記錄、: 分欄位           │
│              └─ 欄位內：ANSI 擴充方言 + $br# 換行       │
└──────────────────────────────────────────────────────┘
```

四層，由外而內：

**① 傳輸層**：原生 TCP，UTF-8，`\n` 分幀。看起來單純，但外面裹著一層 telnet 選項協商——這是最容易漏掉的一層，詳見 006。

**② 行級 opcode**：每一行的前 4 個字元是 `ESC`(U+001B) 加三個英數字元。命中就分派到對應的 UI 區塊，沒命中就當一般訊息。**這一層決定了「這行文字要顯示在哪裡」**。

**③ 欄位結構**：payload 用字串標記切分——`$zj#` 分記錄、`:` 分欄位、`|` 分次級欄位、`║`(U+2551) 分屬性條。沒有長度前綴，全靠分隔符。

**④ 行內樣式**：ANSI SGR 的擴充方言，加上自訂色、字級、超連結、全形模式。

**解決的痛點**：讓同一條連線同時服務「傳統 telnet 玩家」與「圖形客戶端玩家」。不認識 opcode 的客戶端會把 `ESC003north:北` 當成一行奇怪的文字印出來——難看，但不會壞。**這是一個刻意的向下相容設計。**

**踩過的坑**：一開始把 opcode 的規則寫成「ESC + 三個**數字**」，正則是 `/^\d{3}$/`。後來在擴充方言裡撞到 `10a`、`11a`、`2k1`–`2k4` 這種**帶字母**的碼，全部被當成一般訊息，把控制字元連同內容印在畫面上。更狠的是還有一個 `1085`——**四個字元**。

**優點 / 罩門**：分層設計的優點是**每一層可以獨立測試**——本專案的樣式解析器與 opcode 解析器都是純函式，加起來 57 條單元測試不需要任何網路或 DOM。罩門是**分隔符沒有跳脫機制**：如果房間名裡真的出現 `$zj#` 這四個字，協議就會解錯。實務上靠「中文內容不會出現這種組合」硬撐過去。

**設計對照**：

| 協議 | 分幀方式 | 結構化方式 | 向下相容 |
|------|---------|-----------|---------|
| **ZJMUD** | `\n` 逐行 | ESC+opcode ＋ 字串分隔符 | ✅ 舊 telnet 可讀（雖然難看） |
| MXP | telnet 子協商 | 類 HTML 標籤 | ✅ 標籤可被忽略 |
| GMCP | telnet 子協商 | JSON | ❌ 純 telnet 看不到 |
| 純二進位 | 長度前綴 | 自訂結構 | ❌ 完全不相容 |

**效益**：一套協議同時養活兩種客戶端，伺服器端只需要多送幾行帶前綴的文字，不必維護兩條資料路徑。

> 💡 君之一席話
> **最務實的協議演進，往往不是推倒重來，而是在既有的文字流上偷偷加一層記號——讓不認識它的人只覺得有點雜訊，讓認識它的人看見一整個介面。**

> 🔍 老手視角──真正的門道
> 這種「在人類可讀串流上疊機器標記」的設計，在協議史上反覆出現：HTTP 的 header、email 的 MIME、終端機的 ANSI 本身都是。它們的共同代價也一樣——**沒有跳脫序列的分隔符，就是一顆延遲引爆的地雷**。ZJMUD 用 `$zj#` 這種罕見組合降低碰撞機率，但沒有消滅它。真正的解法是長度前綴或跳脫規則，代價是失去人類可讀性。老手在選型時會問一個問題：**「這條串流未來會不會承載使用者可控的內容？」**——如果玩家能把 `$zj#` 打進聊天訊息，這個協議就有注入風險。本專案的伺服器沒有過濾，這是一個已知但未修的弱點。

---

## 005　行級 opcode — 33 個碼撐起整個介面

**標籤**：`#opcode` `#分派器` `#UI映射` `#副作用` `#死碼`
**證據等級**：🟡 伺服器 `zjmud.h` ＋ 🟠 反編譯 ＋ 🟢 實機

**起源**：伺服器端的定義檔 `include/zjmud.h` 是整套協議最權威的來源——27 個啟用中的巨集，每一個都帶中文註解。這份檔案是這個專案最幸運的發現。

**技術核心**：opcode 分成幾個清楚的家族：

| 家族 | 碼 | 職責 |
|------|-----|------|
| **房間** | 002 標題、004 描述、003 出口、005 物件 | 構成主畫面 |
| **反向操作** | 903 刪出口、905 刪物件、913 清空出口 | `9xx` = `0xx` 的反向 |
| **疊層** | 007 詳情、008/009 動作列、010 對話框、011 地圖、013 長文本、020 彈出選單 | 蓋在主畫面上 |
| **訊息** | 015 頂部飄字、016 戰鬥、100 聊天 | 分流到不同區塊 |
| **狀態** | 012 屬性條、006 快捷鈕、021 標題按鈕、022 血條更新 | 周邊 HUD |
| **控制** | 000 登入狀態、014 代送指令、997/998 傳輸模式、999 退出 | 不顯示內容 |

其中 **`ESC014` 特別值得注意**——它讓伺服器可以**叫客戶端主動送出一條指令**。伺服器原始碼裡用了 98 次，是整套流程的驅動骨幹。沒實作它，大量互動會斷在半路。

**另一個關鍵是 `ESC002` 的副作用**。它不只設定房間名，還會**連帶清空出口區、物件區、標題按鈕、戰鬥面板**。這是「換房間」的隱含信號——伺服器不會另外送一個「清空」指令，它假設客戶端知道。

**解決的痛點**：把一條線性的文字串流，拆解成十幾個各自更新的 UI 區塊。

**踩過的坑**：客戶端反編譯裡的分派鏈，`ESC006` **出現了兩次**——第一次是「設定自訂按鈕」，第二次是「依 tag 刪除物件」。因為是 if/else-if 鏈，**第二個分支永遠不可達**。

照著死碼實作，會浪費時間寫一個永遠不會被觸發的處理器，還可能在某天「修好」它而引入真正的 bug。**辨識死碼是反編譯閱讀的基本功**。

**優點 / 罩門**：27 個核心碼撐起一個完整的圖形介面，密度很高。罩門是**編號沒有語意分區**——`045`（開啟網頁）夾在 `024` 和 `100` 中間，`900` 不遵循 `9xx = 反向操作` 的規律。這種歷史累積的不規則，讓新增 opcode 時很容易撞號（第三篇會看到真的撞了）。

**規模對照**：

| mudlib | 定義的 opcode | 實際使用 | 落差原因 |
|--------|--------------|---------|---------|
| LPMud-Name | 27 | 21 | 部分功能未開 |
| 大梦江湖（新協議） | 101 | 81 | 真正的擴充方言 |
| nt7 | **102** | **16** | header 從別處複製，擴充碼從未使用 |

**效益**：實作 27 個核心碼，就能與收藏中 14 個不同 mudlib 互通——這 27 個是所有方言的最大公因數。

> 💡 君之一席話
> **一個 opcode 表最有價值的資訊，不在它列了什麼，而在它的編號空隙與撞號——那是協議演化留下的地層。**

> 🔍 老手視角──真正的門道
> 判斷一套協議的實作優先順序，老手不看 opcode 表的順序，而看**伺服器原始碼裡的使用次數**。本專案掃出來的分佈極度不均：`ZJOBLONG`（詳情）用了 531 次、`ZJOBACTS2`（動作列）448 次、`ZJFORCECMD`（代送指令）98 次，而 `ZJYESNO`（NPC 對話框）在該伺服器**一次都沒用**。這意味著：花力氣做對話框的精美動畫，不如先把「詳情＋動作列」這條主幹做扎實——那才是玩家 95% 的時間會看到的東西。可落地的做法：拿到任何協議規格，**先寫一支統計腳本掃 server 端的實際發送點**，用頻率排出實作順序，而不是照著文件從第一頁做到最後一頁。

---



## 006　telnet 那一層 — 差點讓連線在第一個位元組就死掉

**標籤**：`#telnet` `#IAC` `#MCCP2` `#UTF-8` `#位元組層`
**證據等級**：🟢 實機封包（最高）

**起源**：規格書 v1.0 白紙黑字寫著「原生 TCP，**非** telnet 選項協商」。這個斷言來自客戶端反編譯——`mudsocketd` 就是開一條 socket、包一層 `BufferedReader`，完全沒有處理 IAC 的痕跡。

三方交叉驗證裡的前兩方（反編譯、伺服器 LPC 碼）都沒有反駁它。

然後真的連上去一次。

**技術核心**：伺服器在連線建立的瞬間送出 **18 個位元組**：

```
ff fd 18   IAC DO   TERMINAL-TYPE (24)
ff fd 1f   IAC DO   NAWS          (31)
ff fd 27   IAC DO   NEW-ENVIRON   (39)
ff fb 56   IAC WILL MCCP2         (86)   ← 壓縮
ff fb 46   IAC WILL MSSP          (70)
ff fb 2a   IAC WILL CHARSET       (42)
```

這不是 mudlib 寫的，是 **FluffOS driver 自己送的**——所以翻遍 `.c` 都找不到。這正是「實機封包能推翻原始碼」的典型案例。

**不處理的兩個致命後果**：

**① `0xFF` 不是合法 UTF-8。** 如果用 Rust 的 `BufReader::read_line` 讀進 `String`，會直接回 `InvalidData`，讀取任務當場結束——**連線在第一毫秒就死**，而且錯誤訊息完全指不到真正的原因。

**② 誤答應 MCCP2 會讓整條連線變成 zlib。** 選項 86 是壓縮協商。如果客戶端回一個 `IAC DO 86`，伺服器之後送的全是壓縮串流，你會看到一堆二進位垃圾，且完全不知道發生什麼事。

**正確作法**：

```
1. 在位元組層剝除所有 IAC 序列，才做 UTF-8 解碼
2. 協商一律拒絕：對方 WILL → 回 DONT；對方 DO → 回 WONT
3. 子協商 IAC SB … IAC SE 整段丟棄
4. IAC IAC 是「一個真正的 0xFF 資料位元組」，還原成單一 0xFF
5. UTF-8 用寬容解碼，壞位元組換替換字元，不讓一行毀掉整條連線
```

**解決的痛點**：讓客戶端在面對「文件沒寫、原始碼看不到」的傳輸層行為時仍能存活。

**踩過的坑**：這一層在**兩個地方各實作了一次**——桌面版是 Rust 的 `telnet.rs`（162 行），瀏覽器版是 JS 的 `bridge/telnet.mjs`（63 行）。兩份邏輯必須一致，否則兩種形態的行為會分歧。

處理方式是**共用測試資料**：兩邊的單元測試都直接用那 42 個實機位元組當輸入，斷言剝除後等於 `\r\nver1.0:...\r\n`，且回覆恰好是六組拒絕。**同一份事實，兩份實作，一組斷言。**

**優點 / 罩門**：全部拒絕協商的策略優點是**行為完全可預測**——不會有壓縮、不會有子協商狀態機。罩門是放棄了 NAWS（視窗大小回報），伺服器無法依客戶端寬度調整排版。對這套協議影響不大，因為版面本來就由 opcode 決定。

**處理策略對照**：

| 策略 | 實作成本 | 風險 |
|------|---------|------|
| **全部拒絕**（本專案） | 低，約 60 行 | 放棄 NAWS/CHARSET 等便利功能 |
| 選擇性接受 | 中，需狀態機 | 接受 MCCP2 就要實作 zlib 解壓 |
| 完全忽略（不回覆） | 最低 | 伺服器可能等待逾時；仍須剝除位元組 |

**效益**：一個 60 行的過濾器，換來連線層的絕對確定性。

> 💡 君之一席話
> **文件說沒有、原始碼看不到、兩份反編譯都同意——然後真機告訴你有。這就是為什麼再完美的靜態分析，都欠一次真正的連線。**

> 🔍 老手視角──真正的門道
> 這個坑的真正教訓不是「要處理 IAC」，而是**「框架會替你做你不知道的事」**。mudlib 作者從沒寫過協商邏輯，因為那是 driver 的行為；反編譯者看不到，因為那在 Java 的 socket 層之下。老手在逆向網路協議時有一條鐵律：**先抓一次原始位元組再說**——`tcpdump` 或一支二十行的 socket dump，成本是十分鐘，能省掉的是「為什麼連上就斷」這種毫無線索的除錯。可落地的提醒：任何「文件說很單純」的傳輸層，都值得花十分鐘親眼看一次十六進位。

---

## 007　ANSI 擴充方言 — 那個要人命的冒號

**標籤**：`#ANSI` `#SGR` `#樣式` `#超連結` `#ARGB`
**證據等級**：🟡 伺服器巨集 ＋ 🟢 實機封包

**起源**：payload 內的文字不是純文字，而是帶樣式的。ZJMUD 用 ANSI SGR 當基礎，再加上四項自己的擴充。這一層決定了畫面「好不好看」，也決定了「點不點得動」。

**技術核心**：文字先以 `ESC` 切段，每段開頭是樣式指令，其後是套用該樣式的文字。**樣式是跨段有狀態的**——設定後持續生效，直到 `[0m` 或任何以 `;0m` 結尾的碼把它重置。

標準的部分照抄 ANSI，但**用的是自訂調色盤**（`[32m` 是 `#00bb00` 而不是純綠）。真正的擴充有四個：

| 指令 | 語意 | 陷阱 |
|------|------|------|
| `[f#RRGGBBm` | 自訂前景色 | 色碼可能是 **8 位 ARGB** |
| `[b#RRGGBBm` | 自訂背景色 | 同上 |
| `[s:<n>]` | 字級 = 畫面寬 ÷ n | **以 `]` 收尾，且參數前有冒號** |
| `[u:<url>]` | 超連結 | **同上** |
| `[9m` | 全形模式 | 後續 ASCII 轉 U+FF01–U+FF5E |

**那個冒號**是這一層最貴的細節。伺服器巨集寫得很清楚：

```c
#define ZJURL(w)    ESA + "[u:" + w + "]"
#define ZJSIZE(n)   ESA + "[s:" + n + "]"
```

客戶端取值是 `substring(3, indexOf("]"))`——索引 3 正好跳過 `[`、`u`、`:`。少跳一個字元，URL 就變成 `:cmds:look`，**所有可點擊連結全部失效，而且不報錯**。

實機封包最終證實了這一點：

```
ESC[u:cmds:uplv -l]【角色等级】ESC[0m
```

**ARGB 是第二個陷阱**。伺服器實際送的顏色長這樣：

```c
sprintf("气血.%d:%d/%d/%d:%s", ..., "#99FF0000")
```

`#99FF0000` 是 **alpha 在前**的 ARGB。直接丟給 CSS 會被當成 RGBA 解讀，顏色完全錯。轉換時必須把 alpha 從頭搬到尾：`#99FF0000` → `#FF000099`。

**解決的痛點**：讓伺服器能在純文字裡表達「這幾個字可以點」「這段放大顯示」「這個顏色半透明」，而不需要另一條資料通道。

**踩過的坑**：連結的 scheme 有五種，其中兩種是官方文件才揭露的：

```
cmds:    送出指令（最重要，伺服器用它把任意詞句變成按鈕）
pops:    開啟彈出選單
voice:   播放語音
tel:     撥打電話      ← 官方文件才有
mailto:  開啟郵件      ← 官方文件才有
```

`cmds:` 是整套協議最靈活的機制——它讓伺服器可以把敘述文字裡的任何一個詞變成可點擊的指令，不需要預先定義按鈕。

**優點 / 罩門**：狀態式樣式的優點是**省頻寬**（設定一次持續生效）。罩門是**解析器必須維護狀態**，而且一旦某段的重置碼遺失，後面全部的顏色都會錯——這種 bug 極難重現，因為它取決於前面送過什麼。

**兩種標記形式對照**：

| 形式 | 樣貌 | 使用者 | 安全性 |
|------|------|--------|--------|
| **ANSI 式** | `ESC[u:cmds:look]文字ESC[0m` | 經典與大梦江湖 | 安全，解析器完全控制 |
| **HTML 式** | `<a href="cmds:look"><span>文字</span></a>` | 指游 web 版 | ⚠️ 伺服器可送任意標記 |

**效益**：一套樣式方言讓純文字介面擁有可點擊、可縮放、可著色的能力，且對不支援的客戶端只是多了幾個怪字元。

> 💡 君之一席話
> **一個冒號的差別，能讓整個介面看起來完好無缺、卻一個字都點不動——這種錯誤不會讓程式崩潰，只會讓它安靜地失去靈魂。**

> 🔍 老手視角──真正的門道
> `[s:` 和 `[u:` 以 `]` 收尾而非 `m`，是這套方言最反直覺的設計，也是最容易寫錯的地方——因為 ANSI 解析器的慣性寫法就是「找第一個 `m`」。老手在實作任何「擴充自標準」的方言時，會先做一件事：**把所有不遵守標準的例外列成一張表，放在解析器的最前面優先處理**。本專案的 `ansi.js` 正是這樣排序：先判 `[u`、`[s`（以 `]` 收尾），再判 `[2J`、`[H`（螢幕控制），最後才進入「找 `m`」的通用路徑。這個順序不是美學，是正確性——**例外必須在通則之前被攔截**。另外那個 HTML 式標記值得警惕：它意味著伺服器可以送任意 HTML 到客戶端，實作時絕不能直接 `innerHTML`，必須白名單標籤與屬性。這是協議設計把安全責任外包給客戶端的典型案例。

---

## 008　登入握手 — 三種寫法，三種欄位數

**標籤**：`#握手` `#狀態機` `#方言判別` `#逾時` `#驗證`
**證據等級**：🟡 三份伺服器來源 ＋ 🟢 實機

**起源**：連上之後伺服器送的第一行決定了後續一切。問題是——**這一行有三種寫法**。

**技術核心**：

| 寫法 | 出處 | 對應方言 |
|------|------|----------|
| `ver1.0:<crypt>` | LPMud-Name `logind.c` | 經典／大梦江湖 |
| `ver1.0,<crypt>` | 官方協議文件 | 經典（**逗號不是冒號**） |
| `version 1.0 key::<crypt>` | 指游轉換教程 | 指游 ZY |

只認其中一種，另外兩種伺服器就**完全連不上**。

更麻煩的是**欄位數也隨方言變動**：

| 訊息 | 經典 | 官方文件 | 指游 |
|------|------|----------|------|
| 登入 | `帳號║密碼║密文║email`（4 欄） | `帳號║密碼║密文`（3 欄） | 4 欄 |
| 建角 | `性別║頭像║暱稱`（3 欄） | 3 欄 | **`性別║暱稱`（2 欄）** |

伺服器端是 `sizeof(myinfo)!=N` 的嚴格檢查，**欄位數不對直接退回**。

因此正確的設計是：**用第一行同時決定 opcode 表與後續所有欄位數**。一個字串，三個下游決策。

**登入狀態機**（實機逐步驗證過）：

```
連線 → ver1.0 挑戰
     ├─ 有記住的帳密 ──▶ 兩行連發（版本回覆 + 帳號行）
     └─ 沒有 ──────────▶ 跳出登入表單
                              │
              ┌───────────────┴───────────────┐
              ▼                               ▼
        ESC000 "0007"                   ESC000 "0008"
        登入成功                         帳號無角色 → 建角表單
```

`ESC000` 的 payload **既可能是數字狀態碼、也可能是中文字串**（`重连完毕`），實作不能假設只有一種。

**解決的痛點**：讓同一個客戶端能連上三種不同世代的伺服器。

**踩過的坑**：這一節有本專案**最嚴重的一次設計失誤**——

初版**沒有登入介面**。設計時把「登入」列為已知限制，要使用者自己在指令列打三行：版本回覆、帳號串、建角串。

但伺服器對「連上到登入完成」有時限。人工打字**趕不上**，於是變成：

```
連線 → 逾時被踢 → 自動重連 → 又逾時 → 再踢 → …
```

畫面只會反覆刷 `ver1.0:...` 和「您花在连线进入手续的时间太久了」。**客戶端在功能上完全不可用**，而我把它寫成「已知限制」就放著了。

修法是讓客戶端接手握手：偵測到挑戰就跳表單，送出時**兩行連發在同一個 tick**，不給伺服器逾時的機會。同時加上「連續 3 次逾時就停止自動重連」的止血。

**優點 / 罩門**：由客戶端接手握手的優點是使用者體驗完整，且能在前端先驗格式（ID `^[a-z][a-z0-9_]{3,19}$`、角色名 2–4 個中文字），省一次往返。罩門是**客戶端無法通過官方的完整驗證**——

官方設計是：
```
伺服器 → ver1.0,<crypt(ZJKEY, 0)>
客戶端 → crypt(ZJKEY, str[2..3])       ← 由官方網關產生
客戶端 → 帳號║密碼║crypt(ZJKEY,帳號)+crypt(ZJKEY,密碼)
```

`ZJKEY` **只存在於伺服器端**，第三方客戶端拿不到。實務上絕大多數自架伺服器把這段檢查註解掉了，所以能用固定值矇混——但這是伺服器的寬容，不是協議的允許。

**效益**：一個握手判別函式，讓客戶端同時相容三個世代的 mudlib。

> 💡 君之一席話
> **把「使用者自己打三行指令」寫成已知限制，聽起來很誠實——但如果伺服器根本不給人打字的時間，那不是限制，是不能用。**

> 🔍 老手視角──真正的門道
> 這裡最值得學的是**「已知限制」這四個字的濫用風險**。它本來的意思是「這個功能沒做，但不影響核心可用性」；一旦它掩蓋的其實是「核心流程走不完」，它就變成一張自我安慰的貼紙。老手審視任何「已知限制」清單時，會逐條問一個問題：**「使用者能不能在沒有這個功能的情況下，完成一次完整的核心任務？」**——不能的話，它就不是限制，是缺陷。可落地的做法：把「已知限制」拆成兩欄——**「缺功能」**和**「擋流程」**，後者一律當 bug 處理，不准留在文件裡過夜。

---



# 第三篇　方言與相容性

## 009　定義不等於使用 — 一份 header 騙走你兩天

**標籤**：`#方言` `#相容性` `#統計方法` `#優先順序` `#死定義`
**證據等級**：🟡 16 份 `zjmud.h` ＋ `.c` 使用統計

**起源**：手上有一個 mudlib 收藏，19 個專案、17 份 `include/zjmud.h`。要判斷「客戶端該支援多少 opcode」，第一反應是把所有 header 的定義取聯集。

那個聯集是 **101 個**。

如果照著做，會為了七十幾個永遠不會出現在線路上的 opcode 寫處理器。

**技術核心**：正確的做法是**用 `.c` 的實際使用情形過濾 header 的定義**。一支腳本掃過所有 mudlib，結果是這樣：

| mudlib | 定義 | 實際使用 | 落差 |
|--------|------|---------|------|
| nt7 | **102** | **16** | 86 個從未使用 |
| 大梦江湖（新協議） | 101 | **81** | 真正的擴充方言 |
| 谁与争锋、天涯二、四合一…（共 14 個） | 27 | 19–21 | 部分功能未開 |

**`nt7` 是最極端的案例**：它定義了 102 個 opcode，`.c` 裡一個擴充碼都沒用。那份 header 是從別處複製過來的，等同一份許願清單。

同樣的現象也出現在 `LPMud-Name` 的 `zjmud.h`——裡面有一大段被註解掉的擴充定義，形式完整、註解齊全，但全部處於停用狀態。

**最終的真實數字**：

```
全收藏實際使用的不同 opcode：83 個
  ├─ 經典核心 27 個  ← 14 個 mudlib 共用，最大公因數
  └─ 擴充     61 個  ← 只有「大梦江湖」一個 mudlib 在用
```

**解決的痛點**：把「要支援多少」這個問題，從一個猜測變成一個可以數出來的數字。

**踩過的坑**：一開始只掃 header，得到「74 個缺口」的結論，差點就照著實作。是後來多問一句「這些真的有人在用嗎」，才發現 nt7 那 86 個是死定義。

**這個檢查的成本是一支腳本、十分鐘。省下的是好幾天。**

**優點 / 罩門**：使用頻率統計的優點是**它直接給你實作優先順序**。本專案掃出的分佈極不均：

```
ZJOBLONG   (詳情)      531 次
ZJOBACTS2  (動作列)    448 次
ZJSIZE     (字級)      336 次
ZJMENUF    (版面)      305 次
ZJURL      (連結)      302 次
ZJFORCECMD (代送指令)   98 次
...
ZJYESNO    (對話框)      0 次   ← 定義了但沒人用
```

「詳情＋動作列」佔了絕對多數——那才是玩家 95% 時間看到的東西。而 NPC 對話框在該伺服器一次都沒用。

罩門是這個統計**只反映當下這批 mudlib**。新的伺服器可能啟用你判定為「沒人用」的碼，所以未知 opcode 的降級處理仍不可省。

**判讀方法對照**：

| 方法 | 成本 | 準確度 | 風險 |
|------|------|--------|------|
| 只讀 header | 最低 | 低 | 為死定義做白工 |
| **header ∩ `.c` 使用** | 低（一支腳本） | 高 | 只涵蓋手上這批 mudlib |
| 實機抓包 | 中 | 最高 | 只涵蓋觸發到的功能 |

**效益**：實作範圍從「101 個」收斂到「27 個核心 + 61 個擴充」，且擴充有明確的優先順序。

> 💡 君之一席話
> **一份 header 告訴你作者曾經想做什麼，一次 grep 告訴你他真正做了什麼——中間的落差，就是你省下來的時間。**

> 🔍 老手視角──真正的門道
> 「定義了但沒使用」在協議演化裡幾乎是常態，而不是例外。原因通常是三種：功能被砍、header 被複製、或某個分支版本才用得到。老手判斷一個協議實作的優先順序，永遠是**先量頻率再排工**——這和讀 API 文件的直覺完全相反，文件會把所有端點平等呈現，但真實世界的流量分佈幾乎都是長尾。可落地的做法：面對任何「規格 vs 實作」的落差，寫一支統計腳本比開三次會有效；而且這支腳本應該進版控，因為下次協議更新時，你要的是**差異**，不是重新數一遍。

---

## 010　撞號 — 為什麼不能把所有方言合併成一張表

**標籤**：`#方言` `#profile` `#撞號` `#靜默錯誤` `#架構`
**證據等級**：🟡 兩份 header 直接對照

**起源**：擴充方言做好之後，又發現第三套——指游 MUD 的 `ZY*` 系列（600–618）。順手把它加進同一張 opcode 表。

然後一個檢查腳本吐出了這個：

```
重複的 opcode： ['602', '603', '604', '605']
```

**技術核心**：這四個碼在兩個方言裡意義**完全不同**：

| opcode | 大梦江湖 | 指游 |
|--------|----------|------|
| 602 | `DMZHUYET` 主頁文本 | `ZYCHARHP` NPC 狀態 |
| 603 | `DMZHUYETY` 主頁文本 2 | `ZYFIGHTBTN` 戰鬥按鈕 |
| 604 | `DMZHUJMQH` 判斷界面 | `ZYHANDLERBTN` 操作面板 |
| 605 | `DMZHUJIU` 更改界面 | `ZYCLEARSCREEN` 清空主畫面 |

而在 JavaScript 的物件字面量裡，**後定義的鍵會靜默覆蓋前者**。合併表的那一刻，大梦江湖的四個主頁 opcode 就已經壞了——

**不會報錯、不會有警告、測試依然全綠。** 畫面上的表現是「某些功能莫名其妙變成別的東西」。

**這是本專案架構層面最有價值的一次發現**：它證明了單一合併表在結構上就是錯的。

**正確設計是分方言 profile**：

```javascript
export const PROFILES = {
  classic: { label: '經典 zjmud',      ext: {} },
  dmjh:    { label: '大梦江湖（新協議）', ext: DMJH },
  zymud:   { label: '指游 MUD（ZY）',    ext: ZYMUD },
}
```

由**握手字串決定套用哪一套**（見 008）：

```
ver1.0:  / ver1.0,        → dmjh
version 1.0 key::         → zymud
```

**解決的痛點**：讓同一個客戶端安全地支援多個互相衝突的協議世代。

**踩過的坑**：發現撞號**純屬意外**——寫了一個「列出所有 profile 中意義不同的 opcode」的診斷函式，本來只是想做個健康檢查，結果它第一次執行就吐出四個。

於是這個函式被留了下來，並寫成測試：

```javascript
test('602-605 在兩個方言中意義不同，必須被隔離', () => {
  const ops = conflictingOpcodes().map((c) => c.op).sort();
  assert.deepEqual(ops, ['602', '603', '604', '605'],
    '已知衝突就是這四個；若多出來代表新加的方言撞號了');
});
```

**這條測試的價值不在守住現在，而在守住未來**——下次有人加第四個方言時，如果又撞號，測試會立刻爆掉並指出是哪一個碼。

**優點 / 罩門**：profile 設計的優點是**衝突被結構性地消除**，而且新增方言只是加一張表。罩門是需要可靠的方言判別；判別錯了，整套 opcode 表就錯了。本專案的緩解措施是「沒命中目前 profile 時，若該碼在**所有** profile 中唯一，仍然採用」——讓不衝突的碼跨方言可用，衝突的碼嚴格以目前 profile 為準。

**架構對照**：

| 設計 | 撞號時的行為 | 可測性 | 新增方言成本 |
|------|-------------|--------|-------------|
| **單一合併表** | 靜默覆蓋，無任何徵兆 | 差 | 低，但會壞 |
| **分 profile**（本專案） | 隔離，且能列出衝突 | 好 | 加一張表 |
| 每方言一套解析器 | 不會撞 | 好 | 高，大量重複 |

**效益**：三個方言、89 個擴充 opcode、4 個撞號全部隔離，且經典 mudlib 的行為零退化（有專門的回歸測試守住）。

> 💡 君之一席話
> **最危險的 bug 不會讓你的程式崩潰，它會讓你的程式安靜地做另一件事——而合併命名空間，正是製造這種 bug 的標準工法。**

> 🔍 老手視角──真正的門道
> 撞號的根源是**協議演化時沒有中央註冊**：兩個團隊各自從 600 開始編號，誰也不知道對方。這在私有協議裡極常見，解法從來不是「協調大家」（做不到），而是**在客戶端承認多重宇宙的存在**。真正的門道在於：一旦你決定支援多方言，就必須同時提供三件事——**判別機制**（怎麼知道現在是哪個）、**隔離機制**（衝突時誰贏）、**診斷機制**（能列出所有衝突）。少了第三件，你會在半年後加第四個方言時重蹈覆轍。可落地的做法：把「列出跨命名空間衝突」寫成一個匯出函式並納入測試，成本二十行，回報是每次擴充都自動體檢。

---

## 011　變長 opcode — 一個 `1085` 逼出的消歧義規則

**標籤**：`#opcode` `#解析` `#消歧義` `#邊界條件` `#回歸測試`
**證據等級**：🟡 `others/zjmud.h`

**起源**：指游方言的 header 裡藏著一行：

```c
#define ZYMAP   ESA + "1085"   //地圖查看框
```

**四個字元。** 整套協議只有這一個。

**技術核心**：解析器原本固定取 3 個字元。碰到 `ESC1085地圖內容` 會解成 opcode `108` 加上 payload `5地圖內容`——`108` 不在任何表裡，於是整行降級成一般訊息，把控制字元連同內容印在畫面上。

改成「取 4 個字元」更糟——`ESC004` 後面接「5 個人站在這裡」會被讀成 `ESC0045`。

**安全的消歧義規則**是：

```
1. 先取 3 字元
2. 只有在「4 字元版本認得、而 3 字元版本不認得」時，才改用 4 字元
```

```javascript
const three = line.slice(1, 4);
if (/^[0-9a-z]{3}$/i.test(three)) {
  const four = line.slice(1, 5);
  if (/^[0-9a-z]{4}$/i.test(four) && isKnownOpcode(four) && !isKnownOpcode(three)) {
    return { op: four, payload: line.slice(5) };
  }
  return { op: three, payload: line.slice(4) };
}
```

這個規則的關鍵在**兩個條件都要成立**：4 字元的認得、3 字元的不認得。少了後者，`ESC0045…` 就會被誤判。

**解決的痛點**：在不破壞既有 99% 情況的前提下，支援一個例外。

**踩過的坑**：這一節的真正教訓不是「支援四字元」，而是**原本的正則寫錯了兩次**：

```javascript
/^\d{3}$/       // 第一版：只認數字 → 10a、2k1 全部失效
/^[0-9a-z]{3}$/ // 第二版：認英數 → 1085 仍失效
```

擴充方言裡有 `10a`、`11a`、`2k1`–`2k4` 這六個**帶字母**的 opcode。第一版正則會把它們全部當成一般訊息。

**優點 / 罩門**：這個規則的優點是**向後完全相容**——所有既有的三字元碼行為不變。罩門是它依賴 `isKnownOpcode()`，也就是**依賴表的完整性**。如果某天出現一個沒登記的四字元碼，它會被解成三字元加殘字。

因此配套的防線是：**未知 opcode 一律降級成一般訊息並保留完整原文**，同時在事件上標記 `unknownOpcode`，方便日後補表。**絕不靜默丟棄**——那會讓新 mudlib 的內容憑空消失且無從察覺。

**邊界案例對照**：

| 輸入 | 期望 | 為什麼 |
|------|------|--------|
| `ESC1085地圖` | op=`1085` | 4 認得、3 不認得 |
| `ESC0045 個人…` | op=`004`，payload=`5 個人…` | 3 認得，不可誤判 |
| `ESC2k1內容` | op=`2k1` | 帶字母的三字元碼 |
| `ESC[31m紅` | op=`null` | ANSI 樣式碼不可被誤判成 opcode |
| `ESCzzz某些` | op=`null`，標記 unknown | 降級但保留原文 |

這五條全部寫成測試，因為每一條都代表一種曾經或可能的解錯。

**效益**：一條十行的消歧義規則，讓解析器同時容納 3 字元與 4 字元 opcode，且零退化。

> 💡 君之一席話
> **支援例外的正確姿勢，不是放寬通則，而是替例外加一道「只有它成立、通則不成立」的窄門——放寬通則會傷到所有人。**

> 🔍 老手視角──真正的門道
> 這類「格式突然多一個字元」的問題，在協議演化裡有個標準診斷法：**看它是不是唯一的例外**。如果只有一個，用窄門規則；如果超過三個，代表格式本身已經變了，該考慮版本化（例如用握手字串切換解析規則）。本專案只有 `1085` 一個，所以窄門是對的選擇。老手還會多做一件事：**把消歧義規則的每一個分支都寫成測試，包括「不該觸發」的那些**——因為這種規則的 bug 幾乎都出在誤觸發，而不是漏觸發。可落地的提醒：任何帶「只有在…才…」的條件判斷，測試至少要有兩條，一條證明它會生效，一條證明它不會亂生效。

---



# 第四篇　客戶端架構

## 012　把協議關進純函式 — 原客戶端最大的架構債

**標籤**：`#架構` `#純函式` `#單向資料流` `#可測性` `#關注點分離`
**證據等級**：🟠 原客戶端反編譯（反面教材）＋ 本專案實作

**起源**：反編譯原客戶端時，最刺眼的不是協議有多複雜，而是**協議解析與 UI 操作完全混在一起**。

opcode 分派器裡直接寫著 `setVisibility()`、`addView()`、`setTextColor()`。要理解「`ESC003` 的 payload 格式」，你得同時讀懂 Android 的 View 樹。

更糟的是樣式解析器的狀態是**靜態欄位**（`static ForegroundColorSpan colsp`），多處共用同一份狀態。

**技術核心**：本專案的第一個架構決策是**把協議關進純函式**：

```
protocol.js   行 → 事件物件      不碰 DOM、不碰 store、不送指令
ansi.js       文字 → span 樹     不碰 DOM
     ↑ 這兩個檔案不依賴任何東西，可以在 Node 裡直接測
```

`decodeLine()` 的輸出是一個 discriminated union：

```javascript
{ type: 'room.title',   text }
{ type: 'room.exits',   exits: [{ dir, slot, label, cmd }] }
{ type: 'stat.bars',    layout, bars: [{ label, a, b, max, color, cmd }] }
{ type: 'msg.chat',     text }
```

**解析器不知道 DOM 存在**，它回傳的是資料。UI 層訂閱這些資料。

依賴方向嚴格單向：

```
        main.js
       ╱   │   ╲
   net.js  │   ui.js
      │    │    │
      │  store.js
      │    ▲    │
      └────┴────┘
           │
      protocol.js ──▶ ansi.js
```

**沒有任何模組反向依賴 `main.js`。**

**解決的痛點**：讓「協議正確性」與「畫面正確性」變成兩個可以獨立驗證的問題。57 條協議測試不需要瀏覽器、不需要伺服器、跑完只要 180 毫秒。

**踩過的坑**：純函式的邊界很容易在不知不覺中被打破。本專案有一次差點失守——`ansi.js` 的 `[2J`（清空主訊息區）需要通知 UI 清畫面。誘惑是直接在解析器裡呼叫 UI，但那會讓 `ansi.js` 從此依賴 DOM。

最後的做法是**把它變成回傳值的一部分**：

```javascript
return { spans, clearScreen };   // 由呼叫端決定要不要清
```

**一個布林值，守住了整個模組的純度。**

**優點 / 罩門**：純函式的優點是可測、可重放、可以拿實機封包當 fixture 直接餵。罩門是**有狀態的東西必須顯式傳遞**——樣式狀態跨段延續，所以 `parseStyled()` 接受一個可選的 `state` 參數，由呼叫端決定要不要延續。這比靜態變數囉嗦，但它讓「同時解析兩條獨立串流」變成可能。

**架構對照**：

| 設計 | 協議變更成本 | 可測性 | 典型症狀 |
|------|-------------|--------|---------|
| **解析器直接操作 UI**（原版） | 高，要動十個檔案 | 幾乎不可測 | 改協議就改壞畫面 |
| **純函式 + 事件**（本專案） | 低，只動一個檔 | 好 | — |
| 全域狀態機 | 中 | 中 | 狀態互相汙染 |

**效益**：協議層 170 條測試裡有 100 條完全不需要 DOM 或網路，在 CI 裡秒級跑完。

> 💡 君之一席話
> **把協議關進純函式，等於把「我理解得對不對」和「我畫得對不對」拆成兩個問題——能分開問的問題，才能分開答。**

> 🔍 老手視角──真正的門道
> 「解析器不碰 UI」聽起來是常識，但真正困難的是**守住它**——每一次「就這一行、就這一次」的例外，都是邊界的裂縫。老手的做法不是靠自律，而是靠**物理隔離**：讓純函式模組跑在一個沒有 DOM 的環境裡（本專案就是 `node --test`），一旦有人偷偷引用 `document`，測試立刻爆炸。這比 code review 可靠得多。可落地的提醒：**如果你的「核心邏輯」測試需要 mock 瀏覽器，那它就還不夠核心。**

---

## 013　雙形態 — 同一份前端，兩條傳輸路徑

**標籤**：`#Tauri` `#WebSocket` `#Transport抽象` `#跨平台` `#橋接`
**證據等級**：本專案實作 ＋ 🟢 實機驗證

**起源**：協議是**原生 TCP**。瀏覽器開不了 raw socket，所以純網頁版必須有一支橋接程序。而桌面版如果用 Tauri，Rust 端可以直接連 TCP，完全不需要橋接。

兩種形態，兩種取捨：

| | 桌面版 | 瀏覽器版 |
|---|--------|----------|
| 傳輸 | Rust 直接開 TCP | WebSocket → 橋接 → TCP |
| 需要 | Rust 工具鏈 | 只要 Node.js |
| 手機可用 | ✗ | **✓** |
| 額外程序 | 無 | 一支橋接 |

**技術核心**：關鍵設計是**一個 Transport 介面，兩個後端**：

```javascript
tauriBackend      → __TAURI__.core.invoke('mud_connect' | 'mud_send')
websocketBackend  → new WebSocket(`${scheme}//${location.host}/mud`)
```

兩者實作同一組方法（`open` / `send` / `close`），共用同一台**連線狀態機**（含指數退避重連）。上層的 `main.js` 與 `ui.js` 完全不知道自己跑在哪一種底下。

環境判別只有一行：

```javascript
const backend = isTauri() ? tauriBackend(...) : websocketBackend(...);
```

**前端程式碼一行都不用改**——3,107 行 JS 在兩種形態下完全相同。

**解決的痛點**：不必為了「桌面要效能、手機要可及性」而維護兩套客戶端。

**踩過的坑**：橋接必須**代替瀏覽器做 telnet IAC 處理**。瀏覽器收到的是 WebSocket 文字幀，沒有位元組層可以剝 IAC——所以那 63 行的 `bridge/telnet.mjs` 是 Rust 版 `telnet.rs` 的 JS 對應實作。

**兩份實作、一組斷言**：兩邊的測試都用同一份 42 位元組的實機資料，確保行為不會分歧。

另一個坑是**開放代理風險**。橋接如果讓前端指定任意 `host:port`，它就變成一個公開的 TCP 代理。本專案的預設是**只允許連到啟動時設定的目標**，要放寬得明確加 `--allow-any`：

```javascript
if (!ALLOW_ANY && want.host !== MUD_HOST) {
  sendState({ state: 'error', message: `此橋接只允許連到 ${MUD_HOST}:${MUD_PORT}` });
  ws.close();
}
```

這條規則有專門的測試守著。

**優點 / 罩門**：雙形態的優點是**覆蓋面最大**——桌面要效能有效能，手機開瀏覽器就能玩。罩門是**兩份 telnet 實作要同步維護**，這是真實的重複成本，只能靠共用測試資料緩解。

**方案對照**：

| 方案 | 手機 | 額外程序 | 前端改動 |
|------|------|---------|---------|
| 只做 Tauri | ✗ | 無 | — |
| 只做瀏覽器 | ✓ | 橋接 | — |
| **雙形態**（本專案） | ✓ | 瀏覽器版需橋接 | **零** |

**效益**：一份前端、兩種部署形態；瀏覽器版讓手機在同區網直接連上，這是原本 Tauri-only 方案完全做不到的。

> 💡 君之一席話
> **當兩種平台的差異只在「怎麼把位元組送出去」，那就別讓它蔓延到第三個檔案——抽象的價值不在優雅，在於把差異關在一個盒子裡。**

> 🔍 老手視角──真正的門道
> 這個抽象之所以成立，是因為 Transport 的介面**極窄**——只有連線、送一行、關閉、兩個回呼。窄介面是抽象能活下來的前提；一旦介面開始長出 `setTimeout`、`getStatistics`、`enableCompression` 這種東西，兩個實作就會開始各自漂移。老手設計跨平台抽象時有個經驗法則：**如果介面超過五個方法，先問是不是抽象抓錯層次了**。另外值得注意的是，本專案的橋接刻意**不解析協議**——它只做 IAC 剝除與分行，opcode 解析仍然全在前端。這讓橋接保持在 165 行、幾乎不需要維護；如果當初把協議解析也搬進橋接，就會變成第三份需要同步的實作。**中介層要薄，薄到你不想在它裡面加功能。**

---

## 014　失敗隔離 — 一個 `scrollTo` 讓整個客戶端變成擺設

**標籤**：`#錯誤處理` `#失敗隔離` `#可觀測性` `#掛載` `#防禦性設計`
**證據等級**：🟢 實機（四次故障中的第一次）

**起源**：初版的 `mount()` 是一長串直接呼叫：

```javascript
UI.Toast(...)
UI.ConnBadge(...)
UI.MessageList($('msg-main'), ...)   // ← 這裡拋例外
bindCommandInput(...)                 // ← 從未執行
bindConnectForm(...)                  // ← 從未執行
```

`MessageList` 內部呼叫了 `root.parentElement.scrollTo(...)`。`Element.scrollTo()` 不是所有執行環境都有——一拋例外，**整個 `mount()` 中斷**。

結果是：畫面渲染得完全正常（那些都是 CSS 和先跑完的元件），但「連線」按鈕**沒有任何事件處理器**。按下去毫無反應，也沒有任何錯誤提示。

**更狡猾的是**：連線面板的 host/port 欄位顯示著 `127.0.0.1` 和 `5001`——那其實是 HTML 的 **placeholder，不是值**。因為 `bindConnectForm()` 從沒跑到，沒有填值。畫面看起來一切正常。

**技術核心**：修法有兩層。

**表層**：`scrollTo` 換成 `scrollTop = scrollHeight`——等價但所有環境都支援。

**深層**：讓 `mount()` 逐項獨立保護：

```javascript
const mountErrors = [];
function step(name, fn) {
  try { fn(); }
  catch (err) { mountErrors.push(`${name}: ${err.message}`); console.error(...); }
}

step('MessageList', () => UI.MessageList(...));
step('ConnectForm', () => bindConnectForm());   // 這一項掛掉 = 客戶端完全不能用
...
if (mountErrors.length) showConnectError(mountErrors.join('\n'));
```

**任何一個元件失敗，都不會拖垮其他元件的事件綁定。**

**解決的痛點**：把「一個小元件的環境相容問題」與「整個客戶端能不能用」解耦。

**踩過的坑**：這個 bug 之所以能活到使用者手上，是因為**當時的 98 條測試全綠**——它們全都繞過了「HTML + main.js」這一層，也就是使用者唯一會碰到的那一層。

修完之後補了一支 jsdom 冒煙測試，**真的載入 `index.html`、真的執行 `main.js`、真的派發 click 事件**。它一跑就抓到這個 bug：9 條裡有 8 條失敗。

其中一條斷言特別關鍵：

```javascript
assert.equal(host.value, '127.0.0.1', 'main.js 未執行或 bindConnectForm 失敗');
```

**檢查的是「欄位有沒有被填值」，而不是「畫面上看不看得到文字」**——因為 placeholder 會讓後者通過。

**優點 / 罩門**：失敗隔離的優點是**局部故障不會全域癱瘓**，而且錯誤被收集起來顯示。罩門是它可能**掩蓋問題**——如果沒有把 `mountErrors` 顯示出來，就變成安靜地少了一個元件。因此隔離必須配上可觀測性，兩者缺一不可。

**錯誤處理策略對照**：

| 策略 | 局部故障 | 可診斷性 | 適用 |
|------|---------|---------|------|
| 不處理（原版） | 全域癱瘓 | 無 | — |
| 全域 try/catch | 全域癱瘓但不崩 | 差 | 頂層兜底 |
| **逐項隔離 + 收集**（本專案） | 局部降級 | 好 | 掛載期 |

**效益**：一個元件的環境相容問題，從「整個客戶端不能用」降級成「少一個訊息區」。

> 💡 君之一席話
> **一個永遠不會失敗的初始化流程是幻想；能在失敗之後還讓使用者按得動「連線」的初始化流程，才是工程。**

> 🔍 老手視角──真正的門道
> 這個故障的真正教訓藏在順序裡：**最關鍵的綁定排在最後**。`bindConnectForm()` 是使用者唯一的操作入口，卻排在十幾個裝飾性元件之後——前面任何一個失敗，它就永遠等不到。老手在寫初始化流程時會做一件事：**把「沒有它就完全不能用」的項目排到最前面**，或者至少確保它們彼此獨立。這不是效能考量，是失敗語意的排序。可落地的提醒：初始化清單寫完後，逐項問一句「這個掛了，使用者還能不能完成核心任務？」——不能的話，它就該被獨立保護，而且該往前排。

---



# 第五篇　工程實務

## 015　七次事故 — 同一個症狀，七種死法

**標籤**：`#事故` `#除錯` `#可觀測性` `#測試盲區` `#復盤`
**證據等級**：🟢 全部為實機發生

**起源**：協議層測試全綠、Rust 編譯零錯誤、實機封包逐行驗證通過。交到使用者手上，按下「連線」——**毫無反應**。

修好，再交，再無反應。**連續四次。**

第五次是能連上了，但流程走不完。
第六次是流程走得完了，但**使用者打不完字**——輸入完帳號，游標被彈回帳號欄，密碼永遠打不進去。

第七次最嚴重：它不是「不動」，是**動得太多**——客戶端每秒對伺服器登入一次，
等我們看伺服器日誌時，已經寫進去 **50,251 次登入**。

七次的表面症狀成因**完全不同**。

**技術核心**：

| # | 成因 | 為什麼既有測試抓不到 | 修法 |
|---|------|---------------------|------|
| 1 | `Element.scrollTo()` 在某些環境不存在，於 `mount()` 期間拋錯 → 後續 `bindConnectForm()` 從未執行 → 連線鈕沒有處理器 | 98 條測試全繞過 HTML + main.js 這一層 | 換 `scrollTop`；加失敗隔離 |
| 2 | **Tauri v2 ACL**：缺 `capabilities/default.json`，`event.listen` 被權限系統拒絕 | 編譯完全成功；權限是**執行期**才檢查 | 新增 capabilities 白名單 |
| 3 | **npm 平台錯配**：從 WSL 執行 `npm install`，裝到 `@tauri-apps/cli-linux-x64-gnu`，Windows 版原生 binary 被覆蓋 | 與程式碼無關，是環境問題 | 一律在 Windows 端跑 `npm install` |
| 4 | **`[hidden]` 被 CSS 蓋掉** | jsdom 斷言的是 `hidden` **屬性**，不是實際可見性 | `[hidden] { display: none !important; }` |
| 5 | **沒有登入介面**：要使用者自己打三行，但伺服器有逾時 | 設計時當成「已知限制」寫進文件就放著了 | 客戶端接手握手 |
| 7 | **重連風暴**：Rust 的 `connect()` 先關舊連線 → 舊連線 emit `closed` → 前端當成非預期斷線而重連 → 又關舊的 → 迴圈。退避 1/2/4/8/16/30 秒**從未升級**，因為每次重連都成功、成功就把 `retries` 歸零 | 假的 `__TAURI__` 對 `mud_connect` 直接「假裝成功」，**從不模擬真 Rust 會關掉舊連線**——引發 bug 的那個動作在測試裡不存在 | Rust 區分 `StopReason::{UserClose, Replaced}`；前端擋重入 |
| 6 | **登入視窗被反覆重開，focus 一直跳回帳號欄**：伺服器等待輸入時會重送 `ver1.0:` 挑戰行，每一行都呼叫 `openLoginModal()`，而它無條件 `focus('login-id')` | 測試只斷言 `login-modal.hidden === false`，從不看 `document.activeElement`；而且測試只送**一行**挑戰，真實伺服器送很多行 | `openLoginModal()` 改成等冪：`if (!m.hidden) return;` |

**第 4 個影響範圍最廣，也最值得細講。**

`[hidden]` 屬性的 `display: none` 來自**瀏覽器 UA 樣式表，優先級最低**。只要作者樣式對同一個元素設了 `display`，`hidden` 就完全失效。

本專案有大量元件符合這個條件：

```css
.connect-panel  { display: grid; }   /* 連線面板 */
.modal-backdrop { display: grid; }   /* 對話框、彈出選單 */
.overlay        { display: flex; }   /* 互動面板、地圖、長文本 */
.entity-list    { display: flex; }   /* 物件清單 */
.exit-cell      { display: flex; }   /* 八方向鍵 */
```

於是 `panel.hidden = true` **毫無視覺效果**。連線其實**每次都成功**，只是面板不消失——使用者看到的是「按了沒反應」，實際上是每按一次就多連一條（那堆 `TIME_WAIT` 就是證據）。

**一行 CSS 修好全部**：

```css
[hidden] { display: none !important; }
```

**解決的痛點**：六次事故各自解決了一個層次的盲區——DOM 綁定、執行期權限、建置環境、視覺呈現、流程完整性、**重複事件對使用者當下狀態的破壞**。

**踩過的坑**：最大的坑不是這五個 bug，而是**我把「編譯成功」當成「能用」，直接交出去**。前三次都是使用者當測試員，一次修一個。

第四次才建了截圖工具，**第一次用就抓到面板根本沒消失**。

**優點 / 罩門**：事後補上的兩層測試（jsdom 冒煙 + 實機截圖）把這類問題擋在交付之前。罩門是它們比純函式測試慢得多——jsdom 那 26 條要跑兩秒多，實機截圖需要真的開視窗。**但相對於「使用者連續四次替你除錯」，這個成本便宜到荒謬。**

**盲區對照**：

| 測試層 | 抓得到 | 抓不到 |
|--------|--------|--------|
| 純函式 | 協議解析錯誤 | 事件綁定、視覺、權限 |
| jsdom | 事件綁定、DOM 契約 | **CSS 級聯、視覺可見性** |
| 實機截圖 | 視覺、版面 | 難以自動斷言 |
| 真機連線 | 傳輸層、權限、環境 | 需要完整環境 |

**效益**：七個 bug，七種盲區，最終形成六層測試策略（見 016）。

**第 7 個最值得學的是診斷方法，不是修法。**
真因不是靠讀程式碼猜的，是三個互相獨立的量測把範圍縮到一行：

| 量測 | 結果 | 排除了什麼 |
|------|------|-----------|
| `grep -c` 伺服器 log 的密碼驗證警告 | 50,251 次 | 確認規模，不是偶發 |
| 8 秒內 log 增加 4000 bytes | **穩定 1 Hz** | 排除退避重連（退避頻率必定遞減） |
| 關掉客戶端 → log 增長歸零 | 立刻停止 | 排除伺服器自身、排除其他客戶端 |

**「穩定 1 Hz」是破案的那一刀。** 它與任何指數退避都不相容，
只可能來自「每次都成功、成功就把重試計數歸零、然後又重來」。
看到固定頻率的重複行為，不要往「重試邏輯寫壞了」找，
要往「**成功路徑把狀態洗掉了**」找。

**第 7 個帶來的紀律更根本：測試替身必須忠實模擬真實後端的副作用，包括不討喜的那些。**
它不是「漏寫一條測試」——覆蓋率、測試數、全綠狀態全都正常，
但假的後端從不做出「關掉舊連線並通知前端」這個真實會發生、而且會讓呼叫端出錯的動作。
**替身比真實寬容，是最難察覺的測試失效形態**，因為所有指標都告訴你一切正常。
判準只有一句：**這個替身有沒有做出真實後端會做、而且可能讓呼叫端出錯的那些事？**

**第 6 個帶來一條新的測試紀律**：
測試通常只餵**一次**事件，真實伺服器會餵**很多次**。凡是由伺服器推送而觸發的 UI 動作，
測試就該送**至少兩次**，並斷言第二次沒有破壞使用者當下的狀態——
focus 在哪、捲到哪、輸入框裡打了什麼、哪個面板已展開。
這四樣東西沒有一樣會出現在「功能有沒有做出來」的斷言裡。

> 💡 君之一席話
> **同一個「它不動」，可以是綁定沒跑、權限被擋、套件裝錯平台、CSS 蓋掉屬性、流程走不完、或是它動得太勤把你的游標搶走——症狀相同不代表病因相近，這正是為什麼「先讓錯誤說話」比「猜哪裡壞了」重要一百倍。**

> 🔍 老手視角──真正的門道
> 這六次事故有一條共同線索：**它們全都不會拋出使用者看得到的錯誤**。編譯成功、測試全綠、畫面正常渲染——每一層都在說「沒問題」，只有最後那個按鈕沉默。老手面對這種「安靜的失敗」有一個標準動作：**先花二十分鐘把錯誤變得可見，再開始找 bug**。本專案在連線面板加了錯誤列與環境診斷列之後，第 2 個 bug（Tauri ACL）是**它自己把答案印出來的**——`event.listen not allowed. Permissions: core:event:allow-listen`。在那之前，同樣的問題只表現為「按了沒反應」。可落地的提醒：**可觀測性不是上線後的運維需求，是開發期的除錯槓桿**；沒有它，你的每一次修復都是在黑箱裡擲骰子。

---

## 016　測試分層 — 為什麼 98 條全綠還是不能用

**標籤**：`#測試策略` `#分層` `#jsdom` `#視覺驗證` `#回歸`
**證據等級**：本專案 171 條測試

**起源**：專案初期只有三層測試：純函式、狀態容器、傳輸整合。98 條全綠。

**然後連續四次交付都是「按鈕沒反應」。**

原因很簡單：那三層**全都繞過了 HTML + main.js + CSS**，也就是使用者唯一會碰到的那一層。

**技術核心**：最終的六層策略：

| 層 | 檔案 | 數量 | 涵蓋 |
|----|------|------|------|
| 1 純函式 | `protocol.test.mjs` | 57 | 樣式方言、每個 opcode 的 payload、對話框、畸形輸入 |
| 2 狀態 | `store.test.mjs` | 8 | 訂閱通知上下傳播、容量裁切 |
| 3 對照真伺服器 | `server-fixtures` / `live-capture` | 11+14 | 伺服器巨集展開的輸出；**實機擷取的原始封包** |
| 4 傳輸整合 | `integration` / `bridge` | 8+11 | 真開 TCP：假伺服器 → 分行 → 解析 → store |
| 5 **UI 冒煙** | `ui-smoke.test.mjs` | **26** | **jsdom 真載入 index.html、真跑 main.js、真派發 click** |
| 6 方言相容 | `dialects.test.mjs` | 33 | 三個 profile、撞號隔離、變長 opcode |

**第 5 層是分水嶺。** 它是唯一會執行下列東西的測試：

- `index.html` 與 `main.js` 之間的 **DOM id 契約**
- `mount()` 是否真的跑完（欄位有沒有被填值，而不是只剩 placeholder）
- 按鈕的**事件處理器有沒有真的綁上**
- 伺服器封包 → DOM 的完整渲染路徑

用一個假的 `window.__TAURI__` 攔截 IPC，所以不需要 Tauri 就能在 CI 跑。

**第 3 層的實機封包**是另一個關鍵。53 行真實封包被固化成 fixture，例如：

```javascript
npc: `${E}005地藏王:look /d/register/npc/dizangwang#1686`,
```

這一條守住了「物件 id 含 `/` 與 `#`，只能切第一個冒號」這個規則——那是實機才會出現的形態，任何人造測試都想不到。

**解決的痛點**：把「我以為我測了」變成「我知道我測了哪一層、沒測哪一層」。

**踩過的坑**：第 5 層仍然抓不到視覺問題。**jsdom 不做完整 CSS 級聯**——`panel.hidden = true` 在 jsdom 裡斷言會通過，但真實瀏覽器中若作者樣式設了 `display`，`[hidden]` 會被蓋掉而畫面完全沒變。

這正是第 4 次事故的成因。所以又加了第七層：**實機截圖**。

```powershell
powershell -File tools\win\shot.ps1     # 擷取視窗
powershell -File tools\win\drive.ps1    # 用剪貼簿逐行送指令
```

改動 UI 之後**看一眼截圖**，成本幾秒鐘，能抓到所有純 DOM 斷言抓不到的問題。

**另一個坑是測試隔離**。UI 冒煙測試在同一個 jsdom 實例裡跑，模組層級的 `login` 物件會跨測試殘留。有一條「自動填入不該觸發自動登入」的測試因此失敗——不是程式碼錯，是前一條測試留下的登入狀態還在。

修法是在該測試開頭先點「登出」清乾淨。**這個修法本身也揭露了一個真實時序**：`net.close()` 會解除事件監聽，要等自動重連把監聽綁回來，中間送的封包會沒人收。

**優點 / 罩門**：分層的優點是**每一層跑的速度與涵蓋面不同，可以分開排程**——純函式在每次存檔跑，jsdom 在提交前跑，實機截圖在交付前跑。罩門是層數多了維護成本上升，且第 3 層的 fixture 會隨伺服器改版而過期。

**成本效益對照**：

| 層 | 執行時間 | 需要環境 | 抓到過幾個真 bug |
|----|---------|---------|-----------------|
| 1 純函式 | 180 ms | 無 | 多數協議解析錯誤 |
| 5 jsdom | ~2 s | jsdom | **2 個**（第 1、4 次事故） |
| 7 實機截圖 | ~15 s | 真視窗 | **1 個**（第 4 次事故的視覺確認） |

**效益**：測試程式碼佔產品程式碼 40%（1,995 / 4,977 行）。這個比例不是刻意追求的，是被四次事故逼出來的。

> 💡 君之一席話
> **「測試全綠」只回答了一個問題：你測的那些是對的。它從不回答另一個問題——**你有沒有測到使用者會碰的那一層。

> 🔍 老手視角──真正的門道
> 測試分層真正的價值不在覆蓋率數字，而在**每一層都明確聲明自己抓不到什麼**。本專案的每個測試檔開頭都寫了這件事——`ui-smoke.test.mjs` 明確標註「jsdom 不做完整 CSS 級聯，視覺問題請用截圖驗證」。這行註解的價值超過十條測試，因為它讓下一個人知道**該去哪裡補**。老手審視測試策略時只問一個問題：**「從使用者的手指到最底層的位元組，這條路徑上有哪一段沒有任何測試經過？」**——把那段找出來，就是下一層測試該待的位置。可落地的做法：畫一條從 UI 事件到 socket 的路徑圖，把每個測試檔標在它覆蓋的區段上，空白處一目了然。

---

## 017　環境陷阱 — 程式碼沒錯，錯的是它跑的地方

**標籤**：`#環境` `#WSL` `#跨平台` `#建置` `#編碼`
**證據等級**：🟢 全部為實機踩到

**起源**：本專案原始碼放在 Windows 磁碟，但開發指令常從 WSL 下達。這種混合環境有幾個必踩的坑，而且它們的共同特徵是——**程式碼完全正確，錯的是環境**。

**技術核心**：

| 陷阱 | 症狀 | 對策 |
|------|------|------|
| **npm 原生套件平台錯配** | 從 WSL 跑 `npm install`，依 Linux 解析 optionalDependencies，裝成 `*-linux-x64-gnu`，Windows 版消失，客戶端起不來 | **`npm install` 一律在 Windows 端跑**。純 JS 套件不受影響 |
| **WSL 連不到 Windows 的 127.0.0.1** | 明明伺服器在跑，WSL 卻 `ECONNREFUSED` | WSL2 有獨立網路命名空間，要用預設閘道 IP |
| **cmd 不吃 UTF-8 BOM** | `.bat` 第一行變成 `﻿@echo off` 而報錯 | 存成**無 BOM** 的 UTF-8 |
| **REM 註解裡的中文** | 冒出 `'x' is not recognized`，但功能其實正常 | cmd 在 `chcp 65001` 生效前預讀後續位元組。**註解一律用 ASCII**，中文只留在 `echo` 行 |
| **debug 版 exe 無法單獨執行** | 雙擊得到 `ERR_CONNECTION_REFUSED` | `tauri dev` 另起靜態伺服器供應前端，debug binary 內嵌的是那個 URL。要獨立執行檔就 `tauri build` |
| **AV 誤判新編譯的 Rust exe** | 防毒警告、可能隔離檔案 | 無簽章 + 全球唯一雜湊 + 連外行為 → 啟發式命中。把 `target` 與 `.cargo` 加入排除清單 |

**解決的痛點**：讓「為什麼在我機器上不能跑」有一份可查的清單，而不是每次重新推理。

**踩過的坑**：`npm` 平台錯配這一條**造成了第 3 次事故**。當時為了裝 jsdom 從 WSL 跑了 `npm install`，結果 Tauri 的 Windows 原生 binary 被 Linux 版覆蓋，客戶端直接起不來，錯誤訊息是：

```
Cannot find native binding. npm has a bug related to optional dependencies
```

這個訊息完全沒提到「你在錯的平台安裝」，只說「找不到原生綁定」。**環境問題的錯誤訊息通常最不友善**，因為工具作者沒預期到你會這樣用。

`REM` 中文那條也很典型：批次檔**功能完全正常**，只是輸出多了兩行看不懂的錯誤。這種「壞得不明顯」的問題最消耗信任——使用者會開始懷疑其他一切。

**優點 / 罩門**：把環境陷阱寫成清單的優點是**下次撞到能秒認**。罩門是清單會過期，而且它只涵蓋你踩過的。

**跨環境成本對照**：

| 做法 | 開發便利 | 環境風險 |
|------|---------|---------|
| 全在 Windows | 低（缺 Unix 工具） | 最低 |
| 全在 WSL | 高 | 原生套件、GUI、網路都有坑 |
| **混合**（本專案） | 最高 | **中，需明確規則** |

本專案的規則很簡單：**分析與測試在 WSL，安裝與建置在 Windows。**

**效益**：六條規則，把一整類「不是程式碼的錯」的問題變成可預防。

> 💡 君之一席話
> **最難除的 bug，往往不在你寫的那幾千行裡，而在「你以為到處都一樣」的那個假設裡。**

> 🔍 老手視角──真正的門道
> 混合環境的問題有一個共同結構：**某個工具用「當前平台」做了隱含決策**。npm 用當前平台選 optionalDependencies、cmd 用當前 codepage 讀檔案、WSL 用自己的網路命名空間解析 localhost——每一個單獨看都合理，組合起來就變成地雷。老手的應對不是記住所有陷阱，而是建立一條分界線：**「哪些指令必須在哪一側跑」，寫成規則貼在專案 README 的最上面**。本專案最後把這條寫進了啟動腳本的註解裡，讓下一個人在跑 `npm install` 之前就會看到警告。可落地的提醒：**任何跨環境專案，第一件事是畫出那條線，而不是等它咬你。**

---



# 第六篇　實作規格

> 這一篇的目的與前五篇不同。前五篇講「為什麼」，這一篇講**「照著做」**。
>
> 目標：一個從未看過本專案原始碼的實作者（含 LLM），只讀這一篇加上第二、三篇的協議規格，
> 就能寫出**行為等價**的客戶端，並通過附錄 C 的驗收清單。
>
> **凡本篇與程式碼不一致者，以程式碼為準，並回頭修正本篇。**

---

## 018　模組切分與依賴律

**標籤**：`#規格` `#模組` `#依賴方向` `#純函式` `#檔案清單`

### 018.1　檔案清單（含行數量級）

| 檔案 | 行數 | 職責 | 可否碰 DOM |
|------|------|------|-----------|
| `src/js/ansi.js` | ~233 | 行內樣式方言 → span 樹 | ❌ |
| `src/js/protocol.js` | ~574 | 行 → 事件物件 | ❌ |
| `src/js/dialects.js` | ~262 | 方言 profile 表 | ❌ |
| `src/js/store.js` | ~208 | 狀態容器 + pub/sub | ❌ |
| `src/js/prefs.js` | ~61 | localStorage 偏好 | ⚠️ 只碰 localStorage |
| `src/js/net.js` | ~242 | Transport 抽象（雙後端） | ⚠️ 只碰 WebSocket / IPC |
| `src/js/ui.js` | ~680 | DOM 元件工廠 | ✅ |
| `src/js/main.js` | ~847 | 組裝 + reducer + 登入狀態機 | ✅ |
| `src-tauri/src/main.rs` | ~55 | Tauri 指令註冊 | — |
| `src-tauri/src/mud.rs` | ~200 | TCP 連線管理 | — |
| `src-tauri/src/telnet.rs` | ~162 | IAC 剝除 | — |
| `bridge/server.mjs` | ~165 | WebSocket ↔ TCP + 靜態檔 | — |
| `bridge/telnet.mjs` | ~63 | IAC 剝除（`telnet.rs` 的 JS 版） | — |

### 018.2　依賴律（**強制**）

```
        main.js
       ╱   │   ╲
   net.js  │   ui.js
      │    │    │
      │  store.js
      │    ▲    │
      └────┴────┘
           │
      protocol.js ──▶ ansi.js ──▶ dialects.js
```

**四條不可違反的規則**：

1. `ansi.js`、`protocol.js`、`dialects.js`、`store.js` **不得引用 `document` / `window` / `localStorage`**
2. `ui.js` 只讀 `store`、只呼叫 `ctx.*`，**不得引用 `net.js` 或 `protocol.js`**
3. 沒有任何模組反向依賴 `main.js`
4. `protocol.js` 的輸出是**資料**，不是副作用

**驗收方式**：在 `node --test`（無 DOM 環境）中直接 `import` 前四個模組，不得拋錯。

---

## 019　`ansi.js` — 樣式方言解析

**標籤**：`#規格` `#ansi` `#純函式` `#狀態機`

### 匯出

```javascript
export const ESC = '';

export function parseStyled(raw, options?) → { spans, clearScreen }
export function stripStyles(raw) → string
export function classifyLink(url) → { kind, value }
```

### `parseStyled(raw, options)`

| 參數 | 型別 | 說明 |
|------|------|------|
| `raw` | `string` | 含 `ESC` 的文字。非字串或空字串回傳空結果 |
| `options.state` | `object?` | 跨呼叫延續的樣式狀態；不給則每次從乾淨狀態開始 |
| `options.dayMode` | `boolean?` | 白天模式色彩改寫 |

**回傳**：

```javascript
{
  spans: [{ text: string, style: Style }],
  clearScreen: boolean     // 遇到 [2J 時為 true
}
```

**Style 物件**（每個欄位預設 `null` / `false`）：

```javascript
{ fg, bg, bold, size, link, fullwidth }
```

### 演算法（**逐步照做**）

```
1. 若 raw 以 ESC + "[2;37;0m" 開頭（9 字元）→ 重置 style，raw 切掉前 9 字元
2. 以 ESC 切段
3. 第 0 段是純文字（在任何 ESC 之前）
4. 其餘每段呼叫 applySegment(seg, style)，就地更新 style，回傳剩餘文字
5. 若 style.fullwidth，文字先過 toFullWidth()
6. 每段產生一個 { text, style: {...style} }（**必須淺拷貝**，否則所有 span 共用同一個物件）
```

### `applySegment` 的判斷順序（**順序不可改**）

```
① seg 以 "[u" 開頭 → 找 "]"，取 slice(2, end)，剝除開頭冒號 → style.link
② seg 以 "[s" 開頭 → 同上，parseInt → style.size = +(30 / n).toFixed(3)
③ seg 以 "[2J" 開頭 → clearScreen = true，切掉 3 字元
④ seg 以 "[H"  開頭 → 切掉 2 字元（無視覺效果）
⑤ 其餘：找第一個 "m"
     - 找不到或不以 "[" 開頭 → 整段是文字
     - code === "[0m" 或以 ";0m" 結尾 → 重置全部
     - "[1m" → bold
     - "[9m" → fullwidth
     - 以 "[f#" 開頭 → fg = normalizeColor(...)
     - 以 "[b#" 開頭 → bg = normalizeColor(...)
     - 正則 /^\[(1;)?(\d{2})(;1)?m$/ → 查調色盤
     - 都不match → 忽略指令、保留文字（降級，不丟棄）
```

> ⚠️ **①②必須在⑤之前**。`[u:` 與 `[s:` 以 `]` 收尾，用「找第一個 m」會解錯。

### 調色盤（**逐值照抄**）

```javascript
FG    = ['#000000','#aa3300','#00bb00','#eeee00','#0000aa','#aa00aa','#00bbbb','#aaaaaa']
FG_HI = ['#000000','#ff3300','#88ff00','#ffff00','#0000ff','#ff00ff','#88ffff','#ffffff']
BG    = ['#222222','#aa0000','#00aa00','#aaaa00','#0000ff','#aa00aa','#00aaaa','#aaaaaa']
BG_HI = ['#000000','#ff0000','#00ff00','#ffff00','#0000ff','#ff00ff','#00ffff','#ffffff']
```

**白天模式改寫**：索引 `{2,3,6,7}`（即 32/33/36/37）一律改成索引 4（藍）。

### `normalizeColor(hex)`

| 輸入 | 輸出 | 說明 |
|------|------|------|
| `#RRGGBB` | 原樣 | |
| `#AARRGGBB`（8 位） | `#RRGGBBAA` | **ARGB → RGBA，alpha 從頭搬到尾** |
| `#RGB` | 原樣 | |
| 其他 | `null` | 呼叫端保留原值 |

### `toFullWidth(s)`

`0x20` → `U+3000`；`0x21`–`0x7E` → 加 `0xFEE0`；其餘不變。

### `classifyLink(url)`

| 前綴 | kind | value |
|------|------|-------|
| `cmds:` | `'cmd'` | 去前綴 |
| `pops:` | `'pop'` | 去前綴 |
| `voice:` | `'voice'` | 去前綴 |
| 其他 | `'external'` | 原樣 |

> `tel:` 與 `mailto:` 歸入 `external`，由呼叫端交給系統處理。

---

## 020　`protocol.js` — opcode 解析

**標籤**：`#規格` `#opcode` `#事件` `#純函式`

### 匯出

```javascript
export const SEP    // 分隔符常數
export const LOGIN  // 登入握手常數

export function parseLine(raw)                → { op, payload, resetStyles }
export function takeLayout(payload, defaults) → { layout, rest }
export function decodeLine(raw)               → Event
export function parseDialog(payload)          → Dialog
export function expandInputTemplate(tpl, v)   → string
export function expandDialogCommands(cmds, n) → string[]
export function isChallenge(line)             → boolean
export function buildLoginLine({...})         → string
export function buildCharLine({...})          → string
export function applyDialectFromChallenge(l)  → string
```

### `SEP` 常數（**逐值照抄**）

```javascript
{ REC:'$zj#', REC2:'$z2#', BR:'$br#', DIALOG:'$dh#', CMDSEQ:'$sock#',
  FIELD:':', SUB:'|', BAR:'║', NUM:'$N', KEEP:'$txt#' }
```

### `LOGIN` 常數

```javascript
{ CHALLENGES: ['ver1.0:', 'ver1.0,', 'version 1.0'],
  VERIFIED: '版本验证成功',
  OK: '0007', NEED_CHAR: '0008', BAD_NAME: '0009',
  SEP: '║', ANY: 'x' }
```

### `parseLine(raw)` — 變長 opcode（**關鍵**）

```
1. 非字串 → { op: null, payload: '' }
2. 若以 ESC+"[2;37;0m" 開頭 → resetStyles = true，切掉 9 字元
3. 長度 < 4 或首字元非 ESC → { op: null, payload: line }
4. three = line.slice(1,4)
   若 three 不match /^[0-9a-z]{3}$/i → { op: null, payload: line }
5. four = line.slice(1,5)
   若 four match /^[0-9a-z]{4}$/i 且 isKnownOpcode(four) 且 !isKnownOpcode(three)
       → { op: four, payload: line.slice(5) }
6. 否則 → { op: three, payload: line.slice(4) }
```

`isKnownOpcode(op)` = 在核心表 **或** 任一 profile 的擴充表中。

> ⚠️ 第 5 步的**兩個條件缺一不可**。少了 `!isKnownOpcode(three)`，`ESC004` + 內容「5 個人…」會被誤讀成 `ESC0045`。

### `takeLayout(payload, defaults)`

payload 以 `$` 開頭時，取到第一個 `#` 之間的字串，以 `,` 切成 4 個整數；非數字者用 `defaults` 對應位補。回傳 `{ layout: number[4], rest }`。

**各 opcode 的預設值**：

| opcode | defaults | 語意 |
|--------|----------|------|
| `008`/`009` | `[1,3,9,30]` | cols, widthDiv, heightDiv, fontDiv |
| `012` | `[0,0,22,35]` | cols(0=自動), 未用, heightDiv, fontDiv |
| `020` | `[1,2,8,25]` | cols, widthDiv, heightDiv, fontDiv |

`012` 的 `cols` 為 0 時，取 `ceil(bars.length / 2)`。

### `decodeLine(raw)` → 事件對照表（**完整**）

| opcode | 事件 `type` | 額外欄位 |
|--------|------------|---------|
| `000` | `conn.notice` | `text`, `reloginLook`（payload==='重连完毕'） |
| `900` | `conn.relocate` | `host`, `port` |
| `997`/`998` | `conn.multiline` | `value: false/true` |
| `999` | `conn.quit` | — |
| `014` | `conn.echo` | `cmd` |
| `002` | `room.title` | `text`, `resetStyles` |
| `004` | `room.desc` | `text` |
| `023` | `room.descToggle` | `hidden`（payload==='屏蔽描述'） |
| `003` | `room.exits` | `exits: [{dir, slot, label, cmd}]` |
| `903` | `room.exitRemove` | `dir`, `slot` |
| `913` | `room.exitClear` | — |
| `005` | `room.objects` | `objects: [{label, cmd}]` |
| `905` | `room.objectRemove` | `tag` |
| `022` | `room.objectBar` | `tag`, `a`, `b`, `max` |
| `021` | `ui.titleButtons` | `buttons: [{label, cmd}]` |
| `012` | `stat.bars` | `layout`, `bars` |
| `006` | `ui.quickButtons` | `buttons: [{slot, label, cmd}]` |
| `015` | `msg.toast` | `text` |
| `016` | `msg.combat` | `text` |
| `017` | `msg.combatClose` | — |
| `100` | `msg.chat` | `text` |
| `024` | `msg.float` | `text` |
| `001` | `overlay.prompt` | `text`, `template` |
| `007` | `overlay.detail` | `text` |
| `008`/`009` | `overlay.actions` | `column: 1/2`, `layout`, `items` |
| `010` | `overlay.dialog` | `dialog` |
| `011` | `overlay.map` | `text` |
| `013` | `overlay.pagedText` | `text` |
| `020` | `overlay.popMenu` | `layout`, `items` |
| `045` | `overlay.web` | `url` |
| *其他* | 查擴充表，見 §021 |
| *未知* | `msg.main` | `text`（**完整原文**）, `unknownOpcode` |

### 各 payload 的解析規則

**`003` 出口**：`dir:label[:cmd]` × `$zj#`。第 3 欄省略時 `cmd = dir`。
`slot` 由 `DIR_SLOT` 表查得，未列出者為 `null`（額外出口）。

```javascript
DIR_SLOT = {
  north:'n', northup:'n', northdown:'n',
  south:'s', southup:'s', southdown:'s',
  east:'e',  eastup:'e',  eastdown:'e',
  west:'w',  westup:'w',  westdown:'w',
  northwest:'nw', northeast:'ne', southwest:'sw', southeast:'se',
}
```

**`005` 物件**：`label:cmd` × `$zj#`，**只切第一個冒號**（指令含 `/` 與 `#`）。payload 長度 ≤ 4 視為空清單。

**`006` 按鈕**：先把 `$br#` 換成 `\n`，再 `slot:label:cmd` × `$zj#`。slot ∈ `bs`, `b1`–`b17`。

**`008`/`009` 動作**：`主標題[|副標題]:指令` × `$zj#`。
- 指令含 `ESC020` → `popup` = 其後內容
- 指令含 `$txt#` → `keepOpen: true`

**`012` 屬性條**：`║` 分隔；每筆 `標籤:數值:顏色[:指令]`。數值三種形態：

| 形態 | mode | 說明 |
|------|------|------|
| `a/b/max` 且 max>0 | `'dual'` | 雙層條 |
| `a/max` 且 max>0 | `'single'` | 單層條，b=max |
| 其他 | `'text'` | 不畫條，`text` = 原字串 |

**`020` 彈出選單**：`label|cmd` × **`$z2#`**（不是 `$zj#`）。

**`022` 血條**：`tag$zj#a:b:max`。

**`010` 對話框**（`parseDialog`）：payload 以 `$dh#` 切塊，逐塊看前 5 字元：

| 前綴 | 動作 |
|------|------|
| `ok11.` | 推入 `okCmds` |
| `no11.` | 設 `cancelCmd` |
| `numb.` | `needNumber = true`（**長度等於 5 也要接受**） |
| 其他 | 內容塊，以 `$br#` 切行 |

內容行：

| 前綴 | block |
|------|-------|
| `$exp#` | `{ kind:'exp', text }` |
| `$god#` | `{ kind:'money', text }` |
| `$obj#` | `{ kind:'item', tag, image, tier }`（`tag,image,品階` 以 `,` 切） |
| `#RRGGBB` | `{ kind:'text', color, text }` |
| 其他 | `{ kind:'text', color:null, text }` |

品階對照：`1`白 `2`綠 `3`藍 `4`紫 `5`金 `6`紅 `7`灰，其他 `default`。

### 兩個佔位符（**不可混用**）

```javascript
expandInputTemplate(tpl, v)   // ESC001：含 $txt# 則替換，否則 tpl + ' ' + v
expandDialogCommands(cmds, n) // ESC010：$N 替換成 n，再以 $sock# 拆成多條
```

### 登入輔助

```javascript
isChallenge(line)   // 前綴命中 LOGIN.CHALLENGES 任一
buildLoginLine({ id, password, email, secret='byname666', fields=4 })
  // fields===3 → id║pw║secret
  // 否則        → id║pw║secret║email
buildCharLine({ sex='男', avatar='', name, fields=3 })
  // fields===2 → sex║name
  // 否則        → sex║avatar║name
```

> 💡 君之一席話
> **一份能被 1:1 還原的規格，不是把程式碼翻譯成中文，而是把「為什麼是這個順序」也寫進去——順序才是真正的規格。**

> 🔍 老手視角──真正的門道
> 寫可還原規格時，最常被漏掉的是**判斷順序**與**預設值**。本篇 `applySegment` 那五步的順序、`takeLayout` 各 opcode 的不同 defaults，都是實作者最容易自己「合理化」成別的樣子的地方——而一旦順序錯了，測試會通過大半、只在特定輸入上崩。老手交付規格時會刻意在每個順序敏感處加上「⚠️ 順序不可改」與**一個會失敗的反例**，因為反例比正例更能鎖死行為。可落地的做法：規格裡每一條「必須先做 X 再做 Y」，都要附一句「否則 Z 會被誤判成 W」。

---



## 021　`dialects.js` — 方言 profile 表

**標籤**：`#規格` `#方言` `#profile` `#撞號` `#查表`

### 匯出

```javascript
export const KIND          // 17 種泛型結構
export const PROFILES      // { classic, dmjh, zymud }
export const PANEL_TITLE   // panel 名 → 中文標題
export function setDialect(name) → string
export function getDialect() → string
export function detectDialect(challengeLine) → string
export function lookupExtended(op) → def | null
export function extendedCount() → number
export function totalOpcodeCoverage() → number
export function conflictingOpcodes() → [{ op, defs }]
```

### `KIND`（**逐值照抄**）

```javascript
TEXT:'text'  LIST:'list'  ACTIONS:'actions'  BARS:'bars'
ENTITY_ADD:'entity.add'  ENTITY_DEL:'entity.del'
MESSAGE:'message'  TITLE:'title'  CLOSE:'close'
// 指游 ZY 的「客戶端能力」類
CLEAR_MAIN:'clearMain'  RELOGIN:'relogin'  NETSTAT:'netstat'
HINT:'hint'  DAMAGE:'damage'  STORY:'story'  VIBRATE:'vibrate'  IGNORE:'ignore'
```

### profile 結構

```javascript
PROFILES = {
  classic: { label: '經典 zjmud',        ext: {} },
  dmjh:    { label: '大梦江湖（新協議）',  ext: DMJH },   // 73 筆
  zymud:   { label: '指游 MUD（ZY）',     ext: ZYMUD },  // 20 筆
}
```

每一筆的形狀：

```javascript
'511': { name:'XYKILL', kind:KIND.ENTITY_ADD, panel:'combat', side:'enemy' }
'515': { name:'XYKILLMIAO', kind:KIND.MESSAGE, channel:'combat' }
```

| 欄位 | 必填 | 說明 |
|------|------|------|
| `name` | ✅ | 原始巨集名，除錯用 |
| `kind` | ✅ | 決定用哪個解析器 |
| `panel` | `MESSAGE`/能力類以外必填 | 去哪個面板 |
| `slot` | 選填 | 面板內的欄位；缺省為 `'body'` |
| `side` | `ENTITY_*` 必填 | `'ally'` / `'enemy'` |
| `channel` | `MESSAGE` 必填 | `'chat'` / `'combat'` / 其他→toast |

### `detectDialect(line)`（**方言判別**）

```
以 'version 1.0' 開頭 → 'zymud'
以 'ver1.0'      開頭 → 'dmjh'
其他                  → 維持現值
```

### `lookupExtended(op)`（**撞號處理**）

```
1. 目前 profile 的 ext 有 op → 回傳
2. 否則掃所有 profile：
     恰好只有一個 profile 有 op → 回傳（跨方言相容）
     有兩個以上（撞號）        → 回傳 null（嚴格以目前 profile 為準）
```

> ⚠️ 第 2 步的「恰好一個」是關鍵。放寬成「取第一個」會讓 602–605 的行為取決於物件鍵順序。

### 已知撞號（**必須維持隔離**）

| opcode | dmjh | zymud |
|--------|------|-------|
| `602` | `DMZHUYET` | `ZYCHARHP` |
| `603` | `DMZHUYETY` | `ZYFIGHTBTN` |
| `604` | `DMZHUJMQH` | `ZYHANDLERBTN` |
| `605` | `DMZHUJIU` | `ZYCLEARSCREEN` |

`conflictingOpcodes()` 必須恰好回傳這四個。**多出來代表新方言撞號了。**

### 擴充事件的產出（`decodeExtended`）

| kind | 事件 `type` | 額外欄位 |
|------|------------|---------|
| `TITLE` | `panel.title` | `text` |
| `TEXT` | `panel.text` | `text` |
| `LIST` | `panel.list` | `items` |
| `ACTIONS` | `panel.actions` | `layout`, `items` |
| `BARS` | `panel.bars` | `layout`, `bars` |
| `ENTITY_ADD` | `panel.entityAdd` | `side`, `id`, `label`, `bar` |
| `ENTITY_DEL` | `panel.entityDel` | `side`, `id` |
| `CLOSE` | `panel.close` | — |
| `MESSAGE` | `msg.chat`/`msg.combat`/`msg.toast` | `text` |
| `CLEAR_MAIN` | `ui.clearMain` | — |
| `RELOGIN` | `conn.relogin` | `delayMs` |
| `NETSTAT` | `ui.netStat` | `enabled`（`關閉/关闭/off` → false） |
| `HINT` | `ui.hint` | `items: [{selector, text}]` |
| `DAMAGE` | `msg.float` | `target`, `text`, `color`, `durationMs`（預設 1900） |
| `STORY` | `overlay.story` / `overlay.storyClose` | `text`, `speedMs`（預設 80）, `background` |
| `VIBRATE` | `ui.vibrate` | `durationMs`（預設 200） |
| `IGNORE` | `ui.ignored` | — |

所有擴充事件都帶 `opcode`、`macro`、`panel`、`slot` 四個共同欄位。

**`ENTITY_ADD` 的 payload**：`id$zj#名字$zj#a:b:max`。
`bar` 僅在數值 ≥2 個且最後一個 >0 時建立，否則 `null`。

---

## 022　`store.js` — 狀態容器

**標籤**：`#規格` `#狀態` `#pubsub` `#通知傳播`

### 匯出

```javascript
export const DEFAULT_LIMITS = { main:500, chat:500, sys:500, combat:200 }
export function createStore(limits = DEFAULT_LIMITS) → StoreAPI
```

### `StoreAPI`

```javascript
{ get(path), set(path, value), patch(path, partial), sub(path, cb),
  pushMessage(channel, msg), clearMessages(channel),
  resetRoom(), reset(),
  extSet(panel, patch), extSlot(panel, slot, entry),
  extEntity(panel, side, entity), extEntityRemove(panel, side, id), extClose(panel),
  get state() }
```

### 初始狀態（**完整結構**）

```javascript
{
  conn:  { state:'IDLE', host:null, port:null, retries:0, nextRetryMs:0,
           multiline:true, lastError:null },
  room:  { title:'', titleCmd:null, desc:'', descHidden:false,
           descForcedHidden:false, exits:[], titleButtons:[], objects:[] },
  stats: { bars:[], layout:null },
  msgs:  { main:[], chat:[], sys:[], combat:[] },
  target:{ id:null, name:null },
  overlay:{ kind:null, detail:'', promptTemplate:null, needNumber:false,
            actions1:null, actions2:null, dialog:null, map:'',
            paged:'', pagedPage:0, story:null, web:'', popMenu:null },
  quick: { slots:{}, showCustom:false },
  ext:   { panels:{}, active:null },
  ui:    { theme:'night', fontScale:1, combatVisible:false,
           chatTab:'chat', netStatVisible:false },
}
```

`conn.state` ∈ `IDLE | CONNECTING | OPEN | RECONNECTING | FAILED`

### `notify(path)` — 雙向傳播（**關鍵**）

```
① 往上：通知 path 與所有祖先 path 的訂閱者
     'room.title' → 通知 'room.title'，再通知 'room'
② 往下：通知所有以 path + '.' 為前綴的訂閱者
     'room' → 通知 'room.title'、'room.exits'、'room.objects'…
③ 最後通知 '*' 的訂閱者
```

> ⚠️ **少了②會有實際 bug**：`resetRoom()` 只 `notify('room')`，訂閱 `room.titleButtons` 的元件收不到通知，換房間後舊按鈕殘留在畫面上。

### `set(path, value)`

值未變（`===`）時**不通知**。

### `pushMessage(channel, msg)`

推入後套用 `limits[channel]`，超過時從頭 `splice` 移除最舊，然後 `notify('msgs.'+channel)`。

### `resetRoom()`（`ESC002` 的副作用）

清空 `room.exits`、`room.objects`、`room.titleButtons`、`room.titleCmd`、
`ui.combatVisible=false`、`msgs.combat` 清空、`quick.showCustom=false`，
然後 notify `room` / `ui.combatVisible` / `msgs.combat` / `quick`。

### `ext*` 系列

面板結構：

```javascript
panels[name] = { title:'', slots:{}, entities:{ ally:[], enemy:[] } }
```

- `extSlot(panel, slot, entry)`：`slot` 缺省為 `'body'`
- `extEntity(panel, side, entity)`：**依 `id` 去重**，已存在則覆寫
- 任何 ext 寫入時，若 `ext.active` 為空則設為該 panel
- `extClose(panel)`：刪除該 panel；若它是 active，改指向剩下的第一個或 `null`
- 全部以 `notify('ext')` 通知

---

## 023　`main.js` — reducer 與登入狀態機

**標籤**：`#規格` `#reducer` `#登入` `#不變式`

### 資料流（**單向，不可逆**）

```
TCP 一行 → loginIntercept(raw) → decodeLine(raw) → applyEvent(ev) → store → UI
使用者操作 → ctx.* → sendCommand → net.send
```

### **不變式（可測）**

> `decodeLine` 能產生的每一種事件 `type`，`applyEvent` 都必須有對應 `case`。

違反時的症狀是「伺服器叫客戶端做某件事，客戶端什麼都沒做，且不報錯」。
附錄 C 有一條測試用正則掃兩個檔案來守這條。

### `applyEvent` 對照表（**完整**）

| 事件 | 動作 |
|------|------|
| `conn.notice` | `reloginLook` 為真 → `sendCommand('l')`；再依 `text` 前 4 字元判斷 `0007`（登入成功，關閉兩個 modal、更新帳號標籤）／`0008`（開建角 modal）／`0009`（建角錯誤） |
| `conn.multiline` | `store.set('conn.multiline', value)` |
| `conn.echo` | `sendCommand(cmd)` |
| `conn.relocate` | toast → `net.close()` → `net.connect(host, port)` |
| `conn.quit` | `confirm()` 後關閉視窗（**不可無條件 exit**） |
| `conn.relogin` | toast → `delayMs` 後 `net.close()` + 重置登入 + 開登入 modal |
| `room.title` | **先 `store.resetRoom()`**，再 `set('room.title')` |
| `room.desc` / `room.descToggle` | 設對應欄位 |
| `room.exits` / `exitRemove` / `exitClear` | 設 / 過濾 / 清空 `room.exits` |
| `room.objects` / `objectRemove` / `objectBar` | 設 / 過濾 / map 上 `bar` |
| `stat.bars` | `set('stats', { bars, layout })` |
| `ui.titleButtons` | `set('room.titleButtons')` |
| `ui.quickButtons` | `bs` 槽 → `room.titleCmd`；其餘寫入 `quick.slots`；有非 bs 槽時 `quick.showCustom = true` |
| `msg.main/chat/toast/combat` | `pushMessage`；`toast` 另外顯示橫幅並寫入 `sys`；`combat` 另外開戰鬥面板 |
| `msg.combatClose` | 關面板 + 清 `msgs.combat` |
| `msg.float` | 飄字動畫 |
| `overlay.*` | `store.patch('overlay', {...})`，`kind` 決定顯示哪一個 |
| `overlay.web` | **不內嵌**，改用外部瀏覽器開啟（CSP） |
| `panel.*` | 呼叫對應 `store.ext*` |
| `ui.clearMain` | `clearMessages('main')` |
| `ui.vibrate` | `navigator.vibrate?.(durationMs)`，包 try/catch |
| `ui.netStat` | `set('ui.netStatVisible', enabled)` |
| `ui.hint` | 每筆 `text` 推入 `sys` 頻道 |
| `ui.ignored` | 無動作（官方標為未實現） |

### `sendCommand(cmd)`（**唯一送出路徑**）

```javascript
if (cmd == null || cmd === '') return;
const line = store.get('conn.multiline') ? cmd : cmd.split('\n').join(';');
net.send(line);
```

### 登入狀態機

```javascript
login = { stage:'idle', id:'', password:'', email:'', manual:false,
          failures:0, dialect:'dmjh', charFields:3, loginFields:4 }
```

`loginIntercept(raw)` 在 `decodeLine` **之前**執行，處理三件事：

| 條件 | 動作 |
|------|------|
| `isChallenge(raw)` | 判別方言、設 `charFields`（zymud=2，否則 3）；有帳密→`doAutoLogin()`，否則開登入 modal |
| 含「账号在别处登录」 | `net.stopReconnect()` → **先開 modal 再寫錯誤訊息** |
| 含「花在连线进入手续的时间太久」 | `failures++`；≥3 → 停止重連 + 開 modal + 錯誤訊息 |

> ⚠️ `openLoginModal()` 會清空錯誤列，所以**必須先開 modal，再呼叫 `showLoginError()`**。順序反了訊息會消失。

### `doAutoLogin()`（**兩行連發**）

```javascript
net.send(LOGIN.ANY);                                   // 'x'
net.send(buildLoginLine({ ...login, fields: login.loginFields }));
```

兩行必須在**同一個 tick** 送出。伺服器對登入有逾時，中間不可有人工延遲。

### ★ 自動登入的帳密來源（**安全關鍵**）

```javascript
if (rmEl.checked) {          // 只有勾了「記住」才讀
  login.id = prefs.accountId;
  login.password = prefs.accountPw;
} else {
  login.id = '';
  login.password = '';
}
```

> ⚠️ **絕不可從 DOM 輸入框讀值**。輸入框帶 `autocomplete="username"/"current-password"`，
> 瀏覽器密碼管理員會自動填入——那是「幫使用者少打字」，不是「使用者同意自動登入」。
> 讀 DOM 會讓全新的瀏覽器設定檔也跳過登入表單直接進遊戲。

### `mount()`（**逐項隔離**）

```javascript
const mountErrors = [];
function step(name, fn) {
  try { fn(); } catch (err) { mountErrors.push(`${name}: ${err.message}`); }
}
```

掛載順序（**關鍵項目必須各自獨立保護**）：

```
Toast → FloatText → ConnBadge → RoomHeader → RoomDesc → EntityList
→ ExitPad → StatBars → QuickBar×2 → MessageList×4
→ InteractSheet → PopMenu → DialogModal → SimpleOverlay×2
→ CommandInput → ChatTabs → CombatPane → Settings → ConnectForm
→ LoginForms → ExtPanels
```

最後：還原偏好、套用主題與字級、若有 `mountErrors` 顯示在連線面板。

另需安裝全域 `error` 與 `unhandledrejection` 攔截，把未捕捉例外寫進連線面板錯誤列。

> 💡 君之一席話
> **一份規格能不能被還原，看的不是它列了多少 API，而是它有沒有把「這裡順序反了會怎樣」寫出來——正確的程式碼有很多種寫法，錯誤的方式往往只有那幾種。**

> 🔍 老手視角──真正的門道
> 本節那條「解碼器產生的事件，reducer 必須全部處理」的不變式，是這份規格裡最有價值的一行——因為它**可以被機器檢查**。老手寫規格時會刻意區分兩種條款：**敘述性的**（「應該分層」）與**可驗證的**（「用正則掃這兩個檔案，差集必須為空」）。前者靠自律，後者靠 CI。本專案的 ZY 方言就是在補完之後才發現有 7 種事件被解出來卻沒人處理——**如果那條不變式早一天存在，這個缺口根本活不到被發現**。可落地的做法：規格裡每寫一條「必須」，就問一句「這條能不能寫成測試？」——不能的話，它多半只是願望。

---



## 024　`ui.js` — 15 個元件工廠

**標籤**：`#規格` `#UI` `#無框架` `#差異更新`

### 024.1　共同形狀

**每個元件都是一個工廠函式**，簽名一律是：

```javascript
export function ComponentName(root, { store, ctx, ...opts })
```

- `root` — 已存在於 `index.html` 的掛載節點
- `store` — 狀態容器（元件**只讀**）
- `ctx` — 回呼包（元件**只呼叫**，不自己送封包）
- 回傳值多半是 `undefined`；`MessageList` 回傳 `{ destroy }`

**三條鐵律**（違反其一，這一層就不再可測、不再可換）：

1. 元件**不解析協議**。它拿到的 `store` 裡已經是解析好的結構。
2. 元件**不直接呼叫 `net.send`**，一律走 `ctx.send(cmd)`。
3. 元件在建構時訂閱 `store.sub(path, render)`，之後只做**差異更新**，不整棵重建。

### 024.2　`ctx` 回呼契約（**15 個鍵**）

`main.js` 建立一個 `ctx` 物件傳給所有元件。少一個，對應的互動就靜默失效：

| 回呼 | 何時被呼叫 |
|------|-----------|
| `send(cmd)` | 任何按鈕／連結要送指令 |
| `onLink(link)` | 點到樣式方言的 `[u:…]` 連結 |
| `selectTarget(obj)` | 點現場物件 |
| `requestMap()` | 點方向盤中心 |
| `runAction(item)` | 點互動表單的動作 |
| `submitPrompt(text)` | 互動表單的文字輸入送出 |
| `confirmDialog(num)` / `cancelDialog()` | 對話框確定／取消 |
| `closeOverlay()` | 關閉任何疊層 |
| `editQuickSlot(slot)` | 長按或右鍵快捷鍵 |
| `setExtPanel(name)` / `closeExtPanel()` | 擴充面板切頁／關閉 |
| `retryNow()` | 連線徽章的「重新連線」 |
| `savePref(partial)` | 元件層改動需要持久化的偏好 |
| `dayMode` | **不是函式**，是布林值，供 `parseStyled` 決定日間配色 |

### 024.3　DOM 小工具（3 個）

```javascript
el(tag, attrs = {}, children = []) → HTMLElement
```

`attrs` 的特殊鍵：`class` → `className`；`text` → `textContent`；`html` → `innerHTML`；
`on*` 且值為函式 → `addEventListener(小寫事件名)`；`style` 且為物件 → `Object.assign`。
其餘走 `setAttribute`，值為 `true` 時設成空字串。**值為 `null`／`false` 的鍵直接跳過。**

```javascript
clear(node)             // while (firstChild) removeChild
scrollToBottom(node)    // ★ node.scrollTop = node.scrollHeight，包在 try/catch
```

> **★ `scrollToBottom` 不可改用 `Element.scrollTo()`。**
> jsdom 與部分 WebView 沒有這個方法，會拋 `TypeError`；而它是在 `mount()` 期間被呼叫的，
> 一拋就中斷整個掛載流程，後面的 `bindConnectForm()` 從未執行——
> 症狀是「畫面看起來正常，但『連線』鈕完全沒有反應」。這是本專案第一次事故的成因。

### 024.4　`renderStyled(raw, ctx)` — 純資料與 DOM 之間唯一的橋

```javascript
→ { frag: DocumentFragment, clearScreen: boolean }
```

演算法：

1. `parseStyled(raw, { dayMode: ctx?.dayMode })` 取得 `spans`
2. 每個 span 建一個節點：有 `style.link` 用 `<a class="lnk">`，否則用 `<span>`
3. **文字一律走 `textContent`**（★ 絕不 `innerHTML`——指游方言可帶任意標籤）
4. 樣式逐項套：`color` / `backgroundColor` / `fontWeight:'700'` / `fontSize: ${size}em`
5. 連結節點另設 `dataset.linkKind`、`dataset.linkValue`、`href='#'`，
   `click` 時 `preventDefault()` 後呼叫 `ctx.onLink(link)`

### 024.5　元件清單（**15 個工廠，`mount()` 共掛 20 個實例**）

| 元件 | 掛載點 | 訂閱路徑 | 產出 |
|------|--------|---------|------|
| `MessageList` | `msg-main` / `msg-combat` / `msg-chat` / `msg-sys` | `msgs.<channel>` | `div.msg` 逐則 append |
| `RoomHeader` | `room-header` | `room.title` `room.titleCmd` `room.titleButtons` `room.descHidden` `room.descForcedHidden` | `button.room-title` + `div.title-buttons > button.chip` + `button.icon-btn`（摺疊鈕） |
| `RoomDesc` | `room-desc` | `room.desc` `room.descHidden` `room.descForcedHidden` | 樣式化文字；兩個 hidden 任一為真就隱藏 |
| `EntityList` | `entity-list` | `room.objects` | `button.entity` > `span.entity-name` + 選配 `span.entity-bar > i.bar-b, i.bar-a` |
| `ExitPad` | `exit-pad-host` | `room.exits` | 3×3 `button.exit-cell`；中心為 `button.exit-center`（`⌂`，觸發 `requestMap()`）；不在九宮格的出口進 `div.exit-extra > button.chip` |
| `StatBars` | `stat-bars` | `stats` | 有 `text` → `div.stat.stat-text`；否則 `div.stat > div.stat-track > i.bar-b, i.bar-a` + `span.stat-label`。有 `cmd` 時加 `.clickable` |
| `QuickBar` ×2 | `quick-main` / `quick-bottom` | `quick.slots` | `button.quick`（空的加 `.quick-empty`）；右鍵或長按 **550 ms** → `editQuickSlot` |
| `InteractSheet` | `overlay-interact` | `overlay` | `sheet-detail` + `sheet-num`（文字輸入 + 確定）+ 兩欄 `action-col > button.action` |
| `PopMenu` | `overlay-popmenu` | `overlay` | `div.pop-grid > button.pop-item`；點擊後送指令並自動關閉 |
| `DialogModal` | `overlay-dialog` | `overlay` | `div.dialog-box`；區塊型別對應 `dialog-items` / `dialog-exp` / `dialog-money` / `dialog-line`；需要數量時插 `input.num-input[type=number][min=1]` |
| `SimpleOverlay` ×2 | `overlay-map`（等寬）/ `overlay-paged` | `overlay` | 純文字 + `button.icon-btn.sheet-close` |
| `Toast` | `toast-host` | — | `ctx` 驅動，`div.toast` |
| `FloatText` | `float-host` | — | `ctx` 驅動，`div.float-text` |
| `ConnBadge` | `conn-badge` | `conn` | `i.dot` + `span.conn-label` + `button.btn.btn-sm`（重新連線） |
| `ExtPanels` | `ext-panels` | `ext` | `ext-tabs > button.tab`（當前頁加 `.active`）+ `ext-body` |

### 024.6　`MessageList` 的差異更新演算法（**最容易寫錯的一段**）

```javascript
let rendered = 0;
function render(list) {
  if (rendered > list.length) { clear(root); rendered = 0; }      // ① store 裁切或清空過
  while (root.childElementCount > list.length) root.removeChild(root.firstElementChild);
  for (let i = rendered; i < list.length; i++) {                  // ② 只畫新增的
    const line = el('div', { class: 'msg' });
    const { frag, clearScreen } = renderStyled(list[i], ctx);
    if (clearScreen) { clear(root); rendered = 0; }               // ③ ESC[2J
    line.append(frag);
    root.append(line);
  }
  rendered = list.length;
  if (limit && root.childElementCount > limit) { … }              // ④ DOM 上限
  scrollToBottom(root.parentElement);                             // ⑤ 捲的是外層 scroll-pane
}
```

四個必須同時成立的條件：**只 append 新的**（500 則時整串重繪會明顯卡頓）、
**裁切時同步移除舊節點**、**`ESC[2J` 要能清空**、**捲動對象是 `root.parentElement`**。

### 024.7　`ConnBadge` 的狀態文案

```javascript
const CONN_LABEL = {
  idle: '未連線', connecting: '連線中…', open: '已連線',
  reconnecting: '重連中…', failed: '連線失敗',
};
```

`i.dot` 以 `data-state` 屬性上色；`button` 只在 `failed` / `reconnecting` 時顯示。

---

## 025　`prefs.js` — 本地偏好

**標籤**：`#規格` `#持久化` `#安全`

### 匯出

```javascript
export function loadPrefs() → Prefs        // 有記憶體快取
export function savePrefs(partial) → Prefs // 淺層合併後寫回
export function pushHistory(cmd) → string[]
```

### 儲存鍵與預設值（**完整**）

```javascript
const KEY = 'zjmud.prefs.v1';

const DEFAULTS = {
  theme: 'night',            // night | day | mud
  fontScale: 1,
  descHidden: false,
  lastHost: '127.0.0.1',
  lastPort: 5001,            // ★ UTF-8 埠。5003 是 GBK，連了會亂碼
  autoConnect: true,
  rememberAccount: false,    // ★ 預設 false —— 帳密是 opt-in 才記
  accountId: '',
  accountPw: '',
  accountEmail: '',
  quickSlots: {},            // slot → { label, cmd }
  history: [],
  historyLimit: 200,
};
```

`loadPrefs()` 讀不到或 JSON 壞掉時，**靜默回退到 `DEFAULTS`**，不可拋例外——
它在 `mount()` 最前面被呼叫，一拋就整個畫面起不來。

`pushHistory(cmd)`：空字串不記；**與上一筆相同不重複記**；超過 `historyLimit` 從頭截斷。

### ★ 帳密的儲存立場

密碼以明文存在 `localStorage`。這是刻意的取捨，理由必須寫進交接文件：

- ZJMUD 協議本身就是**明文傳輸**，本地加密不會提高真實安全性，只會製造安全感
- 但仍設成 **opt-in**：`rememberAccount` 預設 `false`，使用者不勾就完全不寫

**與此配套的一條規則在 §023**：自動登入的帳密**只能來自 `prefs`**，
永遠不可從 DOM 輸入框讀取——否則瀏覽器的密碼管理員自動填入的內容，
會在使用者毫不知情的情況下被送到伺服器。這是本專案第五次事故。

> 💡 君之一席話
> **UI 層最好的規格不是「畫面長什麼樣」，而是「它被允許呼叫誰」——把 15 個回呼列清楚，這一層就再也不會偷偷去解協議。**

> 🔍 老手視角──真正的門道
> 這一章值得注意的是 024.2 那張 `ctx` 表：它其實是一份**能力清單**（capability list）。無框架的 DOM 程式碼最常見的腐化路徑，是某個按鈕「順手」直接 import 了 `net.js` 去送封包，於是 UI 層開始知道協議、測試開始需要真實連線、換後端時漏改一處。把回呼集中成一個明確物件並寫進規格，等於在架構層設了一道**編譯器管不到、但 code review 管得到**的邊界。可落地的做法：任何被多個模組共用的 `ctx`／`deps` 物件，都在文件裡列成表格並註明「少一個會怎樣」——因為缺一個回呼的症狀永遠是「點了沒反應」，而那是最難從程式碼推回原因的錯誤形態。

---



## 026　DOM 契約 — 50 個 id 一個都不能少

**標籤**：`#規格` `#DOM` `#契約` `#CSS` `#響應式`

### 026.1　`index.html` 的 50 個 id（**完整**）

`main.js` 以 `const $ = (id) => document.getElementById(id)` 取用它們。
**少一個就有一個元件靜默失效**——而且不會拋錯，只會「點了沒反應」。

| 區域 | id |
|------|-----|
| 頂列 | `conn-badge` `theme-select` `font-range` `who-label` `logout-btn` `disconnect-btn` |
| 連線面板 | `connect-panel` `host-input` `port-input` `autoconnect-check` `connect-btn` `connect-error` `connect-env` |
| 房間 | `room-header` `room-desc` |
| 左欄 | `entity-list` `exit-pad-host` |
| 中欄 | `msg-main` `combat-pane` `msg-combat` `cmd-input` `cmd-send` `quick-main` `quick-bottom` |
| 右欄 | `stat-bars` `msg-chat` `msg-sys` |
| 疊層 | `overlay-interact` `overlay-map` `overlay-paged` `overlay-dialog` `overlay-popmenu` `float-host` `toast-host` |
| 擴充面板 | `ext-panels` `ext-tabs` `ext-body` `ext-close` |
| 登入 | `login-modal` `login-id` `login-pw` `login-email` `login-remember` `login-error` `login-submit` `login-manual` |
| 建角 | `char-modal` `char-name` `char-submit` `char-error` |

另有 `data-chat-tab="chat"` 與 `data-chat-tab="sys"` 兩個屬性選擇器。

> **驗收**：一支測試比對 `index.html` 的 id 集合與 `main.js` 的 `getElementById` 集合，
> 兩者差集必須雙向為空。本專案實測 **50/50 完全吻合**：沒有多餘的 id，也沒有取用不存在的 id。

### 026.2　結構骨架

```html
<body>
  <header class="topbar">…</header>

  <div id="connect-panel" class="connect-panel">
    <div class="connect-card">…</div>
  </div>

  <main class="layout">
    <aside class="col col-left">   現場 / 出口 </aside>
    <section class="col col-center">
      <div id="room-header" class="room-header"></div>
      <div id="room-desc" class="room-desc"></div>
      <div class="center-body">        ← position:relative，疊層的定位容器
        <div class="scroll-pane"><div id="msg-main" class="msg-list"></div></div>
        <section id="combat-pane" class="panel combat-pane" hidden>…</section>
        <div id="overlay-interact" class="overlay sheet" hidden></div>
        <div id="overlay-map"      class="overlay overlay-plain" hidden></div>
        <div id="overlay-paged"    class="overlay overlay-plain" hidden></div>
        <div id="float-host" class="float-host"></div>
      </div>
      <div class="composer">…</div>
      <div id="quick-main"   class="quick-row"></div>
      <div id="quick-bottom" class="quick-row quick-row-bottom"></div>
    </section>
    <aside class="col col-right">  狀態 / 聊天 </aside>
  </main>

  <div id="ext-panels" class="ext-panels" hidden>…</div>
  <div id="login-modal" class="modal-backdrop" hidden>…</div>
  <div id="char-modal"  class="modal-backdrop" hidden>…</div>
  <div id="overlay-dialog"   class="modal-backdrop" hidden></div>
  <div id="overlay-popmenu"  class="modal-backdrop" hidden></div>
  <div id="toast-host" class="toast-host"></div>

  <script type="module" src="js/main.js"></script>
</body>
```

### 026.3　★ `[hidden]` 規則（**最前面，帶 `!important`**）

```css
[hidden] { display: none !important; }
```

**這一行必須放在樣式表最前面。**

`[hidden]` 的 `display:none` 來自瀏覽器 UA 樣式表，優先級最低。本專案有大量元件設了 `display`：

```css
.connect-panel  { display: grid; }
.modal-backdrop { display: grid; }
.overlay        { display: flex; }
.entity-list    { display: flex; }
.exit-cell      { display: flex; }
.panel          { display: flex; }
```

少了那一行，`element.hidden = true` **完全沒有視覺效果**——面板永遠不消失，症狀是「按鈕好像沒反應」，但實際上每次都成功了。

### 026.4　設計 token（`tokens.css`）

```
節奏  --r-sm/md/lg  --sp-1..5
字體  --font-ui  --font-mono  --font-scale  --fs-sm/base/lg  --leading
動效  --ease  --dur-fast  --dur-mid
色彩  --bg-0..3  --line  --line-soft  --fg-0..2
      --accent  --accent-soft  --accent-ink
      --hp  --mp  --good  --warn  --bad
其他  --shadow  --radius
```

三套主題以 `:root[data-theme="night|day|mud"]` 切換，**只換 token，元件樣式一行不動**。

| 主題 | 特徵 |
|------|------|
| `night`（預設） | 暖褐深底 `#12100e`，強調色琥珀 `#d9a441` |
| `day` | 淺底 `#efe9dd`；伺服器色碼另有「綠黃青白→藍」改寫（見 §019） |
| `mud` | 純黑 + `#aaaaaa`，`--radius: 0`，`--font-ui` 改為等寬 |

### 026.5　版面

| 斷點 | 版面 |
|------|------|
| ≥ 900px | `grid-template-columns: 280px minmax(0,1fr) 320px` |
| < 900px | 單欄堆疊；左欄改橫向捲動；右欄 `order:3`；動作列改直向 |
| < 560px | 頂列換行；字級縮小 |

**無障礙**：所有動效包在 `@media (prefers-reduced-motion: reduce)` 中停用；`:focus-visible` 有 2px 強調色外框。

---

## 027　`net.js` — Transport 抽象

**標籤**：`#規格` `#傳輸` `#雙後端` `#重連` `#狀態機`

### 匯出

```javascript
export function isTauri() → boolean          // globalThis.__TAURI__ != null
export function environment() → 'tauri' | 'browser'
export function createTransport({ onLine, onState }) → TransportAPI
```

### `TransportAPI`

```javascript
{ connect(host, port), send(line), close(), retryNow(), stopReconnect(),
  get state(), get backend() }
```

### 後端介面（兩個實作必須一致）

```javascript
{ name, available() → boolean, unavailableReason: string,
  open(host, port) → Promise, send(line) → Promise, close() → Promise }
```

| 後端 | 選用條件 | 通道 |
|------|---------|------|
| `tauriBackend` | `isTauri()` | `__TAURI__.core.invoke` + `event.listen` |
| `websocketBackend` | 否則 | `new WebSocket(`${scheme}//${location.host}/mud`)` |

`scheme` 依 `location.protocol` 為 `https:` 決定 `wss:` 或 `ws:`。

### 連線狀態機

```
IDLE ──connect()──▶ CONNECTING ──成功──▶ OPEN
                          │失敗              │斷線
                          ▼                  ▼
                    RECONNECTING ◀───────────┘
                       │  │
                 成功──┘  └──重試上限──▶ FAILED
```

**指數退避**：`[1000, 2000, 4000, 8000, 16000, 30000]` 毫秒，最多 **8** 次。

`stopReconnect()` 把 `retries` 設為上限但**不斷線**——供登入逾時止血用。

### `onState` 回呼參數

```javascript
{ state, host, port, retries, nextRetryMs?, lastError? }
```

### Tauri 後端細節

```javascript
await t.event.listen('mud://line',  e => onLine(String(e.payload ?? '')));
await t.event.listen('mud://state', e => { /* closed|error → onClosed */ });
await t.core.invoke('mud_connect', { host, port });
```

**每次 `open()` 必須先解除舊監聽**，否則重連後同一行會被處理多次。

### WebSocket 後端細節

第一則訊息是連線請求：

```javascript
ws.send(JSON.stringify({ host, port }));
```

之後每一則訊息 = 一行指令。

**伺服器→客戶端的訊息有兩類**：

| 形態 | 判斷 | 處理 |
|------|------|------|
| 狀態 | 以 `{"__state"` 開頭 | `JSON.parse(...).__state` |
| 遊戲資料 | 其他 | 直接 `onLine(text)` |

狀態物件：`{ state: 'open'|'closed'|'error', host, port, reason?, message? }`

---

## 028　Rust 端與橋接

**標籤**：`#規格` `#Rust` `#Tauri` `#telnet` `#橋接` `#安全`

### 028.1　Tauri 指令（4 個）

```rust
#[tauri::command] async fn mud_connect(app, state, host: String, port: u16) -> Result<(), String>
#[tauri::command] async fn mud_send(state, line: String)                    -> Result<(), String>
#[tauri::command] async fn mud_disconnect(state)                            -> Result<(), String>
#[tauri::command] async fn mud_is_connected(state)                          -> Result<bool, String>
```

### 028.2　事件

```rust
pub const EVENT_LINE:  &str = "mud://line";   // payload: &str（一行，已去 \r\n）
pub const EVENT_STATE: &str = "mud://state";  // payload: ConnState
```

```rust
#[serde(tag = "state", rename_all = "lowercase")]
enum ConnState { Open{host,port}, Closed{reason}, Error{message} }
```

### 028.3　★ `capabilities/default.json`（**缺了 `event.listen` 會被 ACL 擋**）

```json
{
  "identifier": "default",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:allow-listen",
                  "core:event:allow-unlisten", "core:window:allow-close"]
}
```

`windows` 陣列要對得上 `tauri.conf.json` 裡視窗的 `label`（本專案明確設為 `"main"`）。

### 028.4　★ `tauri.conf.json` 的兩個必要設定

```json
"app": {
  "withGlobalTauri": true,          // 純 HTML 前端靠 window.__TAURI__，預設是 false
  "security": { "csp": "… connect-src 'self' ipc: http://ipc.localhost" }
}
```

沒有 `withGlobalTauri`，前端會判定「不在 Tauri 環境」而完全無法連線。

### 028.5　讀取管線（**不可用 `read_line`**）

```
socket bytes
   → telnet::filter()          剝 IAC；WILL→DONT、DO→WONT 全部拒絕
   → 以 b'\n' 切行             去掉行尾 \r
   → String::from_utf8_lossy   壞位元組降級成替換字元
   → emit("mud://line")
```

`MAX_LINE_BYTES = 64 * 1024`。兩處保護缺一不可：
**① 切出來的行超過上限 → `truncate`**；
**② 緩衝區沒有換行卻已超過上限 → `clear()`**（否則異常伺服器可以只送不換行就把記憶體吃光）。

`app.emit` 回傳 `Err` 代表前端已關閉，此時**直接 return 結束讀取任務**。

寫入走 `mpsc::Sender<Vec<u8>>`（**不是 `String`**，因為協商回覆是二進位），單一 writer 任務。

### 028.6　`telnet::filter(buf)` → `{ data, reply }`

```
buf[i] != IAC(255)     → data
IAC IAC                → data 推一個 0xFF
IAC SB … IAC SE        → 整段丟棄
IAC WILL(251) opt      → reply: IAC DONT(254) opt
IAC DO(253) opt        → reply: IAC WONT(252) opt
IAC WONT/DONT opt      → 吃掉，不回覆
其他兩位元組命令        → 吃掉
序列不完整（尾端截斷）  → 丟棄，不可外洩成資料
```

**驗收資料**（實機抓到的開場，兩份實作都用它當測試輸入）：

```
輸入：ff fd 18 ff fd 1f ff fd 27 ff fb 56 ff fb 46 ff fb 2a 0d 0a "ver1.0:" 0d 0a
data ：\r\nver1.0:\r\n
reply：IAC WONT 18, IAC WONT 1f, IAC WONT 27, IAC DONT 56, IAC DONT 46, IAC DONT 2a
```

> `0x56` = MCCP2。**必須拒絕**，答應了之後整條連線會變成 zlib 串流。

### 028.7　`bridge/server.mjs`

| 參數 | 預設 | 說明 |
|------|------|------|
| `--port` | 8080 | HTTP + WebSocket |
| `--mud-host` | 127.0.0.1 | 轉發目標 |
| `--mud-port` | 5001 | 同上 |
| `--bind` | 0.0.0.0 | 監聽位址 |
| `--allow-any` | 關 | 允許前端指定其他位址 |

**三個職責**：供應 `src/` 靜態檔、接 WebSocket、代開 TCP 並做 IAC 剝除與分行。
**它不解析 ZJMUD 協議**——opcode 解析全在前端，與 Tauri 版共用同一份 `protocol.js`。

**兩條安全規則**：

```javascript
// ① 防目錄穿越：解析後必須仍在 WEB_ROOT 底下
const file = path.resolve(WEB_ROOT, '.' + rel);
if (!file.startsWith(WEB_ROOT)) return res.writeHead(403).end('Forbidden');

// ② 不是開放代理：預設只允許連到啟動時設定的目標
if (!ALLOW_ANY && want.host && (want.host !== MUD_HOST || Number(want.port) !== MUD_PORT)) {
  sendState({ state: 'error',
    message: `此橋接只允許連到 ${MUD_HOST}:${MUD_PORT}（要放寬請以 --allow-any 啟動）` });
  ws.close();
}
```

兩條都有專門的測試守著。

> 💡 君之一席話
> **雙形態的代價不是寫兩份，而是要證明兩份行為一致——所以那 42 個實機位元組，必須同時是 Rust 測試和 JS 測試的輸入。**

> 🔍 老手視角──真正的門道
> 這一節最容易被低估的是 026.3 和 026.4 那兩個 JSON 設定：它們不在任何原始碼裡，卻能讓一個編譯零錯誤、測試全綠的程式**完全無法連線**。老手交接跨框架專案時，會刻意把「設定檔裡的關鍵開關」單獨列一節，而不是散落在架構說明中——因為設定檔的錯誤不會出現在編譯期、不會出現在單元測試，只會在執行期以一句語焉不詳的錯誤現身。本專案的 Tauri ACL 問題就是這樣：`event.listen not allowed`，而 `event.listen` 明明存在。可落地的做法：任何「不寫就會壞、但編譯器不管」的設定，都值得在規格裡加一個 ★ 標記與一句「沒有它會怎樣」。

---



# 第七篇　交付

## 029　4 GB 的中間檔 — Rust 的體積從哪裡來

**標籤**：`#Rust` `#建置產物` `#交付` `#磁碟` `#AI輔助開發`
**證據等級**：🟢 本專案實測

**起源**：專案要交給別人用，先量一下有多大。

```
webclient/          3.7 GB
├── src-tauri/      3.6 GB   ← 幾乎全部在這
│   └── target/
│       ├── debug/    2.5 GB
│       └── release/  1.2 GB
├── node_modules/    40 MB
└── src/            172 KB   ← 這才是我們寫的東西
```

**我們親手寫的前端 172 KB，建置中間檔 3.6 GB——比例是 1 : 21,000。**

`cargo clean` 的回報更直白：

```
Removed 8417 files, 4.0GiB total
```

同一份功能如果用 C 寫，`.o` 檔加執行檔大概幾 MB。差距不是倍數，是量級。

**技術核心**：體積來自四個機制，而且它們會相乘。

| 機制 | C 的做法 | Rust 的做法 | 後果 |
|------|---------|------------|------|
| **依賴處理** | 連結系統既有的 `.so` / `.dll` | 把每個 crate 的**原始碼抓下來、在你的機器上從頭編成靜態庫** | 一個 Tauri 專案動輒編譯數百個 crate |
| **多版本共存** | 一個系統一份庫 | 依賴樹裡若有 `tokio 1.x` 與 `tokio 0.2`，**兩份都編** | 體積直接加倍 |
| **偵錯符號** | `-g` 產生的符號相對精簡 | debug profile 預設 `debug = 2`（完整），為了精準 backtrace | 幾十 KB 的 C 執行檔，在 Rust debug 下輕鬆破 50 MB |
| **增量編譯快取** | 沒有等價物 | `target/debug/incremental/` 隨修改次數線性增長 | 改越多次、越肥 |

**踩過的坑：AI 輔助開發是這件事的放大器。**

本專案整段開發都在 AI 代理的協助下進行，而代理的行為模式**剛好踩滿四個機制**：

1. **高頻編譯**。代理每改一次就想確認「還編得過嗎」，於是 `cargo check` / `cargo build` 跑得比人類頻繁得多，增量快取快速堆積。
2. **傾向引入現成 crate**。C 裡要手寫的東西，Rust 生態多半有現成的；代理為了快速完成任務會直接引入，而一個 crate 背後常牽出數十個間接依賴。
3. **測試會各自產生執行檔**。Rust 的 `#[test]` 每個測試單元編出獨立的二進位，是隱形的空間殺手。
4. **debug 與 release 並存**。開發用 `tauri dev`（debug），交付用 `tauri build`（release），兩份都留著——本專案就是 2.5 GB + 1.2 GB。

> 這一節的因果要講清楚：**Rust 不是「寫出來的程式很肥」，是「編出來的過程很肥」**。
> 本專案交付的 release 執行檔只有 **3.2 MB**——比那 4 GB 中間檔小了 1,250 倍。
> 混淆這兩件事，會得出「Rust 產物很大」這種完全錯誤的結論。

**效益**：知道體積來自「過程」而非「產物」，清理策略就很明確——**清過程，留產物**。

---

## 030　交付前的清理 — 清過程，留產物

**標籤**：`#交付` `#cargo-clean` `#gitignore` `#工程衛生`
**證據等級**：🟢 本專案實測（3.9 GB → 335 MB）

**起源**：「我要給其他人用。」

**技術核心**：依交付對象分兩種做法。

### 交付「原始碼」——給要自己編譯的人

```bash
cargo clean
```

**直接刪掉整個 `target/`**，專案回到只剩 `.rs` 與 `Cargo.toml` 的狀態。
用 `cargo clean` 而不是手動 `rm -rf target/`，是因為它認得 workspace 佈局與
`CACHEDIR.TAG`，也會回報實際刪了什麼——手動刪只會得到一片沉默。

### 交付「執行檔」——給要直接跑的人

**順序很重要，而且很容易做錯：**

```bash
cargo build --release        # ① 一定要 release，絕不交付 debug 版
cp target/release/<name>.exe  <專案目錄之外>   # ② 先把產物移出去
cargo clean                  # ③ 再清（clean 會把 exe 一起刪掉）
```

**②③ 的順序是本節唯一的重點。** `cargo clean` 不會替你保留執行檔，
先清再找就來不及了——本專案的做法是先備份 exe、`cargo clean`、再放回
`target/release/`，因為啟動腳本檢查的就是那個路徑。

> ⚠️ 清完務必**實際執行一次**那顆 exe。
> 「檔案還在」不等於「還能跑」——這正是本書第五篇七次事故的共同教訓：
> 編譯成功 ≠ 能用，檔案存在 ≠ 能執行。

### 讓它從一開始就不要肥

三個層次，由輕到重：

**① `.gitignore`**——最低限度，至少別讓 4 GB 進版控：

```gitignore
webclient/src-tauri/target/
!webclient/src-tauri/target/release/zjmud-client.exe   # 保留交付物
node_modules/
```

**② `Cargo.toml` 減肥設定**：

```toml
[profile.dev]
debug = 1                       # 預設 2（完整符號）→ 1（僅行號）

[profile.dev.package."*"]
opt-level = 3                   # 第三方套件直接用優化模式編，減少中間產物

[profile.release]
opt-level = "z"                 # 以體積為最優先
lto = true                      # 連結期優化，移除未使用的程式碼
codegen-units = 1               # 優化做得更徹底
panic = "abort"                 # 不產生 backtrace 相關資訊
strip = true                    # 移除偵錯符號
```

> **證據等級 🔴 推論**：上面這組 `[profile.release]` 設定**本專案尚未實測**。
> 本專案目前的 release exe 是 3.2 MB（未套用任何減肥設定）。
> 一般說法是可以縮到數百 KB，但**在你自己的專案上量過之前，不要引用那個數字**——
> 實際效果高度依賴依賴樹大小，而 Tauri 專案內嵌了整個 WebView 綁定，未必縮得那麼多。
> 驗證方式很簡單：加上設定 → `cargo build --release` → 比對前後 exe 大小。

**③ 把 `target/` 移出專案目錄**——治本：

```toml
# .cargo/config.toml
[build]
target-dir = "C:\\Users\\<你>\\AppData\\Local\\Temp\\rust-target"
```

這樣不管背景編譯多頻繁，**專案目錄永遠是輕的**，要打包時也不必先 `cargo clean`。
代價是換機器／清系統暫存後要全部重編一次。

### 與 AI 代理協作時的節流時機

什麼時候可以要求代理停止高頻編譯？三個判準：

| 時機 | 判準 | 指令 |
|------|------|------|
| **語法定型** | 型別、生命週期、所有權錯誤都清完，`cargo check` 過得去，接下來只調演算法與命名 | 「接下來專注邏輯重構，不要執行 `cargo build`／`cargo check`，等我要求再統一驗證」 |
| **寫文件階段** | 功能完成，在補註解／README／重新命名 | 同上 |
| **微調測試** | 只在改測試的輸入輸出數值 | 「只跑 `cargo test -- <測試名>`，不要全量 build」 |

`cargo check` 比 `cargo build` 輕非常多（不做程式碼產生），**在「只想知道語法對不對」的
階段應該是預設值**。本專案後期驗證 Rust 改動時用的就是 `cargo check`，1.18 秒完成。

**踩過的坑**：清理不是只有 Rust。本專案清完 `target/` 之後才發現，
伺服器的 `debug.log` 有 **8.6 MB、51,574 行，其中 51,253 行是自己造成的重連風暴警告**
（見 §015 第七次事故）。清理的範圍要涵蓋「**你的程式在別人系統上留下的東西**」，
不只是「你的建置目錄」。

**效益**：3.9 GB → 335 MB，可執行檔保留、測試 177 條全過、書籍網頁照常開。

---

## 031　WebAssembly 的邊界 — 能搬什麼，不能搬什麼

**標籤**：`#WebAssembly` `#瀏覽器沙箱` `#程式碼共用` `#架構邊界`
**證據等級**：🟡 依原始碼相依性分析（尚未實際編譯驗證）

**起源**：一個很自然的問題——**既然桌面版的核心是 Rust，瀏覽器版能不能也用同一份 Rust，編成 WebAssembly 就好？**

這個問題的答案不是「可以」或「不可以」，而是**逐個模組看它碰不碰得到 I/O**。

**技術核心**：把 Rust 端兩個模組攤開看依賴。

| 模組 | `use` 宣告 | 能上瀏覽器 WASM？ |
|------|-----------|-----------------|
| `telnet.rs`（162 行） | **一個都沒有** | ✅ 可以，而且幾乎不用改 |
| `mud.rs`（200 行） | `tokio::net::TcpStream`、`tokio::io`、`tauri::AppHandle` | ❌ 不可能 |

判準只有一條：**純計算可以搬，I/O 不行。**

`telnet.rs` 的簽名是 `filter(buf: &[u8]) -> Filtered { data, reply }`——
輸入位元組、輸出位元組，沒有 socket、沒有時間、沒有檔案、沒有全域狀態。
這種形狀的程式碼是 WASM 的甜蜜點。

`mud.rs` 則是**架構上不可能**：瀏覽器沙箱沒有 raw TCP socket API，
這是瀏覽器的限制而**不是 Rust 或 WASM 的限制**。把 `mud.rs` 編成 WASM 在技術上會失敗
（wasm32 目標下 `tokio::net` 不可用），就算勉強編過，執行時也沒有 socket 可以開。

> **所以本書第四篇的 WebSocket 橋接不是臨時方案，是架構必然。**
> 無論前端用 JS、WASM 還是任何未來技術，只要協議是原生 TCP、
> 而執行環境是瀏覽器，**中間就一定要有一個能開 socket 的程序**。
> 這一點不會因為換技術而改變。

**那搬 `telnet.rs` 有什麼好處？** 有一個很具體的：**消除重複實作**。

目前 IAC 剝除邏輯存在**兩份**：

```
src-tauri/src/telnet.rs   162 行   Rust，桌面版用
bridge/telnet.mjs          63 行   JavaScript，瀏覽器版用
```

兩份必須行為完全一致，否則同一個伺服器在兩個版本會表現不同。
本書 §028.6 因此把那 42 個實機位元組同時當成 Rust 測試與 JS 測試的輸入——
**用測試來釘住兩份實作的等價性**。若 `telnet.rs` 編成 WASM 給橋接用，
這份重複與它的等價性負擔就一起消失了。

**踩過的坑（誠實揭露）**：這一節是**依相依性靜態分析得出的結論，尚未實際編譯驗證**。
要升級到 🟢 需要：

```bash
rustup target add wasm32-unknown-unknown
cargo build --target wasm32-unknown-unknown   # telnet.rs 要先拆成獨立 crate
```

在那之前，本節對 `telnet.rs` 的判斷應視為**高信心推論**，不是實測結果。
本書的規矩是證據等級要標清楚——**沒量過就不能標 🟢**，即使推論看起來再穩。

**成本評估（決定要不要做的關鍵）**：省下的是 63 行 JavaScript 與一份等價性負擔；
付出的是 wasm32 工具鏈、一個額外的 crate 邊界、橋接的 WASM 載入流程，
以及讓「純 HTML 無建置步驟」的前端原則出現第一個例外。
**以本專案 63 行的規模，這筆帳不划算**——記錄它是為了說清楚邊界在哪裡，
而不是建議照做。若 `telnet.rs` 有 1,600 行而非 162 行，結論會相反。

> 💡 君之一席話
> **「能不能編成 WebAssembly」從來不是語言問題，是 I/O 問題——純計算搬得走，碰硬體與網路的搬不走；先看 `use` 清單，再談可行性。**

> 🔍 老手視角──真正的門道
> 這一篇兩個主題看似無關（磁碟體積、WASM 可行性），其實共用同一種思考習慣：**先量，再談**。談體積時，「Rust 專案很肥」是沒有資訊量的抱怨；「產物 3.2 MB、過程 4.0 GB、比例 1:1250」才能導出正確的行動（清過程、留產物）。談 WASM 時，「Rust 可以編 WASM」是正確但無用的事實；「`telnet.rs` 零依賴所以可以、`mud.rs` 用了 `tokio::net` 所以不行」才是能拿來做決策的答案。老手在面對「這個能不能做」的問題時，第一個動作幾乎都不是回答，而是**把問題換成一個可量測的形式**——體積換成「哪個目錄多大」，可行性換成「相依清單裡有沒有 I/O」。可落地的做法：任何技術可行性評估，先寫下你要量什麼、以及什麼結果會讓你改變答案；量不出來的可行性評估，本質上是猜測換了一個比較體面的說法。

---

## 032　前提被拿掉的那一天 — 伺服器搬進分頁

**標籤**：`#WebAssembly` `#架構前提` `#重入` `#競態` `#閉環驗證`
**證據等級**：🟢 實測（收藏中 17 個 mudlib 全部打包並實跑，17 個都走完註冊→建角→進世界）

**起源**：§031 的結論寫得斬釘截鐵——

> 只要協議是原生 TCP、而執行環境是瀏覽器，中間就一定要有一個能開 socket 的程序。

三個月後，FluffOS 官方發了 WebAssembly build。**那句話沒有錯，但它的前提被拿掉了。**

**技術核心**：§031 的推理鏈是「瀏覽器沒有 raw socket → 所以要有代理」。
成立的隱含前提是**伺服器在遠端**。WASM 版把整台 driver 搬進分頁之後：

```
橋接版   瀏覽器 ── WebSocket ──> bridge/server.mjs ── TCP ──> driver.exe
WASM 版  瀏覽器 ── 函式呼叫 ──> driver（同一個分頁、同一份記憶體）
```

**沒有網路，所以不需要能開 socket 的程序。** 介面只有五個匯出函式與兩個回呼：

| 匯出 | 作用 |
|------|------|
| `fluffos_boot(config)` | 啟動（mudlib 已在 MEMFS 裡） |
| `fluffos_tick(now)` | 心跳。原生版由 libevent 驅動，這裡由頁面 `setInterval` 手動泵 |
| `fluffos_connect()` | 開一條虛擬連線，回傳 id |
| `fluffos_input(id, bytes)` | client → server |
| `Module.fluffos.onOutput(id, bytes)` | server → client |

**而 `mud.rs` 依然編不成 wasm32**（`tokio::net` 在該 target 不存在）——§031 對它的判斷沒有變。
變的是**它不再需要存在**：它的位置被 driver 自己的 `src/wasm/comm_wasm.cc` 佔走了。

**踩過的坑：三個只有「伺服器在分頁裡」才會出現的失效模式。**

三個症狀完全相同——**送出去的東西伺服器毫無反應**——但成因一個比一個難看見。

| # | 症狀 | 真因 | 怎麼找到的 |
|---|------|------|-----------|
| 1 | 收到握手行後回覆，伺服器沒反應 | 在 `onOutput` 回呼裡直接呼叫 `fluffos_input`，等於在 driver 的輸出路徑中途重入它 | 官方參考前端的註解寫著 “sends must be queued” |
| 2 | 排隊之後**還是**沒反應 | 誤判成時序問題，先加了「撥號後 grace 期」，再加了「輸出靜置窗」——**兩個都無效** | 加了才發現無效，這兩層後來留著當縱深防禦，但不是解 |
| 3 | 排隊、延遲都試過還是沒反應 | `send()` 用 `connId === null` 判斷「還沒連上」。而 `fluffos_connect()` **會在自己回傳之前**就同步回呼 logon 的輸出——此時 `connId = ccall(...)` 那行賦值還沒發生，`connId` 仍是 null，於是整包被靜默丟棄 | 在 `rawSend` 印一行追蹤，發現**它一次都沒被呼叫** |

第 3 個是真因，而前兩次的修法都建立在「一定是時間問題」這個錯誤假設上。

**分辨這兩者的方法只有一個：不要猜東西為什麼沒送到，先確認它到底有沒有被送出。**
`rawSend` 的呼叫次數是 0 還是 1，這件事不需要推理，加一行 `console.error` 就有答案；
而在拿到那個答案之前，任何關於「延遲多久才對」的討論都是在錯誤的問題上做優化。

> 本書 §015 的七次事故裡，第 1 次（`scrollTo` 不存在打斷 `mount()`）與這次是同一種：
> **靜默失敗**。差別在於當時花了四次交付才發現，這次花了三次嘗試——
> 因為這次一開始就有一個能自己跑完的驗證迴路（下一段）。

**解決的痛點**：能不能上線不再是「感覺應該可以」。

打包管線的每一個 mud 都會被 `tools/boot-test.mjs` 真的跑一次：

```
撥號 → 等 ver1.0: 握手 → 送帳號密碼 → 收 ESC000 0008（要求建角）
     → 送 性別║頭像║暱稱 → 收 ESC000 0007 → 送 look → 統計收到哪些 opcode
```

走完才給 `playable`，只開得起來給 `limited`，連握手都沒有給 `noboot`。
**分級是量出來的，不是標上去的**，所以清單不會隨時間變成謊言——
driver 升版或 mudlib 改壞，下一次建置就會自己降級。

跑完整批、把每一次失敗都追到真因之後：**17 個收錄，17 個都 playable**
（這個數字曾經停在 14，最後三個的收尾見下方）。
而它們不是「本來就能跑」——每一個都是被下面這些**通用**修正救回來的，
每一條修正都對應一次實測失敗，而不是預防性猜測：

| 修正 | 症狀 | 真因 |
|------|------|------|
| 設定檔名 | `boot exit(-1)` | 檔名從 `config.ini` 到 `config.hell` 都有，得從啟動批次檔反推 |
| `#define static` | master.c syntax error | FluffOS 移除了 `static` 修飾詞；補一行不夠，要照上游用 `__SENSIBLE_MODIFIERS__` **互斥**分岔 |
| `external_port_1` | 連上但編碼分支跑錯 | wasm 用第一個 external_port 當來源埠 |
| `is_chinese()` | 建角卡在「請您用中文取名字」 | 逐位元組的中文判斷（GBK `%2`、UTF-8 `%3`）在碼點索引下全數失效 |
| 日誌子目錄 | `fluffos_connect` 回 -1 | `log_file("static/logon")` 找不到目錄 → logon 失敗 → driver 踢掉連線 |
| config 絕對路徑 | `Bad mudlib directory: 'c:\hell'` | 設定裡寫著原營運者機器上的路徑 |
| `is_killing(ob)` | 登入後生不出身體 | 宣告 `string` 卻傳 object；舊 MudOS 放行、FluffOS 不放行 |
| `valid_write` | 建角時 `Denied write permission` | 找不到 SECURITY_D 就一律拒絕，而建角正好發生在它載入之前（同檔的 `valid_read` 是 fail-open——**同一個檔案裡兩個相反的預設**） |

**最後 3 個是另一類問題，值得單獨說**：它們不是相容性壞了，是**封存本身不完整**——
nt 家族的來源快照少了 simul_efun 缺件，其中 `nt7` 甚至整個玩家層與世界都不在
（`clone/user/` 是空目錄、沒有 `d/`）。這種缺口硬修等於替別人重寫 mudlib。
最後的解法不是修，是**補件**：用同一款遊戲的**另一份快照**把缺的部分接上去
（kungfu 檔名 99.7% 同名，可以逐檔比對；補件明細記在 `libs/nt7/mud.json`）。

> **「修不好」與「資料不全」是兩種完全不同的失敗**，混在一起會讓人一直在錯的方向上加班。
> 分辨的方法很直接：**看缺的是行為還是檔案**。行為不對是相容性問題，
> 檔案不在是封存問題——後者再怎麼讀原始碼也讀不出來。

**最貴的一次錯誤不在上表**：把說明寫成 config 的**行尾註解**，而 FluffOS 的設定檔
沒有行尾註解——值取的是冒號後整行。三個原本已經修好的 lib 因此變回 `noboot`，
而且修正腳本每重跑一次就再疊一層註解。**自動修正工具本身必須是冪等的**，
否則它會變成另一種損壞來源。

**優點 / 罩門**：優點是整條交付鏈沒有任何伺服器端程式碼，靜態託管即可，
而且每個訪客拿到的是一份全新的世界，沒有濫用面。
罩門三個，而且都不是 bug 是**本質**：沒有多人（同分頁可多開連線，跨使用者不行）、
重整即失（MEMFS 不落地）、每個 mud 要下載 20–60 MB。
**它是展示品與教材，不是正式服**——把這件事寫在顯眼處，比事後解釋便宜。

> 💡 君之一席話
> **一個結論的壽命，取決於它的前提而不是它的推理**——推理再嚴密，前提被換掉那天它就過期了；所以寫下結論時，把前提一起寫下來，比把推理寫得更漂亮更有價值。

> 🔍 老手視角──真正的門道
> §031 那句話當時是對的，今天依然對，只是**不再適用**。老手與新手在這裡的差別不是誰預見了 WASM driver 的出現——沒有人預見得到——而是結論被寫成什麼形狀：寫成「瀏覽器版一定要有橋接」的人，會在新前提出現時整段推翻重來，而且很可能不服氣；寫成「協議是遠端 TCP 時一定要有橋接」的人，只要把前提一比對就知道該換路了。可落地的做法：任何架構結論都加一句「這個結論在什麼情況下會失效」。寫得出來，代表你真的理解為什麼；寫不出來，那句結論其實是一個信念。**而信念不會因為證據改變，只會因為挫折改變——那通常太晚了。**

---



# 第八篇　源流：連線的另一端是什麼

> 前七篇都站在**客戶端**這一側：位元組進來、opcode 解析、畫面更新。
> 這一篇把鏡頭轉過去——**送出那些位元組的東西，到底是什麼？**
>
> 答案是一台 1989 年設計、2026 年還在維護的虛擬機。
> 本書那 116 個 opcode 不是它的協議，它甚至不知道 opcode 存在——
> 這件事聽起來很怪，但它解釋了本書前面一半的謎題：
> 為什麼協議可以那麼隨便、為什麼會撞號、為什麼換了 driver 協議還在。

---

## 033　1989 年的那次分家 — driver 與 mudlib 為什麼要拆開

**標籤**：`#LPMud` `#架構史` `#虛擬機` `#關注點分離` `#熱更新`
**證據等級**：🟡 上游一手史料（`fluffos/docs/archive/Credits.LPmud`）＋ 🟡 官方文件

**起源**：1989 年 4 月，瑞典查爾摩斯理工學院的 Lars Pensjö 寫了一個 MUD 伺服器。
他遇到的問題非常具體：**他想讓別人來幫他蓋世界，但他不想讓別人碰 C 原始碼。**

那個年代的 MUD 伺服器（DikuMUD、AberMUD）都是把遊戲內容編進 C 裡的。
加一間房間要改 `.c`、重編譯、踢掉所有線上玩家、重啟。
一個十人團隊要協作寫一個世界，這條路走不通。

Lars 的解法是把伺服器切成兩半，這個切法後來定義了整個 LPMud 家族：

```
┌──────────────────────────────────────────────┐
│ mudlib —— 用 LPC 寫的遊戲框架                  │
│   房間、NPC、武器、戰鬥、登入流程、指令          │
│   ★ 沒有編譯步驟，改完存檔就能載入新版本         │
├──────────────────────────────────────────────┤
│ driver —— 用 C 寫的執行引擎                    │
│   LPC 編譯器 + 位元組碼虛擬機 + 網路 + efun     │
│   ★ 幾年才換一次版本                           │
└──────────────────────────────────────────────┘
```

**技術核心**：這不是「加一層腳本」，而是**把整個遊戲搬到腳本層**。
driver 裡沒有任何一行知道什麼是「劍」或「客棧」；
mudlib 裡沒有任何一行知道什麼是 socket 或 `malloc`。

`Credits.LPmud` 這份 30 行的檔案，是 LPMud 現存最早的一手史料，
它記下了幾個至今還在跑的決定：

| 一手紀錄 | 今天的樣子 |
|---------|-----------|
| 「Lennart Augustsson **說服我**去實作一個虛擬堆疊機的編譯器。他也實作了內建的前處理器」 | §037 描述的雙堆疊機，架構沒變過 |
| 「shadow 這個想法是 John S. Price 從 Team Cthulhu **轉述**給我的」 | `shadow()` efun，本專案的 mudlib 有整個 `shadow/` 目錄 |
| 「regexp 套件是 Henry Spencer 做的」 | 後來被 libpcre 取代 |
| 「Sean T Barrett 做了 `smalloc.c`」 | 後來被 jemalloc 取代，WASM 版又換回 dlmalloc（§041） |

注意第一條的動詞是「說服」。**LPMud 一開始並不打算做成虛擬機**——
是有人說服作者多做這一步，而正是這一步讓 mudlib 得以熱更新，
讓「driver / mudlib 分家」從一個檔案組織方式變成一個執行模型。

**解決的痛點**：三個，而且每一個在今天都還是問題：

1. **協作**——寫世界的人只要會 LPC，不必碰 C、不必有編譯環境。
2. **熱更新**——`update /d/city/inn` 就換掉一間客棧，線上玩家無感。
3. **隔離**——mudlib 寫爆了是 LPC runtime error，driver 不會死。

第 3 點是本專案親眼驗證過的：§032 那批 mudlib 相容性問題裡，
`/adm/daemons/kuafu` 因為呼叫不存在的 `socket_create` 而**編譯失敗**，
driver 照常完成開機——只是那個物件不存在而已（`libs/lpmudname/NOTES.md`）。
換成把遊戲編進 C 的架構，同一個錯誤是連結期失敗，整台伺服器起不來。

**踩過的坑**：這條分界線的代價，本書第三篇整篇都在寫。
**driver 不認識 mudlib 的協議**，所以 mudlib 想加一個 opcode，不必跟任何人報備——
於是三個世代的方言各自加碼，撞了 4 個號（§010）。
分家帶來的自由度，同時也是「沒有註冊中心」這件事的來源。§043 會把這條因果講完。

**設計對照**：

| 系統 | 遊戲內容住在哪 | 改一間房要做什麼 | 誰能改 |
|------|--------------|----------------|--------|
| **LPMud（1989）** | LPC 檔案，driver 之外 | 存檔 → `update` | 會 LPC 的人 |
| DikuMUD（1990） | C 原始碼 + 資料檔 | 改 `.are` 資料檔或重編譯 | 有原始碼與編譯環境的人 |
| TinyMUD／MOO | 資料庫內的物件，可線上編輯 | 在遊戲裡打指令 | 有權限的玩家 |
| 現代遊戲伺服器 | 通常也是「引擎 + 腳本」 | 熱載腳本 | 腳本作者 |

第四列才是重點：**這個分法後來變成了業界常識**。
Unreal 的 Blueprint、Unity 的 C#、各種伺服器的 Lua 層，都是同一個形狀。
LPMud 在 1989 年就把它做完了，而且做到了「腳本層 = 整個遊戲」的極端程度。

**優點 / 罩門**：優點是三十七年後，一份 1990 年代的 mudlib 還能在 2026 年的 driver 上跑起來——
本專案收錄的 17 個 mud 就是證據。罩門是**相容性責任全落在 driver 這一側**：
mudlib 從來不需要宣告它依賴哪個版本的什麼，driver 只能盡量不要動到既有行為。
§035 會看到這筆帳最後是怎麼算的。

**效益**：對本專案而言，這個分家決定了**逆向的著力點在哪裡**。
ZJMUD 的協議不在 driver 裡、不在任何 RFC 裡，
它在 `include/zjmud.h` 這個 mudlib 檔案裡——一份 LPC 的標頭檔。
本書第二篇之所以能拿到 27 個 opcode 的中文註解，正是因為 1989 年那次分家
把「遊戲的協議」留在了**可讀的腳本層**，而不是編進 5.5 MB 的執行檔裡。

> 💡 君之一席話
> **把系統切成「幾年才換一次的引擎」與「每天都在改的內容」，不是為了好看——是為了讓改動的頻率決定它住在哪一層；而一旦分對了，三十年後那份內容還讀得懂。**

> 🔍 老手視角──真正的門道
> 分層架構人人會說，但 LPMud 的分法有一個常被忽略的細節：**它把「網路協議」留在了上層**。多數人設計引擎／腳本分層時，會直覺地把協議算成引擎的責任（它離 socket 最近），結果就是加一個欄位要改引擎、要跟引擎團隊排期。LPMud 反過來——driver 只保證「一條可以雙向送位元組的線」，線上跑什麼由 mudlib 說了算。代價是本書第三篇那些撞號，好處是**協議可以用內容的速度演化**，而這正是那三個世代的方言得以出現的原因。可落地的做法：設計引擎邊界時，把「這個東西改動的頻率」畫成一條軸，比「這個東西在技術上屬於哪一層」更能決定它該住哪裡——**頻率相同的東西放在一起，才不會互相綁架**。

---

## 034　1992 年的改名風波 — MudOS 是怎麼來的

**標籤**：`#MudOS` `#分支` `#相容性宣告` `#開源治理` `#一手史料`
**證據等級**：🟡 一手史料（1992-08-28 `rec.games.mud.lp` 貼文，收在 `fluffos/docs/archive/History.MudOS`）

**起源**：本專案的 driver 是 FluffOS，FluffOS 說自己是 MudOS 的後繼者。
那 MudOS 又是誰？這件事有一份**一手證據**：
1992 年 8 月 28 日，Erik Kay 在 `rec.games.mud.lp` 貼了一篇 190 行的長文，
標題就叫〈MudOS (long)〉。它至今原封不動躺在 FluffOS 倉庫的 `docs/archive/` 裡。

這種東西在逆向工程裡的價值極高：**它不是後人整理的年表，是當事人在事發當下寫的**。

**技術核心**：分支的成因，按貼文自己的說法有三條：

> we changed the name in the version string because
> (a) we're not compatible with standard 3.0 mudlibs.
> (b) we wanted a convenient version numbering scheme
> (c) we wanted to differentiate ourselves from being "yet another custom hacked lpmud driver".

(b) 這條特別有意思。他們原本的版號長這樣：

```
3.0.53.A2.2
 └─┬──┘ │ └── 自己的第二層修訂
   │    └──── A = American version（相對於 Lars 的官方版）
   └───────── 跟著 Lars 的版號
```

三個人在三個不同的地方各自改 code，掛在別人的版號後面往下加，
最後版本控制先崩潰了。於是他們**從 0.8 重新開始編號**——
不是 1.0，因為「我們知道還有一大堆要改才算做完」。

名字則是玩笑話變成的：他們原本在聊一個「driver + mudlib 一起發」的計畫叫 mudos，
後來有人半開玩笑地提議把驅動叫做 “MudOS enhanced LPmud driver”，
覺得聽起來還挺酷，就這麼定了。再後來嫌太長，砍成 `MudOS driver`，最後連 driver 也砍掉。

**解決的痛點**：(a) 才是技術上的真理由，而且它的形狀值得記下來——
**不相容不是他們的目的，是他們的處境**：

> Our communications are done completely differently. The backend was completely
> rewritten to be more efficient, and to deal with socket efuns. Function handling
> was completely rewritten. Much of the internal storage was completely rewritten.

改到這個程度，還叫「Lars 的 driver 的一個版本」，只會讓人拿舊 mudlib 上去撞牆。
**改名是為了讓相容性宣告誠實**。貼文裡抱怨的正是這件事：
「用我們 driver 的人一直在問為什麼 compat mode 不能用」——
因為名字讓他們以為可以。

**踩過的坑（1992 年的，不是本專案的）**：這篇貼文是在一場網路罵戰裡寫的。
社群指控他們搶 Lars 的功勞、指控他們「不過是又一個魔改版」。
貼文花了三分之一篇幅回應人身攻擊，結尾寫著 “All flames to /dev/null.”

技術上的教訓在中段：他們試過社群共治。
TMI 找了約十個人開會、開了兩個月的佈告欄收集意見——

> However, nobody really had the time to get together and work on the code. […]
> But to get *everyone* together to agree? That would be next to impossible,
> and the result would probably be crap.

**「開會的人很多，寫 code 的只有兩個」**——這句話在三十四年後讀起來一點都不陌生。

**優點 / 罩門**：分支的優點是 MudOS 真的把 driver 往前推了一大截，
而且它的「batteries-included」路線（大量內建 efun、套件化）
後來成為中文 MUD 圈的事實標準——本專案收藏的 17 個 mudlib，
以及 `mudlibs.fluffos.info` 上那 199 個，幾乎全部長在 MudOS 系的 driver 上。

罩門是**社群分裂**。1992 年之後 LPMud 家族再也沒有統一過：
Lars 的 3.x 線由別人接手成為 LDMud，MudOS 走自己的路，Genesis 那邊改叫 CD。
一份 mudlib 從此必須知道自己是給哪一支寫的。

**設計對照**：

| 分支時的做法 | MudOS 的選擇 | 後果 |
|-------------|-------------|------|
| 版號 | **重新從 0.8 起算** | 版號終於能表達自己的進度 |
| 名字 | **改掉** | 沒有人再以為它和 3.0 mudlib 相容 |
| 相容宣告 | **明說不相容** | 承受一次罵戰，但沒有累積技術債 |
| 上游關係 | 全發行版裡都掛 Lars 的名字 | 功勞歸屬與相容宣告分開處理 |

最後一列是這件事做對的地方：**「我們不相容」與「這是他做的」可以同時是真的**。
罵戰的多數混亂來自把這兩句話當成同一句。

**效益**：對本專案而言，這段歷史有一個非常實際的用途——
**它告訴你手上這份 mudlib 的年紀與血統**。
本專案收錄的 lib 裡，`is_killing(ob)` 宣告成 `string` 卻傳 object（§032），
舊 MudOS 放行、FluffOS 不放行。看到這種錯，你就知道這份 code 是在
「driver 對型別很寬鬆」的年代寫的，而不是作者當時寫錯了沒發現。
**知道 code 寫於哪個年代，比逐行讀 code 更快定位相容性問題。**

> 💡 君之一席話
> **改名不是背叛，是誠實**——當你的東西已經和上游不相容，繼續共用一個名字，受害的是每一個以為自己在用同一個東西的人。

> 🔍 老手視角──真正的門道
> 這篇貼文最值得學的不是分支的技術理由，是**分支的溝通順序**。他們先改了版號（解決自己的痛），再改名（解決使用者的誤解），最後才被迫解釋動機（收拾社群的情緒）。順序倒過來的專案——先開大會宣布願景、再談相容性、最後才發現版號早就爛掉——通常什麼都沒做成。老手看一個分支專案是否會活下來，第一個看的不是它的技術路線圖，而是**它敢不敢在版本字串裡寫「我和你不一樣」**：敢寫的，它的相容性宣告可以信；不敢寫的，你得自己一個一個試。可落地的做法：任何 fork 的第一個 commit 就把 identity 改掉（名字、版號、`--version` 輸出），這比任何 README 上的說明都有效——**因為使用者不讀 README，但一定會看到版本字串**。

---

## 035　四條血脈 — 從 1989 到 2026，以及相容性的價錢

**標籤**：`#FluffOS` `#LDMud` `#DGD` `#相容性` `#技術債` `#長期維護`
**證據等級**：🟡 FluffOS 官方 README／文件 ＋ 🟢 本專案實測（17 個 mudlib 的相容性落差）

**起源**：本專案為什麼能把一份 1990 年代末的中文 mudlib，
丟進一個 2026 年編出來的 WebAssembly driver 裡跑起來？
答案不是運氣，是**有人花了三十年守著一個相容性承諾**。

**技術核心**：LPMud 家族的四條主線：

| 年份 | 名稱 | 誰 | 定位 | 與 LPMud 的關係 |
|------|------|-----|------|----------------|
| 1989 | **LPMud** | Lars Pensjö | 原型；driver/mudlib 分家；Genesis mudlib | 本體。官方線走到 2.4.5 / 3.x |
| **1991 末** | **CD** | Johan Andersson 等（Chalmers Datorförening） | **為 Genesis 量身維護**；至今仍在跑 1989 年那個世界 | 從 Lars 的 3.0 開發版分出。**分家比 MudOS 還早** |
| 1992 | **MudOS** | Erik Kay、Truilkan、Jacques 等（TMI） | 「batteries-included」分支，大量 efun 與套件 | 從 3.0.5x 分出，**明說不相容** |
| 1993 | **DGD** | Felix “Dworkin” Croes | 極簡核心、完整持久化、原子函式、JIT | **概念衍生，非程式碼衍生**——重寫的 |
| 1997 | **LDMud** | Lars “Mateese” Düning | 接手 3.2（Amylaar driver）並現代化 | LPMud 官方線的延續 |
| 至今 | **FluffOS** | 社群 | MudOS 的現代化延續：C++、跨平台、CI、**WebAssembly** | **保持與 MudOS 相容** |

> **這張表只給定位，不給技術細節。** 五支 driver 在 apply／週期機制／安全模型／
> 持久化／intermud 上做了**方向相反**的選擇——完整的技術對照在**第十篇 §063–§065**。
> 那裡也有一份文獻化石：**CD 與 FluffOS 各自封存的 `CREDITS`，
> 開頭一字不差，然後在 1991 年分家處各自往下長了十年。**

五條線裡，只有 FluffOS 把「相容」寫進了自我定義：
官方 README 的說法是 *“the modern successor to MudOS”*，
並明列 *“Full backward compatibility with existing MudOS code”*。
這句話就是本專案 17 個 mudlib 能跑起來的全部理由。

FluffOS 在保持相容的同時換掉的東西不少：

| 面向 | MudOS 時代 | FluffOS 今天 |
|------|-----------|-------------|
| 語言 | C | C++（`src/` 全面 C++、clang-format/clang-tidy） |
| 事件迴圈 | `select()` 手寫迴圈 | libevent（WASM 版換成 host 驅動的 tick，§040） |
| 記憶體 | `smalloc` / `wrappedmalloc` | jemalloc（WASM 版 dlmalloc） |
| 字串 | byte 字串 | **UTF-8 原生 + ICU**，含 EGC（延伸字素叢集）感知 |
| 整數 | 32 位元 | 64 位元整數與浮點 |
| 網路 | telnet | telnet + **WebSocket + TLS**，IPv6 |
| 資料庫 | — | SQLite3／MySQL／PostgreSQL，執行緒池非同步 |
| 平台 | Unix | Linux／macOS／Windows(MSYS2)／Alpine／**WebAssembly** |
| 品質 | 靠使用者回報 | GTest 單元測試 + `testsuite/` 的 LPC 整合測試 + GitHub Actions |

**這張表是本書 §032 那八條修正的背景**。相容不是無縫，是**有價的**。

**踩過的坑：相容性的價錢，本專案付了八次。**
§032 列了修正清單，這裡補上它們各自對應到上表的哪一格——
**每一條落差都是「三十年來某樣東西被收緊」的痕跡**：

| §032 的症狀 | 背後被收緊的東西 | 屬於哪種相容性債 |
|------------|----------------|----------------|
| `master.c` syntax error（`static` 修飾詞） | LPC 語法：`static` 語意含混，FluffOS 改用 `nosave`/`protected` 並提供 `__SENSIBLE_MODIFIERS__` 分岔 | **語言層**：舊語法被移除 |
| `is_killing(ob)` 宣告 `string` 卻傳 object | 型別檢查：舊 MudOS 放行，FluffOS 不放行 | **語言層**：檢查變嚴 |
| `is_chinese()` 逐位元組判斷失效 | 字串模型：byte 索引 → **碼點／字素索引** | **執行期語意**：同一段 code 意思變了 |
| `valid_write` 找不到 SECURITY_D 就拒絕 | 安全模型：無 uid 環境下的預設 | **環境**：不是 driver 變了，是跑法變了 |
| `log_file("static/logon")` 找不到目錄 | 檔案 I/O：不再自動建立中間目錄 | **執行期語意** |
| 設定檔名 `config.ini` / `config.hell` 不一 | 沒有慣例可循 | **生態**：從來就沒統一過 |
| config 裡寫著 `c:\hell` 絕對路徑 | 原營運者機器的殘留 | **封存品質**，與 driver 無關 |
| `external_port_1` 決定編碼分支 | WASM 版的來源埠規則 | **移植層**（§041） |

**八條裡只有三條是「driver 真的改了」**，其餘是環境、封存與生態問題。
這個比例本身就是結論：**一個宣稱相容的 driver，三十年下來真的只欠你三件事，
其他都是你自己那邊的問題。**

**優點 / 罩門**：優點是**時間軸上的可攜性**——一份沒人維護的 mudlib，
在原作者消失二十年後，還能被別人跑起來、看見它當年長什麼樣。
本書收錄的 `mudlibs.fluffos.info` 那 199 個中文 mudlib 就是這件事的規模化版本。

罩門有兩個。其一，**相容性承諾會凍結錯誤**：
`sscanf("%s %s")` 那個和 C 不一樣的語意（第二個 `%s` 吃掉剩下全部）
是 1989 年的設計，今天所有人都得繼續忍受它，因為改了會壞掉幾千個 mudlib。
其二，**相容不等於等價**：本專案 17 個 lib 全部「跑得起來」，
但跨服 daemon、GBK 埠、MySQL 都是不存在的（§041）——
「開得起來」與「和當年一樣」中間，隔著一整個套件矩陣。

**效益**：對逆向工作的直接效益是**斷代**。
拿到一份陌生 mudlib 時，先看它踩到上表的哪幾格，就能推它寫於哪個年代、屬於哪一支血脈；
知道血脈，就知道該去哪份文件找 opcode 語意、該預期哪些 efun 存在。
本專案 17 個 lib 的匯入之所以能從「不可行」變成「幾分鐘」，
一半靠工具（§附錄 B），另一半靠**這張表把「未知的錯誤」變成「已知的八類」**。

> 💡 君之一席話
> **相容性不是一個布林值，是一份帳單**——宣稱相容的人替你擋下了大部分的改動，剩下那幾筆會直接寄到你桌上；先把帳單分類，再開始付。

> 🔍 老手視角──真正的門道
> 這一節真正的門道在「**誰承擔相容性**」。LPMud 家族把它全部壓在 driver 一側：mudlib 從來不宣告自己依賴什麼版本、什麼 efun、什麼語意，driver 只能盡量不要動。這個設計在 1989 年是對的（那時 mudlib 只有一份），在今天則造成一個很有意思的現象——**driver 不敢改，mudlib 不能改**（原作者早就不在了），於是相容性由第三方（像本專案）在中間手動彌合。現代生態的解法是把契約明寫出來：`package.json` 的版本範圍、Rust 的 edition、C++ 的 `-std=`。它們的共同點是**讓「我依賴什麼」變成可讀的資料，而不是靠跑跑看**。可落地的做法：任何要活過十年的東西，第一天就給它一個能宣告依賴的欄位——哪怕當下只有一個值。沒有這個欄位，十年後的維護者只能用本書 §032 那種方式：**跑一次、看它死在哪、修、再跑**，一個一個試出來。

---



## 036　三個方向的呼叫 — efun、apply 與 simul_efun

**標籤**：`#FluffOS` `#driver` `#efun` `#apply` `#simul_efun` `#邊界`
**證據等級**：🟡 FluffOS 官方文件（`docs/concepts/general/*`、`docs/apply/*`）

**起源**：讀 mudlib 原始碼時，最先卡住人的不是 LPC 語法，是**一個函式到底是誰在呼叫誰**。
`write()` 是誰？`create()` 是誰呼叫的？`is_chinese()` 又是哪來的？
這三個問題的答案分別是三種不同的機制，而它們的**方向不一樣**。

**技術核心**：FluffOS 的三層模型，以及層與層之間的三條呼叫路徑。

```
        ┌───────────────────────────────────────────┐
        │   mudlib（LPC 檔案：房間、NPC、登入流程）      │
        └───────────────────────────────────────────┘
              │  ①efun          ↑ ②apply       ↺ ③simul_efun
              │  往下呼叫         │ 往上回呼        （同層，繞道 simul_efun 物件）
              ▼                 │
        ┌───────────────────────────────────────────┐
        │   driver（C++：LPC 編譯器 + 位元組碼 VM      │
        │           + 網路 + 600 多個 efun）           │
        └───────────────────────────────────────────┘
```

| # | 機制 | 方向 | 誰定義 | 例子 | 找不到時 |
|---|------|------|--------|------|---------|
| ① | **efun** | mudlib → driver | driver（C++） | `write()`、`clone_object()`、`crypt()`、`set_encoding()` | 編譯錯誤 |
| ② | **apply** | driver → mudlib | mudlib（LPC） | `create()`、`init()`、`heart_beat()`、`logon()`、`connect()` | **靜默略過**（沒定義就是不做） |
| ③ | **simul_efun** | mudlib → mudlib | mudlib（單一物件） | `is_chinese()`、mudlib 自訂的一切工具函式 | 編譯錯誤 |

**② 是最容易誤解的一個**。apply 不是「你去呼叫 driver」，是「**driver 在特定時刻來呼叫你**」。
它沒有註冊步驟、沒有介面宣告——**你只要把函式名字寫對，driver 就會找到它**。
反過來說，名字打錯就是靜默失效：driver 找不到 `heart_beat`，
不會報錯，只會安靜地什麼都不做。

這個「靜默」的性質，和本書 §015 的七次事故是同一個家族的東西。
apply 的失效不會有例外、不會有 log、不會有紅字——
**它只是不發生**。這是讀 LPC mudlib 最需要建立的直覺。

**三類 apply，按呼叫時機分**：

| 類別 | 定義在哪個物件 | 典型成員 | 何時被呼叫 |
|------|--------------|---------|-----------|
| **object applies** | 每個一般物件 | `create` `init` `reset` `heart_beat` `clean_up` `id` `move_or_destruct` `on_destruct` `virtual_start` | 物件生命週期事件 |
| **master applies** | master 物件（本專案是 `/adm/single/master`） | `connect` `logon` `error_handler` `crash` `preload` `compile_object` `valid_read` `valid_write` `valid_shadow` `valid_override` `valid_seteuid` `get_root_uid` `include_file` `inherit_program` | 全域事件、安全決策、編譯決策 |
| **interactive applies** | 玩家連線所附著的物件 | `logon` `net_dead` `process_input` `write_prompt` `terminal_type` `window_size` `telnet_suboption` `catch_tell` `receive_message` `gmcp` `msdp` `mxp_tag` `zmp` | 該連線的網路事件 |

第三列請特別看一眼：**`gmcp` / `msdp` / `mxp_tag` / `zmp` 是 driver 提供的 apply**，
也就是說這四套 MUD 擴充協議 **driver 是知情的**——它會做 telnet 子協商、
把解出來的資料交給 mudlib。
**而 ZJMUD 不在這張表上。** 這件事的全部後果，§043 會展開。

**① efun 的規模**：FluffOS 官方 README 的說法是「600 多個內建函式」，
按主題分成套件（package）：`core`、`math`、`matrix`、`parser`、`sockets`、`db`、
`crypto`、`compress`、`pcre`、`async`、`external`、`ffi`、`uids`、`mudlib_stats`……
套件是**編譯期開關**——關掉的套件，它的 efun 就**不存在**，
mudlib 用到就是編譯錯誤。§041 會看到這件事在 WebAssembly 版上的實際形狀。

**③ simul_efun 的門道**：mudlib 可以定義一個物件（本專案是 `/adm/single/simul_efun`），
裡面的 **public 函式會被當成「假 efun」全域可見**。
編譯器遇到一個既不是本物件函式、也不是內建 efun 的裸呼叫時，
會把它編成對 simul_efun 物件的**專用指令**（不是一般的 `call_other`），
而且因為原型在編譯期已知，**回傳值不必轉型**，即使開了 `#pragma strict_types`。

它最有意思的用法是**覆蓋 efun**：定義一個和 efun 同名的 simul_efun，
全 mudlib 的呼叫就都會走你的版本；你在裡面做完檢查，再用 `efun::move_object()`
呼叫真正的 driver 實作。哪些物件能用 `efun::` 前綴繞過，
由 master 的 `valid_override()` 決定。

**解決的痛點**：這三條路徑合起來，回答了「driver 與 mudlib 的邊界到底畫在哪」——
**不是畫在檔案上，是畫在呼叫方向上**。
同一個名字（比如 `move_object`）可以同時是 efun 與 simul_efun，
真正決定行為的是誰在什麼方向上呼叫它。

**踩過的坑**：本專案 §032 的 `is_killing(ob)` 就是這個機制的受害者。
它宣告成收 `string`，實際被傳了 object。
如果它是 efun，driver 的參數檢查會擋下來；
但它是 **simul_efun**，型別檢查發生在 LPC 編譯期，
而舊 MudOS 的檢查比較寬鬆——於是這個錯誤在原伺服器上跑了很多年沒事，
換到 FluffOS 才爆。**「它在原機上跑得好好的」不構成它是對的證據。**

**優點 / 罩門**：優點是 mudlib 可以在不碰 C 的情況下改變幾乎任何行為，
連 efun 都能換掉。罩門是**除錯路徑會變長**：
看到 `write(...)` 你不能假設它是 driver 的 `write`——
得先去 simul_efun 物件裡確認有沒有人蓋掉它。

**效益**：對逆向工作的直接效益是**知道去哪裡找**。
本專案要回答「這個 ESC 開頭的字串是誰送出去的」時，路徑是固定的：
先在 mudlib 裡 grep 巨集名，找到 `write()` 呼叫點，
再確認 `write` 有沒有被 simul_efun 蓋掉——三步，不必碰 driver 一行 C++。

> 💡 君之一席話
> **搞清楚一個系統，先搞清楚它有幾種呼叫方向**——往下呼叫的是能力，往上回呼的是契約，同層繞道的是慣例；三者混在一起看，永遠看不出邊界在哪。

> 🔍 老手視角──真正的門道
> apply 這個機制值得單獨想一想：它是**用命名慣例取代介面宣告**。好處是零樣板——你不必 implement 任何 interface、不必註冊任何 callback，寫個叫 `heart_beat` 的函式就會被呼叫。代價是打錯字沒有人告訴你。這個取捨在現代生態裡反覆出現：pytest 的 `test_*`、Go 的 `TestXxx`、React 的 `useXxx`、Rails 的 convention over configuration，全是同一招。它們共同的緩解手段也一樣——**linter**：既然編譯器不管，就讓另一個工具管。老手看到「靠命名生效」的系統，第一個動作是問「有沒有東西會告訴我名字打錯了」；沒有的話，就自己加一條檢查進 CI。可落地的做法：任何靠慣例綁定的專案，**寫一支腳本把所有慣例名字列出來與實際定義比對**，成本一個下午，能擋掉一整類「它就是不動，也不知道為什麼」的問題。

---

## 037　雙堆疊虛擬機 — LPC 是怎麼被執行的

**標籤**：`#虛擬機` `#位元組碼` `#堆疊機` `#引用計數` `#沙盒`
**證據等級**：🟡 FluffOS 官方文件 `docs/driver/stackmachine.md`（**這份文件本身就是史料**）

**起源**：§033 引的那句「Lennart Augustsson 說服我去實作一個虛擬堆疊機的編譯器」，
它的產物就是這一節要講的東西。而最有意思的是——
**FluffOS 倉庫裡那份描述它的文件，文風與內容都還停在 Lars 的年代**，
開頭甚至還是全大寫的：

> THIS IS A DOCUMENT THAT DESCRIBES HOW A VIRTUAL STACK MACHINE HAS BEEN DEFINED,
> TO EXECUTE COMPILED LPC CODE.

三十七年，執行模型沒有換過。

**技術核心**：兩個堆疊。

```
    值堆疊（value stack）                 控制堆疊（control stack）
    ┌─────────────────────┐              ┌──────────────────┐
    │  ...                │              │  返回位址          │
 fp→│  參數 0             │              │  前一個 frame ptr  │
    │  參數 1             │              │  參數個數          │
    │  ...                │              │  區域變數個數      │
    │  區域變數 0          │              │  extern_call 旗標  │
    │  區域變數 1          │              └──────────────────┘
    │  ...                │
    │  暫時值              │
    ▼                     │
```

- **值堆疊**放所有東西：求值中間結果、區域變數、**以及參數**——
  參數就是區域變數，只是編號在前面。存取靠 **frame pointer 加偏移量**，
  所以是 O(1) 的索引，不是查表。
- **控制堆疊**放返回位址、前一個 frame pointer、參數與區域變數的個數。
  把個數存在這裡，`F_RETURN` 才知道要釋放多少東西，
  指令本身不必攜帶這個資訊。

每個值堆疊元素的型別是 `struct svalue`——這個名字在 FluffOS 的 C++ 原始碼裡至今還在。

**幾個關鍵指令的形狀**（原文件的位元組佈局）：

| 指令 | 格式 | 語意 |
|------|------|------|
| `F_FUNCTION` | `b0=F_FUNCTION, b1b2=函式編號, b3=引數個數` | 呼叫本物件函式。**同時初始化 frame pointer**，並把引數個數「調整」成被呼叫者要的數量——不足補 0、過多彈掉 |
| `F_RETURN` | `b0` | 釋放整個 frame，只留堆疊頂端一個值。若 `extern_call` 旗標為真就從求值器返回 |
| `F_CALL_OTHER` | `b1=F_CALL_OTHER, b2=引數個數` | 呼叫**其他物件**的函式（`ob->fn()`） |
| `F_AGGREGATE` | `b1, b2b3=陣列大小（最大 0xffff）` | 從堆疊頂端取 N 個元素組成陣列 |
| `F_CATCH` | `F_CATCH, b1b2=catch 之後的位址` | 執行時做 `setjmp()` 並**遞迴呼叫 `eval_instruction()`**；`F_THROW` 做 `longjmp()` |
| `F_SSCANF` | 引數個數為單一位元組 | 特例：前兩個引數傳值，其餘用 **`T_LVALUE`** 型別傳參考 |

三個細節值得停一下：

1. **`F_FUNCTION` 會自動調整引數個數**。LPC 呼叫函式時給多給少都不會錯——
   少的補 0、多的丟掉。這解釋了為什麼老 mudlib 裡到處是引數個數對不上的呼叫
   卻能跑；也解釋了為什麼**它們錯得很安靜**。
2. **`F_CATCH` 是靠 `setjmp`/`longjmp` 加遞迴求值器實作的**。
   所以 LPC 的 `catch()` 不只是語法糖，它會真的開一個新的求值 frame。
   本專案的 `master.c` 就用它包住登入物件的建立：
   `err = catch(login_ob = new(LOGIN_OB));`——**登入物件炸了，連線還在，
   還能寫一行「現在有人正在修改使用者連線部份的程式」給玩家**。
3. **`sscanf` 需要一個專門的 lvalue 型別**，因為 LPC 沒有指標。
   這是「語言少了一個特性，就得在 VM 裡補一個型別」的教科書級例子。

**記憶體模型**：沒有 `malloc`、沒有 `free`、沒有 `delete`。
`allocate(n)` 的單位是**元素**不是位元組。回收靠**引用計數**：
計數歸零就立刻回收。所以 LPC 沒有 GC 停頓，
但**有循環引用洩漏**——FluffOS 為此專門有一份 `docs/concepts/general/reference_loops.md`。

**沙盒**：LPC 是給不受信任的作者寫的（想想 1989 年的原始情境：讓玩家來蓋世界），
所以 driver 必須能中止失控的程式碼。手段是 **evaluation cost**：
每執行一條指令扣一點額度，額度用完就中斷該次執行並拋 LPC 錯誤。
`config` 裡的預設值是：

| 限制 | 預設值 | 擋的是什麼 |
|------|--------|-----------|
| `maximum evaluation cost` | 30,000,000 | 無窮迴圈 |
| `maximum call depth` | 150 | 無窮遞迴 |
| `evaluator stack size` | 65,536 | 堆疊爆掉 |
| `inherit chain size` | 30 | 繼承鏈失控 |
| `maximum array size` | 15,000 | 記憶體炸彈 |
| `maximum mapping size` | 150,000 | 同上 |
| `maximum string length` | 1,048,576 位元組 | 同上 |

**踩過的坑**：這套沙盒**在 WebAssembly 版上是關掉的**。
原生版的 eval limit 實作依賴 POSIX 的 `SIGVTALRM` 計時器，
WASM 沒有——官方文件把它列為已知限制的第一條：
「`while(1);` 在 LPC 裡會卡死整個分頁」。
（macOS 與 Windows 上其實也沒有。）
本專案的 `boot-test.mjs` 因此**必須自己帶逾時**：
不能假設 driver 會替你中止跑瘋的 mudlib。

**優點 / 罩門**：優點是這套設計極度耐久——三十七年、四個分支、
從 32 位元 Unix 到 64 位元 WebAssembly，執行模型一行沒改。
堆疊機的簡單性是它活下來的原因：**它沒有什麼可以過時的東西**。
罩門是效能天花板：純解譯的堆疊機沒有 JIT、沒有暫存器分配，
所有效能都靠 efun（也就是把熱點推回 C++）。
這正是為什麼一個 MUD driver 會有 600 多個內建函式——
**efun 數量是解譯器慢的直接後果**。

**效益**：對逆向工作而言，知道執行模型能回答一類具體問題：
「這段 LPC 執行時，`this_player()` 是誰？」——答案在控制堆疊；
「為什麼這個引數是 0？」——因為 `F_FUNCTION` 補的；
「為什麼這個錯誤沒有讓伺服器死掉？」——因為某個上層有 `catch()`。

> 💡 君之一席話
> **活得最久的設計通常不是最先進的那個，是最沒有東西可以過時的那個**——一台雙堆疊機沒有依賴任何一代硬體的特性，所以它可以從 1989 年的 Unix 搬到 2026 年的瀏覽器分頁裡，一行都不用改。

> 🔍 老手視角──真正的門道
> `F_FUNCTION` 自動補齊引數這件事，是一個非常值得警惕的設計。它讓 mudlib 極度好寫（加一個參數不會弄壞既有呼叫點），但它把**一整類錯誤變成了資料錯誤**：你不會拿到「引數個數不符」，你會拿到一個 0，然後那個 0 一路流進業務邏輯，最後在離現場很遠的地方變成一個看不懂的行為。這種「寬容輸入」的設計在協議層也有完全一樣的形狀——本書 §004 那個沒有跳脫機制的分隔符、§009 那個「定義了 102 個 opcode 只用 16 個」的 header，都是同一種寬容。老手評估寬容設計時只問一個問題：**寬容的代價由誰承擔？**如果是寫的人省事、除錯的人加倍痛苦，而這兩者又不是同一批人（在 mudlib 的情境裡，往往差了二十年），那這筆交易就是虧的。可落地的做法：在寬容的邊界上加一條可關閉的嚴格模式——LPC 有 `#pragma strict_types`，用它；協議層則是「解析器有一個嚴格模式，CI 跑嚴格模式，正式環境跑寬容模式」。

---

## 038　物件的一生 — clone、apply 與心跳

**標籤**：`#物件模型` `#clone` `#heart_beat` `#call_out` `#熱重載` `#生命週期`
**證據等級**：🟡 FluffOS 官方文件（`docs/apply/object/*`、`docs/concepts/general/hot_reload.md`、`docs/driver/config.md`）

**起源**：LPC 的物件模型和主流 OOP 差一個關鍵概念，
不先講清楚，讀 mudlib 會一路困惑：**檔案本身就是一個物件**。

**技術核心**：**blueprint（藍本）與 clone（複製體）**。

```
/inherit/weapon/sword.c
   │
   ├─ load_object()  →  藍本物件  /inherit/weapon/sword
   │                     （一份，第一次被碰到時編譯並建立）
   │
   └─ clone_object() →  複製體    /inherit/weapon/sword#1234
                          #1235
                          #1236 ...（要幾把有幾把）
```

一個 `.c` 檔就是一個類別**兼**一個實例。
`clone_object()` 產生的複製體共用同一份編譯後的位元組碼（program），
但各自有一份變數。所以「一萬把劍」的記憶體成本是一萬份變數，不是一萬份程式。

**物件的生命週期 apply**，按時間順序：

| 時機 | apply | 說明 |
|------|-------|------|
| 編譯後、變數初始化 | `__INIT` | 由編譯器產生，初始化有初值的變數 |
| 建立時 | **`create()`** | 相當於建構子。**LPC 沒有 `main()`，有的是 `create()`** |
| 每次有 living 物件進入 | **`init()`** | 典型用途是 `add_action()` 註冊指令 |
| 週期性 | **`reset()`** | 重生怪物、補貨。預設 `time to reset : 900` 秒 |
| 週期性（需開啟） | **`heart_beat()`** | 預設 `heartbeat interval msec : 1000`。**要 `set_heart_beat(1)` 才會跑** |
| 長時間沒人碰 | `clean_up()` | 預設 `time to clean up : 600` 秒。物件可以自己決定要不要 `destruct` |
| 記憶體不足／閒置 | （swap） | `time to swap : 300`，0 為停用 |
| 被摧毀時 | `on_destruct()` | 收尾 |
| 環境被摧毀時 | `move_or_destruct()` | 決定跟著死還是搬家 |

**兩種週期機制不要搞混**：

| | `heart_beat()` | `call_out()` |
|---|---------------|-------------|
| 形式 | apply（driver 定時來呼叫你） | efun（你排一個未來的呼叫） |
| 頻率 | 固定間隔，預設 1 秒 | 一次性，指定幾秒後 |
| 開關 | `set_heart_beat(1/0)` | 排一次跑一次 |
| 典型用途 | 戰鬥回合、狀態衰減、飢餓值 | 延遲效果、逾時、排程 |
| 成本 | **每個開了心跳的物件每秒都在跑** | 只在到期時跑 |

第五列是效能上的重點：一個 MUD 卡不卡，
通常直接取決於**有多少物件開著 `heart_beat`**。

**時間的粒度**由 `gametick msec` 決定（預設 1000 毫秒）——
這是「遊戲內時間的最短可見間隔」。
§040 會看到，WebAssembly 版把這個時鐘的推進權從 libevent 交給了瀏覽器分頁。

**熱重載**：LPMud 的招牌能力，機制比想像中細緻。
`update` 一個檔案不能只重載它自己，因為：

- 每個 `#include` 的標頭是**編譯期併進去**的；
- 每個 `inherit` 的父程式是**編譯期綁進子程式**的——
  子程式連結的是它編譯當下的那一份父程式。

所以改了 `/std/base.c`，只重載自己檔案改過的物件，
**每一個繼承者都還在跑舊 code，而且完全沒有徵兆**。
FluffOS 給的解法是兩個編譯期 master apply：

```c
mixed inherit_program(string from, string path, int priv);
mixed include_file(string compiled, string from, string path);
```

driver 在**每一次編譯**時，對每個 `inherit` 呼叫前者、每個 `#include` 呼叫後者。
mudlib 只要**原樣回傳 `path`**（保持原行為）**並記下這條邊**，
就免費得到一張完整的相依圖，可以據此做出正確的自動熱重載。

這個設計的漂亮之處：**它沒有新增任何機制**——
兩個 apply 本來就是為了安全檢查（決定准不准 include／inherit）而存在的，
熱重載只是把它們的**引數**拿來用。

**踩過的坑**：`create()` 與 `reset()` 的關係在 MudOS 系與 LPMud 官方線上不一樣，
這是跨血脈移植 mudlib 最常撞的一堵牆。
本專案沒有踩到，因為 17 個 lib 全是 MudOS 系；
但 §035 的表提醒過：**知道血脈，才知道該預期哪一套語意**。

另一個真的踩到的是 `preload`：master 的 `preload()` apply 回傳一張開機時要載入的清單。
本專案 `libs/lpmudname` 裡有三個 daemon 因為 WASM 版沒有 `package sockets`
而編譯失敗（`Undefined function socket_create` 等 13 處），
**driver 照常完成開機**——preload 清單裡的項目載入失敗，
只會讓那個物件不存在，不會中止啟動。
這正是本專案 `playable` / `limited` 分級的分界線：**看的是「進不進得了世界」**。

**優點 / 罩門**：優點是「藍本 + 複製體」讓一份 code 服務上萬個實例，
而熱重載讓開發迴圈短到近乎即時。
罩門是**狀態與程式的版本會分岔**：熱重載換掉的是 program，
物件裡既有的變數還在——如果新版程式對變數的假設變了，
你會得到一個「跑著新程式、帶著舊資料」的物件。
這是 LPMud 家族最經典的一類幽靈 bug。

**效益**：對本專案的直接效益是**知道 boot-test 要等什麼**。
`boot-test.mjs` 判斷一個 mud 開得起來的依據不是「driver 沒有退出」，
而是走完「註冊 → 建角 → 進世界」——
因為 preload 失敗、reset 沒跑、心跳沒開，driver 都不會抱怨。
**在一個「失敗是靜默的」系統上，唯一可信的驗證是端到端跑一遍。**

> 💡 君之一席話
> **熱重載真正的難點從來不是「重新編譯」，是「誰依賴了我」**——沒有相依圖的熱重載，只是把一個明顯的錯誤換成一個安靜的錯誤。

> 🔍 老手視角──真正的門道
> `inherit_program` / `include_file` 這一手很值得抄：**FluffOS 沒有為了熱重載新增任何 API**，它把兩個既有的安全檢查點的引數拿來當相依圖的來源。這是「機制複用」的典範——同一個 hook 點，一個消費者用它的**回傳值**（安全決策），另一個消費者用它的**引數**（可觀測性）。老手在設計擴充點時會刻意留這個餘裕：hook 的簽名盡量帶齊上下文，即使當下的用途只需要一個布林值。因為擴充點的真正價值往往不在你設計它時想到的那個用途上。可落地的做法：任何 callback／middleware／攔截器，**簽名裡多帶「這件事發生在什麼情境下」的參數**（誰、為了誰、在編譯／處理哪個東西時），成本幾乎為零，而它會在兩年後救你一次。

---

## 039　config — driver 的全部旋鈕，以及本專案在上面栽的跟頭

**標籤**：`#設定檔` `#config` `#external_port` `#冪等` `#工具設計`
**證據等級**：🟢 本專案實測（17 個 mudlib 的匯入）＋ 🟡 FluffOS `docs/driver/config.md`

**起源**：driver 啟動時只吃一個東西：一個設定檔的路徑。
`driver.exe config.ini`——之後的一切（mudlib 在哪、master 是誰、開哪些埠）
全部來自那個檔案。所以**要把一份陌生的 mudlib 跑起來，
第一件事永遠是把它的設定檔弄對**。

本專案匯入 17 個 mudlib 的過程，一半的時間花在這個檔案上。

**技術核心**：本專案上游 `LPMud-Name/world/config.ini` 的關鍵欄位，逐條說明——
這份檔案剛好把最重要的幾類旋鈕都用到了：

```ini
name : 江湖论剑

external_port_1 : telnet 5001         # ★ UTF-8
external_port_2 : telnet 5003         # ★ GBK（由 mudlib 自己切換）
external_port_3 : websocket 5004

mudlib directory : ./
include directories : /include
master file : /adm/single/master
simulated efun file : /adm/single/simul_efun
global include file : <globals.h>
log directory : /log
debug log file : debug.log
websocket http dir : www

default fail message : [35m指令错误，请输入 help 查看游戏帮助。[2;37;0m
```

| 欄位 | 作用 | 為什麼重要 |
|------|------|-----------|
| `master file` | §036 那一整排 master apply 住在哪 | **弄錯就完全開不了機** |
| `simulated efun file` | §036 的 ③ 從哪裡來 | 弄錯的話 mudlib 到處是「未定義函式」 |
| `include directories` | `#include <...>` 的搜尋路徑 | 冒號分隔多個 |
| `global include file` | **每個檔案自動 include 這個** | 讀 mudlib 時常見的「這個巨集哪來的」，答案通常在這 |
| `external_port_N` | 開哪些埠、講哪種協議（`telnet`／`websocket`／`ascii`／`binary`，可加 `_tls`） | ★ 見下方 |
| `websocket http dir` | WebSocket 埠同時當靜態網頁伺服器 | driver 本身就能發網頁 |
| `default fail message` | 指令不存在時的訊息 | **值可以直接包含裸 ESC 字元**——本書所有 ANSI 方言的最外層來源之一 |

driver 的旋鈕遠不只這些。與本書關係最近的幾類，附官方預設值：

| 類別 | 代表旋鈕（預設值） |
|------|------------------|
| 時序 | `gametick msec`(1000)、`heartbeat interval msec`(1000)、`time to reset`(900)、`time to clean up`(600)、`time to swap`(300) |
| 限制 | `maximum evaluation cost`(30,000,000)、`maximum call depth`(150)、`evaluator stack size`(65,536)、`maximum array size`(15,000)、`maximum string length`(1 MiB) |
| 雜湊表 | `object table size`(4096，建議是物件數的 1/4)、`hash table size`(65,536，建議是相異字串數的 1/5，且應為質數)、`living hash table size`(256，**只能是 4/16/64/256/1024/4096**) |
| 語意開關 | `sane explode string`(1)、`sane sorting`(1)、`old range behavior`(0)、`this_player in call_out`(1) |
| reset 行為 | `no resets`(0)、`lazy resets`(0)、`randomized resets`(1) |

**「語意開關」那一列是相容性層的真面目**：
`sane explode string`、`old range behavior` 這種旗標的存在本身就是歷史證據——
**driver 知道自己以前的行為是錯的，但不敢直接改，只能給一個開關**。
§035 說的「相容性承諾會凍結錯誤」，在這裡是可以逐行讀出來的。

**踩過的坑（三個，全部是本專案實測）：**

**① `external_port_1` 決定 WebAssembly 版走哪個編碼分支。**
本專案的 mudlib 在 master 裡寫著：

```c
object connect(int port)
{
    if (port == 5003) {
        set_encoding("GBK");
    }
    ...
}
```

而 WASM 版的 ICU 資料被裁掉了表格式字集（§041），
真的走進 GBK 分支會直接 raise error。
它之所以沒炸，是因為 driver 的 `wasm_console_connect()`
把每條連線都標成「來自**第一個** external_port」
（`src/wasm/comm_wasm.cc:124-126`）——也就是 `telnet 5001`，UTF-8 分支。

> **本專案因此定下一條打包規則：第一個 `external_port` 必須是 UTF-8 的那一個。**
> 這條規則寫在 `libs/lpmudname/NOTES.md` 的「沒有踩到的坑」一節——
> **沒踩到的坑也要記**，否則下一個人把兩個埠的順序調換，會得到一個非常難查的錯誤。

**② 這個設定檔沒有行尾註解。**
官方文件寫得很清楚：*“Lines beginning with `#` are comments”*——
**只有整行註解**。值取的是冒號後面的整行。

本專案在自動修正腳本裡把說明寫成行尾註解：

```ini
external_port_1 : telnet 5001   # 改成 UTF-8 埠
                              └──────┬──────┘
                                 這整段都是值的一部分
```

三個原本已經修好的 lib 因此變回 `noboot`。

**③ 而且那支修正腳本不是冪等的。**
它每重跑一次就再疊一層註解，於是「修一次壞一點」。
這是本專案整批匯入過程中**最貴的一次錯誤**，教訓寫在 §032：

> **自動修正工具本身必須是冪等的，否則它會變成另一種損壞來源。**

第三個坑比前兩個嚴重得多，因為前兩個是**知識**問題（不知道規則），
第三個是**工程**問題（工具的性質不對）。知識可以查，
而一個非冪等的修正工具會在你查資料的時候繼續破壞現場。

**優點 / 罩門**：優點是所有行為集中在一個純文字檔，
沒有環境變數、沒有命令列旗標的組合爆炸，`diff` 兩份 config 就能比較兩台 mud。
罩門是**沒有 schema、沒有驗證**：打錯欄位名不會有人告訴你，
只會得到預設值或一個很遠的錯誤（本專案看過的極端例子是設定裡寫著
原營運者機器上的絕對路徑 `c:\hell`，症狀是 `Bad mudlib directory`）。

**效益**：本專案把「設定檔對不對」變成建置管線的一部分——
`boot-test.mjs` 真的用那份 config 開一次機、走一次註冊建角。
**沒有 schema，就用端到端測試當 schema。**

> 💡 君之一席話
> **修東西的工具，本身必須能安全地重跑**——不能重跑的修正工具，第二次執行時就從解決方案變成故障源。

> 🔍 老手視角──真正的門道
> 那條「第一個 external_port 必須是 UTF-8」的規則，形狀值得注意：它是一條**跨層的隱性契約**——`config.ini`（設定層）的**順序**，決定了 `master.c`（mudlib 層）走哪個分支，而中間的因果藏在 driver 的 C++ 原始碼裡（`comm_wasm.cc` 的三行）。這種契約最危險的地方在於它在任何一層看起來都不像契約：config 看起來只是列了兩個埠，master.c 看起來只是判斷了一個埠號。老手處理這類契約的方法只有一個——**把它寫在會被讀到的地方，並附上「憑什麼」**。本專案的做法是寫進該 lib 的 `NOTES.md` 並附上 `comm_wasm.cc:124-126` 的行號；更強的做法是讓打包腳本直接**檢查**這件事並在違反時拒絕打包。可落地的判準：一條規則如果只存在於某個人的腦袋裡，它的半衰期大約是三個月；寫進註解大約兩年；寫進會失敗的檢查，才真的活得比你久。

---



## 040　三個沒有瀏覽器對應物的東西 — Transport、反轉的迴圈、VFS

**標籤**：`#WebAssembly` `#移植` `#介面抽取` `#事件迴圈` `#連結期分派`
**證據等級**：🟡 上游原始碼與文件（`fluffos/src/wasm/README.md`、`src/net/transport.h`）

**起源**：§032 從**客戶端**這一側寫了「伺服器搬進分頁」這件事——
症狀、三次誤判、真因。這一節換到**driver**那一側：
一個 1989 年設計、假設自己獨佔一個 Unix 行程的東西，
要怎麼搬進一個「不准阻塞、沒有 socket、沒有檔案系統」的分頁裡？

上游的答案很乾淨：**只有三樣東西在瀏覽器裡沒有對應物，就只抽三個接縫。**

**技術核心**：

### 接縫一：socket → `Transport`

原本 `comm.cc` 直接對 fd 讀寫。移植的做法是抽出一個抽象位元組管道：

```cpp
// net/transport.h — 每個 interactive_t 擁有一個
write() / flush() / schedule_command() / close()
```

三個實作，**連結期擇一**：

| 實作 | 檔案 | 用在哪 |
|------|------|--------|
| `SocketTransport` | `net/transport_libevent.cc` | 原生 telnet（bufferevent／TLS） |
| `WebsocketTransport` | 同上 | 原生 WebSocket（libwebsockets） |
| `WasmConsoleTransport` | `wasm/comm_wasm.cc` | **瀏覽器分頁** |

關鍵是它上面的東西**一行都沒改**：`comm.cc`（使用者、指令佇列、`input_to`、
提示、snoop）與 `net/telnet.cc`（完整的 telnet 協商，走 libtelnet）
在每個平台上編譯的是同一份原始碼。

`WasmConsoleTransport` 的兩個方向：
- **出**：位元組交給 JS 的 `Module.fluffos.onOutput(id, bytes)`；
- **入**：`fluffos_input(id, bytes)` → `comm_telnet_received()`——
  **和 socket 讀取走的是同一條路徑**。

而 `fluffos_connect()` 則是原生 accept 流程的鏡像：
`user_add()` → telnet 初始化與初始協商 → `master::connect(port)` → `logon()`。
沒有監聽 socket（`init_user_conn` 是空實作）。

> **這件事對客戶端的直接後果**：driver 送出的是**真正的 telnet 串流**。
> 沒有中間程序可以代勞剝 IAC，瀏覽器得自己來——
> 這就是本專案 `src/js/telnet.js` 存在的理由（§附錄 B）。

### 接縫二：事件迴圈被反轉

原生 driver 永遠阻塞在 libevent 的 `event_base_loop()` 裡。
**分頁不能阻塞——事件迴圈是頁面的，不是你的。**

上游的處理方式很值得學：**只把「推進時間的那個迴圈」換掉，排程核心留在原地**。
gametick 佇列、`add_gametick_event()`、各種維護事件全都還在共用的 `backend.cc`；
`wasm/backend_wasm.cc` 只換掉迴圈本身，用一個純粹的 wall-time 優先佇列，
**完全不用 libevent**。頁面在計時器裡呼叫 `fluffos_tick(now_ms)`，
它排掉所有到期的事件，並把時間推進「已經過了幾個 gametick」
（分頁被暫停時會補跑，但**有上限**——最多 100 拍）。

心跳、`call_out`、reset、回收——driver 的所有排程本來就走這個 API，
**所以它們上面的一切都不知道自己被移植了**。

### 接縫三：檔案系統 → MEMFS

driver 的檔案 I/O（編譯、`save_object`、`read_file`、`ed`、log）
是普通的 POSIX 加 `ghc::filesystem`，Emscripten 直接把它映到記憶體檔案系統上——
**driver 端零修改**。

mudlib 用 Emscripten 的 `file_packager` 打包成單一 `mudlib.data` 映像
加一個 `mudlib.js` 載入器，在 runtime 起來之前掛載好。
寫入落在 MEMFS，**只活到分頁關掉為止**。

**解決的痛點**：三個接縫換來一件事——**共用邏輯裡沒有任何 `#ifdef __EMSCRIPTEN__`**。
上游文件明講：平台條件編譯只剩兩處（`net/net_compat.h` 的型別宣告，
以及 `tracing.cc` 裡一個執行緒能力判斷）。
其他 per-target 的單例全部用同一招——**連結期選實作**：

| 介面 | 原生 | WASM |
|------|------|------|
| 事件迴圈（`backend.h`） | `backend_libevent.cc` | `wasm/backend_wasm.cc` |
| 連線傳輸 | `net/transport_libevent.cc` | `wasm/comm_wasm.cc` |
| TLS（`net/tls.h`） | `net/tls.cc` | **完全不連結**（唯一的共用呼叫點 `sys_reload_tls` 從該平台排除） |
| DNS 解析 | `dns_libevent.cc` | `dns_stub.cc` |
| 當機處理 | `crash_handler.cc`（backward-cpp） | `crash_handler_wasm.cc` |

**踩過的坑（上游的，不是本專案的）**：TLS 那一列的處理方式特別誠實——
**不是給一個假的實作，是連呼叫它的那個 efun 都從這個平台移除**。
所以 WASM 版的 mudlib 呼叫 `sys_reload_tls()` 會得到「這個函式不存在」的編譯錯誤，
而不是一個安靜地什麼都不做的樁。
**在移植層，「明確地不存在」比「存在但無效」好**——
後者會讓 mudlib 以為自己開了 TLS。

**優點 / 罩門**：優點是這個移植沒有製造第二份 driver：
telnet 協商、使用者管理、`input_to`、提示、snoop、編譯器、VM 全部是同一份 code，
所以「原生會動、WASM 不會動」這種分岔幾乎不可能出現在上層。
罩門是**下層分岔全部集中在三個檔案裡**，而這三個檔案的行為差異
（沒有網路延遲、沒有 eval limit、沒有真 DNS）會以非常間接的方式冒出來——
§042 的三個陷阱全部屬於這一類。

**效益**：對本專案而言，這三個接縫直接決定了客戶端要負責什麼：

| 接縫 | 客戶端因此必須做的事 | 本專案的哪個檔案 |
|------|-------------------|-----------------|
| Transport 是位元組管道 | 自己剝 telnet IAC、自己分行 | `src/js/telnet.js` |
| 事件迴圈由頁面驅動 | 自己用 `setInterval` 泵 `fluffos_tick` | `src/js/wasmdriver.js` |
| 檔案系統是 MEMFS | 自己下載映像、掛載、清楚告知「重整即失」 | `src/js/mudlibimage.js`、`wasmboot.js` |

> 💡 君之一席話
> **移植一個東西時，先數清楚「目標環境真的沒有的東西」有幾樣**——通常比想像的少；抽那幾個接縫，其餘一行都別碰，否則你得到的不是移植，是分岔。

> 🔍 老手視角──真正的門道
> 「連結期分派」這一招在今天有點被遺忘，值得重新提一下：同一個標頭、多個 `.cc`、建置系統選一個連進去——沒有虛擬函式表的成本、沒有 `#ifdef` 把共用檔案切得七零八落、也沒有執行期的分支。它的隱形好處是**編譯器會逼你把介面補完**：少實作一個函式就是連結錯誤，而 `#ifdef` 少寫一段只會安靜地少一段行為。老手在做跨平台時的順序通常是：先用 `#ifdef` 快速跑通（探索期），再把 `#ifdef` 收斂成介面 + 多實作（穩定期）——**跳過第一步會過度設計，停在第一步會腐爛**。可落地的判準：當同一個 `#ifdef` 在三個以上的檔案裡出現，就該把它換成一個介面了。

---

## 041　被拿掉的東西 — 套件矩陣，與 mudlib 上看得見的形狀

**標籤**：`#套件` `#編譯期開關` `#ICU` `#編碼` `#落差揭露`
**證據等級**：🟡 上游 `src/wasm/README.md` 的套件矩陣 ＋ 🟢 本專案實測（17 個 lib 的 `NOTES.md`）

**起源**：§036 說過，efun 是按套件（package）打包的，套件是**編譯期開關**。
WASM 版關掉了一批。這一節要回答的是：
**關掉的那些，在 mudlib 上長什麼樣子？**

這件事本專案有第一手數據——17 個 mudlib 各自的 `NOTES.md` 就是逐個量出來的落差表。

**技術核心**：先看第三方函式庫這一層。

| 元件 | WASM 版 | 為什麼 |
|------|--------|--------|
| libevent | **移除** | 換成 host 驅動的 tick 佇列（§040） |
| libwebsockets、`net/websocket.cc` | **移除** | 頁面**就是**客戶端，沒有監聽 socket |
| OpenSSL、`net/tls.cc` | **移除** | TLS 由瀏覽器負責，不需要在這裡終結 |
| **libtelnet、`net/telnet.cc`** | **保留** | 純 C、可攜；**頁面講的是真 telnet** |
| **ICU** | **保留**（交叉編譯） | 字素迭代、字集轉換、`sprintf` 寬度計算 |
| libpcre（8.x） | **保留**（JIT 關閉——wasm 沒有可執行頁） | `pcre_*` efun |
| zlib | **移除** | MCCP 與 compress 套件都關了 |
| jemalloc | **移除** | 換成 emscripten 的 dlmalloc |
| backward-cpp | **移除** | wasm 沒有原生 unwinder |
| POSIX eval-limit 計時器 | **自動停用** | 只支援 `__linux__` |

再看套件矩陣：

| 開著 | 關掉 | 只有 WASM 才有 |
|------|------|--------------|
| core、ops、math、matrix、trim、uids、sha1、parser、contrib、develop、mudlib_stats、**pcre** | **sockets**、compress、external、async、**db**、crypto、ffi | **jsbridge**（`js_eval()` / `js_call()` / `js_export()`：LPC ↔ 頁面 JavaScript 雙向） |

**踩過的坑（本專案實測）：關掉的套件不會在開機時告訴你。**

`libs/lpmudname/NOTES.md` 記的是這樣：

> WASM build 沒有 `package sockets`，以下 preload daemon 在載入時編譯失敗
> （`Undefined function socket_create` 等共 13 處）：
>
> | 物件 | 原本做什麼 | 在 WASM 上的後果 |
> |------|-----------|-----------------|
> | `/adm/daemons/kuafu` | 跨服連線（socket 監聽） | 跨服功能不存在 |
> | `/adm/daemons/qqd` | QQ 相關對外服務 | 不存在 |
> | `/adm/daemons/miraid` | 對外通報 | 不存在 |
>
> driver **照常完成開機**——這三個是 `preload` 清單裡的項目，
> 載入失敗只會讓那個物件不存在，不會中止啟動。

這正是 §038 講的：**preload 失敗是靜默的**。
所以「開得起來」與「功能完整」是兩件事，而 driver 不會替你區分。
本專案的分級（`playable` / `limited` / `noboot`）
之所以要靠**實際跑一遍註冊建角**來決定，原因就在這裡。

**★ 最值得注意的一項落差：ICU 被裁掉了表格式字集。**

上游為了體積把 ICU 的資料檔（原本約 30 MB）用 `icupkg` 裁到約 780 KB，
只留下斷詞規則（brkitr）與轉換器別名表。理由是 driver 只從資料檔裡拿斷詞資料——
字元屬性與 NFC 已編進 `libicuuc`，而 UTF-8／UTF-16／Latin-1／ASCII 的轉換是**演算法式**的，
不需要資料表。

**代價是：GBK、Big5、Shift-JIS 這些表格式字集消失了。**
`set_encoding()`／`string_encode()`／`buffer_transcode()` 一旦指向它們就會 raise LPC error。
（LPC 可以用 `__WASM__` 判斷並自行調整。）

對一本講**中文 MUD 協議**的書來說，這一條不是細節，是主線：
本專案的 mudlib 在 `master.c` 第 14 行寫著

```c
if (port == 5003) { set_encoding("GBK"); }
```

而 §039 已經說過它為什麼沒炸——`wasm_console_connect()`
把連線標成來自第一個 `external_port`，也就是 UTF-8 的 5001。
**兩個獨立的限制剛好對消了**，而這種「剛好沒事」必須被寫下來，
否則有一天有人調換兩個埠的順序，會得到一個非常難查的錯誤。

**其他已知落差**（上游明列，本專案逐條確認過）：

| 落差 | 具體症狀 |
|------|---------|
| **沒有 eval limit** | LPC 裡 `while(1);` 卡死整個分頁（§037） |
| 沒有真 DNS | `query_ip_number()` 一律回 `127.0.0.1`；`resolve()` 下一拍合成回傳 |
| 沒有 zlib | 壓縮的 `write_file`（flag 2）報錯；壓縮的 `save_object` 退化成純文字存檔；`.gz` 不會被自動解壓 |
| MEMFS 不落地 | 重整即失（持久化是上游的下一階段：IDBFS／OPFS 疊在 `/data` 上） |
| 分頁在背景時計時器被暫停 | 醒來時 gametick 補跑，**上限 100 拍** |
| `crypt()` | 屬 core，**不受 crypto 套件關閉影響**；只會看到 `old crypt() password detected` 警告 |

最後一列是本專案特地去確認的（`libs/lpmudname/NOTES.md` 的「沒有踩到的坑」）：
`crypt()` 是登入流程的關鍵（§044 會看到握手行就是它算出來的），
如果它跟著 crypto 套件一起消失，17 個 mud 一個都登不進去。
**它沒有消失，因為它住在 core 而不是 crypto**（`src/packages/core/core.spec:191`）。
這種「憑什麼沒事」的確認，和「這裡壞了」一樣值得記錄。

**優點 / 罩門**：優點是**體積**——`fluffos.wasm` 約 3.6 MB 原始大小，
過線約 0.8 MB（brotli）／1.1 MB（gzip），加約 110 KB 的 JS glue。
一台完整的 MUD driver 用不到一 MB 就送到瀏覽器裡。
罩門是**落差清單會隨版本改變**，而 mudlib 不會知道。
本專案的緩解方式是把落差變成建置產物：
每個 lib 的 `NOTES.md` 都由 `boot-test.mjs` 實測後寫回，
driver 升版就重跑一次——**落差表不是文件，是測試結果**。

**效益**：這張矩陣把「這個 mud 為什麼有些功能不見了」
從一個要現場除錯的問題，變成一個查表就能回答的問題。
17 個 lib 匯入時，絕大多數「這個 daemon 為什麼載不起來」
都可以在十秒內對應到矩陣上的某一格。

> 💡 君之一席話
> **移植的成品清單有兩份：能跑的東西，以及不見了的東西**——只交前者的人，會在半年後被人問「為什麼跨服聊天沒有反應」，而那時已經沒有人記得答案。

> 🔍 老手視角──真正的門道
> 這一節最該學的是**落差的記錄方式**。多數專案把「已知限制」寫成 README 裡的一段話，三個月後它就過期了——因為沒有東西會在它變錯的時候告訴你。本專案的做法是把它變成**建置產物**：`boot-test.mjs` 實跑一次註冊建角，把載入失敗的物件、收到的 opcode、最終分級寫回每個 lib 的 `NOTES.md`，並在檔案裡標明「由建置產生，勿手改」。這個轉換的價值不在自動化省了多少時間，在於**它讓落差清單擁有了一個過期機制**：driver 升版、mudlib 改壞，下一次建置就自己降級。老手看一份「已知限制」文件，第一個問的是「它上次被驗證是什麼時候」；答不出來的，就當它不存在。可落地的做法：任何長期文件裡的事實性斷言，想辦法讓它由一支會失敗的腳本產生——**能自己出錯的文件，才是活的**。

---

## 042　五個匯出、兩個回呼，以及三個只有這裡才有的陷阱

**標籤**：`#匯出介面` `#重入` `#時鐘` `#靜默失敗` `#閉環驗證`
**證據等級**：🟢 本專案實測（三個陷阱各有 before/after）＋ 🟡 上游 `src/wasm/README.md` §4

**起源**：接縫抽好了、套件選好了，剩下的就是**頁面怎麼開這台 driver**。
介面小得驚人：五個匯出函式、兩個回呼。

**技術核心**：上游文件給的完整形狀——

```js
const M = await createFluffOS({ print, printErr, locateFile });
M.FS.chdir('/testsuite');                       // mudlib 掛載點
M.fluffos = {
  onOutput:     (id, bytes) => {...},           // server → client 的線路位元組
  onDisconnect: (id)        => {...},
};
M.ccall('fluffos_boot',    'number', ['string'], ['etc/config.test']);
setInterval(() => M.ccall('fluffos_tick', 'number', ['number'],
                          [performance.now()]), 50);
const id = M.ccall('fluffos_connect', 'number', [], []);
M.ccall('fluffos_input', null, ['number','array','number'], [id, bytes, n]);
// 另外還有：fluffos_flag（對應 master::flag）、fluffos_disconnect、fluffos_shutdown
```

| 匯出／回呼 | 對應原生的什麼 |
|-----------|--------------|
| `fluffos_boot(config)` | `main()` 讀設定、載入 master、preload |
| `fluffos_tick(now_ms)` | libevent 的一圈迴圈 |
| `fluffos_connect()` | accept 一條連線（`user_add` → 協商 → `master::connect` → `logon`） |
| `fluffos_input(id, bytes, n)` | 一次 socket read |
| `fluffos_disconnect(id)` / `fluffos_shutdown()` | 關連線／關機 |
| `onOutput(id, bytes)` | 一次 socket write |
| `onDisconnect(id)` | 對端消失 |

`fluffos_flag` 對應 master 的 `flag()` apply（上游用它跑 LPC 測試套件）。

**還有 `jsbridge`**：只有 WASM 平台才有的套件，讓 LPC 反過來呼叫頁面——
`js_eval("navigator.userAgent")`、`js_call("fetch_json", ({url}), (: cb :))`，
以及 `js_export("inventory", (: ui_inventory :))` 讓頁面 UI 直接觸發 LPC。
**本專案沒有用它**：ZJMUD 客戶端的設計前提是「同一份前端同時服務桌面版、橋接版與 WASM 版」，
用了 `jsbridge` 就會多出一條只有 WASM 才有的路徑，
而本書第四篇整篇都在說為什麼不要那樣做。**記在這裡是為了說清楚那條沒走的路。**

**踩過的坑：三個陷阱，全部只有「伺服器在同一個分頁裡」才會出現。**

### 陷阱一：不能在 `onOutput` 裡呼叫 `fluffos_input`

`fluffos_input()` **會在自己回傳之前就產生輸出**，
而 `onOutput` 是 driver **在自己的輸出路徑中途**回呼頁面的。
此時再呼叫 `fluffos_input`，等於在 telnet 解析器還沒吐完位元組時重入 driver。

上游與官方參考前端都把這件事寫在註解裡：**“sends must be queued”**。
所以正解不是「延遲一點送」，是**一律排隊、只在 tick 的堆疊上送**。

本專案的實測 before/after：直接在回呼裡回覆 → 伺服器只回 2 行、卡在 `authing` 30 秒；
改成排隊 → 一路走到 `ESC000 0008`（要求建角）。

### 陷阱二：`fluffos_connect()` 在回傳之前就開始回呼

```js
connId = M.ccall('fluffos_connect', 'number', [], []);
//        └─ 這一行還沒賦值完，logon 的輸出已經同步回呼進 onOutput 了
```

如果 `send()` 用 `connId === null` 當作「還沒連上」的判準，
那麼**握手行觸發的第一次回覆會被靜默丟棄**——
連佇列都沒進去，`rawSend` 一次都沒被呼叫。

本專案在這裡連續誤判兩次（先怪時序、再加靜置窗），
真正找到它的方法只有一個：**在 `rawSend` 印一行，看它到底有沒有被呼叫**。
修法是把「已關閉」變成一個獨立的旗標，並且**在 `ccall` 之前**就設成 false：

```js
closed = false;                                   // ★ 必須在 ccall 之前
connId = M.ccall('fluffos_connect', 'number', [], []);
```

§032 已經把這個故事寫過一次；這裡補的是它的**通則**——
**同步回呼會在你以為的初始化完成之前抵達，
所以「初始化完成」不能用「賦值語句已執行」來表示。**

### 陷阱三：`fluffos_tick` 要的是「經過多久」，不是「現在幾點」

這一個 §032 沒寫，因為它是後來才浮出來的，而且**只有在真瀏覽器裡才會踩到**。

症狀：`91书剑` 一連上就收到「您花在连线进入手续的时间太久了」。
那是 `clone/user/login.c` 裡的 `call_out("time_check", 30)`——理應 30 秒後才跑。

| 版本 | 送進 `fluffos_tick` 的值 | 結果 |
|------|------------------------|------|
| 第一版 | `Date.now()`（epoch 毫秒） | driver 第一拍就認為過了 **1.7 兆毫秒** |
| 第二版 | `performance.now()` | 仍然錯，只是小一點——那是**分頁載入**起算的，而使用者先看清單、再等 29 MB 下載完才開機，開機時它已經是幾十萬毫秒 |
| 第三版 | `monotonicNow() - clockOrigin`，`clockOrigin` 壓在**開機那一刻** | ✅ |

**第二版最值得記**：它在 node 測試裡完全正常——
因為那邊開機就在行程起頭，`performance.now()` 還很小。
**這是一個只有在真實使用情境（先看清單、再下載幾十 MB）才會顯現的差異**，
而本專案的驗證迴路一開始沒有涵蓋它。

驗證的方式也很直接：把 `performance.now()` 加上 300000 的偏移量重跑，
**逐字重現了使用者截圖裡的畫面**（只收到握手行 + `ESC015` 逾時訊息）。
**能重現，才叫找到原因。**

**優點 / 罩門**：優點是介面小到可以完整背下來——
五個函式、兩個回呼，沒有狀態機、沒有握手、沒有版本協商。
罩門是這個介面**把原本由作業系統與網路提供的保證全部拿掉了**，
而那些保證原本是隱形的：

| 原本誰提供 | 提供了什麼保證 | WASM 版誰負責 |
|-----------|--------------|--------------|
| TCP／作業系統 | 送出與收到之間有 RTT，不會零延遲重入 | **頁面**（排隊 + 靜置窗） |
| 核心的 socket 緩衝 | 寫入不會同步觸發對端的處理 | **頁面**（只在 tick 裡送） |
| 系統時鐘／libevent | 單調、從行程起算的時間源 | **頁面**（自己記原點送差值） |
| 作業系統的行程隔離 | mudlib 跑瘋不會影響別人 | **沒有人**（eval limit 停用，§041） |

第一列解釋了為什麼本專案在橋接版從來沒踩到陷阱一與陷阱二：
**真實 TCP 的 RTT 天然蓋掉了那個空窗**。
把伺服器搬進分頁，等於把這層天然的緩衝拿掉——
**原本被延遲掩蓋的競態，會一次全部現形。**

**效益**：本專案最終把這三個陷阱與所有相容性修正，
一起固化成一條會自己跑完的驗證鏈：
`node webclient/tools/verify-fullstack.mjs`——真 DOM、真 wasm driver、真 HTTP，
選 mud → 開機 → 連線 → 登入 → 建角 → 進世界 → 換另一台 mud。
**三個陷阱都是靜默失敗，而對付靜默失敗的唯一工具就是一條會自己走完全程的路。**

> 💡 君之一席話
> **當你把兩個原本分開的東西塞進同一個行程，你拿掉的不只是網路——你拿掉的是「時間」這個天然的同步機制**；原本靠延遲僥倖成立的假設，會在那一刻全部到期。

> 🔍 老手視角──真正的門道
> 三個陷阱表面上是三種 bug（重入、狀態判斷、時鐘來源），但它們有同一個形狀：**一個原本由環境提供、從來沒有被寫下來的保證，被移植拿掉了**。這類問題最難的地方在於你沒有東西可以查——沒有人會在文件裡寫「本系統假設送出與收到之間有延遲」，因為在原本的環境裡那不是假設，是物理事實。老手處理移植時會刻意做一件事：**把「環境替我做了什麼」列出來**，一項一項問「新環境還做嗎」。時間、順序、隔離、原子性、失敗模式——這五樣是最常被無聲拿掉的。可落地的做法：移植的第一份文件不要寫「我要改什麼」，先寫「舊環境保證了什麼」；那份清單通常就是你未來三週會踩的坑的目錄，而且**寫它的成本遠低於一個一個踩**。

---



## 043　一行 `write()` — ZJMUD 是一套 driver 不知道的協議

**標籤**：`#協議分層` `#mudlib` `#GMCP` `#MXP` `#撞號` `#逆向著力點`
**證據等級**：🟡 伺服器原始碼（`include/zjmud.h`、約 4,000 處發送點）＋ 🟡 FluffOS apply 清單

**起源**：本書前七篇解剖了 116 個 opcode，卻一直沒有回答一個很基本的問題：
**這些 opcode 是誰定義的、誰實作的、誰有權新增？**

答案讓人有點意外：**driver 完全不知道它們存在。**

**技術核心**：把 `include/zjmud.h` 攤開看，整套協議的實作就是幾行巨集。

```c
#define ESA         "\x1b"              // 原始碼裡是一個裸的 ESC 位元組
#define ZJSEP       "$zj#"
#define ZJBR        "$br#"

#define SYSY        ESA + "000"         // 登入狀態
#define ZJTITLE     ESA + "002"         // 房間標題
#define ZJEXIT      ESA + "003"         // 出口
#define ZJLONG      ESA + "004"         // 房間描述
#define ZJOBLONG    ESA + "007"         // 詳情
#define ZJOBACTS2   ESA + "009"         // 動作列
#define ZJFORCECMD  ESA + "014"         // 代送指令
```

`ESA + "002"` 是 **LPC 的字串串接**——沒有任何魔法，
它在編譯期變成一個四字元字串 `"\x1b002"`。

而「送出一個 opcode」長這樣：

```c
write(ZJTITLE + query("short") + "\n");
```

**就這樣**。沒有 driver 支援、沒有 telnet 子協商、沒有註冊、沒有能力協商。
從 driver 的角度看，這只是 mudlib 呼叫了 `write()` efun 送出一串位元組，
其中第一個位元組剛好是 0x1B。

**這件事的三個後果，正好解釋了本書前面留下的三個謎題。**

### 後果一：換 driver，協議還在

driver 不知道協議存在，所以協議不會因為 driver 升級而壞掉。
本專案把一份 1990 年代的 mudlib 丟進 2026 年編出來的 WebAssembly driver，
opcode 一個不少地照樣出現在線上——**因為它們對 driver 而言只是字串**。

這也是為什麼本書 §032 那批相容性修正裡，**沒有任何一條和協議有關**：
壞掉的是 `static` 修飾詞、型別檢查、字串索引、檔案權限——
全是 LPC 語言與執行環境的問題，協議層毫髮無傷。

### 後果二：沒有註冊中心，所以會撞號

任何人拿到 mudlib，都可以加一行 `#define`。不必跟誰報備，
因為根本沒有「誰」可以報備。
於是三個世代的方言各自往同一個編號空間裡加碼，
撞了 4 個號（§010）；`045`（開啟網頁）夾在 `024` 和 `100` 中間（§005）；
一份 header 定義了 102 個 opcode 而實際只用 16 個（§009）。

**這些不是設計失誤，是「沒有中央權威」這個前提的必然結果。**
本書第三篇整篇在描述的現象，根因只有這一句。

### 後果三：逆向的著力點在 mudlib，不在 driver

這是對本專案最實際的一條。
如果協議住在 driver 裡，本書第二篇要做的事會是反編譯一個 5.5 MB 的 C++ 執行檔。
但它住在 mudlib 裡——**一份帶中文註解的 LPC 標頭檔**，
外加約 4,000 處可以直接 grep 的發送點。

§005 那個「按使用次數排實作優先序」的做法之所以可行
（`ZJOBLONG` 531 次、`ZJOBACTS2` 448 次、`ZJFORCECMD` 98 次、`ZJYESNO` **0 次**），
也是因為發送點在腳本層，一支 grep 就能統計。

**設計對照：協議住在哪一層，決定了它的一切性質。**

| | **driver 層協議** | **mudlib 層協議** |
|---|-----------------|------------------|
| 例子 | GMCP、MSDP、MXP、MSP、ZMP、NAWS、TTYPE | **ZJMUD**（以及各家中文 MUD 的自訂方言） |
| driver 支援 | 有專屬 apply（`gmcp` `msdp` `mxp_tag` `zmp` `window_size` `terminal_type`…） | **無** |
| 傳輸方式 | telnet 選項協商 + 子協商（`IAC SB … IAC SE`） | **就是普通的輸出文字** |
| 能力協商 | 有（雙方 `WILL`/`DO` 談好才啟用） | **無**（伺服器一律送，客戶端自己看著辦） |
| 誰能新增欄位 | 要改 driver，或走標準的擴充機制 | **任何拿到 mudlib 的人** |
| 純 telnet 客戶端看到什麼 | 什麼都看不到（在子協商裡） | **看得到，只是有點亂**（§004 的向下相容） |
| 撞號風險 | 低（有事實上的註冊表） | **高**（本書第三篇） |
| 逆向難度 | 要讀 driver 或抓子協商 | **讀 header** |

第五列與第七列是同一枚硬幣的兩面：
**「任何人都能加」與「一定會撞號」是同一件事的兩種說法。**

**踩過的坑**：本書 §004 提過一個沒修的弱點，現在可以說清楚它為什麼難修——
分隔符 `$zj#` 沒有跳脫機制，玩家若把 `$zj#` 打進聊天訊息就是注入。
修它需要在**每一個發送點**做跳脫，而發送點有約 4,000 處，分散在整份 mudlib 裡，
由不同的人在二十年間寫成。
**沒有中央權威的代價，在需要做全域改動時一次付清。**

**優點 / 罩門**：優點是**演化速度**——加一個 UI 面板不必碰 driver、
不必等任何人 review，寫一行 `#define` 加一個 `write()` 就上線了。
本書第三篇看到的三個世代方言（27 → 101 → 102 個 opcode），
是這個自由度的直接產物。
罩門就是上面那三條後果的下半身：撞號、無法做全域改動、沒有能力協商
（伺服器不知道對面是不是認得 opcode 的客戶端，只能一律送，
靠「舊 telnet 客戶端會把它當亂碼印出來」硬撐）。

**效益**：對任何要逆向一套 MUD 私有協議的人，這一節給的是**第一個該問的問題**：
**「這套協議住在哪一層？」**
在 telnet 子協商裡的，去讀 driver 與 RFC；
混在一般輸出文字裡的，去讀 mudlib 的 header——
後者通常只要一個下午。

> 💡 君之一席話
> **協議住在哪一層，決定了誰能改它、誰會撞號、以及你該去哪裡逆向它**——這是拿到一套私有協議時最先要問的問題，比任何一個位元組的意義都更早該問。

> 🔍 老手視角──真正的門道
> ZJMUD 選擇不走 telnet 子協商，這在協議設計上是一個**刻意的降級**，而且降得很聰明。子協商的好處是乾淨（純 telnet 客戶端完全看不到）、有能力協商；代價是你必須讓 driver 參與，而 driver 不是你的。ZJMUD 的作者把協議塞進一般輸出文字，換到的是「**我一個人就能完成這件事**」——不必等 driver 作者、不必說服任何人、不必發版本。老手在評估這種降級時看的不是技術優劣，而是**組織成本**：一個要跨團隊協調才能改的乾淨方案，往往輸給一個當天就能上線的髒方案，而且輸得很徹底。真正該做的是把髒方案的代價**明碼標出來**——本例的代價是「分隔符沒有跳脫」與「一定會撞號」，兩者都可以在第一天就寫進文件，並在編號規則上預留分區（例如「第三方擴充一律用 5xx」）。可落地的判準：當你要在一個不歸你管的東西上加擴充點，先算「請對方加一個 hook」要花幾週；若答案大於三，就設計一個不需要對方參與的方案，**然後把它的代價寫下來**。

---

## 044　握手那一行 — `crypt()`、`input_to()` 與一個沒在驗的驗證

**標籤**：`#握手` `#登入流程` `#input_to` `#安全` `#證據等級`
**證據等級**：🟡 伺服器原始碼（`adm/daemons/logind.c`、`adm/single/master.c`）＋ 🟢 實機封包

**起源**：本書 §008 從**線路上**描述了登入握手：
連上之後伺服器送一行 `ver1.0:<某串東西>`，客戶端回一個東西，然後才輪到帳密。
當時那串東西是什麼、驗什麼、驗不驗得過，**只能從外部觀察**。

現在我們知道 driver 與 mudlib 的關係了，可以直接把 LPC 那一端讀出來。

**技術核心**：整條鏈是四個 apply／efun 接起來的。

### ① 連線抵達 → `master::connect(port)`

```c
object connect(int port)
{
    object login_ob;
    mixed err;
    if (port == 5003) {
        set_encoding("GBK");            // ★ §039／§041 的那條規則
    }
    err = catch(login_ob = new(LOGIN_OB));
    if (err) {
        write("现在有人正在修改使用者连线部份的程式，请待会再来。\n");
        write(err);
        destruct(this_object());
    }
    login_ob->set("port", port);
    return login_ob;                    // ← 這條連線從此附著在這個物件上
}
```

三個細節值得停一下：

- **`connect()` 收到的是埠號**，這是伺服器唯一知道「這條連線從哪個門進來」的方式，
  也是編碼分支的全部依據。
- **整個登入物件的建立被 `catch()` 包住**（§037 的 `F_CATCH`）——
  登入物件炸掉時，連線還活著，還能寫一行人話給玩家。
  這是一段寫得很好的防禦性 LPC。
- 回傳的物件就是這條連線的 **interactive object**，
  §036 那一整排 interactive apply 從此往它身上呼叫。

### ② `logon()` — 握手行在這裡產生

```c
void logon(object ob)
{
    string str;
    if (BAN_D->is_banned(query_ip_name(ob)) == 1) { ... destruct(ob); return; }
    write("\n");
    str = crypt(ZJKEY, "byname666");        // ZJKEY = "123456789abcd"
    write("ver1.0:" + str + "\n");          // ← 線路上看到的那一行
    input_to("jiance", ob, str);            // ← 下一行輸入交給 jiance()
}
```

`crypt(key, salt)` 是傳統 Unix DES crypt，**兩個引數都是原始碼裡的常數**，
所以**這串挑戰值每次連線都一樣**——它不是 nonce，沒有隨機性。

`input_to()` 是 LPC 的互動核心：**「下一行輸入不要當指令，交給這個函式」**。
整個登入流程就是一串 `input_to` 接力。

### ③ `jiance()` — 「版本驗證」

```c
void jiance(string arg, object ob, string str)
{
    if (!arg) { input_to("jiance", ob, str); return; }   // 空行 → 再問一次
    ob->set_temp("web_log", 1);
    if (arg != "//") {
        write("版本验证成功\n");
        input_to("get_user", ob);
    } else {
        start_wiz = 1;
        get_user("name║name1║byname666║byname666", ob);
    }
}
```

**這是這一節最重要的發現，而且只有讀原始碼才看得到：**

> **`jiance()` 根本沒有驗證任何東西。**
> 它把 `str`（剛剛算出來的 crypt 值）當引數收進來，**但一次都沒有用到**。
> 只要送出的不是空行，就一律印「版本验证成功」放行。

從線路上看，這是一個「挑戰－回應」握手；
從原始碼看，它是一個**只檢查「你有沒有回話」的存活探測**。

這解釋了本專案 `src/js/telnetlogin.js` 裡那條看起來很隨便的規則：

```js
nt7: [
  P(/^ver1\.0[,:]/, 'zjclient', 'challenge', { req: false }),
  ...
]
```

**回 `zjclient` 就過**——不是猜對了密碼，是因為**根本沒有密碼可以猜**。
這條規則在寫的時候是實測歸納出來的（🟢），現在有了原始碼佐證（🟡），
兩份證據互相印證。

**★ 安全揭露：`else` 分支是一個後門。**

送出 `//` 這兩個字元，會走進第二個分支：
`start_wiz = 1`，並直接以硬編的憑證
`"name║name1║byname666║byname666"` 呼叫 `get_user()`——
**跳過帳號密碼，以一個固定帳號登入，且帶著 `start_wiz` 旗標**
（該旗標在 `logind.c` 裡有 11 處引用，用來略過各種登入檢查）。

這段程式碼在上游是公開的，本節只是把它的行為說清楚。
**任何要用這份 mudlib 開服的人，第一件事應該是刪掉這個分支。**
本專案的 WASM 展示站不受影響——每個訪客拿到的是一份跑在自己分頁裡、
重整就消失的世界（§032），沒有可以被入侵的對象。
專案的其他憑證與外送問題記在倉庫根目錄的 `SECURITY_NOTES.md`。

### ④ `get_user()` — 帳密欄位

```c
private void get_user(string arg, object ob)
{
    // arg = 账号║密码║密文║email
```

**四個欄位，用 `║`（U+2551）分隔**——這正是本書 §008 從線路上量到的格式，
現在有了它的出處與欄位名。

**解決的痛點**：把 §008 的每一個「觀察到的行為」升級成「知道它為什麼」：

| §008 的觀察（🟢 實機） | §044 的解釋（🟡 原始碼） |
|---------------------|----------------------|
| 連上後先收到一行 `ver1.0:…` | `logon()` 裡的 `write()` |
| 那串值每次連線都一樣 | `crypt()` 的兩個引數都是常數 |
| 回什麼都能過 | `jiance()` 沒有比對 `str` |
| 之後送 `账号║密码║密文║email` | `get_user()` 的引數格式 |
| 埠 5001 是 UTF-8、5003 是 GBK | `master::connect()` 的 `if (port == 5003)` |

**這張表就是「證據等級」這條規矩的價值示範**：
五條斷言全部先由實機封包確立（🟢，可以直接拿來寫客戶端），
再由原始碼補上原因（🟡，讓你知道它在什麼情況下會變）。
**兩者都需要——只有 🟢 你不知道邊界，只有 🟡 你不知道它有沒有被實際執行。**

**踩過的坑**：`input_to()` 的提示**沒有換行**。
`get_user` 這一路還好（zjmud 客戶端有自己的登入視窗），
但 §045 那些傳統 telnet lib 會印「请输入您的英文名字：」然後就停在那裡等，
**行尾沒有 `\n`**。一個逐行分幀的讀取器會把它一直留在緩衝裡，
於是客戶端「什麼提示都沒看到」。
本專案的處理是**提示行沖洗**：伺服器靜默一小段時間（300 ms）後，
把分行器裡未斷行的殘餘當成一行交出去（`telnet.js` 的 `flushPartial`）。

**優點 / 罩門**：優點是這個登入流程完全用 `input_to` 串成，
**可讀性極高**——一個函式一個步驟，順序就是程式順序。
罩門是它**沒有狀態機**：每一步用「下一行輸入交給誰」隱式表達，
所以任何一步忘了呼叫 `input_to`，連線就會安靜地卡在那裡，
既不報錯也不斷線。本專案的 `boot-test.mjs` 因此必須自帶逾時。

**效益**：這一節把本書第二篇的握手規格從「照做就能連上」
升級成「知道哪些部分可以變、哪些不會變」。
具體地說：**挑戰值是常數所以可以硬編，但驗證邏輯是可改的，
所以客戶端不該假設「隨便回都過」永遠成立**——
本專案的 `telnetlogin.js` 因此把它寫成一條**可以被覆寫的 profile 規則**，
而不是寫死在連線流程裡。

> 💡 君之一席話
> **一個看起來像挑戰－回應的握手，可能只是在確認你還活著**——從線路上你分不出這兩者，只有讀到實作那一行才知道；這正是為什麼「實機封包」要排在最前面，而「原始碼」不能被省略。

> 🔍 老手視角──真正的門道
> `jiance()` 這種「收了引數卻沒用」的驗證，在逆向工作裡出現的頻率高得驚人，而且有一個共同的成因：**它曾經是真的**。有人一開始寫了比對，後來為了除錯或相容舊客戶端把它拿掉，卻留下了整個外觀——函式名、引數、印出來的「验证成功」。老手看到一個驗證邏輯時，會先找**它的失敗路徑**：沒有 else、沒有錯誤訊息、沒有斷線處理的驗證，通常根本不驗。這個判準比讀懂驗證演算法本身快得多，而且在只有反編譯的情況下也適用——**看它有沒有處理失敗，比看它怎麼判斷成功更能說明問題**。可落地的做法：逆向一個握手時，優先做的實驗不是「送對的值」，是**送一個明顯錯的值**；對面的反應會在一秒內告訴你這個驗證是真是假，而讀懂演算法可能要一天。

---

## 045　讓 zjmud 客戶端玩不是 zjmud 的 mud — 會話層代打

**標籤**：`#相容層` `#會話層` `#profile` `#取捨` `#邊界`
**證據等級**：🟢 本專案實測（`src/js/telnetlogin.js`，每條規則出自對應 lib 的 logind 原始碼）

**起源**：`mudlibs.fluffos.info` 是 FluffOS 社群做的中文 MUD 博物館——
**199 個 1990 年代以降的中文 LPC mudlib，全部用 WebAssembly 跑在瀏覽器裡，
其中 193 個標為可玩**。本專案的 `libs/` 佈局就是參考它的 `fluffos/mudlibs` 倉庫來的。

於是一個很自然的念頭：既然 driver 一樣、載入方式一樣，
**能不能用 zjmud 客戶端去玩那 193 個？**

答案是：**連得上、看得到招牌、進不去。**

**技術核心**：問題出在會話層，而不是傳輸層或協議層。

```
傳輸層   TCP／WASM 位元組管道            ✅ 一樣
telnet   IAC 協商                        ✅ 一樣（driver 都送）
分幀     \n 逐行                         ✅ 一樣
─────────────────────────────────────────────────────
會話層   登入對話                        ❌ 完全不同
協議層   ESC + opcode                    ❌ 對方沒有
```

zjmud 客戶端的登入視窗收到帳密後，送的是 `账号║密码║密文║email`（§044）。
傳統 telnet lib 看到的是一行含有 `║` 的怪字串——它想要的是
「请输入您的英文名字：」的答案。

**解法：讓客戶端按提示把這段對話演完。**

登入對話（招呼 → 帳號 → 密碼 → 確認 → 建角）是一段
**與遊戲內容無關的固定劇本**。既然如此，客戶端可以代替使用者一行一行回答：
伺服器每印一行，接應器比對這一行像哪一步，把對應的輸入送回去。

```js
const P = (match, send, note = '', opts = {}) => ({ ... });

export const PROFILES = {
  dongfanggushi2: [
    P(/您的英文名字：/,                 '{id}',   'name'),
    P(/新的人物.*\(y\/n\)|您确定吗\(y\/n\)/, 'y',  'confirm'),
    P(/请再输入一次您的密码/,           '{pw}',   'pw-confirm'),
    P(/请设定您的密码：/,               '{pw}',   'pw'),
    P(/电子邮件地址：/,                 'a@b.c',  'email'),
    P(/你的选择：/,                     'human',  'race'),
    P(/男性\(m\)的角色或女性\(f\)/,      'm',      'gender'),
    P(/您的中文名字：/,                 '秦风',   'cname'),
  ],
  // …其他家族，以及 generic-cn 退路
};
```

四個設計決定，每一個都有代價：

| 決定 | 理由 | 代價 |
|------|------|------|
| **規則由上往下比對，一行最多觸發一條** | 提示字串會互相包含（「请再输入一次您的密码」含「密码」） | 順序變成隱性契約，改動要小心 |
| **`once` 語意（觸發過就不再觸發）** | 伺服器重印提示時不要重覆送 | 需要一個 `seen` 集合 |
| **帳密來自登入視窗，其餘（email／種族／中文名）用固定值** | 那些欄位使用者不在乎，也沒有 UI 可問 | 所有用這條路徑建的角色 email 都一樣 |
| **`generic-cn` 退路，字眼取自多個家族 logind 的交集** | 193 個 lib 不可能一個一個寫 profile | 交集規則比專屬 profile 脆 |

**整層都在客戶端**——mudlib 一個位元組都不動，driver 也不用改。

**踩過的坑（每一條都是實測，不是預防性猜測）：**

| 坑 | 症狀 | 真因 |
|----|------|------|
| 性別答錯不能重來 | 建角時 runtime error 直接斷 | `dongfanggushi2` 的 logind 在性別 retry 分支有 bug——**必須一次答對** |
| 中文名被拒 | 卡在「请您用中文取名字」 | 該 lib 的 `is_chinese()` 逐位元組判斷（§032）；所以 profile 直接送一個已知合法的中文名 |
| 提示看不到 | 接應器一條規則都沒觸發 | `input_to` 的提示**沒有換行**（§044），逐行讀取器把它留在緩衝裡 → 需要 `flushPartial` |
| nt7 卡在第一行 | 收到 `ver1.0:` 就沒下文 | 那是 zjmud 握手殼 + telnet 互動登入的混血；補一條「看到挑戰行就隨便回」的規則（§044 解釋了為什麼隨便回就過） |

**★ 這一層的邊界，必須說清楚。**

接應器**只做到第一層：登入 → 進世界 → 純文字可玩**。
房間面板、血條、動作列那些需要**結構化資料**的 GUI 一律沒有——
因為對方根本沒有送 opcode。

要更進一步只有兩條路，而且都不便宜：

| 路線 | 做法 | 代價 |
|------|------|------|
| **A：客戶端剖析文字** | 用規則從「大廳 - 你站在一個寬敞的大廳裡。出口有：north east」剖出房間名與出口 | **脆**。每個 lib 的排版不一樣，改版就壞；本質上是在重蹈本書 §004 說的「沒有跳脫的分隔符」老路 |
| **B：改 mudlib 加 opcode** | 在對方的 mudlib 裡加上 `ESC003` 之類的輸出 | 要動別人的 mudlib，等於替 193 個 lib 各做一次移植 |

本專案選了**兩條都不走**，只交付第一層，並把邊界寫在檔案的頂端註解裡。
取捨的完整討論在 `docs/zjmud_migration_SOP.md` §5。

**優點 / 罩門**：優點是**一份客戶端服務兩個世界**——
zjmud 方言的 mud 走完整 GUI，傳統 telnet 的 mud 走純文字，
使用者只感覺到「有些 mud 沒有面板」。
罩門是 profile 表是**規則的集合而不是狀態機**：
它不知道自己現在在登入流程的第幾步，只知道「這一行像什麼」。
所以一個提示字串出現在意料之外的位置，它就會答錯。
這是刻意的取捨——狀態機要為 193 個 lib 各寫一份，規則表可以共用。

**效益**：`mudlibs.fluffos.info` 那 199 個 lib 有 193 個標為可玩，
本專案收錄 17 個並且 17 個都可玩。
兩者的差別不在 driver（同一份），而在**客戶端要不要理解對方的方言**——
博物館用的是通用 telnet 終端機（xterm.js），
本專案用的是一個懂 ZJMUD 方言的 GUI 客戶端，
所以它必須額外處理「對方不講這個方言」的情況。
**這一節就是那筆額外費用的帳單。**

> 💡 君之一席話
> **相容層最重要的不是它做到了什麼，是它明確地不做什麼**——一個誠實標好邊界的半套方案可以長期維護，一個假裝自己完整的方案會在每個新案例上被打臉。

> 🔍 老手視角──真正的門道
> 這一節的取捨有一個很通用的形狀：**面對 N 個不一致的對端，你可以寫 N 個精確的適配器，或一個模糊的通用規則，或什麼都不做只交第一層。** 多數團隊會不自覺地選第一個，因為每一個適配器單獨看都很合理——結果三年後有 40 個適配器、沒有人敢刪、每次上游改版都要全部重測。本專案選的是「幾個精確 profile + 一個通用退路 + 明確的功能邊界」，關鍵在最後一項：**功能邊界壓低了，適配器就不需要精確**。登入對話只有五六個步驟，猜錯了使用者重試一次就好；如果目標是完整 GUI，每個 profile 就得處理幾十種輸出格式，那才是真正會失控的地方。可落地的判準：在寫第三個適配器之前先停下來問——**「如果我把功能範圍砍一半，還需要幾個適配器？」**答案常常是「一個都不用」。

---

## 046　一個位元組的全程 — 從 LPC 的 `write()` 到瀏覽器的 DOM

**標籤**：`#全鏈路` `#分層` `#收束` `#端到端驗證`
**證據等級**：🟢 本專案實測（`verify-fullstack.mjs` 每次建置都跑）

**起源**：這本書寫到這裡，兩端都攤開了。最後把它們接起來——
**追一個位元組，從它在 LPC 裡被寫出來，到它變成畫面上的一個房間標題。**

**技術核心**：WASM 版的完整路徑，十一站，全部在同一個分頁裡：

```
① LPC        write(ZJTITLE + "大厅" + "\n")
                └─ ZJTITLE 在編譯期已是字串 "\x1b002"（§043）
                          ↓  write() efun
② VM         F_ 指令執行，svalue 進出雙堆疊（§037）
                          ↓
③ comm.cc    使用者輸出緩衝（每個平台同一份 code）
                          ↓
④ telnet.cc  telnet 編碼（0xFF 跳脫成 0xFF 0xFF）
                          ↓
⑤ Transport  WasmConsoleTransport::write()（§040）
                          ↓
⑥ JS         Module.fluffos.onOutput(id, bytes)
                          ↓
⑦ telnet.js  剝除 IAC、產生「一律拒絕」的回覆、逐行切分（§附錄 B）
                          ↓
⑧ protocol.js 取行首 4 字元 → "\x1b002" → opcode 002 → { kind:'title', … }（§005）
                          ↓
⑨ ansi.js    欄位文字裡的樣式方言 → span 描述（§007）
                          ↓
⑩ store.js   reducer 更新狀態；opcode 002 的副作用連帶清空出口／物件／標題按鈕（§005）
                          ↓
⑪ ui.js      DOM 更新 → 玩家看到房間標題變成「大厅」
```

**這條鏈上的每一站，本書都有一節在講**，而它們的分工可以用一句話概括：

| 站 | 誰的責任 | 這本書的哪一篇 |
|----|---------|--------------|
| ①② | mudlib 作者 + LPC VM | 第八篇（§037、§043） |
| ③④⑤ | driver，跨平台共用 | 第八篇（§040） |
| ⑥ | 移植接縫 | 第八篇（§042） |
| ⑦ | 傳輸層清理 | 第二篇（§006） |
| ⑧⑨ | **純函式**，可單元測試 | 第二、三、六篇 |
| ⑩ | 狀態容器 + reducer | 第四、六篇（§022、§023） |
| ⑪ | DOM 元件 | 第六篇（§024） |

**三個觀察，是把兩端接起來之後才看得出來的：**

**其一：橋接版與 WASM 版的差別只有一站。**

```
橋接版：  ⑤ SocketTransport → TCP → bridge/server.mjs → WebSocket → ⑥
WASM 版： ⑤ WasmConsoleTransport ─────── 函式呼叫 ───────────────→ ⑥
```

第 ⑥ 站之後**完全相同**，第 ⑤ 站之前也**完全相同**。
這是本書第四篇那個「雙形態」架構的最終驗收：
**Transport 抽象在客戶端與 driver 兩側各有一份，而且它們的分界線畫在同一個位置。**
這不是巧合——兩邊面對的是同一個問題（「位元組從哪來」與「位元組往哪去」），
所以收斂到同一個形狀。

**其二：協議層（⑧⑨）兩端都不知道對方存在。**

driver（③④⑤）不知道 opcode（§043）；
`protocol.js`（⑧）不知道 driver（它只吃字串陣列）。
**這條鏈上最核心的那個知識，兩端都不持有**——
它只存在於 mudlib 的 header 與客戶端的解析器裡，隔著八站遙相呼應。
本書第一篇之所以能只靠「一份 header + 一個 APK + 53 行封包」還原整套協議，
正是因為協議知識被壓縮在這兩個點上。

**其三：靜默失敗集中在特定幾站。**

| 站 | 出錯時的表現 |
|----|------------|
| ① mudlib apply 名字打錯 | **靜默**（§036） |
| ① preload 物件載入失敗 | **靜默**（§038、§041） |
| ⑤⑥ 重入／connId 未賦值 | **靜默**（§042） |
| ⑦ 沒剝 IAC | 解碼炸掉——**會叫** |
| ⑧ opcode 不認得 | 當成一般訊息印出來——**看得到** |
| ⑩⑪ reducer／DOM | 例外——**會叫** |

**會叫的都在客戶端，靜默的都在伺服器側與接縫上。**
這解釋了本專案為什麼把驗證重心放在端到端：
前半段沒有任何東西會替你報錯。

**效益**：這條鏈最後被固化成一條指令：

```bash
node webclient/tools/verify-fullstack.mjs
```

真 DOM、真 wasm driver、真 HTTP——
選 mud → driver 開機 → 連線 → 登入 → 建角 → 進世界 → 換另一台 mud。
**十一站全部走一遍，每次建置都跑。**

本書開頭那句核心命題，到這裡可以收尾了：

> 逆向工程的盡頭，不是「我看懂了」，而是「我能證明它，而且我的實作經得起真機打臉」。

第八篇補上的，是這句話的另一半——
**「真機」是什麼，你也得看懂**。
一個 1989 年設計的虛擬機、一次 1992 年的分家、一份 2026 年編出來的 WebAssembly，
以及一套 driver 從頭到尾都不知道自己在傳的協議。
**知道對面是什麼，你才知道哪些觀察是穩定的，哪些只是這次剛好。**

> 💡 君之一席話
> **把一條鏈的兩端都攤開之後，才看得出知識集中在哪兩個點上**——而那兩個點，就是逆向該用力的地方，也是文件該寫厚的地方。

> 🔍 老手視角──真正的門道
> 這一節做的事叫「**全鏈路走查**」，它的價值不在發現 bug，在**發現責任的空隙**。把十一站列出來之後，三個原本說不清楚的問題自己有了答案：橋接版與 WASM 版差在哪（一站）、協議知識住在哪（兩個端點）、為什麼測試要做成端到端（前半段全部靜默）。這些結論沒有一個需要新的資訊——它們一直都在，只是分散在八個章節裡，沒有被排成一列。老手在接手一個系統時，很早就會做這件事：**挑一個最典型的輸入，從它進入系統的第一奈秒追到它離開的最後一刻，把每一站列出來，標上「誰負責」與「出錯時會不會叫」**。這張表通常一個下午就能做完，而它回答的問題比讀三天原始碼還多。可落地的做法：把這張表放進 README 或 onboarding 文件的第一頁——新人問的前十個問題，有八個能在上面直接找到答案。

---



# 第九篇　引擎、mudlib 與客戶端：三層各自的架構

> 第八篇回答了「連線的另一端是什麼」。這一篇往下鑽三層，各問一個架構問題：
>
> - **引擎**：MudOS 在 LPMud 上加的到底是什麼？——答案是**一整個能力面**，而且它在編譯期就定死了。
> - **mudlib**：一份 30 年的中文 mudlib 是怎麼組起來的？——答案是**特徵組合 + 屬性樹 + 守護程序**三件事，不是繼承樹。
> - **客戶端**：MUD 客戶端三十年來為什麼都長一樣？——答案是**伺服器只吐文字**，而四代頻外協議都在試圖修正這一點。
>
> 三章共用一個提問方式：**這個設計把知識放在哪一層？**

---

## 047　MudOS 加了什麼 — 一個能力面，而不是一堆功能

**標籤**：`#MudOS` `#能力面` `#套件` `#virtual物件` `#安全模型`
**證據等級**：🟡 上游 `fluffos/docs/archive/`（MudOS 時代的 defines 清單）＋ 🟢 本專案 17 份 mudlib 實測

**起源**：§034 講了 MudOS 為什麼分家，但沒回答**它到底多了什麼**。
從 mudlib 作者的角度，答案不是「多了幾個函式」，而是——
**多了幾件原本做不到的事**，而每一件都在 mudlib 層長出一整個子系統。

**技術核心**：把 MudOS 帶來的能力，按「它讓 mudlib 長出什麼」排列。

| 引擎能力 | mudlib 層因此長出來的東西 | 在本專案 17 份 lib 裡的形跡 |
|---------|------------------------|--------------------------|
| **socket efun**（LPC 直接開 TCP） | 整個 `adm/daemons/network/`：intermud、inetd、ftpd、httpd | 15 份 lib 帶著這整套（§049） |
| **virtual object**（`compile_object` apply） | 一萬間房不必一萬個檔——路徑當參數，物件當場生成 | 兩個家族都有 `virtuald` |
| **shadow**（在既有物件外掛一層攔截） | 狀態效果、詛咒、臨時改寫行為，不必動原物件 | 家族 B 有整個 `shadow/` 目錄 |
| **`save_binary`／binaries** | 編譯結果落地，重開機不必重編一萬個檔 | `dongfanggushi2` 帶著整個 `binaries/` 目錄 |
| **swapping**（閒置物件換出記憶體） | 世界可以大於記憶體 | `dongfanggushi2` 的封存裡還留著 `adm/tmp/swapfile.MudCastle.5555` |
| **uid / euid 安全模型** | 巫師分權、`valid_read`／`valid_write` 那一整套 | §032 那個 `valid_write` fail-closed 的坑 |
| **mudlib_stats** | 依作者／領域統計負載，用來抓寫爆的巫師 | `adm/daemons/` 的統計類 daemon |
| **function pointer（`(: :)`）** | callback 風格的 API：排程、事件、UI 選單 | 遍佈 `call_out` 與各種 `d` |
| **mapping** | §052 的屬性樹——整個遊戲狀態的載體 | 每一份 lib 的核心資料結構 |
| **`parse_command` / `sscanf`** | 自然語言式指令（`get sword from bag`） | `cmds/` 底下的指令實作 |

**這張表的讀法是由右往左**：你在 mudlib 裡看到的每一個子系統，
背後都對應引擎的某一項能力。反過來說——
**引擎沒有的能力，mudlib 就長不出對應的東西**，
而這正是 §041 那份 WASM 套件矩陣會直接打到 mudlib 的原因。

**★ 關鍵性質：能力面是編譯期決定的。**

MudOS 的能力不是執行期查詢的，是**編譯 driver 時用 `#define` 開關的**。
上游 `docs/archive/defines/lpc/` 至今還留著那個年代的旗標清單，
每一個都是「這台 driver 有沒有這個能力」的宣告：

```
HAS_SOCKETS   HAS_SHADOWS   HAS_PRIVS    HAS_ED       HAS_MATH
HAS_MATRIX    HAS_PRINTF    HAS_RUSAGE   HAS_EACH     HAS_LIGHT
HAS_MUDLIB_STATS   HAS_MUDLIB_ERROR_HANDLER   HAS_DEBUG_LEVEL   ...
```

而 mudlib 這一側的用法是**條件編譯**：

```c
#ifdef HAS_SOCKETS
    // 整個 intermud 子系統
#endif
```

**這個設計在 1992 年很自然**（那個年代所有東西都是編譯期決定的），
但它造成了一個影響三十年的後果，值得單獨講一節（§048）。

**解決的痛點**：MudOS 要解的是一個很具體的問題——
**LPMud 的 mudlib 作者不斷需要 driver 沒有的東西**。
想做 MUD 之間互聯，要 socket；想做上萬間房，要 virtual object；
想做狀態效果，要 shadow。
每一個需求原本都得改 C、重編 driver——
而 driver 不是 mudlib 作者的。

MudOS 的答案是**一次把能力面加寬**，讓大部分需求落進 LPC 就能解決的範圍。
它的「batteries-included」路線就是這個意思。

**踩過的坑**：能力面加寬的代價是**能力面變得不整齊**。
`HAS_LIGHT`（光照系統）與 `HAS_SOCKETS`（TCP）並列在同一張旗標表上，
一個是遊戲機制、一個是系統能力——
**引擎開始承擔「遊戲設計意見」**。
這種混雜在後來的 FluffOS 套件矩陣裡還看得到痕跡：
`parser`（自然語言指令）與 `db`（資料庫）也是並列的。

**優點 / 罩門**：優點是 mudlib 作者的天花板被抬高一大截——
§049 那個「用 LPC 寫一整個網路堆疊」的事，沒有 socket efun 根本不可能發生。
罩門有兩個：其一，**能力面越寬，mudlib 越難移植**（§048）；
其二，**引擎開始有遊戲觀點**，而遊戲觀點會過時。

**效益**：對逆向與移植工作，這張表提供的是**一張因果地圖**：
看到 mudlib 裡有 `network/services/`，就知道它假設 driver 有 socket；
看到 `binaries/`，就知道它假設有 `save_binary`；
看到 `shadow/`，就知道它假設 shadow 沒被關掉。
**從 mudlib 的目錄結構，可以反推它對引擎的假設清單**——
而那份清單就是移植風險清單。

> 💡 君之一席話
> **引擎加的不是功能，是可能性的邊界**——邊界推到哪裡，上層就會長到哪裡；所以看一份 mudlib 長成什麼樣，等於在看它底下那台引擎當年開了哪些能力。

> 🔍 老手視角──真正的門道
> 「batteries-included」這條路線有一個很少被講清楚的代價：**它讓「這台引擎能做什麼」變成一個難以回答的問題**。LPMud 時代的答案很短，MudOS 之後變成一張旗標表，FluffOS 今天是 600 多個 efun 加十幾個套件。能力面一寬，上層就會用掉它——然後你的 mudlib 就綁死在一組特定的編譯選項上，而那組選項通常沒有被寫在任何地方。老手在評估這類引擎時會做一件很無聊但很值錢的事：**把上層實際用到的能力掃出來**（grep efun 名字、grep 條件編譯旗標），得到一張「真實依賴清單」，再拿它去對照目標環境。本專案 §041 的套件矩陣加上每個 lib 的 `NOTES.md`，本質上就是這張清單的現代版。可落地的做法：任何依賴「宿主提供能力」的專案（外掛、腳本層、擴充模組），第一天就寫一支掃描器產出依賴清單——**能力面越寬，這支腳本越值錢**。

---

## 048　編譯期決定的能力，執行期才發現的缺口

**標籤**：`#編譯期` `#能力探測` `#可移植性` `#靜默失敗` `#條件編譯`
**證據等級**：🟢 本專案實測（17 份 lib 的落差表）＋ 🟡 上游 `src/wasm/README.md`

**起源**：§047 最後留了一個問題：**能力面在編譯期決定，會怎麼樣？**

答案是：**mudlib 沒有辦法在載入之前知道自己缺什麼。**

**技術核心**：把這條因果鏈完整攤開。

```
①  driver 編譯期：某個套件被關掉
        ↓（沒有任何紀錄留在 driver 對外的介面上）
②  mudlib 載入期：用到該套件 efun 的檔案編譯失敗
        ↓（preload 失敗是靜默的，§038）
③  執行期：那個物件不存在
        ↓（呼叫它的地方拿到 0）
④  玩家：某個功能「就是沒反應」
```

**四個階段之間沒有任何一步會主動報錯給使用者。**
這正是本專案要靠端到端實跑來分級的原因（§041）。

**三種能力探測的形狀**，以及它們各自能問到什麼：

| 手段 | 問的是什麼 | 什麼時候能問 | 局限 |
|------|-----------|-------------|------|
| `#ifdef HAS_SOCKETS` / `__PACKAGE_*__` | **這台 driver 編進了什麼** | 編譯期 | 要 mudlib 事先寫好分岔——**老 mudlib 不會為未來的環境寫分岔** |
| `function_exists()` / `efun::` 探測 | 某個函式在不在 | 執行期 | 要一個一個問，且不知道該問哪些 |
| **實際載入一次，看誰失敗** | 真實可用性 | 執行期 | 破壞性最低但最慢——**這是唯一對老 mudlib 有效的方法** |

第三列是本專案最終採用的：`boot-test.mjs` 真的把每份 mudlib 開一次機、
走一次註冊建角，把失敗的物件記進 `NOTES.md`。
**對一份沒人維護、也不可能回頭加分岔的 mudlib，能力探測只能靠實跑。**

**解決的痛點**：這一節解的是一個判斷問題——
**「這份 mudlib 能不能在這台 driver 上跑」不是一個可以用讀的回答的問題。**

本專案一開始試過讀：掃 mudlib 用了哪些 efun、對照套件矩陣、預測會壞在哪。
結論是**這條路只能抓到一半**，因為：

- 有些 efun 在 core 而不在你以為的套件裡（`crypt()` 就是，§041）；
- 有些失敗不是 efun 缺，是**語意變了**（`is_chinese()` 的逐位元組判斷，§032）；
- 有些失敗跟能力無關，是**封存不完整**（nt 家族的缺件，§032）。

**靜態分析能回答「哪些 efun 不存在」，回答不了「這份 mudlib 會不會動」。**

**踩過的坑**：條件編譯在**老** mudlib 上有一個反直覺的性質——
**它是一種對未來的預言，而預言只涵蓋作者當年想像得到的環境。**

一份 1998 年的 mudlib 會寫 `#ifdef HAS_SOCKETS`，
因為當年真的有人跑沒開 socket 的 driver。
但它不會寫 `#ifdef __WASM__`，也不會為「ICU 沒有 GBK 表」留分岔——
**那些環境當年不存在**。

所以現代移植遇到的缺口，幾乎都落在**老 mudlib 沒有分岔的地方**。
這不是老作者的疏失，是條件編譯這個機制的本質限制：
**它只能處理「已知的變異」，處理不了「未知的環境」。**

FluffOS 為新環境補的旗標（`__WASM__`、`__PACKAGE_*__`）對新 mudlib 有效，
對三十年前的封存無效。

**優點 / 罩門**：編譯期能力面的優點是**零執行期成本、且不可能被繞過**——
沒編進去的 efun 是真的不存在，不會有半殘的實作。
§040 提過 FluffOS 對 TLS 的處理正是這種思路：不給假實作，**連呼叫點都移除**。
罩門就是這整節在講的：**缺口只有在執行期才浮現，而且浮現的方式是靜默的。**

**設計對照：三種「宿主能力」的宣告方式**

| 方式 | 例子 | 缺口何時被發現 | 老程式碼的下場 |
|------|------|--------------|--------------|
| **編譯期旗標** | MudOS `HAS_*`、FluffOS 套件 | 載入期（靜默） | 沒寫分岔就直接失敗 |
| 執行期能力查詢 | 瀏覽器 `'x' in navigator`、OpenGL extension | 執行期（可處理） | 需要事先寫探測 |
| **宣告式依賴** | `package.json`、Cargo feature、`-std=` | **安裝／建置期（明確報錯）** | 至少會告訴你缺什麼 |

第三列是現代生態的答案，也是 §035 那個「相容性帳單」的解法方向：
**把依賴變成可讀的資料，缺了就明確報錯**——
而不是讓它變成一個「某個功能沒反應」的客服工單。

**效益**：本專案因此建立了一條原則，寫在 `libs/README.md` 的流程裡：
**分級由實跑決定，不由靜態分析決定。**
`playable` / `limited` / `noboot` 三級是 `boot-test.mjs` 走完
「註冊 → 建角 → 進世界」之後給的，不是人工標的。
好處是它有過期機制——driver 升版或 mudlib 改壞，下一次建置就自己降級（§041）。

> 💡 君之一席話
> **條件編譯是一種對未來的預言**——它只能覆蓋作者當年想像得到的環境；所以任何要活過二十年的程式碼，真正的可移植性不來自它寫了多少 `#ifdef`，而來自它有多少東西可以用實跑驗證。

> 🔍 老手視角──真正的門道
> 這一節的通用形狀是「**能力宣告的時機**」，它比表面看起來更有分量。編譯期宣告（快、不可繞過、缺口靜默）、執行期探測（可處理、要事先寫）、宣告式依賴（明確報錯、要生態支持）——三者不是優劣關係，是**把發現缺口的時間點往前搬多少**的關係。老手在設計外掛／腳本／擴充系統時，會刻意把這個時間點往前推到「使用者還在裝東西」的階段，因為那是使用者最有耐心、最容易理解錯誤訊息的時刻；推到執行期，錯誤訊息就會變成客服工單。可落地的做法：如果你的系統有「宿主提供的能力」這個概念，**加一個啟動時的能力清單比對**——把上層宣告需要的能力和宿主實際提供的比一次，缺了就在啟動時明確拒絕。這比在第 500 次呼叫時回傳 `undefined` 便宜一個數量級。

---

## 049　用 LPC 寫一整個網路堆疊 — intermud 的架構

**標籤**：`#intermud` `#分散式` `#服務發現` `#查詢應答` `#TMI-2`
**證據等級**：🟢 本專案實測（17 份 lib 中 15 份帶著這整套）＋ 🟡 檔頭一手署名

**起源**：§047 說 socket efun 讓 mudlib 長出了整個網路子系統。
這一節看那個子系統的**架構**——因為它是一個 1993 年的分散式系統，
而它的形狀出乎意料地現代。

**技術核心**：`adm/daemons/network/` 底下是一組成對的服務。

```
adm/daemons/network/
├── dns_master.c              名錄：誰在線上、位址是什麼
└── services/
    ├── ping_q.c   ping_a.c        探活
    ├── rwho_q.c   rwho_a.c        誰在線上
    ├── mudlist_q.c mudlist_a.c    有哪些 mud
    ├── gfinger_q.c gfinger_a.c    查某人的資料
    ├── mail_q.c   mail_a.c        跨 mud 信件
    ├── locate_q.c locate_a.c      找人在哪台
    ├── gtell.c    gchannel.c      跨 mud 私聊／頻道
    ├── startup.c  shutdown.c      上下線通告
    ├── support_q.c support_a.c    能力協商
    ├── tcp.c                      我支援哪些服務
    └── warning.c  gwizmsg.c       管理訊息
```

**架構要點只有三條，但每一條都眼熟：**

1. **`_q` / `_a` 成對 = 請求與回應分離的檔案**。
   一個服務兩個檔：`ping_q.c` 負責「我去問別人」，`ping_a.c` 負責「別人來問我」。
   **客戶端與伺服端的角色被寫成兩個獨立單元**，而同一台 mud 兩邊都跑。
2. **`dns_master.c` = 服務發現**。誰在線上、位址是什麼、走哪個埠，
   由一個名錄 daemon 維護，其他服務向它問路。
3. **`tcp.c` = 能力宣告**。它的內容非常單純——
   一組 `support_tcp_mail()` / `support_tcp_tell()` / `support_tcp_finger()`
   回傳 1 或 0，**讓對面知道我這台支援哪些服務**。

第 3 點請和 §048 對照著看：**這是一個執行期的能力宣告**，
而且是 mudlib 層自己做的——
**引擎的能力面是編譯期的、閉嘴的；mudlib 之間的能力面是執行期的、會說話的。**
同一個問題，兩層給了完全相反的答案。

**★ 這一整套的出身，寫在檔頭裡。**

```c
/* File    : tcp.c
 * Creator : Grendel@Tmi-2
 * When    : 93-08-15
 *
 * This file is part of the tmi mudlib.  Please keep this header intact.
 */
```

**TMI-2** —— 就是 §034 那篇 1992 年貼文裡的 TMI（The Mud Institute），
MudOS 的發源組織。他們的參考 mudlib 叫 TMI-2。

那句 “Please keep this header intact.” 被遵守了**三十三年**：
本專案 17 份中文 mudlib，有 15 份帶著這批檔案，
署名一字未改。它們橫跨兩個完全不同的中文 mudlib 家族（§050），
是這兩族**唯一的共同祖先**。

同一批檔案裡還有另一個署名：`Creator : Pinkfish@Discworld`，
出現在 11 個檔上——Discworld MUD 的核心開發者。
**所以這套網路層本身也是拼接的**：TMI-2 的骨架 + Discworld 的貢獻，
再被中文社群整批繼承下去。

**解決的痛點**：1993 年的問題是——**每台 MUD 都是一座孤島**。
玩家在 A 站，朋友在 B 站，彼此看不見、傳不了話。
intermud 要做的就是把這些孤島連成網，而且**沒有中央伺服器**
（`dns_master` 是名錄，不是樞紐；每台 mud 直接對彼此開 socket）。

**用今天的話講，它是一組 P2P 微服務**：
服務發現、能力協商、查詢／應答分離、每個服務一個獨立單元。
**在 HTTP/1.0 剛出來的那一年，用一個 MUD 腳本語言寫的。**

**踩過的坑（安全）**：這套設計有一個時代性的罩門——
**明文、無認證**。服務之間靠一個共享密碼識別彼此。
本專案的 `SECURITY_NOTES.md` 記著這件事：
上游 `cmwhod.c` 裡帶著一個真實可用的 MUD 互聯共享密碼，公開前已改成 `CHANGE_ME`。

這不是實作疏忽，是**威脅模型的年代差**：
1993 年的網際網路上，能連到你 MUD 埠的人本來就不多，
而且大家互相認識。今天同一份設計直接暴露在公網上，性質完全不同。

**優點 / 罩門**：優點是**它證明了 MudOS 那個能力面的價值**——
socket efun 一開，上層就長出了一個完整的分散式系統，而且完全不需要動 driver。
這是「把能力面推寬」這條路線最好的辯護。

罩門有三個，而且都很現代：
- **沒有認證**（上面說了）；
- **沒有版本協商**——`support_q/_a` 問的是「你支援不支援」，不是「你的版本是幾」，
  所以協議一改就得全網一起改；
- **在 WASM 上整組不存在**——`package sockets` 關掉了（§041），
  這 20 幾個檔連編譯都過不了。本專案因此在 `NOTES.md` 裡明寫
  「這三個 daemon 本來就只在有對外網路時才有意義，**不打算修**——修了也沒有對象可連」。

最後這一條是個乾淨的判斷：**移植時，先分清楚「壞了」與「在這個環境裡沒有意義」。**

**效益**：對本專案的直接效益是**血緣鑑定**。
這 20 幾個檔的檔名、署名、日期在兩個家族裡完全一致，
成為 §050 那套「用檔名集合量血緣」的定錨點——
**當兩份看起來毫無關係的 mudlib 共享同一批 1993 年的檔案時，
它們的關係就不再是推測。**

> 💡 君之一席話
> **好的架構會被抄走並活得比它的作者久**——一組 1993 年的查詢／應答服務，三十三年後還原封不動躺在十五份不相干的中文 mudlib 裡，連檔頭的請求都被遵守了。

> 🔍 老手視角──真正的門道
> `_q`／`_a` 成對這個做法值得單獨想一想：它把「我當客戶端」與「我當伺服端」拆成兩個檔案，而不是一個檔案裡兩個函式。今天的直覺會覺得這很囉唆——同一個協議的兩側，寫在一起不是更好維護嗎？但它有一個很實際的好處：**你可以只實作一半**。一台只想收信不想寄信的 mud，只要放 `mail_a.c`；`tcp.c` 那組 `support_*` 就是用來宣告「我放了哪幾半」的。這在一個**參與者能力參差、且沒有人能強制升級**的網路裡是關鍵性質——它讓部分實作變成一等公民，而不是一種殘缺。老手看分散式協議時會特別注意這件事：**協議允不允許只實作一部分？**允許的（HTTP 的 method、SMTP 的 extension、telnet 的 option）活得很久；要求全有或全無的，通常在第二個實作出現時就分裂了。可落地的做法：設計任何跨組織的介面時，先想清楚「最小可用子集」是什麼，並讓對端**有辦法問出你實作了哪些**——把這件事留到後面再補，通常就補不上了。

---



## 050　17 個 mud，其實是 2 份 mudlib — 用檔名集合量血緣

**標籤**：`#血緣鑑定` `#可重複量測` `#Jaccard` `#封存` `#方法論`
**證據等級**：🟢 本專案實測（17 份映像的檔案索引，可重跑）

**起源**：本專案收了 17 個中文 mud。表面上它們是 17 款不同的遊戲——
不同的名字、不同的世界、不同的武功。
但把它們一個一個修到能開機的過程中，出現了一個很難忽視的現象：
**同一個修正，往往一次修好好幾個。**

那不是巧合。**它們大多是同一份 mudlib 的分身。**

問題是：怎麼證明？口述歷史（「聽說某某是從某某改的」）在本書裡不算證據。
所以本專案用了一個**可重複、可量測**的方法。

**技術核心**：**把每份 mudlib 化約成一個檔案路徑集合，量它們的交集。**

每份 mudlib 在本專案裡都是一份映像 + 一張索引（`mudlib.json`），
索引裡就有完整的路徑清單——不必解開、不必讀任何一行 LPC。
兩份 mudlib 的相似度用 Jaccard 係數：

```
J(A, B) = |A ∩ B| / |A ∪ B|
```

結果非常乾淨：

| 相似度區間 | 對數 | 解讀 |
|-----------|------|------|
| 0.70 – 0.90 | 20 對 | **近親**——彼此是直接的分支 |
| 0.44 – 0.69 | 30 餘對 | **同族**——共同祖先較遠 |
| **0.012** | dongfanggushi2 對**所有**其他 lib | **完全不同的血脈** |

最高的一對是 `shuiyuzhengfeng ↔ zhijianxing`，**0.90**——
**九成的檔案路徑一模一樣**。

而 `dongfanggushi2`（東方故事Ⅱ）對其他 16 份的最高相似度只有 **0.012**。
不是低，是**幾乎沒有交集**。

**於是 17 份 mudlib 分成兩族，而且從目錄形狀就看得出來：**

| | 家族 A（1 份：dongfanggushi2） | 家族 B（16 份） |
|---|---|---|
| 副檔名 | **`.lpc`** | **`.c`** |
| 物件基底 | `std/` | **`inherit/`** |
| 守護程序 | `daemon/` | `adm/daemons/` |
| 武功系統 | 無獨立目錄 | **`kungfu/`**（141 – 3,469 檔） |
| 特徵模組 | 無 | **`feature/`**（43 個 `F_*`） |
| 設定檔 | `config.ES2` | `config.ini` / `config.xajh` / … |
| 其他痕跡 | `binaries/`（`save_binary`）、`adm/tmp/swapfile.*`（swapping） | `clone/`、`grant/`、`fuben/`（副本） |

家族 A 是經典的 LPMud／TMI 風格佈局；
家族 B 是**中文武俠 mudlib 的標準骨架**——
`inherit/` + `kungfu/` + `feature/` 這三件套，是它的指紋。

**★ 但兩族共享 ~170 個檔。**

去掉副檔名之後比對，`dongfanggushi2` 與家族 B 各成員之間，
**有 142 – 182 個同名檔案**，而且全部集中在同一批位置：

```
adm/daemons/           aliasd channeld chard chinesed combatd emoted
                       fingerd itemd logind natured securityd virtuald …
adm/daemons/network/   dns_master + services/ 那整組 _q/_a（§049）
adm/simul_efun/        atoi chinese file gender message object path wizard
adm/etc/               config motd preload welcome wizlist
```

**這就是 §049 那批 TMI-2 的檔案。**
兩個看起來毫無關係的中文 mudlib 家族，
共享的正是 1993 年那份 MudOS 參考 mudlib 的**守護程序層與模擬函式層**。

所以完整的血緣圖是：

```
TMI-2 mudlib（1993, MudOS 參考實作）
   │
   ├──→ 家族 A：東方故事系（保留 std/ + daemon/ 佈局，改寫遊戲層）
   │
   └──→ 家族 B：中文武俠系（保留 daemon 層，**整個物件層重寫**
                            成 inherit/ + feature/ + kungfu/）
                 └─→ 16 份分身，彼此 0.44 – 0.90
```

**解決的痛點**：這個量測回答了三個原本只能靠猜的問題：

1. **「修一個能修幾個？」**——0.9 的近親，修正幾乎必然通用。
   §032 那八條「通用修正」之所以通用，就是因為它們打在共享的骨架上。
2. **「這份封存缺了什麼？」**——把它和近親的檔案集合相減。
   `nt7` 缺整個玩家層與世界（`clone/user/` 空、沒有 `d/`），
   就是這樣看出來的；補件也是拿 0.78 的近親 `nitan7` 來對（§032）。
3. **「這是一款新遊戲還是一次換皮？」**——0.90 的兩份，
   差的是內容不是架構。

**踩過的坑**：這個方法有一個明顯的失效模式，必須說清楚——
**它量的是「檔案路徑」，不是「程式碼」。**

一份 mudlib 可以：路徑全同但內容全改（換皮的反面），
或路徑全改但內容照抄（重新命名）。
本方法對前者會高估、對後者會低估。

本專案接受這個誤差，理由是**成本**：路徑比對讀一張 JSON 就好，
內容比對要解開 17 × 一萬個檔。
而且對「這批東西是不是同源」這個問題，路徑的鑑別力已經足夠——
**0.90 與 0.012 之間沒有模糊地帶**。

真要提高精度，下一步是對共享路徑的檔案做內容雜湊比對；
本專案沒做，因為現有結論沒有一條會因此改變。
**知道自己的量測誤差在哪、且判斷它不影響結論，比追求精度重要。**

**★ 後記：量測做完之後，才去查史料。**

本節的分析全部做完、結論全部寫下之後，本專案才回頭去查中文 MUD 的公開記載。
**三條獨立的線對上了：**

| 史料說 | 本節量到 | 檔案裡寫著 |
|--------|---------|-----------|
| 1993 年底《東方故事》採用 **MudOS ＋ TMI Mudlib**，並改造 MudOS 使其能處理中文；**捨棄 TMI 的遊戲內容自行創作** | 15/17 份共用 ~170 個檔，**全部集中在 daemon／network／simul_efun** —— 正是「保留框架、換掉內容」的形狀 | `Creator : Grendel@Tmi-2` `93-08-15` |
| 1995 年底《俠客行》**在《東方故事2》基礎上**開發 | `dongfanggushi2` 自成一族（對其他 16 份僅 **0.012**） | `config.ES2`；名錄裡有 `es2.muds.net 4000` |
| **1996-05 俠客行程式碼外流**，據此誕生大量改編版本 | 家族 B 那 **16 份彼此 0.44–0.90** —— 同一份 code 的分身 | `Creator : ken@XAJH`（俠客行對網路層的擴充）、`config.xajh`、`d/…/xkxnpc/` |

**完整血緣圖與這三條線的對照，收在附錄 D.3。**

這件事對方法論的意義比對歷史的意義更大：**量測是在不知道答案的情況下做的**，
所以它不可能是「照著史料找證據」。當一個獨立量測與一份獨立史料收斂到同一張圖時，
**兩者互為驗證**——這正是本書第一篇那條規矩要求的形狀。

**優點 / 罩門**：優點是**便宜、可重跑、不需要領域知識**——
不必懂 LPC、不必懂武俠，一支三十行的腳本就能把 17 份封存的關係畫出來。
罩門是它只能告訴你**結構**上的親疏，說不出**誰先誰後**。
方向是靠別的證據補的：TMI-2 檔頭的日期（`93-08-15`）、
家族 A 保留而家族 B 改寫的部分（**改寫的一方通常是後來者**），
以及上面那份史料。

**效益**：對接手一批來歷不明的封存，這個方法提供的是**第一天的地圖**：
哪些是同源、哪些是孤兒、修正該打在哪、缺件該去哪裡借。
本專案 17 份 lib 的匯入之所以能收斂，很大程度靠這張圖
把「17 個獨立問題」壓縮成「2 個家族的問題」。

> 💡 君之一席話
> **考據不必靠傳說**——當你手上有一批來歷不明的東西，先找一個便宜、可重跑的量測把它們的關係量出來；量得出來的結構，比任何人的回憶都可靠。

> 🔍 老手視角──真正的門道
> 這一節的方法在軟體考古之外也很通用：**當你面對 N 個「據說有關係」的東西，先找一個可以自動計算的相似度，把 N 個問題壓成 k 個群的問題。** 檔案路徑集合只是其中一種特徵；相依清單、API 呼叫集合、字串常數集合、錯誤訊息集合都可以拿來當指紋，而且各有各的鑑別力（字串常數對「換皮」特別敏感，相依清單對「同一個框架的專案」特別敏感）。老手做這件事的順序是：**先挑最便宜的特徵試一次，看分佈有沒有明顯的斷層**——本例的 0.90 與 0.012 之間那個大洞，就是「這個特徵有效」的訊號。如果分佈是連續的、沒有斷層，那通常代表你挑錯特徵了，換一個，而不是硬套一個門檻值。可落地的做法：任何「這幾份東西是不是同一個來源」的問題，**先畫分佈再定門檻**；先定門檻再看資料，得到的永遠是你想看到的答案。

---

## 051　特徵組合，不是繼承樹 — mudlib 的物件模型

**標籤**：`#物件模型` `#mixin` `#多重繼承` `#組合` `#架構`
**證據等級**：🟢 本專案實測（`feature/` 43 個模組、`inherit/char/char.c` 繼承 18 個）

**起源**：家族 B 的物件層有一個很鮮明的形狀，
而且它和多數人對「物件導向」的直覺不一樣。

打開一個角色的基底類別，看到的不是一條繼承鏈，而是**一張清單**：

```c
inherit F_ACTION;      inherit F_ALIAS;       inherit F_APPRENTICE;
inherit F_ATTACK;      inherit F_ATTRIBUTE;   inherit F_COMMAND;
inherit F_CONDITION;   inherit F_DAMAGE;      inherit F_DBASE;
inherit F_EDIT;        inherit F_FINANCE;     inherit F_MESSAGE;
inherit F_MORE;        inherit F_MOVE;        inherit F_NAME;
inherit F_SKILL;       inherit F_TEAM;        inherit F_SUIT;
```

**18 個**。整個 mudlib 提供 **43 個** 這樣的 `F_*` 模組。

**技術核心**：這不是繼承，是**組合**——LPC 的多重 `inherit` 被當成 mixin 用。

```
                 ┌─ F_DBASE     資料：屬性樹（§052）
                 ├─ F_MOVE      能被搬動、有 environment
   一個「角色」 = ├─ F_ATTACK    能打架
                 ├─ F_SKILL     有技能
                 ├─ F_CONDITION 有狀態效果
                 ├─ F_TEAM      能組隊
                 └─ …（共 18 個）

   一個「物品」 = F_DBASE + F_MOVE + F_CLEAN_UP + …
   一個「房間」 = F_DBASE + F_CLEAN_UP + …（不含 F_MOVE：房間不會被搬走）
```

**判準很單純：一個 `F_*` 就是一種「能力」，物件要哪些能力就 inherit 哪些。**

這個模型的三個性質：

| 性質 | 說明 |
|------|------|
| **正交** | `F_MOVE`（能被搬）與 `F_ATTACK`（能打架）互不相關，可以任意組合 |
| **扁平** | 沒有深繼承鏈——`char.c` 直接列 18 個，不是 `char → living → object` 疊三層 |
| **可加不可減** | 要新增能力就多 inherit 一個；**但沒有辦法「不要某個能力」** |

第三條是罩門的來源，稍後說。

**解決的痛點**：MUD 的物件天生**不適合單一繼承**。
「一把會說話的劍」既是武器又是 NPC；
「一間會移動的房間」（船艙）既是房間又能被搬動；
「一個可以被拿起來的 NPC」（小動物）既是生物又是物品。

單一繼承要處理這些，只能靠深繼承鏈加大量覆寫，
或者複製貼上。**組合模型讓這些混種變成加法**：
要哪幾種能力就列哪幾個。

**設計對照：同一個問題的四種答案**

| 模型 | 代表 | 組合方式 | 診斷難度 |
|------|------|---------|---------|
| **單一繼承樹** | Java、C# | `extends` 一個 | 低（鏈是線性的） |
| **多重繼承／mixin** | **LPC 的 `F_*`**、Python、Ruby module | `inherit` 多個 | **中高**（要知道方法從哪來） |
| **trait / interface + 預設實作** | Rust trait、Scala、Go 嵌入 | 宣告 + 實作分離 | 中（有型別檢查幫忙） |
| **ECS（實體－元件－系統）** | 現代遊戲引擎 | 掛元件，行為在系統 | 中（資料與行為徹底分離） |

LPC 的 `F_*` 在 1990 年代就落在第二列，
而它處理的問題（遊戲物件的能力組合）今天的主流答案是第四列。
**兩者的共同直覺是一樣的：物件的能力應該是可組合的清單，不是可繼承的分類。**
差別在 ECS 把資料與行為也拆開了，而 `F_*` 沒有——
`F_SKILL` 同時帶著技能的資料存取與技能的邏輯。

**踩過的坑**：組合模型的代價，本專案在移植時全部嚐過一輪：

| 問題 | 具體形狀 |
|------|---------|
| **方法從哪來？** | 看到 `query_skill()` 要先知道它在 `F_SKILL` 裡——沒有型別系統會告訴你 |
| **名稱衝突** | 兩個 `F_*` 都定義同名函式時，**順序決定誰贏**，而順序是 `inherit` 的書寫順序 |
| **不能減** | 某個 `F_*` 的行為不合用時，只能覆寫或用 shadow 蓋掉，**不能把它拿掉** |
| **`create()` 的鏈** | 每個 `F_*` 可能都有初始化，誰先誰後不明顯——§038 那類「跑著新程式帶著舊資料」的幽靈 bug 常出在這 |

第二列在 §032 的相容性修正裡出現過一次變體：
`is_killing(ob)` 型別不符（宣告 `string` 卻傳 object）之所以能潛伏多年，
部分原因就是**組合模型讓「這個函式的正確簽名是什麼」沒有單一答案可查**。

**優點 / 罩門**：優點是**世界觀可以很自由**——
中文武俠 mudlib 那些奇形怪狀的物件（可攜帶的坐騎、會打架的房間機關、
既是裝備又是技能載體的內功心法）都能用加法表達出來。
罩門是**可診斷性**：一個物件為什麼有這個行為，答案分散在 18 個檔案裡，
而且沒有工具會替你合併。

**效益**：對逆向工作，這個模型給的是**一條固定的搜尋路徑**。
要找「某個行為是誰做的」，順序是：

```
物件自己的 .c
  → 它 inherit 的 F_* 清單（逐一）
    → simul_efun 有沒有蓋掉（§036）
      → 才輪到 driver 的 efun
```

本專案追 ZJMUD opcode 的發送點時走的就是這條路，
而下一節會看到一個很有意思的結果：**有些 opcode 根本不在 UI 層送出，
它藏在最底層的那個 `F_DBASE` 裡。**

> 💡 君之一席話
> **當物件的分類方式超過一種，繼承樹就會開始說謊**——把「是什麼」換成「有哪些能力」，架構才不會被世界觀綁架。

> 🔍 老手視角──真正的門道
> mixin 這個模型有一個長期成本很容易被低估：**它把「這個東西是什麼」這個問題從程式碼裡刪掉了。** 單一繼承有一個線性的鏈，你可以回答「這是一個 Living，Living 是一個 Object」；組合模型的答案是「這東西有 18 種能力」，沒有名字、沒有階層、沒有可以印在文件上的分類。這在小系統裡是解放，在大系統裡是負擔——因為**溝通需要名字**。老手用組合模型時通常會補一層命名慣例當作補償：把常用的能力組合固化成幾個有名字的基底（本例的 `char.c` / `npc.c` / `room.c` / `item.c` 就是），讓 90% 的物件從有名字的東西開始，只有真正的混種才手動組合。可落地的做法：**用組合當機制，用命名的預設組合當介面**——直接把 mixin 清單暴露給所有使用者，等於把架構的複雜度轉嫁給每一個新人。

---

## 052　屬性樹 — 一個 mapping 撐起整個遊戲狀態

**標籤**：`#資料模型` `#無schema` `#路徑存取` `#耦合` `#副作用`
**證據等級**：🟢 本專案實測（`feature/dbase.c` 與全 mudlib 的使用形式）

**起源**：§051 那 43 個 `F_*` 裡，有一個是所有物件都會 inherit 的：**`F_DBASE`**。
它是整個 mudlib 的資料層——而它的實作核心只有一個東西：

**一個 mapping。**

**技術核心**：所有遊戲狀態都住在物件的 `dbase` 這個 mapping 裡，
用一對函式存取，鍵可以是**路徑**：

```c
set("name", "王小二");
set("attribute/str", 25);          // ← 斜線 = 巢狀
query("attribute/str");            // → 25
query("family/family_name");       // → 門派名
query("skill/dodge/level");        // → 技能等級
```

`set`／`query` 看到鍵裡有 `/` 就把它拆開，
在 mapping 裡逐層往下走（這件事由另一個模組 `F_TREEMAP` 負責）。

**於是整個遊戲的資料模型是一棵無 schema 的樹：**

```
物件的 dbase
├── name, id, short, long          基本識別
├── attribute/ str con dex int per  五圍
├── combat_exp, level, skill_point  成長（§053）
├── skill/<技能名>/level            技能
├── family/family_name              門派
├── equipped, armor_prop, …         裝備狀態
├── exits/<方向>                     房間出口
└── …（任何人想加的任何東西）
```

**這個設計的三個性質，決定了它上面所有東西的形狀：**

| 性質 | 好處 | 代價 |
|------|------|------|
| **無 schema** | 加一個屬性零成本——`set("我的新欄位", x)` 就有了 | 沒有任何東西知道「合法的欄位有哪些」 |
| **路徑存取** | 巢狀結構不需要宣告型別 | 打錯字 = 一個新欄位，**靜默** |
| **統一介面** | 存檔、讀檔、序列化、除錯都只要處理一種結構 | 效能不佳（每次存取都要解析路徑） |

**第二列是關鍵：`query("atribute/str")` 拼錯不會報錯，會回 0。**
這是 mudlib 最經典的一類 bug，而它是資料模型的直接後果，不是誰粗心。

**★ 這一節最重要的發現：協議被塞進了資料層。**

本專案在追 ZJMUD opcode 的發送點時，
在 `F_DBASE` 的 `set()` 裡找到這樣一段邏輯：

```
set("exits/north", 某房間)
   → 偵測到鍵的第一段是 "exits"
   → 取出目標房間的短描述
   → 立刻對本房間廣播一行 ZJEXIT（ESC003）
```

也就是說——**「設定一個出口」這個資料操作，
會直接產生一個協議封包。**

這件事有幾個很不小的後果：

1. **協議發送點不只在 UI 層**。本書 §005 用「grep 巨集名找發送點」的方法
   統計 opcode 使用頻率，這個方法會漏掉這一類——
   **它的觸發點是資料寫入，不是介面呼叫。**
2. **對客戶端而言，`ESC003` 是一個「資料變更通知」，不是「請畫出口」**。
   這解釋了本書 §005 觀察到的一個現象：出口更新有時會在
   看起來毫無關聯的時機出現——因為那一刻有人動了 `exits/` 底下的資料。
3. **這是資料層與表現層的直接耦合**，而且是最緊的那種。
   換一套協議，要改的不是輸出模組，是**資料存取的核心函式**。

從架構的角度，這是一個明確的越界：
**`F_DBASE` 的職責是存資料，它不應該知道 ZJMUD 協議存在。**
但從實務的角度，它也很好理解——
**這是「保證不漏送」最省事的做法**：
把通知綁在資料變更上，就不可能有人改了資料忘了通知。

**這個取捨在現代有一個名字：反應式資料（reactive data）。**
Vue 的 `ref`、MobX 的 observable、各種 ORM 的 change hook，
做的是同一件事——**把「資料變了」自動變成「通知出去」**。
差別只在現代做法把通知的**目標**抽象掉了（訂閱者自己註冊），
而 `F_DBASE` 直接寫死了「送一個 `ESC003` 給這個房間裡的人」。

**解決的痛點**：無 schema 的屬性樹解的是一個很現實的問題——
**mudlib 是給幾十個業餘作者共同編輯的**。
要求每個人加屬性前先改 schema、跑 migration，這件事在 1995 年的 MUD 巫師群裡不可能發生。
**零成本的擴充是這個社群能運轉的前提。**

**踩過的坑**：本專案在移植時撞到的是它的另一面——
**沒有 schema 就沒有辦法驗證封存的完整性**。

`nt7` 那份缺件的封存（§032），少的是整個玩家層與世界。
如果資料有 schema，缺一整類東西應該在載入時就報錯；
但屬性樹的哲學是「沒有的欄位就是 0」，
所以**缺件表現成「進去之後什麼都沒有」，而不是一個錯誤**。

這再一次回到本書反覆出現的主題：**靜默失敗**。
無 schema 的資料模型，本質上是把所有「缺資料」的錯誤都變成靜默的。

**優點 / 罩門**：優點是**極高的可塑性與極低的協作成本**——
17 份 mudlib 各自往這棵樹上加了幾百個自己的欄位，彼此不衝突。
罩門三個：打錯字靜默、缺資料靜默、以及上面那個**協議耦合進資料層**。

**效益**：對逆向工作，這一節提供了一條很具體的教訓：
**找協議發送點時，不要只找「輸出」相關的程式碼，也要找「資料寫入」的核心路徑。**
本專案的 opcode 使用頻率統計（§005）因此有一個已知的偏差——
資料驅動的那幾個碼會被低估。這件事在規格書裡標了，
是本書「證據等級」規矩的一次實際應用：
**知道自己的量測方法會漏掉什麼，比量得準更重要。**

> 💡 君之一席話
> **把通知綁在資料變更上，就不可能有人忘記通知——代價是資料層從此知道了協議的存在**；這筆交易在小系統裡划算，在要換協議的那一天會一次結清。

> 🔍 老手視角──真正的門道
> 無 schema 的資料模型有一個很有意思的長期軌跡：**它一開始省下的是「定義」的成本，最後付出的是「發現」的成本。** 前三年，加欄位零成本、沒有 migration、沒有版本衝突，所有人都覺得賺到了；第五年，沒有人知道這棵樹上到底有哪些欄位、哪些還在用、哪些是十年前某個巫師加了之後就沒動過的。這條軌跡在 NoSQL、在 JSON 設定檔、在「先用 map 裝著以後再說」的每一個決定裡都會重演。老手的緩解手段通常不是「回頭加 schema」（那時已經來不及），而是**加一支能把實際使用的鍵掃出來的工具**——從資料裡反推 schema，而不是從文件裡查 schema。本專案做同一件事的形式是掃 mudlib 原始碼裡所有 `set(`／`query(` 的字面字串，得到一份「這份 lib 真的用到哪些屬性」的清單。可落地的做法：任何無 schema 的資料層，**第一天就寫那支反推工具**——它的價值會隨著資料長大而上升，而寫它的成本永遠是那半天。

---

## 053　升級、門派、裝備 — 三個機制共用的同一種架構

**標籤**：`#遊戲機制` `#資料模型` `#三分法` `#守護程序` `#逆向可見性`
**證據等級**：🟢 本專案實測（`feature/` 與 `kungfu/` 的結構與規模）

**起源**：§051 講了物件模型、§052 講了資料模型。
這一節看**遊戲機制**——但重點不是「這個 mud 怎麼玩」，
而是**這些機制在架構上長什麼樣，以及那個形狀對客戶端意味著什麼**。

因為它們的形狀出乎意料地一致。

**技術核心：三分法。**

中文武俠 mudlib 的每一個遊戲機制，都被切成同樣的三塊：

```
    資料  ──→  在 dbase 屬性樹裡（§052）        「這個角色現在是什麼狀態」
    行為  ──→  在 feature/ 的某個 F_* 裡（§051） 「這個狀態能做什麼」
    規則  ──→  在 adm/daemons/ 的某個 daemon 裡  「全世界共通的算式與表格」
```

四個代表性機制，套進這個三分法：

| 機制 | 資料（dbase） | 行為（`F_*`） | 規則（daemon） |
|------|-------------|-------------|--------------|
| **成長 / 升級** | `combat_exp`、`level`、`skill_point`、`tianfu_point` | `F_LEVEL`、`F_ATTRIBUTE` | 升級曲線與上限 |
| **技能 / 武功** | `skill/<名>/level`、`skill_type`、`meridian/*` | `F_SKILL`（746 行，最大的一個） | `combatd` + `kungfu/` 底下的技能物件 |
| **裝備** | `equipped`、`armor_type`、`armor_prop`、`weapon_prop` | `F_EQUIP`、`F_SUIT`（套裝） | `itemd` |
| **門派 / 師承** | `family/family_name`、師徒關係 | `F_APPRENTICE` | `familyd` |

**這個三分法就是整份 mudlib 的架構**，
所有機制都是它的實例。理解了它，讀任何一個機制都是同一套動作。

**四個機制各自的架構要點（high level）：**

### 成長：屬性是五圍，成長是兩條軌

角色的基本屬性是 **`str` / `con` / `dex` / `int` / `per`** 五項——
這組欄位名在家族 B 的 16 份 lib 裡完全一致，是這一族的另一個指紋。

成長走**兩條互不相同的軌**：
- **`combat_exp`（實戰經驗）**——打架累積，決定「等級」；
- **`skill_point` / `tianfu_point`（點數）**——用來換技能等級。

架構上的意義是：**「變強」不是單一數值**。
等級由經驗決定、能力由技能決定、技能由點數決定，
三者用不同的速率成長。這讓數值設計有多個獨立的旋鈕，
代價是**狀態面變大**——而狀態面變大，正是後面協議設計的壓力來源。

### 技能：技能是物件，不是數值

這是家族 B 最有特色的架構決定，也是它 `kungfu/` 目錄動輒
**1,200 – 3,469 個檔案**的原因。

一項武功不是 dbase 裡的一個數字，而是：

```
dbase 裡：  skill/<技能名>/level     ← 這個角色練到幾級（狀態）
kungfu/ 裡： 一個 LPC 物件            ← 這門武功「是什麼」（行為）
             ├─ class/      門派功夫
             ├─ skill/      基礎技能
             ├─ special/    特殊技能
             └─ condition/  狀態類
```

**狀態在角色身上，行為在技能物件裡。**
角色施展武功時，`F_SKILL` 拿著等級去呼叫那個技能物件。

這是一個很乾淨的分離，而且它有一個很實際的好處：
**新增一門武功不需要改任何既有檔案**——丟一個新物件進 `kungfu/` 就好。
這正是這個目錄能長到三千多個檔的原因：**它的擴充成本是常數。**

代價是**規模本身**：3,469 個檔案要編譯、要載入、要維護，
而其中絕大多數在任何一場遊戲裡都不會被碰到。

### 裝備：槽位是型別，套裝是規則

裝備系統的架構是**槽位化**——`inherit/armor/` 底下一個槽位一個基底類別：

```
head  neck  cloth  surcoat  wrists  hands  finger
waist  pants  boots  shield  shou  …（13 個槽位）
```

一件裝備 inherit 哪個基底，就決定了它戴在哪。
**槽位不是一個欄位，是一個型別**——
這讓「這件東西能不能戴在頭上」變成編譯期就決定的事，
而不是執行期的字串比對。

在這之上還有 `F_SUIT`（套裝）：**跨物件的規則**。
它的架構位置很有意思——套裝效果不屬於任何一件裝備，
而是「同時穿了某幾件」這個**組合**的性質，
所以它必須住在角色身上（`char.c` inherit 了 `F_SUIT`），
不能住在裝備上。

**「屬性屬於個體、效果屬於組合」——這是所有套裝／羈絆系統的共同結構問題**，
而這份 mudlib 的答案是把它上提到角色層。

### 門派：組織是 daemon，關係是屬性

門派（`family`）的架構是最典型的三分法：
- 角色身上只存 `family/family_name` 一個字串（**資料**）；
- 拜師、逐出、師承查詢在 `F_APPRENTICE`（**行為**）；
- 門派有哪些、各自的功夫、入門條件在 `familyd`（**規則**）。

**組織實體本身不是物件，是 daemon 裡的一張表。**
這個決定讓「加一個門派」變成改一張表，
而不是建立一套新的物件關係。

**★ 這一切對客戶端與協議的意義。**

這才是本書關心的部分。三分法造成一個很具體的後果：

| 三分法的哪一塊 | 客戶端**看得到**嗎 | 為什麼 |
|--------------|------------------|--------|
| **資料**（dbase） | ✅ 看得到 | 屬性條、血條、經驗值都是 opcode 送過來的（`ESC012`、`ESC022`） |
| **行為**（`F_*`） | ⚠️ 只看得到結果 | 客戶端收到的是「你受了 30 點傷」，不是傷害公式 |
| **規則**（daemon） | ❌ 完全看不到 | 升級曲線、套裝效果、門派條件從來不上線路 |

**所以任何 MUD 客戶端都不可能「懂」遊戲——它只能顯示狀態。**

這解釋了本書 §005 那個 opcode 家族分佈為什麼長那樣：
27 個核心碼裡，**狀態類**（屬性條、血條、快捷鈕）和**內容類**
（房間、物件、詳情、動作列）佔了絕大多數，
**沒有任何一個 opcode 在傳輸規則**。

而這一條，正是下一章要談的整個 MUD 客戶端史的起點：
**客戶端只拿得到狀態與文字，拿不到規則——
所以它三十年來一直在做同一件事：從文字裡把狀態猜回來。**

**優點 / 罩門**：三分法的優點是**每一塊的擴充成本都是常數**——
加武功丟一個檔、加門派改一張表、加屬性 `set` 一個新鍵。
這是一個給幾十個業餘作者長期共同編輯的系統該有的性質。
罩門是**沒有任何一處看得到全貌**：
一個「為什麼我打不贏這個 NPC」的問題，答案分散在角色的 dbase、
`F_ATTACK` 與 `F_SKILL` 的邏輯、`combatd` 的公式、以及兩個技能物件裡。

**效益**：對逆向工作，三分法給的是**一張定位表**：
看到一個現象，先判斷它屬於資料、行為還是規則，
就知道該去哪一層找，也知道**它會不會出現在線路上**。
本專案在還原協議時，凡是判斷為「規則」的東西，
一律不去線路上找——**因為那裡不會有**。

> 💡 君之一席話
> **客戶端永遠只看得到狀態，看不到規則**——這不是實作的限制，是分層的必然；認清這一點，才不會在協議裡找一個永遠不會出現的東西。

> 🔍 老手視角──真正的門道
> 「資料／行為／規則」這個三分法值得從架構的角度多看一眼，因為它切的位置很特別：它不是按**領域**切（戰鬥、社交、經濟），而是按**變更頻率與作用範圍**切——資料每秒都在變且屬於個體，行為偶爾改且屬於類別，規則很少改且屬於全世界。這正好呼應了本書 §033 那句「頻率相同的東西放在一起」。它帶來的實際好處是：改一條全域規則不需要碰任何一個物件（改 daemon 就好），而加一個個體屬性不需要碰任何規則（`set` 一下就好）。老手在看一個長期演化的系統時，會刻意去找它**有沒有這種按頻率切開的層**；有的，通常還在健康地長大，因為每種改動都有一個明確且便宜的落點；沒有的，往往是「改一個小需求要動七個檔案」的那種系統。可落地的做法：設計任何要活很久的模組時，先把「每天會變的」「每季會變的」「幾乎不變的」三種東西分開放——**分層的依據是時間，不是名詞**。

---



## 054　客戶端簡史 — 三十年沒變過的四件套

**標籤**：`#MUD客戶端` `#trigger` `#架構史` `#耦合` `#文字介面`
**證據等級**：🟡 公開的客戶端沿革 ＋ 🟢 本專案對協議面的實測

**起源**：§053 最後留下一句話——**客戶端只拿得到狀態與文字，拿不到規則。**
這一節從客戶端這一側看同一件事：**在只有文字的前提下，客戶端能做什麼？**

答案三十年沒變過，而且所有客戶端都收斂到同一個形狀。

**技術核心**：MUD 客戶端的沿革與它們共同的架構。

| 年代 | 客戶端 | 平台 | 帶進來的東西 |
|------|--------|------|-------------|
| 1980s 末 | **TinyFugue** | Unix | 巨集語言 + 完整的觸發系統；早期主要服務 TinyMUD 系 |
| **1992-04** | **TINTIN**（Peter Unold） | Unix | 易學的腳本語言；為 DikuMUD 而生 |
| 1992-10 | TINTIN III | Unix | 該線的最後一版 |
| **1993-07** | **TinTin++ 1.0**（Bill Reiss） | Unix | 開源、無授權限制——**後來 GGMud／MudMaster／Pueblo 都以它為基礎** |
| **1995-12** | **zMUD 1.0**（Mike Potter） | Windows | 把 TinTin++ 的能力搬上圖形介面：可自訂按鈕、地圖；1996-09 起轉共享軟體 |
| — | MUSHclient | Windows | 開源；alias／macro／trigger／腳本／記錄，**內建 MXP 與 MCCP** |
| — | Mudlet | 跨平台 | 開源；強腳本引擎與高速顯示 |

**三十年、六代、兩種作業系統，架構是同一個：**

```
      伺服器文字流
           │
           ▼
   ┌───────────────┐
   │  trigger 引擎  │ ← 對每一行做模式比對
   └───────┬───────┘
           │ 命中
           ▼
   ┌───────────────┐   ┌──────────┐   ┌──────────┐
   │  alias 展開    │   │  macro   │   │  script  │
   │（輸入端縮寫）   │   │（按鍵）   │   │（腳本引擎）│
   └───────┬───────┘   └────┬─────┘   └────┬─────┘
           └────────────────┴──────────────┘
                            ▼
                      送回伺服器 / 改變顯示
```

**四件套：trigger（輸出端模式比對）、alias（輸入端展開）、
macro（按鍵綁定）、script（把前三者串起來的程式語言）。**

**為什麼是這四件套？**因為在「伺服器只吐文字」這個前提下，
客戶端能施力的地方只有兩個：**進來的文字**與**出去的文字**。
trigger 處理前者、alias／macro 處理後者、script 讓兩者可以有狀態。
**這不是設計選擇，是前提的必然結果。**

**解決的痛點**：這四件套解的是一個很真實的痛——
**MUD 的操作密度遠高於人的打字速度**。
戰鬥每回合要下指令、補血要盯數字、走路要記路徑。
trigger 讓「看到血低於三成就喝藥」變成一條規則，而不是一次反應。

**★ 但它有一個結構性的罩門，而且這個罩門定義了後面三十年的協議史：**

> **trigger 耦合的是「輸出的排版」，不是「遊戲的狀態」。**

```
伺服器送：  你的氣血：[####------] 137/450
規則寫成：  比對 /氣血：.*(\d+)\/(\d+)/ → 取第一個數字
                      ↑
        伺服器某天把方括號改成圓括號、或在數字間加一個空格
                      ↓
              整套規則靜默失效
```

玩家的腳本綁在**伺服器的排版**上，而排版是伺服器最常改的東西。
更糟的是失效方式：**規則不會報錯，只是不再命中**——
又一次靜默失敗，而這次連 log 都沒有。

**這就是本書第二篇那套協議存在的全部理由。**
ZJMUD 在文字流上疊 opcode，做的事本質上是：

> **把「客戶端從排版裡猜狀態」換成「伺服器直接說出狀態」。**

換句話說，第二篇解剖的那 116 個 opcode，
是對 §054 這個罩門的一次回答——而它不是唯一的回答，
也不是第一個（§056 會看到另外三代）。

**設計對照：同一個問題的兩種解**

| | **客戶端猜**（trigger） | **伺服器說**（結構化協議） |
|---|---------------------|------------------------|
| 知識在哪 | 玩家的腳本裡 | 伺服器與客戶端之間 |
| 誰維護 | **每一個玩家各自維護** | 伺服器作者 |
| 伺服器改排版 | **全體玩家的腳本一起壞** | 沒有影響 |
| 換一台 mud | 腳本全部重寫 | 只要對方講同一套協議 |
| 上手成本 | 高（要學腳本語言） | 零（客戶端內建） |
| 伺服器要做什麼 | **什麼都不用做** | 要多送資料 |

最後一列是 trigger 三十年不死的原因：**它不需要伺服器配合**。
任何一台 MUD、任何一個年代，trigger 都能用。
而結構化協議永遠需要兩邊都同意——這件事有多難，§056 會看到。

**優點 / 罩門**：四件套的優點是**普適**——
它對所有 MUD 都有效，包括那些作者早已消失的。
本專案 §045 那個「會話層代打」（`telnetlogin.js`）本質上就是一組 trigger，
而它之所以是這個形狀，正是因為對面的 193 份 mudlib 不可能配合修改。
罩門就是上面那條：**耦合在排版上，且失效是靜默的。**

**效益**：對寫客戶端的人，這一節給的是一個定位問題：
**你要做的是第幾種客戶端？**
做 trigger 型的，要投資腳本引擎與規則管理；
做協議型的，要投資方言解析與相容性。
本專案是後者，而它的代價與收穫在 §057 結算。

> 💡 君之一席話
> **當客戶端只能從排版裡猜狀態，伺服器的每一次美化都是一次破壞性變更**——三十年的 MUD 協議史，本質上是在把這句話裡的「猜」字拿掉。

> 🔍 老手視角──真正的門道
> trigger 這個架構有一個很值得學的地方：**它把擴充點開在「資料流」上，而不是開在「程式」上。** 客戶端不需要外掛 API、不需要重編譯、不需要作者同意，任何人都能在文字流上掛規則——這是它能適配所有 MUD 的原因，也是它三十年不死的原因。同樣的形狀在別的地方也很常見：Unix 的管道、瀏覽器的 userscript、log 分析、封包過濾器。它們共同的優點是**零協調成本**，共同的缺點也一樣：**上游一改格式，下游全體靜默失效**。老手在依賴這類擴充點時會做一件事——**替自己的規則加一個「應該要命中」的健康檢查**：如果一條本來每分鐘都會命中的規則，五分鐘沒命中，那不是安靜，那是壞了。可落地的做法：任何基於模式比對的自動化（log 告警、爬蟲、trigger、監控規則），都要有一條「規則本身還活著嗎」的反向監控——**沒有這一條，你的規則會在某個你不知道的時刻停止工作，而系統看起來一切正常。**

---

## 055　telnet 選項層 — 協商的架構，與 6 組實測位元組

**標籤**：`#telnet` `#選項協商` `#能力協商` `#IAC` `#實測`
**證據等級**：🟢 實機封包（本專案）＋ 🟡 上游 `src/net/telnet.cc` 的協商立場表

**起源**：本書 §006 記過一次驚險——連線第一毫秒收到的那串位元組，
差點讓一個嚴格 UTF-8 解碼器當場死掉。
當時的處理是**全部剝除、一律拒絕**，這個策略至今沒變。

這一節補上它的**架構**：那些位元組到底在談什麼，以及為什麼「一律拒絕」是安全的。

**技術核心**：telnet 選項協商是一個**對稱的能力協商框架**。

```
IAC WILL <opt>    我想要啟用 <opt>（我這邊做）
IAC WONT <opt>    我不做 <opt>
IAC DO   <opt>    請你啟用 <opt>（你那邊做）
IAC DONT <opt>    請你不要做 <opt>
IAC SB <opt> …資料… IAC SE    子協商：<opt> 啟用後的實際資料通道
```

四個核心性質：

| 性質 | 說明 |
|------|------|
| **對稱** | 兩邊都能發起，兩邊都能拒絕 |
| **雙向獨立** | 「我做」與「你做」是兩件事——`WILL` 與 `DO` 各談各的 |
| **預設關閉** | 沒談成的選項就是不啟用 |
| **拒絕是合法且無副作用的** | 回 `DONT`／`WONT` 是協議的正常路徑，不是錯誤 |

**第四條就是「一律拒絕」為什麼安全的全部理由。**
拒絕所有選項＝退回最基本的 NVT telnet，
而 MUD 的核心功能（送文字、收文字）**完全不依賴任何選項**。
本專案因此可以用不到一百行處理掉整個協商層。

**FluffOS 這一側的協商立場**（`src/net/telnet.cc` 的 `my_telopts`）——
driver 對每個選項預先宣告自己的態度：

| 選項 | 編號 | 用途 | driver 立場 |
|------|------|------|------------|
| BINARY | 0 | 8-bit 透傳 | WILL / DO |
| ECHO | 1 | **回顯控制（密碼遮蔽靠它）** | WILL / DO |
| SGA | 3 | Suppress Go Ahead——**字元模式的訊號** | WILL / DO |
| TM | 6 | Timing Mark | WILL / DO |
| **TTYPE** | **24** | 終端機型別（`xterm-256color`） | WONT / **DO** |
| **NAWS** | **31** | **視窗大小**，改變時重報 | WILL / DO |
| LINEMODE | 34 | 行模式 | WONT / DO |
| **NEW-ENVIRON** | **39** | 環境變數 | WONT / **DO** |
| **CHARSET** | **42** | 字集協商 | WILL / DO |
| MSDP | 69 | 頻外結構化資料（§056） | WILL / DO |
| **MSSP** | **70** | MUD 伺服器狀態（給爬蟲用） | WILL / DO |
| **COMPRESS2** | **86** | **MCCP2 壓縮** | WILL / DO |
| **MSP** | **90** | 音效（§056） | WILL / DO |
| MXP | 91 | 行內標記（§056） | WILL / DO |
| ZMP | 93 | Zenith MUD Protocol（§056） | WILL / DO |
| GMCP | 201 | 頻外 JSON（§056） | WILL / DO |

注意 TTYPE / NEW-ENVIRON 那兩列是 **`WONT` / `DO`**——
**driver 不提供，但要求客戶端提供**。這是資訊流方向的宣告：
終端機型別與環境是客戶端的知識，伺服器只負責問。

**★ 本專案實測：WASM driver 的首包，逐組解碼。**

```
ff fd 18   IAC DO   24    → 請客戶端報終端機型別（TTYPE）
ff fd 1f   IAC DO   31    → 請客戶端報視窗大小（NAWS）
ff fd 27   IAC DO   39    → 請客戶端報環境變數（NEW-ENVIRON）
ff fb 46   IAC WILL 70    → 我可以提供伺服器狀態（MSSP）
ff fb 2a   IAC WILL 42    → 我可以談字集（CHARSET）
ff fb 5a   IAC WILL 90    → 我可以送音效（MSP）
```

**六組，18 個位元組**，之後才是 `ver1.0:…` 那一行（§044）。

而本書 §006 記錄的**原生版**首包多了一組：

```
ff fb 56   IAC WILL 86    → 我可以做 MCCP2 壓縮
```

**這一組在 WASM 版消失了**——因為 zlib 被移除、compress 套件關閉（§041）。
**§041 那張套件矩陣，在線路的第 10 個位元組上就看得見。**

這是一個很乾淨的閉環：從 driver 的 CMake 選項，
到 C++ 的協商表，到線路上的三個位元組，到客戶端要不要處理壓縮——
**同一個決定，四層都看得到。**

**★ 還有第二個閉環：`config` 的預設值也在這 18 個位元組裡。**

FluffOS 的設定檔有一組協議開關，各有預設值：

| 設定 | 預設 | 選項 | **實測首包裡有嗎** |
|------|-----:|-----:|------------------|
| `enable mssp` | **1** | 70 | ✅ `ff fb 46` |
| `enable msp` | **1** | 90 | ✅ `ff fb 5a` |
| `enable mxp` | 0 | 91 | ❌ |
| `enable gmcp` | 0 | 201 | ❌ |
| `enable zmp` | 0 | 93 | ❌ |
| `enable msdp` | 0 | 69 | ❌ |

**兩個預設為 1 的都出現了，四個預設為 0 的一個都沒有。**

這條對照有一個很實際的用途：**看首包就能反推對方的設定**。
本專案沒有讀過那 17 份 lib 的協議開關設定，
但從協商位元組就能斷定它們全部沒有開 GMCP／MXP——
**這也再一次印證 §056 的結論：中文 MUD 生態停在第 0 代，
不是因為 driver 不支援，是因為沒有人去打開它。**
FluffOS 早就準備好了，開關就在那裡，預設是關的。

**解決的痛點**：telnet 選項層解的是一個 1983 年就存在、
而且至今每個協議都要面對的問題：
**如何在一條已經在用的連線上，讓兩邊逐步協商出共同的能力？**

它的答案有三個很現代的性質：
**逐項協商**（不是全有全無）、**可拒絕**（拒絕是正常路徑）、
**可延後**（連線中途才啟用某個選項也合法）。

把它和本篇前面兩節對照：

| 層級 | 能力宣告的方式 | 何時 | 可否拒絕 |
|------|--------------|------|---------|
| **driver 套件**（§048） | 編譯期 `#define` | 編譯 driver 時 | — |
| **mudlib intermud**（§049） | `support_*()` 回傳 1/0 | 執行期，被問時 | — |
| **telnet 選項**（本節） | `WILL`／`DO` 協商 | **連線期，雙向** | ✅ |

**三層在解同一個問題，而只有最上面這一層做對了。**
telnet 這套在 1983 年定下的框架，
比它上面兩層晚了十年才被 MUD 世界用起來，
卻是三者裡唯一能處理「兩邊能力不對等」的。

**踩過的坑**：本專案在這一層踩過的坑在 §006——
**不是協商邏輯錯，是根本沒想到有這一層**。
一個用嚴格 UTF-8 解碼的讀取器，
碰到 `0xFF` 開頭的位元組序列會直接拋例外，
而那是連線的第一毫秒。

教訓是**分層要從最外層開始驗證**：
本專案的驗收順序後來固定成「位元組 → 分幀 → opcode → 樣式 → 狀態 → DOM」（§046），
而 IAC 剝除是第一站。

**優點 / 罩門**：優點是**極高的向下相容**——
四十多年來加了幾十個選項，而一個 1983 年寫的 telnet 客戶端今天還連得上，
因為所有新東西都在「談不成就不啟用」的框架裡。
罩門是**它把複雜度推給了每一個實作者**：
任何客戶端都必須先寫一個狀態機來剝 IAC，才能看到第一個字。
這對本專案是 138 行 JavaScript 加 162 行 Rust（§附錄 B）——
**在能看到任何有意義的內容之前，先要付的入場費。**

**效益**：對本專案的具體效益是**兩份實作的等價性可以被釘死**。
那 42 個實機位元組同時當成 Rust 測試與 JS 測試的輸入（§028.6），
於是「桌面版與瀏覽器版對協商的處理是否一致」這件事，
變成一條會失敗的測試，而不是一個假設。

> 💡 君之一席話
> **一個能力協商框架的價值，不在它能談成什麼，而在它談不成的時候會不會壞**——telnet 選項四十年不倒，靠的正是「拒絕」是一條正常路徑。

> 🔍 老手視角──真正的門道
> telnet 選項協商有一個常被忽略的設計細節：**`WILL/WONT` 與 `DO/DONT` 是兩組獨立的協商，而不是一問一答。** 這看起來只是形式上的對稱，實際上是它能撐四十年的關鍵——因為它承認了「我能做的」與「我想要你做的」是兩件不同的事。多數自製的握手協議會把這兩件事壓成一次「能力交換」（我把我支援的清單給你，你把你的給我，取交集），那在雙方角色對稱時沒問題，但一旦出現「我不提供但我需要你提供」這種不對稱需求（本例的 TTYPE、NAWS），交集模型就表達不出來。老手設計能力協商時會先問：**這個能力是對稱的嗎？**——對稱的用交集，不對稱的就必須把方向寫進協議裡。可落地的做法：任何 capability negotiation，**把「提供」與「需求」分成兩個欄位**，即使一開始所有能力都是對稱的；等你發現需要方向的那天，協議已經沒辦法改了。

---

## 056　頻外資料的四代 — 語意的定義權一路往上搬

**標籤**：`#GMCP` `#MSDP` `#MXP` `#ATCP` `#頻外資料` `#協議演化`
**證據等級**：🟡 上游協商表與公開規格 ＋ 🟢 本專案對 ZJMUD 的實測定位

**起源**：§054 的罩門是「客戶端只能從排版猜狀態」。
過去三十年，MUD 世界對這個問題給過**四代**答案。
這一節把它們排成一列——不是為了列規格，
是因為**它們的演化方向非常一致，而 ZJMUD 剛好落在起點**。

**技術核心**：四代的架構差異。

| 代 | 代表 | 載體 | 資料格式 | 語意由誰定義 | 純 telnet 看得到嗎 |
|---|------|------|---------|------------|------------------|
| **第 0 代** | **ANSI 色碼、mudlib 行內標記、ZJMUD** | **就在文字流裡** | 自訂字串標記 | **每個 mudlib 自己** | ✅ 看得到（有點亂） |
| **第 1 代** | **MXP**(91)、**MSP**(90) | 文字流內，但先用選項談過 | 類 HTML 標籤 / 命令列式 | 協議規格 | ⚠️ 看得到標籤 |
| **第 2 代** | **ATCP**(200)、**ZMP**(93) | **telnet 子協商** | 自訂字串 / 命令＋參數 | 半規格半自訂 | ❌ 看不到 |
| **第 3 代** | **GMCP**(201)、**MSDP**(69) | telnet 子協商 | **JSON** / 型別化 KV | 有社群共識的命名空間 | ❌ 看不到 |

**四代的方向只有一個，但它很值得說清楚：**

```
第 0 代   語意住在 mudlib 裡         → 每台 mud 一套，撞號（§043）
第 1 代   語意住在協議規格裡          → 但資料還混在文字流中
第 2 代   資料搬出文字流（子協商）     → 語意仍各家自訂
第 3 代   資料搬出文字流 + 語意標準化  → 客戶端可以「通用地」理解
```

**兩件事被逐步分開：資料的通道，與資料的語意。**
第 0 代兩者都在 mudlib 手上；第 3 代兩者都被抬到了協議層。

各代的架構特徵（high level）：

- **第 0 代**是**行內標記**。優點是零基礎設施——
  伺服器多印幾個字元就好，driver 完全不必知情（§043）。
  代價是三件：撞號、沒有跳脫機制（§004）、不認識的客戶端會看到亂碼。
- **第 1 代**開始用 telnet 選項**談過再用**，
  所以不認識的客戶端不會被打擾。MXP 走「類 HTML 標籤」的路
  ——可點擊的連結、可展開的區塊；MSP 專門處理音效觸發。
  它們仍然把資料放在文字流裡，所以**排版與資料還是綁在一起**。
- **第 2 代**是分水嶺：**資料離開文字流，走子協商通道**。
  從此「給人看的」與「給程式看的」在物理上分開，
  排版怎麼改都不會影響資料。
- **第 3 代**把最後一塊補上：**格式標準化**（GMCP 用 JSON），
  並形成命名空間慣例，讓客戶端可以對「沒見過的 mud」也做出合理的預設行為。

**★ ZJMUD 落在第 0 代——而且它是第 0 代裡做得很徹底的一個。**

用本篇的框架重新定位本書第二篇：

| 面向 | ZJMUD 的做法 | 屬於哪一代的性質 |
|------|-------------|----------------|
| 載體 | 行首 `ESC` + 三碼，就在文字流裡 | 第 0 代 |
| 資料格式 | `$zj#` 分記錄、`:` 分欄位、`║` 分屬性 | 第 0 代（自訂字串標記） |
| 語意定義 | `include/zjmud.h`，**每個 mudlib 自己一份** | 第 0 代 |
| 資料完整性 | **狀態明確送出**（房間、出口、屬性、血條各有專屬碼） | **接近第 3 代的野心** |
| 向下相容 | 舊 telnet 客戶端看得到，只是難看 | 第 0 代（刻意保留，§004） |

**它用第 0 代的機制，做了第 3 代想做的事。**
這既是它的成就，也是它所有問題的來源——
116 個 opcode、三個世代方言、4 個撞號（第三篇），
全部是「語意的定義權留在 mudlib 手上」的直接後果。

**那為什麼中文 MUD 沒有走向 GMCP？** 三個原因，都不是技術上的：

1. **時間軸不對**。中文 MUD 的高峰在 1996–2005 年；
   ATCP／GMCP 這一系成形要晚得多。
   **當一個生態的核心期已經過去，新標準就進不去了**——
   不是不能，是沒有人有動機再改一份跑了十年的 mudlib。
2. **需要 driver 配合**。第 2、3 代都走 telnet 子協商，
   而子協商是 driver 的地盤（§036 那張 apply 表：
   `gmcp`／`msdp`／`mxp_tag`／`zmp` 都是 driver 提供的 apply）。
   第 0 代不需要任何人配合——**這正是 §043 說的「組織成本」**。
3. **沒有中央權威**（§043）。要形成共通語意，得有人來定命名；
   而中文 MUD 圈從來沒有這樣一個角色，
   於是每一份 mudlib 各自加碼，撞號成為必然。

**這三條加起來就是一個很普遍的現象：技術上更好的方案輸給了組織成本更低的方案。**

**解決的痛點**：這一節解的不是實作問題，是**定位問題**。
本書前七篇解剖 ZJMUD 時，一直缺一個座標系——
「這套協議算好還是不好」沒有基準可比。
放進四代的框架之後，評價可以精確得多：

> ZJMUD 在「資料完整性」上做得比多數第 0 代方案好得多
> （它真的把狀態明講了，而不是只做顏色與換行）；
> 在「語意的定義權」上則完全停在第 0 代，
> 而那正是它撞號與難以互通的原因。

**踩過的坑**：本專案一開始把「支援哪些 opcode」當成客戶端的核心指標，
後來發現這個指標會誤導——**因為 opcode 數量反映的是方言的分裂程度，
不是協議的能力**。
三個世代方言合計 116 個碼，其中 89 個是擴充碼、4 個撞號（§010）；
而真正撐起完整介面的是那 **27 個核心碼**（§005）——
**最大公因數才是能力，總數只是熵。**

**優點 / 罩門**：第 0 代的優點在 §043 說過了：**一個人就能完成**。
罩門是它把成本從「開發時」推遲到「互通時」——
本專案要同時支援三個方言、隔離 4 個撞號、
再為不講 ZJMUD 的 193 份 mudlib 做會話層代打（§045），
**全部都是那筆延遲付款的利息。**

**效益**：對要設計自訂協議的人，這四代提供的是一張**演化路線圖**：
你現在在第幾代、下一代要付什麼代價、哪一步需要別人配合。
知道這件事，至少可以在第 0 代就把編號分區留好——
**撞號是可以預防的，只要你知道自己終究會走到那一步。**

> 💡 君之一席話
> **協議的演化方向，是把「語意的定義權」一路往上搬**——從每個實作各說各話，到規格說了算；每往上一階，互通性上升一級，而所需的協調成本也上升一級。

> 🔍 老手視角──真正的門道
> 第 2 代那個「資料離開文字流」的轉折，是這四代裡唯一真正的架構突破，值得單獨記住：**它把「給人看的」與「給程式看的」在物理上分開了。** 在此之前，排版就是資料，改排版就是改 API；在此之後，兩者可以各自演化。這個轉折在別的領域反覆出現——網頁從 table 排版走到語意標籤再走到前後端分離的 API、CLI 工具加上 `--json` 輸出、log 從人類可讀走到結構化 log。它們的共同教訓是：**只要人類可讀的輸出同時是機器讀取的介面，你的排版就是公開 API，而且是一個沒有版本、沒有棄用期、改了就有人壞掉的公開 API。** 老手看到任何「我們的輸出格式下游有人在剖析」的情況，第一個動作是給下游一條專用通道（`--json`、結構化事件、頻外通道），哪怕一開始只有一個使用者。可落地的判準：**當你開始因為「怕下游剖析壞掉」而不敢改排版時，你已經欠了一筆技術債了**——那一刻就是該開專用通道的時候。

---

## 057　知識該放在哪一層 — 三種客戶端架構與它們的壽命

**標籤**：`#架構決策` `#耦合` `#壽命` `#取捨` `#收束`
**證據等級**：🟢 本專案實測（三種架構本專案各實作過一部分）

**起源**：§054 是「客戶端猜」，§056 是「伺服器說」。
但這兩者之間其實有三種可落地的架構，而本專案**三種都碰過**。
這一節把它們並排結算——也是本書最後一個架構問題。

**技術核心**：**協議知識住在哪一層？**

```
架構 A：知識在「使用者的腳本」裡
        伺服器 ──文字──▶ 客戶端 ──▶ 玩家寫的 trigger 規則 ──▶ 行為
        代表：TinTin++／zMUD／MUSHclient／Mudlet 的一般用法
        本專案的實例：telnetlogin.js 的 profile 表（§045）

架構 B：知識在「客戶端的程式碼」裡
        伺服器 ──ESC+opcode──▶ 客戶端內建的方言解析器 ──▶ UI
        代表：ZJMUD 客戶端（本書第二～六篇）

架構 C：知識在「協議規格」裡
        伺服器 ──GMCP/MSDP──▶ 任何懂該規格的客戶端 ──▶ UI
        代表：GMCP／MSDP 生態（§056 第 3 代）
```

**六個面向的並排比較：**

| | **A：使用者腳本** | **B：內建方言解析** | **C：標準頻外協議** |
|---|---------------|-----------------|------------------|
| 誰維護那份知識 | **每個玩家各自** | 客戶端作者 | 規格 + 兩邊實作者 |
| 伺服器要配合嗎 | **不用** | 要（得送 opcode） | 要（driver + mudlib 都要） |
| 伺服器改排版 | **全體腳本壞** | 沒影響 | 沒影響 |
| 伺服器改協議 | 沒影響（本來就在猜） | **客戶端要跟著改** | 規格擋著，不常改 |
| 換一台 mud | 腳本重寫 | **要嘛同方言，要嘛做相容層** | ✅ 直接可用 |
| 新玩家上手 | 高（要學腳本） | **零** | 零 |
| 能做到多深的 GUI | 淺（猜得到的才做得到） | **深**（伺服器明講） | 深 |

**本專案為什麼選 B？** 因為前提決定了：
目標是重寫**一個特定 mud 的客戶端**，而那個 mud 已經在用一套私有方言。
選 A 等於放棄它已經送出來的結構化資料；
選 C 等於要求一份原作者已經不在的 mudlib 改用 GMCP。
**B 是唯一與現實相容的選項。**

**★ 但 B 的代價，這本書從頭到尾都在付：**

| B 的代價 | 本書的哪一部分 |
|---------|--------------|
| 協議知識沒有規格書，得自己還原 | 第一、二篇（整個逆向工程） |
| 一個方言不夠，要支援三個 | 第三篇（三個世代、4 個撞號） |
| 知識進了程式碼，就必須有邊界 | 第四篇（純函式、`protocol.js` 不碰 DOM） |
| 沒有規格擋著，只能靠測試釘住 | 第五篇（177 條測試、53 行實機封包固化成回歸測試） |
| 換一台不同方言的 mud 就不通 | §045（會話層代打，且只做得到第一層） |

**換句話說：這整本書，就是架構 B 的帳單。**

而本專案最後**同時用了 A 和 B**——
對講 ZJMUD 的 mud 用 B（完整 GUI），
對不講的用 A（`telnetlogin.js` 的 profile 表，只做到純文字可玩）。
§045 那句「相容層最重要的不是它做到了什麼，是它明確地不做什麼」，
現在可以說得更精確：**A 與 B 的能力天花板不同，所以它們該交付的東西也不同。**

**踩過的坑**：本專案一度想把 A 往上推——
用規則從文字裡剖出房間名與出口，讓不講 ZJMUD 的 mud 也有面板（§045 的路線 A）。
最後沒做，判斷是：**那等於在 193 份排版各異的 mudlib 上重建 §054 那個罩門**，
而且是替使用者重建的——他們不會知道規則什麼時候失效。

**這個判斷的通則是：不要用架構 A 去模擬架構 B 的能力。**
A 的天花板由「猜得準不準」決定，而猜的準確率會隨著對端數量增加而崩潰。

**優點 / 罩門**：三種架構沒有優劣，只有**適用前提**：

- **A 適用於：對端不可能配合**（老 mud、別人的伺服器、第三方服務）；
- **B 適用於：對端已經在送結構化資料，但那套資料是私有的**；
- **C 適用於：你能同時影響兩端，或生態已經有標準**。

罩門也各自對應：A 的知識分散在使用者手上、B 的知識鎖在你的程式碼裡、
C 需要一個你可能沒有的社群共識。

**效益**：這一節其實是本書第四篇「架構」那條線的終點。
第四篇問的是「怎麼讓協議知識不汙染 UI」——那是**同一層之內**的邊界問題；
這一節問的是「協議知識該不該在我這一層」——那是**跨層**的歸屬問題。

兩個問題的答案指向同一個習慣：

> **先問這份知識屬於誰，再問它該放在哪個檔案。**

而本書開頭那句核心命題，到這裡有了它最後一層意思：

> 逆向工程的盡頭，不是「我看懂了」，而是「我能證明它，而且我的實作經得起真機打臉」。

**當協議知識住在你的程式碼裡（架構 B），「真機打臉」就是你唯一的規格書。**
沒有標準可以援引、沒有第三方實作可以對照、沒有規格委員會可以申訴——
只有那 53 行實機封包，和它們固化成的回歸測試。

**這就是為什麼這本書從第一頁就在講證據等級。**

> 💡 君之一席話
> **選架構，本質上是在選「將來壞掉的時候，誰要負責修」**——A 是使用者、B 是你、C 是規格；三個都不是免費的，但只有把它想清楚的人，才不會在壞掉的那天感到意外。

> 🔍 老手視角──真正的門道
> 這三種架構有一個共同的隱藏維度：**知識的更新速度必須配得上它所在那一層的發版速度。** 架構 A 把知識放在使用者手上，使用者可以在五分鐘內改一條規則——所以它能適配變動最快的對端；架構 B 把知識放在客戶端程式碼裡，更新要走發版流程——所以它只適合對端相對穩定的情況；架構 C 把知識放在規格裡，改一次要動整個生態——所以它只適合幾乎不變的東西。多數架構失誤不是選錯了層，是**把變動快的知識放進了發版慢的層**：把業務規則編進客戶端、把費率寫進行動 App、把對端的怪癖硬編進要跑五年的韌體裡。老手在放置任何一塊知識時會問兩個問題：**這東西多久會變一次？我修好它要多久才能送到使用者手上？**第二個數字大於第一個，就放錯層了。可落地的做法：把系統裡的知識按「變動週期」列一張表，對照每一層的「發版週期」——**兩張表對不上的地方，就是未來的緊急發版**。

---



# 第十篇　語言、安全與編譯：把 LPC 這一層講完

> 第八篇講 driver、第九篇講 mudlib 與客戶端，中間漏了一層：**LPC 這個語言本身**。
>
> 這一篇補上它，而且是帶著目的補的——
> 本書 §032 那八條相容性修正裡，有三條的根因就在這一層
> （`static` 修飾詞、型別檢查、`valid_write` 的預設）。
> **不把語言講清楚，那三條永遠只是「照著修就好」的偏方。**
>
> 五個單元的共同提問：**這個設計在 1992 年是對的，為什麼三十年後要付帳？**

---

## 058　一個可以關掉的型別系統

**標籤**：`#LPC` `#型別系統` `#編譯期` `#strict_types` `#技術債`
**證據等級**：🟡 上游 `docs/lpc/types/general.md` ＋ 🟢 本專案實測（§032）

**起源**：讀中文 mudlib 時會看到很多這種東西：

```c
mixed query(string prop, int raw);
object connect(int port);
void logon(object ob);
```

看起來型別齊全。但官方文件對這件事的說法非常直白：

> **Normally, the type information is completely ignored, and can be regarded
> purely as documentation.**

**型別預設是註解。**

**技術核心**：LPC 的型別檢查有三個關鍵性質，缺一不可地解釋了後面所有的坑。

### ① 預設不檢查，`#pragma strict_types` 才檢查

型別可以出現在四個位置（全域變數、函式回傳值、參數、區域變數），
但除非開了 `strict_types`，編譯器只拿它做**最佳化提示**——
例如 `x` 宣告成 `int` 時可以把 `x + 0` 化簡掉，宣告成 `string` 時就不行。

開了 `strict_types` 之後三件事同時成立：
- 函式**必須**宣告回傳型別；
- 所有參數**必須**有型別；
- 呼叫尚未定義的函式**必須**先有原型（`string func(int arg);`）。

### ② ★ 型別檢查**只在編譯期**，執行期完全不檢查

> **The strict type checking is only used by the compiler, not at runtime.
> So, it is actually possible to store a number in a string variable
> even when strict type checking is enabled.**

**這一句就是 §032 那個 `is_killing(ob)` 的完整解釋**：
一個宣告收 `string` 的函式，被傳進一個 object。

```
舊 MudOS：編譯期檢查較寬鬆 → 放行 → 執行期不檢查 → 跑了很多年沒事
FluffOS：編譯期檢查收緊     → 擋下 → 建角流程當場失敗
```

**注意「跑了很多年沒事」不是因為它是對的**，
是因為**沒有任何一層會抓它**：編譯期放行、執行期不看。
本書 §036 說過的那句話在這裡有了機制上的解釋——
**「它在原機上跑得好好的」不構成它是對的證據。**

### ③ `call_other()` 回傳未知型別

```c
(string)call_other(ob, "short");   // 或
(string)ob->short();
```

編譯器無法知道 `ob` 是什麼、`short()` 回傳什麼，
所以 `call_other` 的回傳型別是 unknown。
若 driver 編了 `CAST_CALL_OTHERS`，這個值**必須**轉型；
否則預設當成 `mixed`。

**這是 LPC 型別系統最大的破口**：mudlib 裡跨物件呼叫（`->`）到處都是，
而**每一個 `->` 都是型別資訊的斷點**。
§036 提過 simul_efun 有一個相對的好處——
它的原型在編譯期已知，**所以回傳值不必轉型，即使開了 `strict_types`**。

### 基本型別

| 型別 | 說明 | 傳遞方式 |
|------|------|---------|
| `int` | 32 位元整數（FluffOS 執行期為 64 位元） | **傳值** |
| `float` | 浮點數 | **傳值** |
| `string` | 不限長度（**不允許 `\0`**） | **傳值** |
| `object` | 物件指標 | **傳址** |
| `mapping` | 關聯陣列——§052 那棵屬性樹的載體 | **傳址** |
| `function` | 函式指標（`(: :)`） | **傳址** |
| 陣列 `type *` | `int *arr;`；巢狀用 `mixed *` | **傳址** |
| `void` | 只能用於函式回傳 | — |
| `mixed` | 任何情境都合法——**「不好的實務，除非變數真的會裝不同型別」** | — |

**傳值／傳址的分界有一個很容易踩的後果：**

```c
({ 1 }) == ({ 1 })      // → false
```

因為 `({ … })` **每次都產生一個新陣列**，而陣列的 `==` 比的是「是不是同一個」。
`int` / `string` / `float` 比值，其餘比身分。

**另外一條**：改陣列元素會影響所有指向它的變數，
但**改陣列大小一定會配置一個新的**——所以「改內容」與「改長度」的可見性不同。

**解決的痛點**：可關掉的型別系統解的是 1989 年的實際問題——
**mudlib 是給業餘作者寫的**。強制型別會讓入門門檻上升，
而 LPMud 的整個價值主張就是「讓不會 C 的人也能蓋世界」（§033）。

**踩過的坑**：這個決定的帳單，本專案付了兩次：

| 症狀 | 根因 |
|------|------|
| `is_killing(ob)` 宣告 `string` 卻傳 object（§032） | 編譯期檢查寬鬆 + 執行期不檢查 |
| 一堆 mudlib 檔案**沒有** `#pragma strict_types` | 三十年前不開，今天也不敢補——補了會炸出上百個既有錯誤 |

第二列是關鍵：**型別檢查是可以事後打開的，但沒有人敢打開。**
一份跑了二十年的 mudlib，補一行 `#pragma strict_types` 會得到幾百個編譯錯誤，
其中絕大多數是「一直都錯但沒事」。
**這正是「預設寬鬆」這個決定真正的成本——它不是一次性的，是不可逆的。**

**優點 / 罩門**：優點是入門極低、改動極快。
罩門是**錯誤的發現時機被推遲到不確定的未來**：
一個型別錯誤可能潛伏二十年，直到換一台 driver 才爆——
而那時原作者早就不在了。

**設計對照**：

| 語言 | 型別檢查 | 事後補救的可能性 |
|------|---------|----------------|
| **LPC** | **預設關，可用 pragma 開（逐檔）** | 理論上可以，實務上不敢 |
| Python | 執行期；型別註解可選 + mypy | 逐模組漸進補（`# type: ignore` 當逃生口） |
| TypeScript | 編譯期；`any` 是逃生口 | **設計上就支援漸進**（`strict` 分項開） |
| Rust / Java | 強制 | 沒有這個問題 |

第三列是現代生態的答案：**漸進式型別化必須是設計目標，不能是事後的補救**。
TypeScript 之所以能被大規模採用，很大程度是因為它把「一半有型別」當成一等公民；
LPC 的 `#pragma strict_types` 雖然也是逐檔的，
但它缺了逃生口（`any` 之於 TS、`mixed` 只能用在型別位置而非「不檢查這一段」），
所以實務上是全有或全無。

**效益**：對讀 mudlib 的人，這一節給的是一條判讀規則：

> **看到型別宣告，不要當成保證，當成註解——而且是可能過期的註解。**

本專案在還原協議時對 LPC 函式簽名的態度就是這樣：
簽名說收 `string`，實際傳什麼要看呼叫點。

> 💡 君之一席話
> **一個預設關閉的型別系統，收的不是型別的成本，是「發現錯誤的時機」的成本**——而這筆帳會在你最沒有能力處理的時候寄來：換引擎、原作者消失、二十年後。

> 🔍 老手視角──真正的門道
> 「型別資訊被當成註解」這件事有一個很少被說破的連鎖效應：**它讓型別宣告本身失去了維護動力**。既然寫錯也不會有人告訴你，那麼隨著程式碼演化，簽名與實際用法就會慢慢分岔——`is_killing(string)` 收 object 這種事不是有人故意寫錯，是**某次改動把傳進去的東西換了，而沒有人去改簽名，因為改不改都一樣**。這個現象在所有「文件與程式碼分離」的地方都會發生：註解、README、API 文件、`@param` 標註。老手判斷一份型別／文件可不可信，看的不是它寫得多完整，而是**「它錯了會不會有人痛」**：CI 會失敗的，可信；只是好看的，一律當成寫作當下的快照。可落地的做法：**任何你希望長期正確的宣告，都要想辦法讓它有一個會失敗的驗證**——型別檢查、doctest、契約測試、schema 驗證都行，沒有的話，那份宣告的保鮮期大約是下一次重構。

---

## 059　修飾詞 — 一個關鍵字承載兩個語意的三十年後果

**標籤**：`#修飾詞` `#static` `#nosave` `#相容性` `#語意分裂`
**證據等級**：🟡 上游 `docs/lpc/types/general.md`（特殊型別）＋ 🟢 本專案實測（§032 首條修正）

**起源**：§032 那八條相容性修正的**第一條**，是 17 份 mudlib 全部撞到的：

```
master.c syntax error
```

原因是 FluffOS 移除了 `static` 這個修飾詞。
當時的處理是「補一行 `#define static` 不夠，要照上游用 `__SENSIBLE_MODIFIERS__` **互斥**分岔」。
這一節解釋**為什麼補一行不夠**。

**技術核心**：LPC 的「特殊型別」（修飾詞）可以疊加在基本型別之前。

| 修飾詞 | 對**函式** | 對**變數** |
|--------|-----------|-----------|
| `private` | 不能被 `call_other()` 呼叫，**繼承者也拿不到** | 同上 |
| `public` | **一定**可存取，即使用了 private inherit | — |
| `static` | **≈ private**（不能被 `call_other()` 呼叫） | **不會被 `save_object()` 存、也不會被 `restore_object()` 還原** |
| `nomask` | **不能被繼承覆寫，也不能被 `shadow()` 蓋掉** | 不能被覆寫 |
| `varargs` | 可變引數個數（否則個數會被檢查） | — |

**★ 問題全部出在 `static` 那一列：它對函式與變數是兩個完全不相關的語意。**

```
static int foo() { … }     →  意思是「private」          （可見性）
static int counter;        →  意思是「不要存進存檔」       （持久性）
```

**一個關鍵字，兩個正交的概念。**
在 1992 年這只是個小怪癖（C 的 `static` 本來就一詞多義）；
到了三十年後要維護的時候，它變成一個無法自動遷移的問題。

FluffOS 的處理是**把它拆成兩個意思明確的關鍵字**：

| 舊 | 新 | 意思 |
|----|----|------|
| `static`（函式） | **`protected`** | 可見性 |
| `static`（變數） | **`nosave`** | 持久性 |

並提供 `__SENSIBLE_MODIFIERS__` 讓 mudlib 知道自己跑在哪一套語意上。

**於是「補一行 `#define static`」為什麼不夠就很清楚了：**

```c
#define static protected     // 變數會變成「protected 變數」→ 存檔語意全錯
#define static nosave        // 函式會變成「nosave 函式」  → 可見性全錯
```

**沒有任何一個單一替換是對的**，因為原本那個關鍵字在兩種位置有兩種意思。
唯一正確的做法是**互斥分岔**——依 `__SENSIBLE_MODIFIERS__` 決定用哪一套，
而不是無條件重新定義。

**解決的痛點**：這一節解的是一類很具體的移植問題——
**當上游把一個關鍵字拆成兩個，下游不可能用巨集自動遷移。**

判準是：**拆分是不是資訊無損的？**
- `static`（函式）→ `protected`：無損，機械替換即可；
- `static`（變數）→ `nosave`：無損；
- **但「哪一個是哪一個」需要語法上下文**，而 `#define` 沒有上下文。

**所以這類遷移只能靠三種手段之一**：
① 上游提供相容宏（FluffOS 走這條）；
② 寫一支會解析語法的遷移工具；
③ 人工逐處改。

**踩過的坑**：本專案一開始走的是「補一行」，因為那是最省事的假設。
症狀是**修好一個檔又壞另一個檔**——因為兩種位置的錯誤會交替出現。
真正的教訓不是「要看清楚語意」，而是：

> **當一個修正「修好 A 就壞 B」，通常代表你在用一個維度的手段
> 解一個兩個維度的問題。**

**`#pragma` 全表**（同樣是逐檔生效，從指令處到檔尾）：

| pragma | 作用 |
|--------|------|
| **`strict_types`** | 強制型別檢查（§058）——**新程式碼強烈建議開** |
| `warnings` / `no_warnings` | 開關警告 |
| **`save_types`** | **編譯後保留型別資訊**，讓繼承它的物件能對它做型別檢查 |
| `optimize` | 對這個檔做額外的樹最佳化 |
| `show_error_context` | 舊旗標（預設開），已被 clang 風格診斷取代 |

`save_types` 值得注意：**型別資訊預設在編譯後就丟掉了**，
所以繼承者無法對父程式做型別檢查——除非父檔開了這個 pragma。
**這是 §058 那個「型別是註解」在繼承鏈上的延伸**：
不只執行期沒有型別，連編譯期都只在**單一檔案的範圍內**有。

**`nomask` 的雙重防護**值得單獨記一句：它同時擋住繼承覆寫**與** `shadow()`。
這是 mudlib 保護關鍵函式（例如安全檢查、金錢計算）的標準手段——
§062 會看到 shadow 有多容易被濫用。

**優點 / 罩門**：優點是修飾詞極少（五個），心智負擔低。
罩門就是本節的主題：**極少的關鍵字必然意味著一詞多義**，
而一詞多義在跨版本遷移時是最貴的一種債。

**效益**：對移植老 mudlib 的人，這一節給的是一條檢查清單：
遇到「上游移除了某個關鍵字」時，先問**它原本有幾個意思**；
答案大於一，就不要試圖用巨集解決。

> 💡 君之一席話
> **一個關鍵字承載兩個概念，在寫的時候省下的是一個名字，在遷移的時候付出的是一支語法解析器。**

> 🔍 老手視角──真正的門道
> 這件事最值得學的是 FluffOS 的**處理方式**而不是問題本身：它沒有默默改語意（那會讓舊 code 安靜地跑錯），也沒有永遠保留舊關鍵字（那會讓債永遠不還），而是**新增兩個明確的關鍵字 + 一個讓 mudlib 自我判斷的巨集**（`__SENSIBLE_MODIFIERS__`）。這個模式在生態演化裡有個通用名字：**可偵測的破壞性變更**——破壞是真的，但下游有辦法在編譯期知道自己站在哪一邊，因此可以寫出同時支援兩邊的程式碼。相對地，最糟的做法是「同名不同義」：關鍵字還在、語意悄悄變了，下游沒有任何辦法偵測。老手在做破壞性變更時的順序是：**能不能不破壞 → 破壞的話能不能讓對方偵測到 → 偵測不到的話能不能讓它編譯失敗**；三個都做不到，才考慮預設值改變。可落地的做法：任何要改語意的地方，**同時提供一個可以在編譯期／啟動期查詢的版本旗標**——它的成本是一行，而它讓下游有機會寫出跨版本的程式碼。

---

## 060　繼承的真相 — 程式與資料是分開存的

**標籤**：`#繼承` `#熱重載` `#記憶體模型` `#幽靈bug` `#多重繼承`
**證據等級**：🟡 上游 `docs/lpc/constructs/inherit.md`

**起源**：§051 把 mudlib 的物件模型講成「特徵組合」，
但沒有回答一個更底層的問題：**多個物件 inherit 同一個檔案時，記憶體長什麼樣？**

這個問題的答案，正好是 §038 那個「熱重載的幽靈 bug」的根因。

**技術核心**：driver **把編譯後的程式與全域資料分開存**。

> Because the FluffOS driver internally stores global data and compiled code
> separately, many different objects can use inheritance to share the same
> piece of compiled code. **Each of these objects will have its own local copy
> of any global variables defined by the object.**

```
      /feature/dbase.c  ──編譯一次──▶  program（位元組碼，共用一份）
                                            ▲        ▲        ▲
                                            │        │        │
   inherit F_DBASE 的物件：            角色A     物品B     房間C
                                       各自一份 dbase 變數（資料不共用）
```

**一份程式、N 份變數。** 這正是 §038 說「一萬把劍的成本是一萬份變數，
不是一萬份程式」的機制。

**★ 三條會咬人的規則，全部來自這個分離：**

### ① 重編子物件，不會重編父程式

> Recompiling either of A or B will not cause C to be recompiled.

合理，而且是效能上必要的。

### ② 但重編 A **會讓 A 那一份「C 提供的全域變數」歸零**

> **However, it will cause any global variables provided by C to lose
> whatever data they had.**

而且**只影響 A**——B 的那份不受影響。
**這就是 §038 那句「跑著新程式、帶著舊資料」的另一半**：
不只是資料可能舊，**資料也可能整個消失，而且只消失一部分物件的**。

### ③ ★ 重編父程式，子物件**繼續用舊版**，直到子物件也被重編

> **Note: if object B is recompiled, object A will continue to use the old
> version of object B until object A is also recompiled.**

**這一條就是 §038 熱重載相依圖的全部理由。**
改了 `/std/base.c` 之後，只重載它自己：

```
/std/base.c        ← 重編，新版
  ├─ /d/city/inn   ← 還連結著舊版 base，行為完全沒變
  ├─ /d/city/shop  ← 同上
  └─ …             ← 同上
```

**沒有錯誤、沒有警告、沒有任何徵兆。**
你改了程式、存了檔、下了 `update`，然後行為沒變——
而多數人的第一反應是「我是不是改錯地方了」。

FluffOS 給的解法（§038）是兩個編譯期 master apply
（`inherit_program` / `include_file`）讓 mudlib 自己建相依圖，
**因為 driver 不會替你做這件事**。

### 多重繼承與名稱衝突

§051 說 `F_*` 是 mixin，而 mixin 一定會撞名。LPC 的解法是**顯式限定**：

```c
inherit "/std/weapon";
inherit "/std/armor";

string query_long() {
    return is_equipped() ? armor::query_long() : weapon::query_long();
}
```

`父檔名::函式名()` 直接指定要哪一份。
`inherit_list()` 回報的是**真實檔名**（含副檔名）。

**路徑解析規則**：無副檔名的路徑優先載入 `pathname.lpc`，
沒有才退回 `pathname.c`——**這解釋了 §050 那兩個家族的副檔名差異**
（家族 A 用 `.lpc`、家族 B 用 `.c`），兩者在同一台 driver 上都能跑。

**解決的痛點**：程式／資料分離解的是記憶體問題，
而且解得非常漂亮——這是 LPMud 能在 1990 年代的機器上跑幾千個物件的原因。

**踩過的坑**：代價是**版本一致性從此不是自動的**。
一個物件在任何時刻都是「某個版本的程式 + 某個時刻的資料」的組合，
而這兩者**可以獨立地過期**：

| 情況 | 程式 | 資料 | 症狀 |
|------|------|------|------|
| 改了父檔，只重載父檔 | **舊** | 舊 | **完全沒有變化**（規則 ③） |
| 重載子物件 | 新 | **部分歸零** | 玩家的某些屬性不見了（規則 ②） |
| 重載一部分子物件 | 新舊**並存** | 各自不同 | **同一種物件行為不一致** |

第三列是最難查的：**世界上同時存在兩個版本的同一種劍**，
一個走新邏輯、一個走舊邏輯，而它們的檔名一模一樣。

**優點 / 罩門**：優點是熱重載可以很快、很細粒度。
罩門是**它把「版本」這個概念從系統裡拿掉了**——
LPC 物件沒有版本號，你無法問一個物件「你是用哪一版程式編的」
（除非 mudlib 自己記）。

**效益**：對接手 mudlib 的人，這一節提供三條實務規則：

1. 改了被繼承的檔案，**要重載的是所有繼承者，不是它自己**；
2. 重載會讓該物件那一份「父檔提供的全域變數」歸零——**存檔前先想清楚**；
3. 症狀是「改了但沒反應」時，**先確認你重載的是不是該重載的那個**。

> 💡 君之一席話
> **當程式與資料可以各自獨立地過期，「這個物件是什麼版本」就變成一個沒有答案的問題**——而所有最難查的 bug，都住在沒有答案的問題裡。

> 🔍 老手視角──真正的門道
> 「重編父程式，子物件繼續用舊版」這個行為，在今天的生態裡有一個完全對應的東西：**編譯期綁定 vs 執行期解析**。C 的靜態連結、Java 的 `final static` 常數內聯、Rust 的泛型單態化、C++ 的 header-only inline——全部都有「改了上游、下游沒重編就沒生效」的性質，而且全部都會產生同一種困惑。LPC 特別的地方在於它同時是**熱重載系統**，所以這個性質從「重編一次就好」變成「線上隨時可能有兩個版本並存」。老手處理這類問題的標準手段是**讓版本可見**：給程式一個編譯時間戳或雜湊，並讓執行期可以查詢——不是為了防止版本分岔（那擋不住），而是為了在分岔發生時**五秒內看得出來**。可落地的做法：任何有熱重載／熱更新的系統，**第一天就給每個可重載單元一個版本標記，並在日誌與除錯指令裡印出來**。沒有它，「為什麼這台機器行為不一樣」會變成一個要靠猜的問題。

---

## 061　uid / euid — 從 Unix 借來的安全模型，以及它的兩個相反預設

**標籤**：`#安全模型` `#uid` `#euid` `#fail-open` `#fail-closed` `#授權`
**證據等級**：🟡 上游 apply 文件 ＋ 🟢 本專案實測（`adm/single/master.c` 逐行）

**起源**：§032 那條「建角時 `Denied write permission`」是本專案最難查的相容性問題之一，
因為它的症狀出現在一個和權限毫無關係的地方（建立角色）。
根因在這一節。

**技術核心**：LPC 的安全模型直接照抄 Unix 的 **uid / euid**。

| 概念 | 意思 |
|------|------|
| **uid** | 這個物件「是誰造的」——`getuid()`，通常是巫師名或領域名 |
| **euid** | 這個物件「現在以誰的身分行事」——`geteuid()` |
| `seteuid(x)` | 切換有效身分（要經過 master 的 `valid_seteuid()` 同意） |
| `export_uid(ob)` | 把自己的 uid 給另一個物件 |

**沒有 euid 的物件幾乎什麼都不能做**——
這是 LPC 裡一個很常見的初始化樣板的由來：

```c
void create() {
    seteuid(ROOT_UID);      // 先把身分拿到手
    …
}
```

而 driver 對每一次檔案存取都會問 master：

```c
int valid_write(string file, mixed user, string func);
int valid_read (string file, mixed user, string func);
```

三個參數：**哪個檔、誰要動、透過哪個函式**。
回傳非零就放行。

**★ 本專案的 master 實作，以及那個致命的不對稱：**

```c
int valid_write(string file, mixed user, string func)
{
    object ob;
    if (ob = find_object(SECURITY_D))
        return (int)ob->valid_write(file, user, func);
    return 0;                    // ← 找不到 SECURITY_D：拒絕（fail-closed）
}

int valid_read(string file, mixed user, string func)
{
    object ob;
    if (ob = find_object(SECURITY_D))
        return (int)ob->valid_read(file, user, func);
    return 1;                    // ← 找不到 SECURITY_D：放行（fail-open）
}
```

**同一個檔案裡，兩個相反的預設。**

單看每一個都很合理：
- **寫**失敗只是不能寫，**寫錯了會壞資料** → 保守 → fail-closed；
- **讀**失敗會讓一堆東西看不到，**讀了也不會壞東西** → 寬鬆 → fail-open。

**問題出在「SECURITY_D 找不到」這個條件何時成立。**

它成立的時機不是「安全模組壞了」，而是——**它還沒被載入**。
而 mudlib 的建角流程（`logind` → 建立玩家物件 → 寫入存檔）
在某些啟動順序下**正好發生在 SECURITY_D 載入之前**。

```
啟動 → preload → …（SECURITY_D 還沒到）
   └─ 有人連進來 → 建角 → 寫存檔
        → valid_write → find_object(SECURITY_D) 失敗 → return 0
        → Denied write permission
```

**這不是安全策略在起作用，是初始化順序的競態。**

> **fail-closed 是對的策略，但它假設了「失敗 = 異常」。
> 當失敗的真正原因是「還沒準備好」，同一個策略就從保護變成了故障。**

**解決的痛點**：uid/euid 解的是 LPMud 最原始的問題——
**mudlib 是給不受信任的巫師寫的**（§037 講 eval cost 時的同一個情境）。
一個巫師寫的物件不該能刪別人的檔、不該能改玩家存檔。
把 Unix 那套搬過來是最省事、也最容易被理解的做法。

**14 個 `valid_*` apply 構成完整的授權面**（附錄 E.3.2）：

| 分類 | apply |
|------|-------|
| 檔案 | `valid_read` `valid_write` `valid_link` `valid_save_binary` |
| 物件 | `valid_object` `valid_shadow` `valid_override` `valid_hide` |
| 身分 | `valid_seteuid` |
| 對外 | `valid_socket` `valid_bind` `valid_database` `valid_ffi` |

**每一個都是「driver 要做一件危險的事之前，先問 mudlib」。**
這個設計把安全策略完全交給了 mudlib——
**driver 不決定什麼是安全的，它只保證會問。**

**踩過的坑（本專案）**：除了上面那個順序問題，還有一個環境層面的：
**WASM 版是無 uid 的執行環境**。
§032 提過有 lib 的「安全模型與無 uid 的執行環境相衝」，
指的就是這件事——一個假設有完整 uid 體系的 mudlib，
在一個沒有使用者概念的分頁裡跑，很多檢查會落進沒有預期到的分支。

**優點 / 罩門**：優點是**策略與機制徹底分離**——
driver 提供 hook，mudlib 寫策略，換一套安全觀不必動 driver。
罩門有兩個：其一，**策略散在 14 個 apply 裡**，
沒有一處看得到全貌（和 §053 的三分法是同一種病）；
其二，**預設值的選擇需要考慮「失敗發生在什麼階段」**，
而那是最容易被忽略的維度。

**設計對照：fail-open 與 fail-closed 該怎麼選**

| 情境 | 該選 | 理由 |
|------|------|------|
| 授權檢查（正常運轉中） | **fail-closed** | 檢查不了就別做 |
| 唯讀查詢（正常運轉中） | 視情況 | 讀失敗多半只是體驗變差 |
| **啟動／初始化階段** | **兩者都不對** | **正解是「還沒準備好」要能被表達**——回一個第三種狀態，讓呼叫方等待或重試 |

第三列才是本節真正的結論：**布林值的授權介面沒有辦法表達「我還不知道」**，
於是設計者只能在「先放行」與「先拒絕」之間二選一，而兩個都會在某種情況下錯。

**效益**：對接手 mudlib 的人，這一節給一條除錯捷徑：
**看到權限錯誤出現在不該有權限問題的地方，先確認安全模組載入了沒有。**
在 LPC 裡，「權限不足」與「安全模組還沒起來」是同一個回傳值。

> 💡 君之一席話
> **fail-closed 假設「失敗代表異常」——當失敗的真正原因是「還沒準備好」，最保守的策略就變成了最難查的故障。**

> 🔍 老手視角──真正的門道
> 這一節的通用教訓不是「小心 fail-open/closed」，而是**一個布林回傳值不足以表達授權決策**。真實世界的授權至少有四種結果：**允許、拒絕、我不知道（還沒準備好／查不到）、我拒絕但這是暫時的**。把它們壓成一個 int，就等於強迫呼叫方把「不知道」當成「拒絕」或「允許」——而這個轉換的正確答案取決於呼叫發生在哪個階段，**那是被呼叫方不知道的資訊**。現代的授權系統（OPA、IAM、各種 policy engine）都會把 `INDETERMINATE` 或 `NOT_APPLICABLE` 當成一等結果，原因就在這裡。老手設計任何決策介面時會問：**「不知道」要怎麼表達？**——答不出來的介面，遲早會有人把「不知道」誤譯成一個決定。可落地的做法：授權與健康檢查類的介面，**回傳三態而不是布林**（允許／拒絕／未就緒），呼叫方就能對「未就緒」做重試或延後，而不是當場失敗。

---

## 062　三個「無中生有」的機制 — virtual object、shadow 與編譯管線

**標籤**：`#virtual物件` `#shadow` `#編譯管線` `#hook` `#save_binary`
**證據等級**：🟡 上游 `docs/apply/master/compile_object.md`、`docs/driver/parse_tree.md`、`call_into_vm.md`

**起源**：§047 那張「MudOS 能力面」的表裡，有三項只列了名字沒有解釋：
**virtual object、shadow、save_binary**。
這一節補上它們——而補的過程會發現，**它們是同一個設計形狀的三個實例**。

**技術核心**：

### ① virtual object — 「這個檔案不存在」變成一個 hook

當 mudlib 要載入一個**不存在的檔案**時，driver 不直接報錯，
而是問 master：

```c
object compile_object(string pathname);
```

> The driver calls `compile_object()` in the event that the mudlib instructs
> the driver to load a file that **does not exist**. […] After an association
> is made between an object and a filename, then it will be **as if the file
> did exist** (to the driver).

**回傳 0 = 真的不存在；回傳一個物件 = 從此那個路徑就有東西了。**

這一招在中文 mudlib 裡用得極兇——本專案兩個家族都有 `virtuald`（§050）。
典型用途：

| 用途 | 做法 |
|------|------|
| 迷宮 | `/d/maze/room_12_35` → 從座標算出房間，不必存一萬個檔 |
| 副本 | `/fuben/<玩家id>/room1` → 每個玩家一份獨立的世界 |
| 程序生成 | 路徑就是參數 |

**架構上的意義**：它把「檔案系統的查詢失敗」變成一個可被 mudlib 接管的擴充點。
**路徑從一個位址變成了一個函式呼叫的參數。**

### ② shadow — 在既有物件外面掛一層攔截

`shadow()` 讓一個物件掛在另一個物件前面，**攔截對它的函式呼叫**。
被 shadow 的物件完全不知情。

| 典型用途 | 為什麼要用 shadow 而不是改原物件 |
|---------|--------------------------------|
| 狀態效果（中毒、失明、變身） | 效果是**暫時**的，結束就把 shadow 拿掉 |
| 臨時懲罰／禁言 | 不必汙染角色本體的邏輯 |
| 除錯攔截 | 不動線上程式碼 |

**防護**：`nomask` 的函式**不能被 shadow**（§059）。
這是 mudlib 保護金錢、權限、存檔這類關鍵函式的標準做法。

**罩門很明顯**：shadow 是**隱形的**。
一個函式的行為被改掉了，而原始碼裡完全沒有痕跡——
§036 說過的那條除錯路徑（物件 → `F_*` → simul_efun → efun）
其實還漏了一層，完整版是：

```
shadow（如果有的話，而你看不到它）
  → 物件自己的 .c
    → inherit 的 F_* 清單
      → simul_efun
        → driver 的 efun
```

**LPC 有 `query_shadowing()` 可以問，但你得先想到要問。**

### ③ 編譯管線 — 從 `.c` 到位元組碼

```
LPC 原始碼
   │  ① 前處理器（內建，不是 cpp）：#include / #define / #pragma
   ▼
   │  ② 語法分析 → parse tree
   │     節點型別：NODE_IF / NODE_LOOP / NODE_FOREACH / NODE_CALL /
   │              NODE_EFUN / NODE_BINARY_OP / NODE_TERNARY_OP /
   │              NODE_CONTROL_JUMP / NODE_LVALUE_EFUN …
   ▼
   │  ③ 產生位元組碼（§037 的 F_* 指令）
   ▼
program（可被多個物件共用，§060）
   │
   └─ ④ 選用：save_binary → binaries/ 落地
```

第 ④ 步的證據在本專案的封存裡：
`dongfanggushi2` 帶著整個 `binaries/` 目錄（`.b` 檔），
`91shujian` 等家族 B 的 lib 也有 `#pragma save_binary` 的痕跡。
**它解決的是啟動時間**——一萬個檔案的編譯不是零成本的。

**★ 執行期的錯誤處理：LPC 錯誤 = C++ 例外。**

> LPC VM use C++ Exception to handle **any** error encountered during
> LPC execution.

而 driver 的非 VM 程式碼（網路層那半）要呼叫 LPC 時，必須做完整的儀式：

```
push 引數 → 記住 num_arg → save_context(&econ)
  → try { call_function_pointer(…) }
    catch { restore_context(&econ); pop_n_elems(num_arg); }
  → pop_context(&econ)
```

多數地方用包好的 `safe_apply()` / `safe_call_function_pointer()`；
少數不能用的地方（`apply()`、`call_direct()`、`call_program()`）
就必須自己走完上面那套。

**這解釋了 §037 那個 `F_CATCH` 為什麼要 `setjmp` 加遞迴求值器**——
LPC 的 `catch()` 與 driver 的錯誤處理是同一套機制的兩端。

**★ 三個機制的共同形狀。**

把三者並排，會看到同一個設計反覆出現：

| 機制 | 「查不到／做不到」的那一刻 | 變成了什麼 |
|------|--------------------------|-----------|
| **virtual object** | 檔案不存在 | 一個 master apply（`compile_object`） |
| **shadow** | 函式不該由本體處理 | 一個可插入的攔截層 |
| **錯誤處理** | 執行失敗 | 一個可被 LPC 接住的例外（`catch()`） |
| （§038 的熱重載） | 需要相依圖 | 兩個既有安全 apply 的**引數** |
| （§061 的安全） | 要做危險的事 | 14 個 `valid_*` apply |

**LPMud 家族的核心設計哲學可以濃縮成一句：
「driver 遇到任何需要做決定的時刻，就去問 mudlib。」**

driver 幾乎不持有政策——檔案存不存在、能不能寫、要不要繼承、
錯誤怎麼處理、危險操作准不准，**全部外包給上層**。
這就是 §033 那次分家在三十七年後最完整的樣子。

**優點 / 罩門**：優點是**極高的可塑性**——
mudlib 可以在不碰 C 的情況下改變幾乎任何行為。
罩門是**可診斷性持續下降**：每多一個 hook，
「這個行為是誰造成的」就多一個要查的地方。
本節列的五個 hook 加上 §036 的三個呼叫方向，
合起來就是為什麼讀 mudlib 比讀一般程式碼慢。

**效益**：對逆向工作，這一節補完了那條搜尋路徑的最後一塊。
**完整版是六層**：

```
① shadow（先 query_shadowing() 確認有沒有）
② 物件自己的 .c
③ inherit 的 F_* 清單（含 `父::函式` 顯式呼叫）
④ simul_efun 有沒有蓋掉
⑤ driver 的 efun
⑥ 這個路徑是不是 virtual object（檔案根本不存在）
```

第 ⑥ 層特別容易忘記：**你在檔案系統裡找不到那個檔，
不代表那個物件不存在。**

> 💡 君之一席話
> **「查不到」是一個設計機會**——LPMud 把每一次失敗（檔案不存在、權限未知、執行出錯）都變成一個問上層的 hook，於是引擎三十七年沒有政策，也就三十七年不必改。

> 🔍 老手視角──真正的門道
> 「把失敗變成 hook」這一招威力很大，但它有一個必須配套的東西，而 LPMud 沒有做——**可觀測性**。當檔案不存在會變成一個 virtual object、當函式呼叫可能被 shadow 攔走、當權限檢查外包給 14 個 apply，系統的實際行為就不再能從原始碼推導出來，只能從執行期觀察。現代系統面對同樣的擴充性（middleware、攔截器、AOP、service mesh）都學到了同一課：**擴充點必須配一個「誰在這裡插了東西」的查詢介面**。Express 有 middleware stack、Spring 有 AOP proxy 檢視、Envoy 有 config dump。LPC 有 `query_shadowing()`、`inherit_list()`、`function_exists()`，但它們是零散的 efun，不是一個統一的「這個呼叫實際會走哪條路」的視圖。老手在設計高度可擴充的系統時會刻意加一個**「解釋這次呼叫」的除錯模式**——把實際經過的每一層印出來。它的成本是幾天，而它把「為什麼會這樣」從一個調查變成一次查詢。

---



## 063　CD gamedriver — 最接近原版的那一支

**標籤**：`#CD` `#Genesis` `#hook` `#alarm` `#auth` `#VBFC`
**證據等級**：🟡 上游 `cotillion/cd-gamedriver`（`doc/CREDITS`、`doc/hooks/`、`doc/efun/`、原始碼樹）＋ 🟡 公開史料

**起源**：§035 那張四條血脈的表裡，CD 只有一句話帶過：
「Genesis 那邊改叫 CD」。

這句話漏掉了一件重要的事——**CD 不是 MudOS 的旁支，它是另一條主幹**，
而且**它是唯一還在跑原始那個世界的 driver**：Genesis，1989 年那個 mud，至今仍在營運。

**名字的來歷本身就是證據**：CD＝**Chalmers Datorförening**，
Lars Pensjö 所屬的查爾摩斯理工學院電腦社。
翻回 §033 引的那份 `Credits.LPmud` 第一行：

```
Lars Pensjö, April 1989 (lars@cd.chalmers.se)
                          ↑↑
```

**`cd` 從第一天就在那裡**——它不是後來取的名字，是這個專案的出生地。

1991 年底 Lars 從 Genesis 退休，其餘管理者從他的開發版（LPMud 3.0）分出了 CD。
所以在時間上，**CD 的分家比 MudOS（1992-08）還早**。

**技術核心**：CD 在四個地方做了與 MudOS 系**不同的選擇**，而每一個都很有意思。

### ① hook 而不是 apply

MudOS／FluffOS 叫 apply（§036）；CD 叫 **hook**，而且機制略有不同：

> The callback functions are **named in `master.n`** and are converted into
> **`M_*` named defines** that you should use within the driver.
> Use `apply_master()` to call the master object with the `M_*` defines.
> **If you do not have a function declared within the master object, 0 will be returned.**

差別在於**名字集中管理**：CD 把所有 hook 名字列在一個檔（`master.n`）裡，
編譯期生成 `M_*` 常數，driver 內部一律用常數而不是字串。

MudOS 系則是散在各處的字串 apply。
**這是「命名慣例」與「集中註冊」的差別**——
§036 老手視角提過的那個問題（靠命名生效的系統沒人告訴你名字打錯了），
CD 在 driver 這一側解掉了一半：**driver 不會拼錯，因為它用常數**。
mudlib 那一側仍然是慣例。

CD 的 hook 清單也更小、更聚焦於生命週期：

```
start_boot  preload_boot  final_boot
start_shutdown  cleanup_shutdown  final_shutdown
runtime_error  log_error  parse_exception
object_name  flag  get_vbfc_object
```

**開機與關機各分三階段**——比 MudOS 的 `preload` + `epilog` 細緻得多。

### ② alarm 而不是 call_out ＋ heart_beat

這是 CD 最漂亮的一個設計決定。MudOS 系有兩套週期機制（§038）：
`call_out()`（一次性）與 `heart_beat()`（固定間隔的 apply）。

**CD 把它們合成一個：**

```c
int set_alarm(float delay, float repeat, function func);
```

- `repeat` 為 0 → 一次性，等同 `call_out`；
- `repeat` 為正 → 重複執行，等同心跳，**而且間隔可以自訂**；
- 回傳 alarm id，可用 `remove_alarm` / `get_alarm` / `get_all_alarms` 管理。

> 例：`delay=3.0, repeat=4.0` → 3 秒後第一次，之後每 4 秒一次。

**兩個機制變成一個 API 的兩個參數。**
而且 delay 是 `float`——**次秒級精度**，MudOS 的 `call_out` 是整數秒。

文件裡還留著兩條很實在的警告：

> **NOTA BENE**：`set_alarm()` 呼叫的函式裡，`this_player()` 與
> `this_interactive()` **都沒有定義**——在那裡 `write()` 的輸出會跑進遊戲日誌。
>
> **WARNING**：**永遠不要在重複 alarm 裡再建一個重複 alarm。**
> 產生的 alarm 會呈指數成長，**不到一分鐘整台 mud 就會停擺。**

第一條與 FluffOS 的 `this_player in call_out` 設定（附錄 E.4.5，預設 1）
是同一個問題的兩種答案：**CD 選擇不定義，FluffOS 選擇提供一個開關。**

### ③ auth 而不是 uid / euid

§061 講的 uid/euid 是照抄 Unix 的。**CD 沒有照抄。**

```c
void set_auth(object ob, mixed info);
mixed query_auth(object ob);
```

> It is possible to store authority information **in any format** in the
> hidden authority variable of all objects. […] The gamedriver calls
> `valid_set_auth` in the master object, to determine if the call is legal
> **and possibly to modify the information**. The return value of
> `valid_set_auth` **is stored** in the hidden authority variable.

三個差異，每一個都是往「更泛用」的方向走：

| | MudOS 的 uid/euid | **CD 的 auth** |
|---|-----------------|---------------|
| 型別 | 字串（借 Unix 語意） | **`mixed`——任何格式** |
| 誰決定內容 | mudlib 呼叫 `seteuid()`，master 只能准或不准 | **master 的 `valid_set_auth` 回傳值就是最終存進去的東西**——它可以**改寫** |
| 語意 | 使用者／群組 | **由 mudlib 自己定義** |

**第二列是關鍵**：MudOS 的 `valid_seteuid()` 是一個布林閘門
（§061 說的那個「布林值不足以表達授權」的問題）；
**CD 的 `valid_set_auth` 是一個轉換函式**——它不只決定准不准，還決定**存什麼**。

**這個設計把授權策略徹底交給 mudlib**：
你想用 Unix 式的 uid，可以；想用能力清單（capability list），可以；
想存一個帶到期時間的 mapping，也可以。

### ④ VBFC — 把函式呼叫嵌在字串裡

這是 CD 最特別、也最危險的一個機制：

```c
mixed process_value(string calldescription, void|int security);
```

字串的格式是：

```
"function[:filename][|arg1|arg2|...|argN]"
```

也就是說，**一段字串可以描述一次函式呼叫**，`process_string()` 會把它替換掉。
mudlib 用它做「值由函式呼叫決定」（value by function call）——
房間描述裡可以嵌一段會動態求值的東西，不必寫程式。

文件自己標了警示：

> **CAVEAT**：It is wise to set the security parameter […] as **any function
> in any object can be called with almost any arguments**. You probably don't
> want to have this done with your privileges.

所以有了 `security` 參數與 `get_vbfc_object` hook——
**用一個沒有權限的物件當發起者**。

> 🔬 **本書的角度**：這正是 §004 那個「沒有跳脫機制的分隔符」問題的
> **最極端版本**——ZJMUD 把 `$zj#` 混進文字流最壞的後果是解析錯亂；
> **VBFC 把函式呼叫混進文字流，最壞的後果是任意程式碼執行。**
> 兩者是同一種設計（在人類可讀串流裡埋機器語意）在不同風險等級上的實例。

### ⑤ 其他值得記的分歧

| 面向 | CD | MudOS 系 |
|------|-----|---------|
| mapping API | `m_delete` `m_indexes` `m_values` `m_sizeof` `mkmapping` | `map_delete` `keys` `values` `sizeof` `allocate_mapping` |
| 存檔 | `save_object`／`m_save_object`／**`save_map`／`restore_map`** | `save_object` / `restore_object` |
| 呼叫形式 | `call_other` `call_otherv` **`call_self` `call_selfv`** `applyv` `papplyv` | `call_other`、`evaluate` |
| **JSON** | **`val2json` / `json2val`**（driver 依賴 `libjson-c`） | 無（mudlib 自己實作） |
| 配置器 | **bibopmalloc**（Lennart Augustsson 寫的 BiBOP） | smalloc → jemalloc |
| 雜湊 | **siphash** | 一般字串雜湊 |
| **intermud** | **在 driver 裡**：`udpsvc.c`、`tcpsvc.c`、`hname.c` | **在 mudlib 裡**：socket efun ＋ LPC（§049） |

**最後一列的對比非常值得停下來看。**

§049 講過 TMI-2 用 LPC 寫了整套 intermud（`_q`／`_a` 成對、`dns_master` 名錄），
那是因為 MudOS 給了 socket efun，**把能力推到上層**。

**CD 反過來：把 intermud 服務做進 driver。**
它的啟動旗標裡就有 `-u<port>`（UDP 埠）與 `-p<port>`（service 埠，供 ftp 等用途）——
**這些是 driver 級的設施，不是 mudlib 寫出來的。**

**同一個需求，兩個方向相反的答案**：
MudOS 說「我給你 socket，你自己寫」；CD 說「我幫你做好」。
§047 那句「引擎加的不是功能，是可能性的邊界」在這裡有了對照組——
**邊界推到哪裡，決定了上層要不要重新發明。**

**優點 / 罩門**：CD 的優點是**它一直有一個真實的使用者**——Genesis。
一個 driver 只服務一個世界，設計可以很聚焦、很少妥協，
所以它是「所有變體裡最接近原版 LPMud 精神」的那一支。
罩門也是同一件事：**生態極小**。
32 顆星、174 個 commit（撰寫當下），沒有 MudOS 系那種數千個 mudlib 的規模。
**本專案 17 份封存沒有一份是 CD 系的**，這不是巧合。

**效益**：對本書而言，CD 提供的是**對照組**。
第八、九篇所有關於「LPMud 家族長什麼樣」的敘述，
如果只看 MudOS→FluffOS 這一條，很容易把某些設計當成必然。
CD 證明了它們不是：
**apply 可以集中註冊、call_out 與 heart_beat 可以合併、
uid/euid 可以換成任意格式的 auth、intermud 可以做在 driver 裡。**

> 💡 君之一席話
> **看一個生態的設計是不是必然，最快的方法是找到那個做了相反選擇、而且活得好好的實作。**

> 🔍 老手視角──真正的門道
> CD 的 `set_alarm` 把 `call_out` 與 `heart_beat` 合成一個 API，這件事值得單獨想一下：它們在 MudOS 裡之所以是兩個東西，是因為**實作機制不同**（一個是排程佇列、一個是每 tick 掃過所有開了心跳的物件），而不是因為使用者的需求不同。使用者的需求其實只有一個——「過一段時間叫我」——差別只在要不要重複。**MudOS 把實作的差異洩漏成了 API 的差異**，於是每個 mudlib 作者都得學兩套、記兩套的陷阱、寫兩套管理程式碼。這是 API 設計裡最常見的一種洩漏，而它幾乎總是發生在「兩個功能是不同時期加的」的地方。老手在合併 API 時的判準是：**使用者在選擇時，問的是不是一個關於實作的問題？**——「我該用 call_out 還是 heart_beat」是關於實作的；「延遲多久、要不要重複」才是關於需求的。可落地的做法：任何有兩個相似 API 的地方，試著寫出「用一個 API 加參數」的版本，看看參數是不是自然——是的話，你原本那兩個 API 就是實作洩漏。

---

## 064　DGD — 從頭重寫的那一支，以及它證明了什麼

**標籤**：`#DGD` `#持久化` `#輕量物件` `#原子函式` `#JIT` `#狀態快照`
**證據等級**：🟡 上游官方站（dworkin.nl/dgd）與公開版本史

**起源**：§035 說 DGD 是「概念衍生，非程式碼衍生——重寫的」。
這一節解釋**重寫換到了什麼**——因為 DGD 的技術清單，
有一半是其他三支 driver 至今沒有的東西。

**技術核心**：Felix “Dworkin” Croes 從零重寫 LPMud，
而重寫的第一個決定就定調了後面全部：

> **DGD does away with all of LPMud's game-oriented features,
> which can be implemented in LPC.**

**把所有遊戲導向的功能從 driver 裡拿掉。**

對照 §047 那張 MudOS 能力面的表——
MudOS 的路線是「batteries-included」，把能力面推到最寬；
**DGD 走了完全相反的方向：核心極小，其餘用 LPC 自己蓋。**

這個選擇的結果是 DGD 通用到不像個 MUD driver：
**1990 年代末 Yahoo 的聊天室就是跑在 DGD 上的。**

### DGD 的五項技術創新

| 創新 | 是什麼 | 其他 driver 有嗎 |
|------|--------|----------------|
| **完整持久化** | **所有資料、程式與執行狀態都存在一個資料庫裡**；可做快照、可從快照恢復、重開機續跑 | ❌ MudOS 系只有 `save_object` 這種手動存檔 |
| **輕量物件** | 兩種物件：**輕量物件**（像一般 OOP 的物件，無 id、可大量產生）與**持久物件**（有全域唯一 id、能連網、能排程） | ❌（LDMud 後來借走了這個概念） |
| **原子函式（atomic function）** | **出錯就把這次呼叫的所有改動全部回捲**——保證一致性 | ❌ 這是資料庫的交易語意，出現在一個 MUD driver 上 |
| **就地重編＋hotboot** | 活著升級程式；**hotboot 換掉整個執行檔而不斷線** | ⚠️ MudOS 系有熱重載（§060），但沒有「換執行檔不斷線」 |
| **位元組碼 VM ＋選用 JIT** | 可編成機器碼 | ❌ 其餘三支都是純解譯（§037） |

**「原子函式」這一項特別值得停一下。**

§060 講過 MudOS 系最難查的一類 bug：程式與資料可以獨立地過期、
重載會讓變數歸零、世界上同時有兩個版本的同一種劍。
**這些問題的共同根源是「沒有交易邊界」**——
一次改動不是全成功就是全失敗，而是可能改到一半。

DGD 的原子函式直接把資料庫的交易語意搬進了語言層。
**這不是效能優化，是把一整類 bug 從可能變成不可能。**

### 與 LPMud 的相容關係

> DGD maintains backward compatibility with **LPMud 2.4.5** and mostly
> supports **LPMud 3.1.2**, though some features function differently.

注意它對標的是 **2.4.5 與 3.1.2**——也就是 **Lars 的官方線**，
而不是 MudOS。§034 說過 MudOS 明講自己與 3.0 mudlib 不相容；
**DGD 選擇了另一邊。**

### 版本史（技術轉折點）

| 版本 | 年份 | 轉折 |
|------|------|------|
| 1.0.a3 | 1993-08 | 第一個公開版 |
| 1.0.8 | 1994-08 | 第一個穩定版 |
| **1.4** | **2010-02** | **開源**（AGPL）——商業版權方無力維護後由 Dworkin B.V. 取得 |
| 1.5 | 2014-04 | 動態擴充模組、**hotboot**、位元組碼 VM v2 |
| 1.6 | 2017-04 | **C++ 重寫**；放棄 1.5.9 之前的快照相容性 |
| 1.7 | 2023-01 | 位元組碼 VM v2.4，強化 JIT 支援 |

**2010 年才開源**——這解釋了為什麼 DGD 的生態遠小於 MudOS 系：
在中文 MUD 大爆發的 1996–2005 年，**DGD 是閉源的**（§056 說的時間軸問題，
在這裡是另一個實例）。

另有一個封閉分支 **Hydra**：針對多核心優化、與 DGD 功能對等。

**解決的痛點**：DGD 解的是 LPMud 家族一個長期被忽略的問題——
**世界是易失的**。

MudOS 系的 mud 崩了，玩家會回到上一次 `save_object` 的狀態；
沒存的東西就沒了。這在 1990 年代被當成「就是這樣」。
**DGD 把它當成一個可以解決的問題，而且解法是「整個世界就是資料庫」。**

**踩過的坑（生態層面）**：DGD 的技術優勢從來沒有轉換成生態優勢，
原因有三，而且沒有一個是技術的：

1. **1993 年出現，2010 年才開源**——錯過整個 MUD 黃金期；
2. **不相容於 MudOS 的 mudlib**——當時絕大多數現成 mudlib 都是 MudOS 系的；
3. **極小核心意味著要自己蓋很多東西**——對「拿一份 mudlib 開站」的人來說門檻更高。

**這三條在本專案身上是可以驗證的**：17 份封存全部是 MudOS 系，
一份 DGD 系都沒有。

**優點 / 罩門**：優點是它在 1993 年就做對了很多要到 2010 年代
才被主流理解的事（持久化、交易語意、輕量物件、hotboot、JIT）。
罩門是**它證明了技術領先與生態成功是兩件事**——
而且證明得非常徹底。

**效益**：對本書而言，DGD 是第二個對照組，而且是更極端的那個：
CD 證明了「MudOS 的某些設計不是必然」，
**DGD 證明了「連 driver/mudlib 的分界線本身都可以重畫」**——
把遊戲導向的東西全部推上去，核心只留語言與持久化。

> 💡 君之一席話
> **技術領先十七年而生態沒有跟上，往往不是技術的問題，是時機與相容性的問題**——一個 2010 年才開源的 1993 年好東西，錯過的不是市場，是整整一代人選型的那一刻。

> 🔍 老手視角──真正的門道
> DGD 的「原子函式」值得從一個更高的角度看：**它把一致性從一個紀律問題變成了一個機制問題。** MudOS 系要保證「改到一半不會留下爛攤子」，靠的是作者自己小心（先檢查再改、失敗要手動回復）；DGD 直接給你一個保證。這個轉換在軟體史上反覆出現——記憶體管理從紀律（`free`）變成機制（GC）、資料一致性從紀律變成交易、資源釋放從紀律變成 RAII／`defer`、並行安全從紀律變成型別系統（Rust 的所有權）。它們的共同模式是：**先有一類反覆出現、靠紀律無法根治的 bug，然後有人把紀律編進機制裡。** 老手看到一個團隊反覆在同一類 bug 上跌倒時，第一個念頭不是「加強 code review」，而是「這個紀律能不能變成機制」——因為紀律的失效率不會歸零，而機制的失效率可以。可落地的做法：把團隊過去半年的事故分類，找出**同一種紀律失效超過三次**的那一類，然後問「有沒有一個機制可以讓它不可能發生」。

---

## 065　五支 driver 的技術對照 — 以及一份 CREDITS 的兩個分支

**標籤**：`#對照` `#設計空間` `#分歧` `#文獻化石` `#選型`
**證據等級**：🟡 四份上游倉庫與官方站的交叉比對 ＋ 🟢 本專案對 FluffOS 的實測

**起源**：§033–§035 排了時間軸，§063–§064 拆了 CD 與 DGD。
這一節把五支放在同一張表上——**不是為了排名，是為了看清楚設計空間有多大**。

**技術核心**：五支 driver 的分歧總表。

| 面向 | **LPMud**（1989） | **MudOS→FluffOS**（1992→今） | **LDMud**（1997） | **DGD**（1993） | **CD**（1991） |
|------|------------------|---------------------------|------------------|----------------|---------------|
| 血緣 | 本體 | 從 3.0.5x 分家，**明講不相容** | 接手 3.2（Amylaar 線） | **從零重寫**（概念衍生） | 從 Lars 的 3.0 開發版分家 |
| 路線 | — | **batteries-included**（能力面最寬） | 官方線的現代化延續 | **極小核心**（遊戲功能全推上層） | **服務單一世界**（Genesis） |
| 相容對標 | — | MudOS | LPMud 3.2 | **LPMud 2.4.5 / 3.1.2** | Lars 的 3.0 |
| driver→mudlib 回呼 | apply | **apply**（字串名，69 個） | apply | — | **hook**（`master.n` → `M_*` 常數） |
| 週期機制 | `call_out` + `heart_beat` | 同左（＋`gametick`） | 同左 | 任務模型 | **`set_alarm`（delay + repeat 合一，float 精度）** |
| 安全模型 | uid/euid | **uid/euid + 14 個 `valid_*`** | uid/euid | LPC 自己實作 | **`set_auth`（任意格式，master 可改寫）** |
| 持久化 | `save_object` | `save_object`（＋swap） | `save_object` | **★ 整個世界是資料庫，可快照可續跑** | `save_object` / `save_map` |
| 交易語意 | ❌ | ❌ | ❌ | **★ 原子函式（出錯全回捲）** | ❌ |
| 物件種類 | 一種 | 一種（＋virtual object） | **＋輕量物件**（借自 DGD） | **★ 輕量 + 持久兩種** | 一種 |
| 執行 | 解譯 | 解譯 | 解譯 | **★ 位元組碼 + 選用 JIT** | 解譯 |
| 熱更新 | `update` | 重編物件（§060） | 同左 | **★ 就地重編 + hotboot 換執行檔不斷線** | 重編物件 |
| intermud | — | **mudlib 用 socket efun 自己寫**（§049） | mudlib | LPC | **★ 做在 driver 裡**（`udpsvc.c`/`tcpsvc.c`） |
| 字串內嵌求值 | — | — | — | — | **★ VBFC（`process_string`/`process_value`）** |
| 現代化 | — | **C++、UTF-8/ICU、TLS、WebSocket、WASM、CI** | 持續維護 | C++ 重寫（1.6）、JIT | libjson-c、siphash、bibopmalloc |
| 開源時間 | 一開始就是 | 一開始就是 | 一開始就是 | **2010-02（AGPL）** | 一開始就是 |
| 生態規模 | — | **最大**（本專案 17 份封存全在此系） | 中 | 小（但用在非 MUD 場景） | **小**（主要服務 Genesis） |

**從這張表可以讀出三件事：**

### ① 五支解的其實是三個不同的問題

```
MudOS/FluffOS  →「怎麼讓最多人能開站」   答案：能力面推寬 + 死守相容
LDMud          →「怎麼把官方線接下去」   答案：穩定演進
CD             →「怎麼把一個世界養好」   答案：為 Genesis 量身做決定
DGD            →「LPMud 這個概念的正確實作是什麼」 答案：從零重寫，核心極小
```

**它們不是同一場競賽的參賽者。**
把它們放在一起比「誰比較好」沒有意義；
有意義的是**看它們在同一個設計問題上做了什麼不同的選擇**——
那才是設計空間。

### ② 每一支都有一項別人沒有的東西

| driver | 它獨有的那一項 |
|--------|--------------|
| FluffOS | **WebAssembly**——整台 driver 進瀏覽器分頁（第八篇整篇） |
| DGD | **原子函式 + 完整持久化**——一整類 bug 從可能變成不可能 |
| CD | **auth 可任意格式 + `valid_set_auth` 可改寫**——授權策略完全外包 |
| LDMud | 官方線的連續性——**唯一能宣稱「我是 LPMud 3.x」的那一支** |

**沒有一支是另一支的超集。**

### ③ ★ 一份 CREDITS 的兩個分支——文獻化石

這是本節最有意思的發現，而且它是純文獻證據。

FluffOS 倉庫封存的 `docs/archive/Credits.LPmud`（§033 引用過）
與 CD 倉庫的 `doc/CREDITS`，**開頭完全相同**：

```
The program was written originally by

Lars Pensjö, April 1989 (lars@cd.chalmers.se).

Other credits:

The regexp package was made by Henry Spencer.
The ed package was not done by me. See the file ed.c for information.
Bit manipulations was implemented by pell@lysator.liu.se …
Roland Dunkerley III fixed for-loops, do-while and the operators ++ and --.
Sean T Barrett made smalloc.c.
Lennart Augustsson convinced me to implement a compiler for a virtual
  stack machine. He also implemented the built-in preprocessor.
John S. Price found and fixed many bugs.
The shadow idea was "forwarded" to me by John S. Price from Team Cthulhu …
```

**然後 FluffOS 那份就停了**（結尾是 Petri Wessman 修 SCO unix 與 AIX）。

**CD 那份繼續往下寫了十年**：

```
In 1992 the main development work was done by
  Johan Andersson (jna@cd.chalmers.se)
Until 06.00 the development was mainly done by
  Anders Chrigström and Jacob Hallen
Current development of this version is mainly done by
  Erik Gävert

Mike McGaughy made the shared string implementation.
Joern Renecke made the original implementation of switch and the AVL tree
  implementation of smalloc.
Ronny Wikh and Lennart Augustsson implemented the first version of mappings.
Lennart Augustsson made bibopmalloc.
Thorsten Lockert fixed numerous bugs, rewrote interpret.c and redid much of
  the build procedure.
Dave Richards corrected many performance problems …
```

**同一份檔案，在 1991／1992 年分家之後，兩邊各自長大。**

這件事的價值和 §049 那個 TMI-2 檔頭、§050 那個 `ken@XAJH` 署名是同一類：
**它是系統自己留下的、沒有敘事動機的紀錄**（附錄 D.4 的判準）。
把兩份 CREDITS 並排，分家點就在那個共同前綴的結尾——
**不需要任何人告訴你他們什麼時候分開的。**

而且它還順帶證實了兩件技術上的事：
- **`mapping` 是後來才加的**（Ronny Wikh 與 Lennart Augustsson 做了第一版）——
  所以 §052 那棵撐起整個遊戲狀態的屬性樹，用的是一個**不在最初設計裡**的型別；
- **`switch` 與 smalloc 的 AVL 版都是 Joern Renecke 做的**，
  而 Joern Rennecke 的名字也出現在 FluffOS 那份的「找到並修掉大量 bug」名單裡——
  **同一個人的貢獻跨在分家的兩側。**

**解決的痛點**：這一節解的是選型與理解上的一個常見錯誤——
**把一個生態裡最流行的實作當成那個概念的定義。**

如果只看 MudOS→FluffOS，很容易得出「LPMud 就是這樣」的結論：
apply 是字串、call_out 與 heart_beat 是兩個東西、
安全模型是 uid/euid、intermud 是 mudlib 的事。
**五支並排之後，這四條全部變成「MudOS 的選擇」，而不是「LPMud 的本質」。**

**優點 / 罩門**：這種橫向對照的優點是它會**把隱含假設逼出來**。
罩門是它需要真的去讀別人的實作——
本節的每一格都來自實際的倉庫與文件，而不是從一支推論其他四支。

**效益**：對本書的收束意義：第八篇問「連線的另一端是什麼」，
第九、十篇往下鑽了三層，而這一節是最後一次拉遠——
**你手上那台 driver 的每一個設計，都有至少一個活著的反例。**

而這正是本書從第一頁就在講的那件事的另一種說法：

> **知道一個東西「是什麼」，和知道它「為什麼不是別的樣子」，是兩種不同的理解。**
> 前者讓你會用，後者才讓你知道哪些東西可以改。

> 💡 君之一席話
> **不要把生態裡最流行的實作當成那個概念的定義**——找到那些做了相反選擇而且活著的實作，你才會知道哪些是本質、哪些只是某個人在 1992 年的一個決定。

> 🔍 老手視角──真正的門道
> 這一節示範的方法叫**橫向對照**，它的成本很低但幾乎沒有人做。多數人理解一個技術的路徑是「讀最流行那個的文件 → 讀它的原始碼 → 覺得自己懂了」，而這條路徑有一個結構性的盲點：**你學到的每一個設計決定，都不知道它有沒有替代方案。** 修正的方法只有一個——**去讀那個做了相反選擇的實作**，哪怕只讀它的 API 文件與目錄結構（本節 CD 的部分幾乎全部來自 `doc/` 目錄與檔案清單，沒讀一行 C）。半天的閱讀量，能把「這就是唯一做法」變成「這是三種做法之一，而它們的取捨是……」。可落地的做法：學任何技術時，**同時打開它的一個競爭者的文件目錄**，只比對兩件事——**名詞表**與**目錄結構**。名詞不同的地方就是概念分歧點，目錄不同的地方就是架構分歧點；這兩張圖比任何一份 tutorial 都更快讓你看見設計空間。

---



# 第十一篇　深入各家設計：四支 driver 的解剖

> §065 把五支 driver 排成一張表，那是**廣度**。
> 這一篇往下切——**每一支挑它最能代表自己的那個設計，看清楚它為什麼那樣做、代價是什麼**。
>
> 五個單元的共同提問：**這個設計把「權力」放在誰手上？**
>
> 因為四支 driver 的分歧，歸根究柢是同一個問題的四種答案：
> **driver 該保留多少決定權？**

---

## 066　LDMud 的語言野心 — 把 LISP 放進 LPC

**標籤**：`#LDMud` `#closure` `#lambda` `#coroutine` `#lwobject` `#struct`
**證據等級**：🟡 上游 `ldmud/doc/LPC/`（`closures`、`inline-closures`、`coroutines`、`lwobjects`、`structs`、`unions`）

**起源**：§065 那張表裡 LDMud 只寫了「官方線的連續性」，
那是它的**定位**，不是它的**內容**。

實際打開 `ldmud/doc/LPC/` 會看到一份不太像 1997 年的清單：

```
closures  inline-closures  closure_guide  closures-abstract  closures-example
coroutines  async  await  yield
lwobjects  structs  unions  decltype  references  varargs  modifiers
```

**`async` / `await` / `yield` / `union` / `decltype`。**
這是一支把語言本身當成主戰場的 driver。

**技術核心**：四項語言特性，一項比一項激進。

### ① closure — 不是函式指標，是 LISP

MudOS／FluffOS 的 `(: foo :)` 是**函式指標**：指向一個既有的函式。
LDMud 的 closure 是**另一個層級的東西**。

**先是字面量，帶命名空間前綴：**

```c
closure f;
f = #'this_player;              // efun closure
f = #'efun::move_object;        // 明確指定 efun（繞過 simul_efun）
f = #'sefun::my_helper;         // simul_efun
f = #'lfun::query_name;         // 本物件的 lfun
f = #'var::hp;                  // ★ 指向一個「全域變數」的 closure
```

`#'var::` 那一行值得停一下：**變數也可以被閉包化**——
你可以把「某個全域變數」當成一個值傳來傳去。

**然後是 `lambda()`，而這才是它真正的野心：**

```c
f = lambda( ({ 'x }), ({ #'environment, 'x }) );
funcall(f, this_object());      // → environment(this_object())
```

看清楚第二個引數：`({ #'environment, 'x })`——
**這是一個陣列，而它就是程式碼。**
第一個元素是要呼叫的東西，其餘是引數；`'x` 是**符號**（symbol），
在求值時被綁定。

> **LPC 在這裡變成了同像性（homoiconic）語言：程式碼就是資料結構。**
> `lambda()` 幾乎是 LISP 的 `lambda` 直譯過來——
> 官方文件自己就這樣寫：*“It is much like the lambda function in LISP.”*

**`funcall()` 與 `apply()` 的差別**也是 LISP 傳統：
`apply()` 的**最後一個引數若是陣列，會展開成多個參數**。

```c
mixed eval(object ob, string func, mixed *args) {
    return apply(#'call_other, ob, func, args);   // args 展開
}
```

**這意味著什麼？** 意味著 mudlib 可以**在執行期組出程式碼再執行它**。
§062 講 CD 的 VBFC 時說那是「把函式呼叫嵌在字串裡」——
LDMud 更進一步：**把程式碼放在陣列裡**，型別安全、可組合、可傳遞。

### ② inline closure — 寫在用它的地方

```c
filter(list, function int (mixed x) { return x > 0; });
```

和現代語言的 lambda 一樣，而且**可以捕捉上下文變數**。

### ③ coroutine — `async` / `await` / `yield`

這一項在 MUD driver 上出現，坦白說有點超現實。

> Regular LPC functions have a short life span. **They must finish, before a
> new event like a player command can be processed.** Coroutines are a special
> type of functions, **whose execution can be suspended and continued at a
> later time.**

```c
async void fun() { … }

coroutine cr = fun();
```

三個操作：

| 操作 | 語意 |
|------|------|
| `await(cr[, value])` | 呼叫另一個 coroutine 並**等它 return**；期間本 coroutine 暫停 |
| `yield([value])` | **暫停自己**，把值交給呼叫者；恢復時 `yield()` 的結果是傳進來的值 |
| `yield(value, cr)` | 暫停自己並**直接切換到指定的 coroutine** |
| `return [value]` | 銷毀本 coroutine；有人在等就繼續它 |

兩個實作細節很能說明它的定位：
**coroutine 是傳址的**，而且**結束後會退化成整數 0**——
所以「這個 coroutine 還活著嗎」就是 `if (cr)`。

**為什麼一個 MUD driver 需要這個？**
因為 §037 講過的那件事：LPC 是**事件驅動、無執行緒**的，
一次呼叫必須跑完才能處理下一個事件。
於是所有「跨越多個事件的流程」（多步對話、逐步戰鬥、
需要等玩家輸入的精靈）都得手寫狀態機——
§044 那條 `input_to` 接力就是一個活生生的例子：
**登入流程被拆成 `logon` → `jiance` → `get_user` 三個函式，
只因為中間要等玩家打字。**

**coroutine 讓那條接力可以寫成一個函式。**
LDMud 沒有加執行緒（那會毀掉整個模型），
而是加了**可暫停的函式**——在單執行緒事件迴圈上得到多步流程的表達力。

### ④ lwobject 與 struct — 補上「輕量資料」這一格

**lightweight object**（概念借自 DGD，§064）：

```c
#pragma lightweight          // 這個檔可以被當成輕量物件用（隱含 no_clone）
lwobject lwo = new_lwobject("/obj/foo");
```

> Lightweight objects are **a cross between regular objects and structs**.
> Like regular objects they are built from programs and encapsulate data and
> functions. **Like structs they are automatically destructed, they can be
> copied and saved.**

**struct**：

```c
struct Foo { int one, *two; struct Bar three; };
```

> The name is **resolved when the LPC code is compiled**, making struct member
> access **as fast as array member access**.

**union**（純編譯期，除非開 `#pragma rtt_checks`）：

```c
int|string var;
int|float fun(object|closure f);
<int|string>* arr;              // 陣列成員是 int 或 string
int*|string*  arr;              // 「int 陣列」或「string 陣列」
```

注意最後兩行的差別——**LDMud 認真到需要一套角括號語法來消歧義**，
甚至要提醒你 `< <` 之間要有空白，免得被當成 `<<` 運算子。

**解決的痛點**：LDMud 解的是一個很清楚的問題——
**LPC 作為語言，在 1990 年代中期已經落後了。**

對照 §058 那張 MudOS 的型別表：`int` `float` `string` `object`
`mapping` `function` `mixed` 加陣列，就這樣。
沒有結構、沒有聯集、沒有輕量值、沒有真正的閉包、沒有協程。

**LDMud 的答案是：那就把語言補起來。**

**踩過的坑（生態層面）**：語言補得越好，**離其他分支越遠**。
一份用了 closure、struct、coroutine 的 LDMud mudlib，
在 FluffOS 上連編譯都過不了——不是相容性小問題，是**語言不同**。

這解釋了一件事：§050 那 17 份中文封存全是 MudOS 系，
**不是因為 LDMud 比較差，是因為兩邊的 mudlib 從一開始就不能互換。**
語言分歧比 API 分歧更難跨越。

**優點 / 罩門**：優點是**表達力**——用 closure 寫的 mudlib，
在抽象程度上可以遠超同期的 MudOS mudlib。
罩門有兩個：其一，**心智負擔**（`lambda()` 那套組出來的程式碼很難除錯）；
其二，**單向鎖定**（用了就回不去）。

**效益**：對本書而言，LDMud 提供的是一個很重要的反例——
**§058 說「LPC 的型別是註解」，那是 MudOS 系的事實，不是 LPC 的本質。**
同一個語言家族裡，有一支把型別、結構、聯集、協程全部做進去了。

> 💡 君之一席話
> **當你發現自己一直在手寫狀態機，那不是紀律問題，是語言缺了一個特性**——LDMud 的協程與 §044 那條 `input_to` 接力，是同一個問題的兩個時代的答案。

> 🔍 老手視角──真正的門道
> `lambda(({'x}), ({#'environment, 'x}))` 這種「用陣列表示程式碼」的設計，威力和危險是同一件事：**它讓程式碼可以被程式產生**，於是你能寫出組出查詢、組出規則、組出行為的 mudlib；而代價是**堆疊追蹤失去意義**——出錯時你看到的是一個被組出來的匿名 closure，沒有檔名、沒有行號、沒有作者。這個取捨在現代生態裡到處都是：ORM 組出的 SQL、序列化器產生的類別、裝飾器包出來的函式、Proxy 攔截出來的物件。它們都能大幅減少樣板，也都會在出事時讓你面對一段沒有出處的程式碼。老手用這類機制時會加一件事：**讓產生出來的東西帶著它的來源**——組 SQL 時附註解、產生類別時記下產生器、動態註冊時記下註冊點。可落地的做法：任何「程式碼由程式產生」的地方，**在產物裡留一個指回產生處的字串**。它幾乎不花成本，而它決定了三個月後那個錯誤是十分鐘還是兩天。

---

## 067　LDMud 的 driver 野心 — 把 efun 的實作交給 mudlib

**標籤**：`#driver_hook` `#ERQ` `#compat模式` `#Python` `#權力下放`
**證據等級**：🟡 上游 `ldmud/doc/concepts/`（`hooks`、`erq`、`native`、`python`）與 `doc/hook/`

**起源**：§066 講的是 LDMud 在語言上的野心。
這一節講它在 driver 架構上的——而它比語言那一半更激進。

**技術核心**：

### ① driver hook — 把硬編行為搬進 mudlib

> To allow a greater flexibility of the muds, the gamedrivers since 3.2.1
> **moved several once hardcoded 'underground' activities from the driver
> into the mudlib.**

機制是 `set_driver_hook(編號, 值)`（特權 efun），
編號的符號名定義在 `/sys/driverhooks.h`。**30 個以上的 hook**：

| 分類 | hook |
|------|------|
| **★ efun 的實作** | **`H_MOVE_OBJECT0` / `H_MOVE_OBJECT1`——強制性，用來實作 efun `move_object()`** |
| 身分 | `H_LOAD_UIDS` `H_CLONE_UIDS` `H_LWOBJECT_UIDS`（強制） |
| 生命週期 | `H_CREATE_OB` `H_CREATE_CLONE` `H_CREATE_SUPER` `H_CREATE_LWOBJECT`（＋`_COPY` / `_RESTORE`）`H_RESET` `H_CLEAN_UP` |
| 指令處理 | `H_COMMAND` `H_MODIFY_COMMAND` `H_MODIFY_COMMAND_FNAME` `H_NOTIFY_FAIL` `H_SEND_NOTIFY_FAIL` `H_DEFAULT_METHOD` |
| 編譯 | `H_INCLUDE_DIRS` `H_AUTO_INCLUDE` `H_AUTO_INCLUDE_BLOCK` `H_AUTO_INCLUDE_EXPRESSION` `H_FILE_ENCODING` `H_REGEXP_PACKAGE` |
| 連線 | `H_TELNET_NEG` `H_NO_ECHO` `H_NO_IPC_SLOT` `H_PRINT_PROMPT` `H_DEFAULT_PROMPT` `H_MSG_DISCARDED` |
| 外部程序 | `H_ERQ_STOP` |

**第一列是重點，而且它很不尋常：**

> `H_MOVE_OBJECT0` / `H_MOVE_OBJECT1` are **mandatory hooks to implement
> the efun `void move_object()`**.

**`move_object()` 是一個 efun，但它的實作在 mudlib 裡。**

在 MudOS／FluffOS 的世界觀裡這不成立——efun 是 driver 的 C++ 函式（§036）。
LDMud 把「把物件從 A 搬到 B」這件事判定為**遊戲政策而不是引擎機制**，
於是 driver 只保留名字，行為交給上層。

而且——**hook 的值可以是 unbound lambda closure**（§066）。
**兩套機制在這裡合流：closure 讓 mudlib 可以把「一段行為」當值傳給 driver。**

這條路線和 DGD 的「把所有遊戲導向功能拿掉」（§064）方向一致，
但手段不同：**DGD 是刪掉，LDMud 是留下名字、外包實作。**

### ② ERQ — 一個 driver 外的協處理程序

LPMud 早期用了兩個外部程式：`hname`（解析 IP 成主機名）與 `indent`（排版 LPC）。
3.2.1@61 把它們統一成 **ERQ（External Request Demon）**。

| 面向 | 說明 |
|------|------|
| 啟動 | driver fork 出 `BINDIR/erq --forked …`，經 AF_UNIX socket 走 stdin/stdout |
| 握手 | erq 啟動成功要**寫回一個字元 `'1'`** |
| 執行期替換 | `attach_erq_demon(某個互動物件)`——**把連線從那個物件手上奪走**，當成新的 erq 通道 |
| 擴充 | 可掛任意功能（`--execdir` 指定可呼叫的執行檔目錄） |

**設計意圖很明確：把「會阻塞的事」趕出主行程。**
LPC 是單執行緒事件迴圈（§037），一次 DNS 查詢就能讓整台 mud 卡住。
ERQ 是 1990 年代版本的「工作行程」。

文件裡還留了一句很誠實的話：

> **Unfortunately it was never documented by Amylaar, so the information
> presented here had to be reverse engineered from the sources —
> better take it with a grain of salt.**

**官方文件承認自己是逆向出來的。** 這正是本書第一篇在講的那件事——
連 driver 的維護者都得對自己的前任做逆向工程，
而且他**標了證據等級**（“take it with a grain of salt”）。

### ③ compat / native — driver 有「模式」這個概念

> During the evolution of LPMud there has been a hiatus as **the old driver
> became too restricting**: it did a lot of things the mudlib could do better
> or completely different. Removing these things from the driver weren't a
> problem, **but to keep compatible with the existing mudlibs (namely the
> well-known 2.4.5 lib), it was possible to undo these changes.**

演化路徑：

```
執行期選項  →  編譯期 compat / native 兩種模式
            →  3.2.1 起：差異逐步移進 mudlib（用 driver hook）
            →  3.2.7：native 模式消失，只剩 plain 與 compat
            →  3.2.9：模式可用命令列選項指定
            →  目標：modeless driver
```

**這是一份「如何拆掉相容性包袱」的實作路線圖**，而且它走了十幾年。

對照 §035 的 FluffOS：它選擇**永久保留**相容性
（`old range behavior`、`old type behavior` 這些旗標，附錄 E.4.5）。
**LDMud 選擇把相容性逐步轉移到 mudlib，然後讓 driver 恢復無模式。**

**兩種都是合理的策略，差別在誰承擔轉換成本**：
FluffOS 讓 driver 永遠背著；LDMud 讓 mudlib 分階段接手。

### ④ Python 擴充 — 執行期註冊 efun

最新的一項，也是最能說明 LDMud 性格的：

```python
# 啟動腳本，在 master 物件載入之前執行
import ldmud
ldmud.register_efun("my_efun", my_python_function)
```

> If the Python function has **type annotations** for its arguments and
> return value, **the type is checked at compile and runtime.**
> Union types can be specified as tuples of types.

**用 Python 寫 efun，而且 Python 的型別註解會被 LPC 編譯器拿去做型別檢查。**

對照 §047：MudOS 的能力面是**編譯期**用 `#define` 決定的，
§048 整節在講那個決定的三十年後果。
**LDMud 的答案是把能力面變成執行期可註冊的。**

**解決的痛點**：這四項合起來解的是同一個問題——
**driver 不該替 mudlib 做決定。**

| 曾經硬編在 driver 裡的東西 | LDMud 的處置 |
|--------------------------|-------------|
| `move_object` 的行為 | → `H_MOVE_OBJECT0/1` 交給 mudlib |
| uid 怎麼決定 | → `H_LOAD_UIDS` 等三個 hook |
| include 路徑、檔案編碼、regexp 引擎 | → hook |
| telnet 協商 | → `H_TELNET_NEG` |
| compat 與 native 的差異 | → 逐步移進 mudlib |
| 有哪些 efun | → **Python 執行期註冊** |

**這條線的終點是一個幾乎沒有政策的 driver**——
和 DGD 的目標一致（§064），只是 DGD 用「重寫並刪掉」達成，
LDMud 用「留下介面並外包」達成。

**踩過的坑（可推論的）**：把 efun 的實作交給 mudlib 有一個必然代價——
**強制性 hook**。文件明說 `H_MOVE_OBJECT0/1` 與三個 uid hook 是 mandatory：
**沒設就不能跑。**
於是 LDMud 的 mudlib 有一個 MudOS 系沒有的開機義務：
**在任何東西能動之前，先把 driver 的空缺填滿。**

這是「權力下放」的隱藏成本——**下放的不只是權力，還有責任**。

**優點 / 罩門**：優點是彈性極高，且**相容性可以被逐步償還**而不是永遠背著。
罩門是**入門門檻**：一個新 mudlib 要先實作一堆 hook 才能開機，
而 MudOS 系拿一份現成 lib 就能跑。
§064 說 DGD 的極小核心讓「拿一份 mudlib 開站」的人門檻更高——
**LDMud 走同一條路，就付同一種代價。**

**效益**：對本書而言，這一節補完了「driver 該保留多少權力」這條光譜：

```
權力全在 driver ←─────────────────────────────→ 權力全在 mudlib
   （沒有這一端）
        MudOS/FluffOS          CD            LDMud        DGD
        能力面極寬，          為單一世界      名字留下，    重寫並刪掉
        政策靠 apply 外包      量身決定       實作外包      遊戲導向功能
```

**四支的位置不同，但方向一致：三十年來，driver 一直在往右移。**

> 💡 君之一席話
> **把權力下放給上層，同時下放的是責任**——一個什麼都不決定的引擎，換來的是每個使用者都必須先做完一堆決定才能開機。

> 🔍 老手視角──真正的門道
> `H_MOVE_OBJECT0` 這種「efun 的實作在上層」的設計，本質上是**依賴反轉**（dependency inversion）用在引擎邊界上：driver 定義介面，mudlib 提供實作，driver 呼叫自己不認識的程式碼。它的好處人人都懂，而它的代價很少被誠實討論——**啟動順序變成一個必須被設計的東西**。當引擎在啟動時要呼叫上層提供的實作，「誰先準備好」就成了一個真實的問題，而這正是 §061 那個 `valid_write` 競態的同一種病（安全檢查依賴一個還沒載入的模組）。老手在做依賴反轉時會同時做兩件事：**明確定義「未就緒」的行為**，以及**在啟動時檢查所有必填的實作是否到齊**——LDMud 把後者做成了「mandatory hook，沒設就不能跑」，這是對的。可落地的做法：任何靠上層注入實作的框架，**在啟動完成前做一次完整性檢查並明確失敗**，比在第一次呼叫時得到 null 好上一個數量級。

---

## 068　DGD 的實作決斷 — 輕量物件其實是一個陣列

**標籤**：`#DGD` `#kfun` `#輕量物件` `#builtin型別` `#快照遷移`
**證據等級**：🟡 上游 `dworkin/dgd`（`doc/builtin.md`、`README.md`、`src/kfun/`）

**起源**：§064 列了 DGD 的五項創新，但那是**功能清單**。
這一節看它的**實作**——因為 DGD 的實作方式本身就在說明它的世界觀。

**技術核心**：

### ① kfun — efun 的另一個名字，與一套極簡的定義方式

DGD 不叫 efun，叫 **kfun**（kernel function）。
定義方式是一組巨集，同一個檔案編譯兩次（`FUNCDEF` 有無定義）：

```c
# ifdef FUNCDEF
FUNCDEF("new_type", kf_new_type, pt_new_type, 0)
# else
char pt_new_type[] = { C_TYPECHECKED | C_STATIC, 0, 0, 0, 6, T_OBJECT };

int kf_new_type(Frame *f, int n, KFun *kf) { … }
# endif
```

注意 `pt_new_type` 那一行：**函式原型被編碼成一個位元組陣列**——
旗標（是否型別檢查、是否 static）、引數個數、各引數型別。

**整個 kfun 表就是 `src/kfun/` 底下幾個檔案**
（`std.cpp`、`math.cpp`、`file.cpp`、`extra.cpp`、`builtin.cpp`、`table.cpp`）。
對照 FluffOS 的 451 篇 efun 文件（附錄 E.2）——**DGD 的核心小了一個數量級**，
這是 §064 那句「把所有遊戲導向功能拿掉」的具體長相。

而且 kfun 可以**模組化外掛**：

```
modules = ([ "kfun_module" : "module config" ]);
```

JIT 本身就是這樣掛上去的（`lpc-ext` 是獨立倉庫）。
**核心極小 + 可外掛 = DGD 的整個策略。**

### ② ★ 輕量物件的真面目：一個帶標頭的陣列

`doc/builtin.md` 揭露了一件很有意思的實作細節：

> Ordinary light-weight objects will have **an object as the first element of
> their array**. For builtin types, the first element must be **an integer,
> the type number**; the second element is reserved and must be initialized
> to `Float` 0.0.

**LWO 的表示就是一個 LPC 陣列**，前幾格是標頭：

```
LWO = ({
   [0]  物件（一般 LWO）或型別編號（builtin 型別）
   [1]  保留：必須是 Float 0.0
   [2]  建議：版本號         ← 給你之後改格式時做轉換用
   [3+] 任意 LPC 資料
})
```

**第三格那個「建議放版本號」特別值得記**：
官方文件直接建議你在自訂型別裡**預留版本欄位**，
「以防你之後改變型別的建構方式、想轉換舊的實例」。

> **這是一個在資料結構層級就內建了遷移意識的設計。**
> 對照 §052 那棵無 schema 的屬性樹——那裡連「有哪些欄位」都沒人知道，
> 更不用說版本。兩種資料觀的距離，大約就是二十年。

### ③ builtin type — 用 LWO 擴充型別系統

因為 LWO 就是帶標頭的陣列，**新增一個「內建型別」就變得很便宜**：
在 `object.h` 保留一個編號、在 `Object::builtinName()` 給它名字，
再寫一個產生它的 kfun。名字前綴由 `BIPREFIX` 決定（預設 `/builtin`）。

編譯期它的節點修飾詞是 `T_CLASS`，類別字串由 `builtinName()` 產生——
**所以型別系統看得見它。**

預設這些型別**沒有可從 LPC 呼叫的函式**、
不能被 `function_object()` 探測、`status()` 也查不到它們的資訊；
要有的話得改 `Frame::call()`、`kf_function_object()`、`kf_status()`。

**這段文件本身就是 DGD 的性格**：它不提供一個「擴充型別」的高階 API，
它告訴你去改哪四個函式。**核心極小的代價是擴充要動核心。**

### ④ 快照遷移 = 資料庫遷移

§064 說 DGD「整個世界是資料庫」。這句話的最強證據在 README 的升級說明：

> DGD 1.7 is backward compatible with snapshots from **DGD 1.5.9 and later,
> only**. To upgrade to DGD 1.7, first upgrade to the latest 1.5.x version,
> **recompile all objects, and create a non-incremental snapshot.**

拆開來看：

| 步驟 | 這在資料庫世界叫什麼 |
|------|-------------------|
| 先升到最新的 1.5.x | **中繼版本升級**（不能跳版） |
| 重編所有物件 | **重建索引／重寫資料** |
| 產生一個非增量快照 | **完整備份**（不能用增量鏈） |
| 然後才升到 1.7 | 目標版本升級 |

**這是一份標準的資料庫遷移程序，出現在一個 MUD driver 的 README 裡。**

對照 MudOS 系：升級 driver 就是換一個執行檔，玩家存檔是一堆 `.o` 文字檔，
沒有版本、沒有遷移、沒有相容性宣告（§052 那個「缺件表現成什麼都沒有」）。

**兩種世界觀的差別在這裡完全暴露：
DGD 把世界當成必須被完整保存的狀態，所以升級是一次遷移；
MudOS 把世界當成可以重來的東西，所以升級只是換執行檔。**

**解決的痛點**：DGD 解的是「持久性」，而它為此付出的是**核心的複雜度**。
快照、增量快照、跨版本相容、原子函式的回捲——
這些都是資料庫等級的問題，而它把它們放進了一個語言執行環境裡。

**踩過的坑（生態層面，可從文獻推得）**：
「1.7 只相容 1.5.9 以後的快照」這句話翻譯過來是——
**如果你的 mud 停在 1.5.9 之前，你的世界搬不過來。**
持久化的代價是**你的資料成為一種必須被維護的資產**；
而 MudOS 系的 mud 因為本來就沒真的保存世界，反而沒有這個問題。

**這是「更好的技術帶來新的義務」的典型例子。**

**優點 / 罩門**：優點是 DGD 的每一個設計都彼此咬合——
LWO 是陣列，所以 builtin 型別便宜；核心小，所以 kfun 可外掛；
世界是資料庫，所以有快照與原子函式。**它是一個有整體構思的系統。**
罩門是**它幾乎不可能被漸進採用**：你不能「稍微用一點 DGD」，
它的世界觀是全有全無的。

**效益**：對本書而言，DGD 提供了一面鏡子。
本書從第一篇到第七篇處理的所有問題——
協議沒有規格、資料沒有 schema、缺件表現成靜默、
版本無法辨識、狀態隨時可能不一致——
**在 DGD 的世界觀裡，其中一半根本不會發生。**

不是因為 DGD 比較高明，是因為**它從一開始就把「狀態是有價值且必須被保存的」
當成前提**，而 MudOS 系（以及長在它上面的整個中文 MUD 生態）沒有。

> 💡 君之一席話
> **當升級一個系統需要寫一份遷移程序，那代表它真的保存了東西**——沒有遷移程序的系統，多半不是相容性做得好，是根本沒有什麼值得搬過去。

> 🔍 老手視角──真正的門道
> `doc/builtin.md` 建議在自訂型別的第三格放版本號，這件事的份量比它的篇幅大得多：**它是在資料結構誕生的那一刻就替它的未來留位置。** 多數系統的版本欄位是事後補的——先有 v1（沒有版本欄位），發現要改的時候才發現「我沒辦法區分新舊」，於是只能靠猜（長度、欄位存在與否、magic number）。這件事的成本極不對稱：**事前留一個欄位幾乎免費，事後補則需要一套啟發式偵測**。本書 §附錄 F 那個映像格式（`format: 1`）之所以第一個欄位就是版本，用的是同一個道理。老手在設計任何會被寫進磁碟、送上網路、或存進資料庫的結構時，第一個欄位永遠是版本——**即使當下只有一個版本，而且你確定不會改**。你不會確定，而那個欄位是你唯一的退路。

---

## 069　CD 的節制 — 以及三份跨越三十四年沒改過的文件

**標籤**：`#CD` `#節制` `#文獻化石` `#單一使用者` `#設計自由度`
**證據等級**：🟡 上游 `cotillion/cd-gamedriver`（原始碼樹、`doc/`）＋ 🟢 與 FluffOS 封存的逐段比對

**起源**：§063 拆了 CD 的四個獨特設計。這一節看它的**另一面**——
**它沒有做的事**，以及為什麼那也是一種設計。

**技術核心**：把 CD 的原始碼樹攤開，會發現它出奇地小而且**平**。

```
array.c  backend.c  bibopmalloc.c  call_out.c  comm1.c  debug.c  ed.c
func_spec.c  genfkntab.c  hash.c  hname.c  interface.c  interpret.c
json.c  lex.c  main.c  make_table.c  mapping.c  memory.c  mstring.c
mudstat.c  ndesc.c  net.c  nqueue.c  object.c  otable.c  parse.c
port.c  random.c  regexp.c  signals.c  simul_efun.c  simulate.c
siphash.c  sprintf.c  stdmalloc.c  super_snoop.c  tcpsvc.c  telnet.c
udpsvc.c  wildmat.c
```

**四十幾個 `.c`，全部在根目錄，沒有子系統分層。**
對照 FluffOS 的 `src/`：`vm/`、`compiler/`、`net/`、`packages/`、
`base/`、`thirdparty/`、`wasm/`、`tools/`……

**這不是「比較舊」，是「比較小」。**

**四件 CD 沒有做、而其他三支都做了的事：**

| 其他人做了 | CD 沒做 | 為什麼可以不做 |
|-----------|--------|--------------|
| 套件系統（MudOS 十幾個 package，§047） | 沒有——功能就是編進去或不編 | **只有一個使用者，不需要讓別人選** |
| 相容模式（LDMud 的 compat/native，§067） | 沒有 | **只有一份 mudlib，不需要相容任何人** |
| 大量可調參數（FluffOS 的 config，附錄 E.4） | 命令列旗標，十個左右 | 同上 |
| 語言擴充（LDMud 的 struct/union/coroutine） | 沒有——LPC 大致停在 3.0 的形狀 | **Genesis 不需要** |

**「只有一個使用者」是 CD 全部設計自由度的來源。**

一個服務單一世界的 driver 可以：
- 直接改行為，不必宣告相容性；
- 不必猜別人要什麼，只要問 Genesis 的管理者；
- 不必做設定系統，因為只有一組設定；
- **可以把 intermud 做進 driver**（§063）——因為只有一個 mudlib 需要它。

**這是一種被低估的架構優勢**：本書 §043 講 ZJMUD 的協議為什麼停在第 0 代時
說過「技術上更好的方案輸給了組織成本更低的方案」；
**CD 是反過來的例子——組織成本趨近於零時，可以做出很乾淨的決定。**

### ★ 三份跨越分家的文件化石

§065 已經記過一份（`CREDITS`）。把三份放在一起，
會得到一張**用文件畫出來的分家圖**：

| 文件 | 在 CD | 在 FluffOS | 狀態 |
|------|-------|-----------|------|
| **`CREDITS`** | `doc/CREDITS` | `docs/archive/Credits.LPmud` | **共同開頭，1991 年後各自往下長**（§065） |
| **`STACKMACHINE`** | `doc/STACKMACHINE` | `docs/driver/stackmachine.md` | **七個章節、內容、順序完全相同**——只有 Markdown 排版不同 |
| **TMI-2 檔頭** | —（CD 不用 TMI mudlib） | 🟢 本專案 15/17 份封存帶著它 | 只存在於 MudOS 那一支（§049） |

**第二列是新發現，而且它比第一列更說明問題。**

`STACKMACHINE` 那份文件——全大寫開頭、兩個堆疊、frame pointer 圖、
`F_FUNCTION` / `F_SSCANF` / `F_CALL_OTHER` / `F_AGGREGATE` / `F_CATCH` / `F_RETURN`
七個章節——**在 1991 年分家的兩側，三十四年後仍然逐段對應**。

FluffOS 那一側做的唯一改動是把 `1. CALLING LOCAL FUNCTIONS.`
改成 Markdown 的 `## CALLING LOCAL FUNCTIONS.`。

> **這代表什麼？** 代表 §037 那台虛擬機——雙堆疊、frame pointer 索引、
> 引數自動補齊、`setjmp`/`longjmp` 的 `catch`——
> **不是 FluffOS 的設計，也不是 MudOS 的設計，是 1989 年 Lars 的設計**，
> 而兩條在 1991 年分開的血脈，各自演化三十四年，**都沒有動它**。

本書 §037 說過「活得最久的設計通常不是最先進的那個，
是最沒有東西可以過時的那個」——
**這份文件就是那句話的證據：它連文字都沒有需要更新的地方。**

**解決的痛點**：CD 不解通用性問題，它解的是**一個世界的持續營運問題**。
1989 年開的 Genesis 至今還在跑，這件事本身就是它的規格書。

**踩過的坑（結構性的）**：CD 的節制有一個必然後果——
**它幾乎沒有外部生態**。撰寫當下 32 顆星、174 個 commit。
本專案 17 份封存沒有一份是 CD 系的（§063），
`mudlibs.fluffos.info` 那 199 份也沒有。

**「只有一個使用者」是設計自由度的來源，也是生態規模的天花板。**
兩者是同一件事的兩面，不能只要一邊。

**優點 / 罩門**：優點是**沒有妥協**——每一個決定都可以為 Genesis 最佳化。
罩門是**沒有備援**：driver 與世界互為存在理由，
任何一邊消失，另一邊就失去意義。

**效益**：對本書而言，CD 的價值有兩層。
技術上它是 §063 那些「相反選擇」的來源；
方法論上它示範了**文獻比對可以定位分家點**——
不需要任何人的回憶，把兩份同源文件並排，
共同前綴的結尾就是分家的那一刻。

> 💡 君之一席話
> **只服務一個使用者的系統可以做出所有其他系統做不到的乾淨決定**——代價是它的價值與那個使用者完全綁定，而這通常不是缺點，是一個被誠實接受的前提。

> 🔍 老手視角──真正的門道
> 「一份三十四年沒改的設計文件」這件事，比它聽起來更值得思考。多數人會把它解釋成「這個設計很好」，但更準確的解釋是**「這個設計沒有需要被重新決定的東西」**——雙堆疊機不依賴任何一代硬體、任何一種作業系統、任何一種語言潮流，所以沒有任何外部變化能逼它改。這給了一個很實用的判準：**看一個設計會不會過時，不要看它有多先進，看它依賴了多少會變的東西。** 依賴硬體特性的會過時，依賴當代最佳實務的會過時，依賴某個生態的會過時；而只依賴「有一塊記憶體可以當堆疊用」的，不會。可落地的做法：做長期設計時，把它的假設列出來，然後對每一條問「這個假設在十年後還成立嗎」——列不出假設的設計，通常假設了很多；假設裡有一半是關於當前技術環境的，那它的壽命就是那個環境的壽命。

---

## 070　四種擴充點 — 一個綜合分析

**標籤**：`#擴充點` `#設計空間` `#權力分配` `#綜合` `#收束`
**證據等級**：🟡 四支上游的交叉比對 ＋ 🟢 本專案對 FluffOS 的實測

**起源**：§066–§069 分別解剖了四支。這一節把它們的**同一個機制**放在一起——
因為四支 driver 都要解決同一個問題：

> **driver 執行到一半，需要上層做一個決定。怎麼問？**

四支給了四種答案，而**每一種都反映了它對「權力該在哪」的判斷**。

**技術核心**：四種擴充點的機制比較。

| | **MudOS / FluffOS**<br>`apply` | **CD**<br>`hook` | **LDMud**<br>`driver hook` | **DGD**<br>`kfun module` |
|---|---|---|---|---|
| **綁定方式** | 字串函式名，driver 直接 `apply()` | 名字集中在 `master.n`，編譯成 `M_*` 常數 | **執行期 `set_driver_hook(編號, 值)`** | 編譯期／設定檔載入模組 |
| **值是什麼** | mudlib 裡的一個函式 | 同左 | **可以是 unbound lambda closure、lfun closure 或字串** | 一個 C++ 實作的 kfun 表 |
| **名字打錯會怎樣** | **靜默略過**（§036） | driver 端不會錯（用常數）；mudlib 端仍是慣例 | **編號錯 = 設定失敗，可偵測** | 連結失敗 |
| **強制性** | 全部可選 | 未定義回傳 0 | **部分 mandatory（沒設不能跑）** | — |
| **可以在執行期改嗎** | ❌（改了要重載物件） | ❌ | **✅ 隨時可換** | ❌ |
| **能取代 efun 嗎** | ❌ | ❌ | **✅（`H_MOVE_OBJECT0`）** | ✅（模組即 kfun） |
| **數量** | **69 個** | 12 個 | **30+ 個** | 不限（模組） |
| **權力位置** | driver 保留機制，政策外包 | driver 保留較多 | **driver 幾乎只保留名字** | **核心極小，其餘全外掛** |

**四種答案排在同一條軸上：**

```
driver 決定一切  ←────────────────────────────────→  mudlib 決定一切

              CD          MudOS/FluffOS       LDMud        DGD
          （為單一世界      （能力面寬，      （名字留下，   （核心極小，
            量身決定）      政策靠 apply）     實作外包）    其餘外掛）
```

**三個從這張表讀得出來的通則：**

### ① 「靜默略過」是 apply 模型的原罪

FluffOS 的 69 個 apply 全部是可選的、靠字串名綁定、找不到就當沒有。
這在 §036 被列為「apply 最需要建立的直覺」，
而它在本書裡造成的實際後果不只一次：
§038 的 preload 靜默失敗、§041 的三個 daemon 不存在、
以及 §046 那張「靜默失敗集中在哪幾站」的表。

**CD 用常數解掉了一半**（driver 端不會拼錯），
**LDMud 用「mandatory + 執行期設定」解掉了另一半**（沒設就開不了機）。

**同一個問題，兩種解法，而 MudOS 系兩種都沒採用**——
因為它的優先序是相容性（§035），而收緊 apply 會弄壞既有 mudlib。

### ② 執行期可換 = 擴充點變成了狀態

LDMud 的 `set_driver_hook()` 是四者中唯一可以在執行期改的。
**這帶來彈性，也帶來一個新的問題類別**：
「這台 mud 現在的 `H_MODIFY_COMMAND` 是什麼」變成一個**執行期狀態**，
而不是原始碼裡的一個事實。

§060 那個「程式與資料可以獨立地過期」的問題，在這裡多了一層：
**連 driver 的行為都可以在執行期被換掉。**

**彈性與可診斷性的交換，在這裡是一比一的。**

### ③ 擴充點的數量反映的是「driver 想保留多少」

```
CD 12 個 hook   →  driver 保留最多決定權
FluffOS 69 個   →  政策幾乎全外包，但機制自己做
LDMud 30+ 且含 efun 實作  →  連機制都可以外包
DGD 模組化 kfun →  核心只留語言與持久化
```

**注意 CD 的 12 個是最少的，但它不是「最不可擴充」——
它是「最不需要擴充」**，因為它只服務一個 mudlib（§069）。

**這一點很容易被誤讀**：擴充點多寡不是能力指標，
是**「這個系統預期要服務多少種不同需求」的指標**。

**解決的痛點**：這一節解的是選型與設計上的一個常見混淆——
**把「可擴充性」當成一個單一維度的好東西。**

實際上它至少有四個維度，而且彼此衝突：

| 維度 | 提高它的代價 |
|------|------------|
| **擴充點數量** | 每一個都是要維護的介面；上層要學的東西變多 |
| **綁定時機**（編譯期→執行期） | 越晚綁越彈性，**也越難診斷** |
| **可取代的深度**（政策→機制→核心） | 越深越強大，**上層的開機義務越重** |
| **失敗的可見性** | 提高它會弄壞既有的、靠「靜默略過」運作的東西 |

**四支 driver 各自在這四個維度上選了不同的點，而沒有一個是全對的。**

**踩過的坑（本專案，回頭看）**：本書 §046 那張「靜默失敗集中在哪幾站」的表
現在可以更精確地解釋了——
**那些站全部落在 apply 模型的責任範圍內**：
mudlib apply 名字打錯、preload 物件載入失敗、WASM 接縫的重入與狀態判斷。

**如果本專案的 driver 是 LDMud，第一類與第二類會在開機時就失敗**
（mandatory hook、可偵測的設定失敗）；
**如果是 DGD，第三類的一部分會被原子函式擋掉**（§064）。

**這不是說本專案選錯了 driver**——選 FluffOS 是因為 17 份封存全是 MudOS 系（§050），
而且只有它有 WebAssembly build（§032）。
**這是說：本專案花在「靜默失敗」上的時間，有一部分是 driver 選型的必然成本，
而不是工程能力問題。**

**知道哪些成本是選型帶來的，才知道哪些值得優化、哪些只能接受。**

**優點 / 罩門**：這種橫向綜合的優點是它把「可擴充性」拆成了可討論的維度。
罩門是它需要四份一手材料，而多數人只會讀一份。

**效益**：本書第八篇到第十一篇的收束——

> 第八篇問：**連線的另一端是什麼？**
> 第九篇問：**引擎／mudlib／客戶端各自把知識放在哪一層？**
> 第十篇問：**這個設計是本質，還是某人在 1992 年的一個決定？**
> 第十一篇問：**四支給了四種答案，那條軸線本身是什麼？**

答案是這一節開頭那句話：**driver 該保留多少決定權。**

而四支三十七年的演化方向是一致的——**往外包移**。
MudOS 外包政策、LDMud 外包實作、DGD 外包一切非核心。
**沒有任何一支往回走。**

這件事對任何在做平台、引擎、框架的人都有意義：
**你今天保留的每一個決定，都是未來某個使用者要繞過的東西。**

> 💡 君之一席話
> **可擴充性不是一個維度，是四個**——擴充點多少、什麼時候綁、能取代到多深、失敗看不看得見；四個一起看，才知道一個系統是靈活還是失控。

> 🔍 老手視角──真正的門道
> 四支 driver 三十七年一致往「權力外包」移動，這個方向性本身值得警惕地看待——**因為它有一個沒有被說出來的終點：一個什麼都不決定的引擎，等於把所有決定推給了一群比你更沒有時間思考的人。** DGD 與 LDMud 的極小核心路線在技術上很漂亮，但它們的生態規模都遠小於 MudOS 系，而原因之一就在這裡：**大部分使用者要的不是「我可以決定一切」，是「不必決定就能開始」。** 老手在設計平台時會刻意同時提供兩層——**一組不需要任何決定就能跑起來的預設，以及一條可以逐項接管的通道**（Rails 的 convention over configuration、Next.js 的預設路由、Docker 的官方 image 都是這個形狀）。可落地的判準：如果你的系統「必須先做完 N 個決定才能看到第一個結果」，那 N 就是你的入門門檻，而它與你的擴充性同樣重要——**擴充性決定天花板，預設值決定有多少人走到天花板底下。**

---



# 第十二篇　英文線：MudOS 的社群史

> 附錄 D.3 記了中文線：東方故事（1993）→ 俠客行（1995）→ 外流（1996）→ 本專案的 17 份封存。
> **但那條線是從 TMI-2 mudlib 分出去的支流。** 主流在英文世界，而書裡一直沒寫。
>
> 這一篇補上 1992–2010 年代的英文 MudOS 社群史，四個單元：
> **一個為了寫 driver 而開的 MUD、三份 mudlib 的競賽、
> MudOS 的三次死而復生、以及 FluffOS 的建國宣言。**
>
> 它回答一個本書一直沒問的問題：
> **本專案手上那台 driver，中間經過了哪些人的手？**

---

## 071　TMI — 一個為了寫 driver 而開的 MUD

**標籤**：`#TMI` `#MudOS` `#社群史` `#intermud起源` `#一手史料`
**證據等級**：🟡 一手史料（`Credits.MudOS`、`History.MudOS`）＋ 🟡 George Reese（Descartes）1995 年整理的 LPMud 編年
　　　　　　　※ 編年的作者本人列名於 `Credits.MudOS`——**他是參與者，不是旁觀者**。這既是它可信的理由，也是它有立場的理由。

**起源**：§034 引用了 1992 年那篇 Usenet 貼文，講 MudOS 為什麼分家。
但貼文裡有一個縮寫沒有解釋，而它是整條英文線的起點：

> Originally, people at **TMI** got together and asked everyone who was
> modifying their driver to get together and discuss bringing all of those
> changes together.

**TMI = The MUD Institute。**

**技術核心**：TMI 是一個很不尋常的東西——**一個為了開發軟體而開的 MUD**。

> **1992 年 2 月**：_TMI, The MUD Institute_ 對公眾開放，
> 作為一個**「用來開發新的 LPMud server 與 mudlib」的 MUD**。
> 除此之外，TMI 還立了一個章程：**教 LPC**。

它同時是三樣東西：一個遊戲、一個開發專案、一所學校。
**而這個組合直接決定了 MudOS 的性格。**

### 時間軸：TMI 的十八個月

| 日期 | 事件 |
|------|------|
| **1992-02** | TMI 開放。目標：新的 server + 新的 mudlib + 教 LPC |
| **1992-02-18** | **LPMud 3.1.2-A 專案改名為 MudOS** ← §034 那場改名的**精確日期** |
| **1992-04-23** | **LPC socket 加入 MudOS driver**。TMI 據此做出一個很粗糙的 TCP intermud 網路 |
| 1992-06 | Blackthorn 把 Genocide 搬到新的 MudOS driver。「當時 driver 充滿新功能，也**同樣充滿 bug**」——Genocide 整個夏天成了 MudOS 的測試場 |
| 1992-07 | Descartes 接手 Nightmare 的 mudlib，**丟掉它那份過時的 LPMud 2.4.5 mudlib 與 driver**，改用 MudOS |
| **1992-08** | **TMI 關站。** |
| 1992-08 | **TMI-2 開放**——目標更窄，**放棄了教學的章程** |

**三件事值得停下來看：**

### ① 改名的日期，補上了 §034 的空缺

§034 引的那篇貼文寫於 1992-08-28，講的是**改名之後**的爭議。
現在知道改名發生在 **1992-02-18**——**罵戰是在半年後才爆發的**。

這個時間差本身就說明了問題：改名當下沒有人在意，
是等到「用我們 driver 的人一直在問為什麼 compat mode 不能用」（§034）
累積了半年，事情才炸開。**相容性的爭議從來不是在改動當下發生的。**

### ② ★ 1992-04-23：本專案那 15 份封存裡的 intermud，源頭在這裡

> LPC sockets are added to the MudOS driver. **This allows TMI to create a
> very rough TCP intermud network.** This protocol is later replaced first by
> the **CDlib UDP protocols**, and later by **Intermud 3**.

把這一行和 §049 對起來看：

```
1992-04-23  MudOS 加入 socket efun
     ↓      （§047 說的「引擎加的不是功能，是可能性的邊界」）
1992       TMI 用 LPC 寫出第一版 TCP intermud
     ↓
1993-08-15 Grendel@Tmi-2 寫下 tcp.c 的檔頭
     ↓      「This file is part of the tmi mudlib. Please keep this header intact.」
1993 年底   東方故事採用 MudOS + TMI mudlib（附錄 D.3）
     ↓
2026       🟢 本專案 17 份封存中的 15 份，仍帶著那批檔案
```

**§049 那組 `_q`／`_a` 成對的服務、`dns_master` 名錄、
以及 `nitan7` 封存裡那份 118 筆的中文 MUD 名錄快照（附錄 D.4）——
它們的祖先是 1992 年 4 月 TMI 那個「很粗糙的 TCP intermud」。**

而那一行還記了它後來被取代兩次：先是 **CDlib 的 UDP 協議**
（就是 §063 那支 CD driver 的 mudlib——**它把 intermud 做進了 driver**），
再來是 **Intermud 3**。

**中文線接手的是 TMI 那一版**，而且**再也沒有升級**——
所以三十三年後，本專案在封存裡挖到的是 1993 年的實作。

### ③ 「為了寫軟體而開的 MUD」是那個年代的協作方式

沒有 GitHub、沒有 CI、沒有 issue tracker。
`Credits.MudOS` 的致謝名單裡列的不是貢獻者，是**測試場**：

> We would like to thank the following muds for extensive testing and
> numerous bug reports:
> **TMI, Portals, Overdrive, Genocide, TMI-2, Vincent's Hollow,
> DreamShadow, Nightmare, Nanvaent**, and many others.

**九個 MUD 就是九個生產環境的測試叢集。**
「driver 有 bug」的回報路徑是：玩家在 Genocide 掉線 →
管理者 Blackthorn 回報 → Truilkan／Jacques／Wayfarer 在 Portals 上改 code。

而那個「開發站」是誰提供的，寫在同一份檔案的最後一行：

> Thanks to **Adam Beeman (Buddha)** for making numerous suggestions on
> improving the MudOS driver and **for providing the TMI site as the original
> MudOS development site**.

**解決的痛點**：TMI 解的是 1992 年一個非常實際的問題——
**LPMud 的 driver 由一個人維護，而那個人很忙**（§034）。
社群的答案不是 fork 一份 code，是**開一個地方讓大家在裡面一起改**。

**踩過的坑**：TMI 開了六個月就關站。
§034 那篇貼文的自述解釋了為什麼：

> About 10 people showed up and we all talked for a while. For a month or two,
> we had a bulletin board where all of us posted ideas / suggestions / etc.
> **However, nobody really had the time to get together and work on the code.**

**開會的人很多，寫 code 的只有兩個。**
TMI-2 接手時明確**放棄了教學章程**——把目標收窄到只做 mudlib。

**優點 / 罩門**：優點是它真的產出了東西——MudOS、TMI-2 mudlib、
以及第一版 intermud，三樣都活了三十年以上。
罩門是**它把「社群治理」與「軟體開發」綁在一起**，
而這兩件事的節奏完全不同：治理需要共識，開發需要有人動手。
TMI 死於前者，MudOS 活下來是因為後者被少數幾個人扛走了。

**效益**：對本專案而言，這一節解釋了一件事——
**為什麼本專案手上的 mudlib 帶著一批 1993 年、來自一個已經關站的 MUD 的檔案。**
不是因為誰偷懶沒更新，是因為**那批檔案在它被複製出去的那一刻，就已經是孤兒**：
TMI 1992 年就關了，TMI-2 的 mudlib 1993 年發布，
而中文線 1993 年底接手——**接手時上游已經不在了。**

> 💡 君之一席話
> **一個為了開發軟體而開的社群，最後留下來的往往不是社群，是軟體**——TMI 存在了六個月，而它寫的那批檔案還在三十三年後的封存裡。

> 🔍 老手視角──真正的門道
> 「用九個生產環境當測試叢集」這件事，在今天看起來很原始，但它有一個現代 CI 給不了的性質：**那些 bug 是真的玩家在真的世界裡踩出來的**。Genocide 整個夏天當測試場，回報的不是「單元測試失敗」，是「三十個人同時上線的時候崩了」。老手都知道這類 bug 最難用測試覆蓋——它們是負載、時序、狀態累積的交互作用。今天的對應物是金絲雀部署、影子流量、feature flag 逐步放量，而它們解決的是同一個問題：**在真實環境裡取得早期訊號，但不要一次賠上全部使用者**。1992 年的做法是靠「有幾個管理者願意當白老鼠」，2026 年的做法是靠流量切分——**機制變了，原理沒變**。可落地的判準：如果你的系統只在 CI 裡被驗證過，那你其實只驗證了你想得到的那些情況；**永遠要有一個「真實但可犧牲」的環境**。

---

## 072　三份 mudlib 的競賽 — 而競賽的主軸是指令解析器

**標籤**：`#mudlib` `#Nightmare` `#Discworld` `#TMI-2` `#LIMA` `#指令解析`
**證據等級**：🟡 George Reese 的 LPMud 編年 ＋ 🟢 本專案封存的實測對照

**起源**：§050 用檔名集合量出本專案的 17 份封存來自兩個家族，
而兩族的共同祖先是 **TMI-2**。
但 TMI-2 在英文世界只是**三份之一**——而且不是最強的那一份。

**技術核心**：MudOS 的 mudlib 生態，1992–1995 的四年。

| 日期 | mudlib | 定位 |
|------|--------|------|
| **1992-12** | **Nightmare Mudlib**（Descartes） | **第一份公開可用的 MudOS mudlib**。當時 MudOS「在眾多 driver 中還被視為新來的」 |
| **1993-03** | **Discworld Mudlib** | 「**當時最先進的指令解析器與使用者介面**」——mudlib 的選擇幫助推高了 MudOS 的人氣 |
| **1993-04** | **TMI-2 Mudlib** | 「讓 MudOS 擁有三份廣泛可得的 mudlib」 ← **🟢 本專案 15/17 份封存的祖先** |
| 1995-05-15 | **Foundation II** | **第一份為非遊戲用途設計的 LPC 函式庫**；**第一份可商業授權的 MudOS mudlib** |
| 1995-06-15 | **Nightmare IV** | 完全重寫，**清掉一路追溯到 LPMud 2.4.5 的遺留程式碼** |
| 1995-07-21 | **LIMA**（pre-alpha） | 「**LPMud 上見過最先進的指令解析**」，**基於老 Infocom 遊戲（Zork）的解析方式** |

另外還有兩本教科書，說明這個生態已經大到需要教材：

| 日期 | 書 |
|------|---|
| 1993-04 | **LPC Basics** —— 第一本涵蓋 LPC 所有實作的完整教科書 |
| 1993-11 | **Intermediate LPC** |

**★ 這張表最重要的訊息在「先進」這個詞指的是什麼。**

Discworld（1993）與 LIMA（1995）被稱讚的都是**指令解析器**。
這不是巧合——**在文字遊戲裡，指令解析器就是使用者介面。**

```
玩家打：  get the red sword from the wooden chest
          └───────────── 這一行要被拆成 動詞／限定詞／物件／介系詞／容器
```

MudOS driver 有 `parse_command()` efun（§047 那張表裡的「自然語言式指令」），
但**它只是機制**——把它變成「玩家可以自然地說話」需要 mudlib 做非常多工作。

**LIMA 直接對標 Infocom 的 Zork**，那是文字冒險解析的天花板。

> 🔬 **本書的角度**：這正好是 §054 那條線的另一端。
> §054 說 MUD 客戶端三十年的四件套（trigger／alias／macro／script）
> 是因為「伺服器只吐文字」；
> **這裡看到的是同一個限制在伺服器那一側的形狀——
> 既然只有文字，那就把文字的「輸入」與「輸出」兩端都做到極致。**
>
> 英文 mudlib 把力氣花在**輸入端**（指令解析器）；
> 而 §043 那套 ZJMUD 方言把力氣花在**輸出端**（把狀態明講）。
> **兩條路線都是對「純文字」這個前提的回應，只是選了不同的那一端。**

**Discworld 這條線特別值得標記**，因為它會在 §074 再出現一次：
1993 年做出最先進解析器的那份 mudlib，
它的核心開發者 **David Bennett（Pinkfish）** 同時在改 MudOS driver 本身——
`Credits.MudOS` 列他的貢獻是：

> provided enhancements for **parse_command** and for the **telnet negotiation
> code in comm.c**, and the **resolve()** efun.

**mudlib 的需求直接推動了 driver 的功能。**
Discworld 要更好的解析器，於是 `parse_command` 被加強；
要更好的終端支援，於是 telnet 協商被改寫——
**而本書 §055 量到的那些協商位元組，走的就是那段被 Pinkfish 改過的程式碼的後裔。**

**解決的痛點**：三份 mudlib 並存解的是 §034 沒解的那個問題——
**「拿一份現成的東西開站」**。
MudOS 在 1992 年被視為「新來的」，
而它在 1993 年變得流行，靠的不是 driver 本身變好，
是**同時有三份可用的 mudlib**。

**踩過的坑（結構性的）**：三份並存的代價是**分裂**。
Nightmare、Discworld、TMI-2 的 API 彼此不相容，
一份為 Nightmare 寫的區域搬不到 Discworld。
§050 量到的那個現象（兩個家族、Jaccard 0.012）在英文世界是三倍嚴重。

而且——**中文線在 1993 年接走了 TMI-2 那一份之後，
就與後面的所有發展斷了聯繫**：
Foundation II（1995）、Nightmare IV（1995）、LIMA（1995）
在本專案的 17 份封存裡**一個痕跡都沒有**。

**優點 / 罩門**：優點是競爭真的推動了技術——
1993 到 1995 年，MudOS mudlib 的指令解析從「動詞＋名詞」
走到了 Infocom 等級。
罩門是**每一份 mudlib 都是一個孤島**，而孤島越多，
單一孤島的維護人力越少。

**效益**：對本專案而言，這一節校正了一個容易產生的誤解。
§049–§053 把 TMI-2 的架構講得很細（因為那是本專案手上的東西），
但**TMI-2 從來不是英文世界的主流選擇**——
它是三份之一，而且不是解析器最強的那一份。

**本專案的 17 份封存繼承的，是 1993 年那個生態的一個切片，
而不是那個生態的最終狀態。**
這件事在做任何「中文 MUD 的技術水準如何」的判斷時都必須先說清楚。

> 💡 君之一席話
> **一個平台流行起來，通常不是因為它自己變好了，是因為它上面終於有了三個可以直接拿來用的東西。**

> 🔍 老手視角──真正的門道
> 「mudlib 的需求推動 driver 的功能」這個方向值得注意，因為它和多數人的直覺相反：**不是引擎先提供能力、上層才長出應用，而是上層先痛、再回頭改引擎。** Pinkfish 為了 Discworld 的解析器去改 MudOS 的 `parse_command`，為了終端體驗去改 `comm.c` 的 telnet 協商——他是使用者，也是貢獻者。這種「使用者即貢獻者」的迴路是開源生態最健康的形態，而它有一個前提：**引擎與應用的作者要在同一個溝通半徑內**。TMI 那種「開一個 MUD 讓大家在裡面一起改」的做法，本質上就是在人為製造這個半徑。老手評估一個開源專案的健康度時，看的不是 star 數，而是**「最近半年的 commit 裡，有多少來自真的在用它的人」**——全部來自核心維護者的專案，通常已經在解決想像中的問題了。可落地的做法：如果你在做平台，**想辦法讓自己成為它的一個真實使用者**；做不到的話，至少要有一條讓真實使用者的痛點在兩週內傳到你手上的通道。

---

## 073　MudOS 的三次死而復生

**標籤**：`#MudOS` `#BeekOS` `#LPC到C` `#維護者交接` `#compat_buster`
**證據等級**：🟡 George Reese 編年 ＋ 🟡 一手史料（`Credits.MudOS`、`README.MudOS`、`ChangeLog.mudos` 2,217 行）

**起源**：本書從 §034 之後就一直把 MudOS 當成一條連續的線在講。
**它不是。** 它至少停過三次，而且有兩次差點就沒有下文了。

**技術核心**：

### 第一次：1993 年底 —— 「MudOS 正在死去」

> **1993 年 11 月**：由於 LPMud 的速度、DGD 的優雅，
> 以及**MudOS 這邊單純缺乏進展**，MudOS 正在死去。
> **Beek 於是做了 BeekOS。**

BeekOS 是什麼：

> BeekOS 基本上是一個 MudOS 核心，加上**LPC→C 的動態編譯**，
> 把編譯出來的機器碼**動態連結進正在執行的 server**。

**這是 §037 那個「純解譯堆疊機的效能天花板」的第一次突圍嘗試。**
本書 §037 說過：「所有效能都靠 efun（把熱點推回 C++），
這正是為什麼一個 MUD driver 會有 600 多個內建函式」。
**Beek 的答案是：那就把 LPC 本身編成 C。**

值得注意的是**時序**：DGD 在 **1993-09-16** 的 1.0.a4 版
成為第一個支援 LPC→C 編譯的 driver；
**兩個月後 Beek 就在 MudOS 上做出了對應的東西。**
編年裡明說 BeekOS 的動機之一就是「DGD 的優雅」——
**競爭產生的技術壓力，在這裡有明確的日期可以對上。**

這些改動後來在 Beek 接手 MudOS 之後被併回主線。

### 第二次：1994 年初 —— 唯一的維護者也停手了

> **1994 年初**：Genocide 為了速度轉回 LPMud。
> 結果 **Blackthorn 停止了那涓滴般的 bug 修復，
> 而那正是當時 MudOS 開發的全部。**

**「that trickle of bug-fixes which had been the whole of MudOS development」**——
一句話說完那個時期的狀態：整個 MudOS 的開發，
就是一個人在他自己的 MUD 需要時順手修幾個 bug。
他的 MUD 一換 driver，**開發就歸零**。

### 第三次：1994 年中 —— 三個人接手

> **1994-06-26**：**Beek、Robocoder、Symmetry 接手 MudOS 開發。**
> 這標誌著在**將近九個月的無開發狀態**之後，
> MudOS 重新引起興趣。

`Credits.MudOS` 對這三個人的記載，正好對應他們接手後做的事：

| 人 | 貢獻（`Credits.MudOS` 原文） |
|----|---------------------------|
| **Tim Hollebeek (Beek)** | **LPC→C and patches, compiler rewrite, new function pointers, etc** |
| **Anthon Pang (Robocoder)** | various patches, **optimize function table search**, amiga support |
| **Ti-Ming Chiang (Symmetry, Cloud)** | **Countless optimizations; array, mapping and save_object rewrites** |

**三個人分別接手了編譯器、函式表、與資料結構**——
這是一次很典型的「重寫核心」分工。

**Beek 那一行的 “compiler rewrite” 與 “new function pointers”
就是今天 FluffOS 裡那個編譯器與 `(: :)` 語法的來源。**

### 版本編號與「compat buster」

`README.MudOS` 記下了 MudOS 的發行慣例，而其中一條很值得學：

> In particular, note the changes marked **"compat buster"** since these
> describe changes to the driver which **may cause pre-existing mudlibs
> (such as Lil) to break** in some minor fashion (the ChangeLog should give
> enough information to allow affected mudlibs to be fixed).

**他們在 ChangeLog 裡替破壞性變更加了一個標籤。**

對照 §059 講 FluffOS 拆 `static` 時說的「**可偵測的破壞性變更**」——
MudOS 在 1990 年代用的是更原始但同樣有效的手段：
**在人類可讀的變更紀錄裡，把會弄壞你的那幾行標出來。**

其他發行慣例：

| 慣例 | 內容 |
|------|------|
| 版號 | `v20` / `v21` / `v22`（單一數字＝含文件的完整包）；`v20.26`（小版號＝只有原始碼） |
| alpha/beta | **只以 patch 形式提供**，且有獨立的 `ChangeLog.alpha` / `ChangeLog.beta` |
| COMPAT | **`README.MudOS` 開宗明義：「It does not support COMPAT mode」**——§034 那個不相容宣告，寫在使用者第一眼會看到的地方 |
| 支援站 | `http://www.mudos.org/`，含 bug 資料庫；當機時先讀 `IT_CRASHED` 收集資訊 |

### 移植狂潮：MudOS 被搬上了所有東西

`Credits.MudOS` 裡有一整批貢獻者的功勞是**移植**，
把它們排出來就是一份 1990 年代的作業系統清單：

| 平台 | 誰 |
|------|-----|
| AIX 3.2 | Dwayne Fontenot (Jacques) |
| Sequent Symmetry（System V R3） | Dave Richards (Cynosure/Cygnus) |
| System V R4 | Michael Bundy |
| 386BSD | Luke Mewborn (Zak) |
| **Linux 0.99.3** | Olav Kolbu (Aragorn) |
| DEC Alpha | Bob Farmer (Blackthorn) |
| Amiga | John Fehr (Wildcard)、Anthon Pang、Maarten de Jong |
| Win32（原始版） | **George Reese (Descartes)** |

**`Linux 0.99.3`** 那一行是個時間戳：那是 1993 年的核心版本。
**MudOS 在 Linux 1.0 出來之前就跑在上面了。**

而 §035 那張「FluffOS 支援 Linux／macOS／Windows／Alpine／WebAssembly」的表，
其實是這個傳統的延續——**這條血脈從一開始就有很強的移植文化**。

**解決的痛點**：這三次死而復生解的其實是同一個問題——
**開源專案的維護者風險（bus factor）**。

三次的模式一模一樣：

```
維護只靠一個人（或一個 MUD 的需求）
   → 那個人 / 那個 MUD 的處境改變
     → 開發歸零
       → 有人受不了，自己做一個分支（BeekOS）
         → 分支被併回，做分支的人成為新維護者
```

**而 §074 會看到，這個循環在十年後又跑了一次。**

**踩過的坑**：`ChangeLog.mudos` 有 2,217 行，
而它的開頭第一句話很坦白：

> This file is the ***brief*** version of what has been changed;
> see `ChangeLog.alpha`, `ChangeLog.beta`, and `ChangeLog.old/`
> for all the gory details.

**這是「簡短版」。** 一個沒有 issue tracker、沒有 PR、
沒有自動化測試的年代，變更紀錄就是專案的全部記憶——
所以它必須非常詳細，而詳細到 2,217 行還只是簡短版。

**優點 / 罩門**：優點是這條線**每次都有人接住**。
罩門是每次接住的人都不一樣，而**每一次交接都伴隨一次核心重寫**
（BeekOS 的 LPC→C、Beek 的 compiler rewrite、Symmetry 的 array/mapping/save_object 重寫）。
§035 說 FluffOS 承諾與 MudOS 相容——
**而 MudOS 自己在 1994 年就把編譯器、陣列、mapping、存檔全部重寫過一輪。**

**效益**：對本書而言，這一節補上了 §035 那張血脈表看不到的東西：
**一條線「還活著」不代表它一直有人在推。**
MudOS 從 1992 到 1997 這五年裡，至少有九個月是完全停擺的，
而它能撐過去，靠的是三個人在 1994 年 6 月 26 日那天決定接手。

> 💡 君之一席話
> **一個開源專案的健康度，不看它有多少功能，看它換過幾次維護者而還活著**——換得過去的，才證明它不是某一個人的私有物。

> 🔍 老手視角──真正的門道
> BeekOS 這個模式很值得單獨記：**當上游停滯而你有真實需求時，正確的做法往往不是等，也不是抱怨，是做一個分支證明它可行**——然後那個分支通常會被併回，而你會成為維護者。這在開源史上反覆出現（io.js 之於 Node、Blink 之於 WebKit、本書 §074 的 FluffOS 之於 MudOS），而它成功的關鍵條件只有一個：**分支必須解決一個真實且可展示的問題**。BeekOS 解的是「太慢」，而它拿得出 LPC→C 的實作；純粹因為「上游不理我」而開的分支，幾乎都會死。老手看到自己在等上游時，會問三個問題：**這個需求真實嗎？我做得出來嗎？做出來之後上游有理由不要嗎？**三個都是 yes，就該動手了——而且要從一開始就以「將來要被併回」的方式寫。可落地的做法：分支時**保持與上游的可合併性**（不要順手重排目錄、不要換 code style），你的分支能不能回家，八成取決於這一點。

---

## 074　FluffOS 的建國宣言 — 以及 Discworld 這條暗線

**標籤**：`#FluffOS` `#分支` `#Discworld` `#Pinkfish` `#模式重演`
**證據等級**：🟡 一手史料（`ChangeLog.fluffos` 全文與 `Credits.MudOS`）

**起源**：本專案用的 driver 叫 FluffOS。
它是怎麼從 MudOS 變成 FluffOS 的？答案在 `ChangeLog.fluffos` 的**第一行**——
而那一行是一句宣言：

> **As MudOS is moving too slow to keep our driver hacks apart,
> we now call our own FluffOS :)**

**技術核心**：拆開這一句話，四個資訊全在裡面。

### ① 「MudOS is moving too slow」

**和 1993 年 Beek 做 BeekOS 的理由一模一樣**（§073：
「simple lack of progress on the part of MudOS」）。

**同一個專案、同一個原因、隔了大約十年，發生了第二次。**

### ② 「our driver hacks」——他們早就在改了

分支不是決定，是**追認**。
他們手上已經有一堆改動，而維護「我們的改動」與「上游」之間的差異
（keep them apart）成本越來越高，於是乾脆給它一個名字。

**這與 §034 MudOS 自己的分家理由結構相同**：
1992 年 TMI 那批人的版號變成 `3.0.53.A2.2`，
維護「掛在別人版號後面的自己的線」也是一樣的成本問題。

**兩次分家，同一個機制：差異的維護成本超過了分家的社交成本。**

### ③ 「we now call our own」——先改名，再談其他

§034 的老手視角說過：MudOS 的溝通順序是**先改版號（解決自己的痛）、
再改名（解決使用者的誤解）、最後才解釋動機**。

FluffOS 這一句把前兩步壓成了一步，而且**連解釋都省了**——
只有一個笑臉。

### ④ `:)` ——這是一份 ChangeLog，不是一份公告

沒有 RFC、沒有郵件列表投票、沒有基金會。
**一個人在變更紀錄的最上面加了一行。**

### FluffOS 1.0 的內容，以及它是誰做的

ChangeLog 的最末端是它的起點：

```
FluffOS 1.0:
changes since MudOS v22.2b11 (as far as i can remember)
Type warnings added
compressed save files
eval limit is now based on time taken, rather than substracting random
	numbers to account for actions.

Many bug fixes :)

Wodan.
```

四個訊息：

| 觀察 | 意義 |
|------|------|
| **「changes since MudOS v22.2b11」** | 分支點是 MudOS 的一個 **beta** 版——不是穩定版 |
| **「as far as i can remember」** | **他自己也不確定改了什麼**——這正是「差異維護成本過高」的症狀 |
| **eval limit 改成基於時間** | §037 那個 eval cost 沙盒的重做（後來 FluffOS 1.31 又改用 signals） |
| **署名 `Wodan.`** | 一個人 |

### ★ 暗線：Discworld

`ChangeLog.fluffos` 裡有一行看起來只是普通的 bug 修復：

> **fixes to compile with other settings than the DW ones**（FluffOS 1.28）

**`DW` = Discworld。**
「讓它能用 Discworld 以外的設定編譯」——
**意思是在那之前，FluffOS 的預設設定就是 Discworld 的設定。**

再往下看貢獻者名字：

```
FluffOS 1.4:  added compressedp efun, it shows if an interactive uses MCCP (pinkfish)
FluffOS 1.5:  fixed crasher after errors in save_object. (reported by pinkfish)
FluffOS 1.28: terminal_colour fix (pinkfish)
```

**Pinkfish。**

翻回 §072 引的 `Credits.MudOS`：

> **David Bennett (Pinkfish)** - provided enhancements for **parse_command**
> and for the **telnet negotiation code in comm.c**, and the **resolve()** efun.

**同一個人，出現在 1990 年代的 MudOS 貢獻者名單，
也出現在 2000 年代的 FluffOS 變更紀錄裡。**

而 §049 還記得第三次出現：本專案的封存裡，
`adm/daemons/network/` 底下有 **11 個檔案署名 `Creator : Pinkfish@Discworld`**。

**於是這條暗線完整了：**

```
Discworld MUD（1992 開站）
   │
   ├─ 1993-03  發布 Discworld Mudlib：「當時最先進的指令解析器與 UI」（§072）
   │
   ├─ 1990s    Pinkfish 改 MudOS 的 parse_command、telnet 協商、resolve()
   │              ↳ 🟢 §055 那 6 組實測協商位元組，走的是這段程式碼的後裔
   │
   ├─ 1993     Pinkfish 的 11 個檔案進了 TMI-2 的 network/ 目錄
   │              ↳ 🟢 本專案 15/17 份中文封存至今帶著它們（§049）
   │
   └─ 2000s    FluffOS 從 Discworld 的 driver hacks 誕生
                  ↳ 🟢 本專案今天用的就是它
```

**本專案手上這台 driver，與封存裡那 11 個檔案，來自同一個 MUD。**
而中文線在 1993 年接走 TMI-2 的時候，
**根本不知道它同時也接走了 Discworld 的一部分。**

### 之後：從一個人的 hack 到現代 C++ 專案

`ChangeLog.fluffos` 記錄的 1.0–1.36 是「一個人修 crasher」的階段。
今天的 FluffOS（§035）是 C++、UTF-8/ICU、64 位元、TLS/WebSocket、
GitHub Actions CI、跨五個平台含 WebAssembly——
**中間又換過一輪維護者**，而這正是 §073 那個循環的第四次。

**解決的痛點**：這一節解的是本書一個很基本、卻一直沒回答的問題——
**「我們用的這台 driver，是誰做的？」**

答案是：**沒有「誰」。**
1989 年 Lars 寫了核心；1992 年 TMI 那批人把它變成 MudOS；
1994 年 Beek 三人重寫了編譯器與資料結構；
2000 年代 Wodan 從 Discworld 的 hacks 裡分出 FluffOS；
之後又有人把它現代化到今天。

**每一段都是「上一個人停下來了，下一個人接手」。**

**踩過的坑（模式層面）**：這條線三十七年裡至少分家或停擺五次
（1991 CD、1992 MudOS、1993 BeekOS、1994 停擺九個月、2000s FluffOS），
**而每一次的觸發條件都相同：上游太慢，而下游有真實需求。**

§073 的老手視角給了那三個問題（需求真實嗎／做得出來嗎／上游有理由不要嗎），
**FluffOS 三個都是 yes，所以它活了下來。**

**優點 / 罩門**：優點是這個模式讓一個沒有公司、沒有基金會、
沒有資金的專案活了三十七年。
罩門是**每一次交接都伴隨知識流失**——
「as far as i can remember」這句話就是證據，
而 §067 提到 LDMud 的官方文件承認 ERQ 是「逆向出來的」，
是同一個病在另一條線上的症狀。

**效益**：對本書的收束意義——

本書從第一頁講的就是**逆向工程**：
面對一個沒有規格書的黑箱，怎麼把猜測逼成證據。

而這一節說明了一件事：**那個黑箱不是別人惡意造成的。**
它是三十七年、五次交接、無數次「as far as i can remember」累積出來的。
本專案花在還原 ZJMUD 協議上的力氣，
與 LDMud 維護者逆向 Amylaar 的 ERQ、
與 FluffOS 維護者搞清楚 MudOS v22.2b11 到底改了什麼，
**是同一件事。**

> 💡 君之一席話
> **「as far as i can remember」是每一個長壽專案最誠實的一行文件**——它承認了知識已經流失，而承認流失，是重新把它找回來的第一步。

> 🔍 老手視角──真正的門道
> 這一篇四個單元排在一起，會看到一個很清楚的循環：**建立社群 → 社群解散但軟體活著 → 維護者疲乏 → 有人分支 → 分支成為主線 → 回到第一步。** 三十七年跑了至少五輪。老手在評估一個要依賴很久的開源專案時，看的不是它現在有多活躍，而是**它有沒有跑完過這個循環至少一次**——因為第一次交接是最難的，跨過去的專案證明了它的價值不綁在任何一個人身上。反過來說，一個從未換過維護者的專案，無論多活躍，它的 bus factor 都是 1，而你看不出來。可落地的判準：查一個專案**最早的三個 commit 作者是否還在提交**——都在的，要問「如果他們走了會怎樣」；都不在而專案還在長，那它已經證明過自己了。**這是唯一一個不用讀 code 就能做的架構風險評估。**

---



# 第十三篇　周邊：工具鏈、標準庫與參考實作

> 第八到十二篇把 driver、mudlib、客戶端、語言、社群史都拆過一輪，
> 但漏了**driver 旁邊那一圈東西**：
> 六支命令列工具、一整套 LPC 標準庫、一個官方的 Web 客戶端與追蹤系統，
> 以及——**一個和本專案在做同一件事的官方參考站**。
>
> 最後一項是這一篇的重點。`mudlibs.fluffos.info` 收了 199 份中文 mudlib，
> 用同一台 driver、同一種打包方式。
> **它是本專案最直接的對照組，而兩邊在方法論上獨立收斂到了同一個做法。**

---

## 075　driver 之外的六支程式 — 被忽略的工具鏈

**標籤**：`#CLI` `#lpcc` `#portbind` `#o2json` `#工具鏈` `#靜態檢查`
**證據等級**：🟡 上游 `fluffos/docs/cli/`

**起源**：本書從第八篇起一直在講 `driver` 這支程式。
但 FluffOS 的建置產物不只一支——**還有五支**，
而其中至少兩支能直接改善本專案的工作流程。

**技術核心**：

### `driver` — 主程式，以及它被忽略的旗標

```bash
./driver [flags] config_file
```

| 旗標 | 作用 |
|------|------|
| `-f<flag>` | 初始化後、**開網路之前**，呼叫 master 的 `flag(string)`（§042 的 `fluffos_flag` 就是它的 WASM 版） |
| `-d<category>` | 開分類除錯日誌，可重複；等同 `set_debug_level()` |
| `--tracing <file>` | 開 LPC 追蹤（§077） |
| **`--generate-config`** | **印出一份含全部選項、預設值與註解的起始設定檔** |

**最後一行值得單獨標記。**
§039 記過本專案在設定檔上栽的三個跟頭——
其中兩個（設定檔名不一、行尾註解不存在）
**如果一開始就用 `--generate-config` 產一份範本來比對，都不會發生。**

14 個除錯分類，粒度比想像中細：

```
call_out  d_flag  connections  mapping  sockets  comp_func_tab
LPC  LPC_line  event  dns  file  add_action  telnet  websocket
```

`telnet` 與 `connections` 那兩個，正是 §055 那 6 組協商位元組的來源；
**本專案當時是自己在客戶端印位元組，其實 driver 這一側就有開關。**

### ★ `lpcc` — 把編譯結果傾印出來

```bash
./lpcc config_file lpc_file
```

它會載入 config、載入 simul_efun 與 master，然後把目標檔的編譯結果印出來：

```
NAME: /single/master.c
INHERITS:
      name                    fio    vio
      ----------------        ---    ---
        inherit/master/valid.c      0      0
FUNCTIONS:
      name                  offset  mods   flags   fio  # locals  # args # def args
      --------------------- ------  ----  -------  ---  --------  ------ ----------
   0: get_inherit_called         0   +--  i-s----    0
   1: valid_shadow               0   +--  i-s----    1
   …
```

**每個函式的修飾詞、旗標、區域變數數、引數數、預設引數數**——
§059 講的那些修飾詞（`private` / `static` / `nomask` / `varargs`）
在這裡是可以被機器讀出來的。

它同時會印出所有編譯警告：

```
/std/base64.c line 56: Warning: Unused local variable 'p'
```

**這件事對本專案的意義很直接。**
§048 的結論是「能力探測對老 mudlib 只能靠實跑」，
因為靜態分析抓不到語意變化與封存缺件。
**那個結論仍然成立，但 `lpcc` 補上了靜態那一半**——
它能在不啟動世界的情況下，把**每一個檔案**編譯一次並回報錯誤。

而 §078 會看到：**上游的參考實作正是這樣做的**
（`lpcc_check.sh` 批次編譯每一個檔）。
本專案的 `boot-test.mjs` 只會發現「開機路徑上」的問題；
一個玩家永遠不會走到的房間裡的語法錯誤，它抓不到。

### `o2json` / `json2o` — 存檔與 JSON 雙向轉換

```bash
./o2json [-prtty] [-ascii] o_file json_file    # 支援 - 代表 stdin/stdout
```

輸出格式：

```json
{ "program_name": "...", "variables": [ { "name": "...", ... } ] }
```

**LPC 的 `.o` 存檔可以被 `jq` 處理，改完再轉回去。**

§附錄 F.5 記過本專案從 `nt7` 移除了 **630 個含密碼欄的玩家存檔**——
當時是整檔刪除。**有了 `o2json`，那件事可以做得更精細**：
把敏感欄位挑掉、其餘保留。
（本專案的判斷仍然是刪除比較安全，但「只能整檔刪」不再是理由。）

### `portbind` — 特權埠與權限分離

```bash
./portbind -p 23 -u mud -g mud -d ./driver config
```

四個步驟：

```
① 以 root 綁定特權埠（<1024）
② 把綁好的 socket 經 檔案描述子 6 交給 driver
③ 卸除權限到指定的 uid/gid
④ exec driver
```

**這是 Unix 「特權分離」的教科書實作**：
需要 root 的只有 `bind()` 那一瞬間，之後整個 driver 以無特權身分執行。

對照 §061 講的 uid/euid——**那是 mudlib 內部的權限模型**；
`portbind` 是**作業系統層級**的。兩者互不相干，而且都需要。

### `symbol` / `generate_keywords` — 建置期工具

`generate_keywords` 產生 LPC 關鍵字表（§040 提過 WASM 交叉編譯要先建原生工具，
就是為了跑這類東西）；`symbol` 處理符號。

**解決的痛點**：這六支工具解的是同一類問題——
**在不啟動整個世界的情況下，對 mudlib 做點什麼。**

`driver` 是唯一需要完整世界的；其餘五支都是離線工具。

**踩過的坑（本專案的，回頭看）**：本專案的整個匯入管線
（§附錄 F.5：轉碼、改 `set_encoding`、移除存檔、掃憑證）
**都是用 Node.js 自己寫的**，因為當時不知道有這些工具。

結果是：
- 設定檔範本靠反推（`--generate-config` 可以直接產）；
- 協商位元組靠客戶端印（`-dtelnet` 可以開）；
- 編譯錯誤只在開機路徑上被發現（`lpcc` 可以全掃）；
- 存檔只能整檔刪（`o2json` 可以逐欄處理）。

**這不是錯誤，是資訊不足。** 而它示範了一件很常見的事：
**當一個工具的文件在官網的一個不起眼分頁裡，
它等於不存在。**

**優點 / 罩門**：優點是這套工具讓 mudlib 的維護可以離線、批次、自動化。
罩門是**它們幾乎沒有能見度**——`docs/cli/` 底下七篇文件，
其中 `lpcc.md` 有 50 KB（幾乎全是輸出範例），
但沒有任何一份 getting-started 會提到它們。

**效益**：對任何要維護一份 mudlib 的人，這是一組立即可用的清單：

```bash
./driver --generate-config > config.ref   # 拿一份權威範本來 diff
./lpcc config <每一個檔>                   # 全庫靜態編譯檢查
./driver -dtelnet -dconnections config    # 協議層真相
./o2json - - < player.o | jq …            # 存檔資料處理
```

> 💡 君之一席話
> **一個沒有能見度的工具等於不存在**——本專案花了三週自己寫的東西，有一半在上游的 `docs/cli/` 底下躺著。

> 🔍 老手視角──真正的門道
> 這一節真正的教訓不是「要讀完文件」（沒有人讀得完），而是**接手任何專案時，先把它的建置產物列一遍**。`make install` 到底裝了哪些執行檔？`bin/` 底下有什麼？這個動作三十秒，而它會告訴你這個專案的作者認為哪些操作值得做成工具——**那是一份沒有寫成文件的需求清單**。本專案如果第一天就跑一次 `ls build/bin/`，會看到 `lpcc`、`o2json`、`portbind` 這些名字，然後至少會去查它們是什麼。可落地的做法：**評估任何新依賴時，除了讀 README，再做兩件事——列出它的建置產物、列出它的設定項**（`--generate-config` 這類旗標特別值錢）。這兩張清單合起來，比 README 更誠實地描述了這個專案能做什麼。

---

## 076　LPC 標準庫與 TUI — 伺服器這一側的反擊

**標籤**：`#stdlib` `#simul_efun` `#TUI` `#readline` `#終端能力`
**證據等級**：🟡 上游 `fluffos/docs/stdlib/`（含 `tui.md`）

**起源**：§036 講過 simul_efun 是「mudlib 自己的假 efun」。
但 FluffOS 還提供了**一整套現成的 simul_efun**——
`docs/stdlib/` 底下有一個分類齊全的標準庫，而本書一直沒提。

其中最值得看的是 **TUI**，因為它直接回應了本書的兩個主題。

**技術核心**：

### 標準庫的分類

| 分類 | 內容 |
|------|------|
| `arrays` | `reduce` |
| `mappings` | `element_of_weighted`（加權隨機取樣） |
| `numbers` | `sum`、`array_sum`、`percent`、`percent_of` |
| `objects` | `all_environment`、`present_clone` |
| `string` | `break_string`（斷行排版）、`base64encode/decode`、**`bitmap_font`** |
| `db` | `database` |
| `ffi_util` | FFI 輔助 |
| **`tui`** | **整套終端 UI 工具庫** |

`break_string` 那一項很能說明 LPC 生態的性質——
**「把一段文字排成固定寬度的段落」竟然需要一個標準庫函式**，
因為在 CJK 環境裡它牽涉全形寬度計算，而且每個 mudlib 都要用。

### ★ TUI — 用純 LPC 寫的 readline + ncurses

> The testsuite mudlib ships a terminal-UI toolkit **written in pure LPC**,
> layered like **`readline` + `ncurses` + `pterm`**: line editing, history and
> incremental search for the prompt line, inline select/multiselect/confirm
> prompts and pretty-printers for ordinary output, plus a **screen buffer,
> diff renderer and widget set** for full-screen applications.

三層架構：

| 層 | 對應的傳統工具 | 提供什麼 |
|----|--------------|---------|
| 提示行 | **readline** | 行編輯、歷史、增量搜尋、補全 |
| 內嵌提示 | **pterm** | select / multiselect / confirm / 美化輸出 |
| 全螢幕 | **ncurses** | 螢幕緩衝、**差分渲染**、widget |

**★ 它取代的東西是 `input_to()`：**

```c
void get_command() {
    tui_readline((: got_command :), ([ "prompt": "> " ]));
}

void got_command(mixed line, int state) {
    if (state != TUI_RL_DONE) return;   // TUI_RL_ABORT (^C) / TUI_RL_EOF (^D)
    do_command(line);
    get_command();
}
```

**這一段值得和 §044 對照著讀。**

§044 逐行解剖過本專案 mudlib 的登入流程，
那是一條 `logon` → `jiance` → `get_user` 的 `input_to` 接力——
**三個函式，只因為中間要等玩家打字**。

而 §066 說 LDMud 的答案是**協程**（`async`/`await`/`yield`）。

**TUI 是第三種答案**：不改語言，用回呼 + 狀態碼，
但把「等一行輸入」升級成「等一次完整的行編輯會話」，
中途的 Ctrl-C / Ctrl-D 都有明確狀態（`TUI_RL_ABORT` / `TUI_RL_EOF`）。

同一個問題（**單執行緒事件迴圈上如何表達多步流程**）的三種解：

| 解法 | 誰 | 代價 |
|------|-----|------|
| `input_to` 接力 | 傳統 LPC（§044） | 流程被拆成 N 個函式，狀態靠物件變數傳遞 |
| **協程** | LDMud（§066） | 要改語言 |
| **回呼 + 狀態碼** | FluffOS TUI | 仍是回呼，但**語意完整**（成功/中止/EOF 分得清楚） |

### 它用到的終端能力，正是 §055 那些選項

TUI 提供的功能清單，每一項都對應一個 telnet 選項或終端特性：

| TUI 功能 | 靠什麼 |
|---------|--------|
| 密碼遮蔽（`"masked": 1`） | **ECHO**（選項 1） |
| 逐鍵輸入（方向鍵、Ctrl-R） | **SGA**（選項 3）＝字元模式 |
| **即時 resize** | **NAWS**（選項 31）→ `window_size` apply |
| 終端能力判斷 | **TTYPE**（選項 24）→ `terminal_type` apply |
| bracketed paste、alternate screen | 終端逃逸序列 |
| **CJK 寬字元感知渲染 ＋ 水平捲動** | ICU 的字素/寬度計算（§041） |

**§055 那張表裡「FluffOS 立場」欄位為什麼要談那些選項，到這裡才完整**：
driver 不是為了好看才協商 ECHO/SGA/NAWS/TTYPE，
**是因為 mudlib 這一層真的有東西要用它們。**

**解決的痛點**：TUI 解的是一個和本書主線互為鏡像的問題。

§054 的結論是：**因為伺服器只吐文字，客戶端三十年來只能靠 trigger 猜狀態。**
而 §056 的四代頻外協議，是**伺服器主動多說一點**的路線。

**TUI 是第三條路：不多說，而是把終端本身的能力用到極致。**

```
路線 A（客戶端猜）      trigger / alias        —— §054
路線 B（伺服器多說）    ESC opcode / GMCP      —— §043 §056
路線 C（用滿終端能力）  TUI：readline + ncurses —— 本節
```

**三條路線的共同前提都是「只有一條文字通道」，但施力點完全不同。**
路線 C 的優點是**不需要任何自訂協議**——
它用的全是 telnet 與 ANSI 的標準能力，
所以**任何現代終端機都能用，不需要專用客戶端**。

而本專案走的是路線 B，代價已經在 §057 結算過。

**踩過的坑（推論，標為未驗證）**：TUI 對本專案的 WASM 展示站有一個潛在障礙——
它需要**字元模式**（SGA）與逐鍵串流，
而本專案的前端是一個以「行」為單位的 GUI（§024 的 18 個元件）。
上游的參考前端用 xterm.js 處理這一層（§077）。
**兩種前端假設不同的輸入模型，這是路線 B 與路線 C 難以並存的原因。**
本專案沒有實測過，此處標為 🔴 推論。

**優點 / 罩門**：優點是 TUI **完全用 LPC 寫成**，可以複製到任何 FluffOS mudlib
（只要帶上 `include/tui.h`）——**不需要改 driver**。
這是 §036 那三個呼叫方向的漂亮示範：
一整套終端 UI 框架，住在 simul_efun 與繼承層裡。
罩門是它假設對面是一個**真的終端機**，
而本書整個第四到六篇做的是一個**不是終端機的客戶端**。

**效益**：對本書而言，TUI 補上了「三條路線」裡缺的那一條，
讓 §057 那個「知識該放在哪一層」的分析完整了：

| | 知識在哪 | 需要專用客戶端嗎 |
|---|---------|----------------|
| A trigger | 使用者腳本 | ❌ |
| B 方言解析 | 客戶端程式碼 | **✅** |
| B' 標準頻外 | 協議規格 | ⚠️（要支援 GMCP） |
| **C TUI** | **伺服器端 mudlib** | **❌ 任何終端機** |

**C 是唯一「知識全在伺服器、且不需要專用客戶端」的組合。**

> 💡 君之一席話
> **在只有一條文字通道的世界裡，你可以讓客戶端猜、讓伺服器多說、或者把那條通道本身用到極致**——三條路，而第三條不需要任何人配合。

> 🔍 老手視角──真正的門道
> TUI 這種「用既有標準把能力做滿」的路線，在工程上常常被低估，因為它不產生新東西——沒有新協議、沒有新規格、沒有可以拿出來講的創新。但它有一個其他路線都沒有的性質：**零協調成本**。ANSI 逃逸序列與 telnet 選項已經被所有終端實作了四十年，用它們不需要說服任何人。這個模式在別處也很常見：用 HTTP header 而不是自訂欄位、用 SQL 而不是自訂查詢語言、用檔案系統而不是自訂儲存層。老手在面對「我需要一個新能力」時，第一個問題永遠是**「既有標準裡有沒有一個被低估的角落可以用？」**——因為既有標準的相容性是白送的，而新協議的相容性要靠三年時間去換。可落地的判準：在設計自訂協議之前，**先把目標平台的標準能力清單讀一遍**；本書 §055 那張 telnet 選項表列了 18 個選項，而多數 MUD 只用到其中三、四個。

---

## 077　可觀測性與現代網路層 — driver 自己就是一台伺服器

**標籤**：`#tracing` `#效能剖析` `#WebSocket` `#TLS` `#資料庫` `#可觀測性`
**證據等級**：🟡 上游 `docs/concepts/general/`（`tracing`、`websocket`、`tls`）＋ `docs/stdlib/db/database.md`

**起源**：§041 那張套件矩陣把 `db`、`compress`、`crypto` 列為「WASM 上關掉」，
但沒說它們在原生版上是什麼。而 `tracing` 這一項本書從未提過。

**這一節補的是：FluffOS 作為一台現代伺服器，它自己帶了什麼。**

**技術核心**：

### ① tracing — 產出 Chrome Trace Event Format

```c
trace_start("/log/performance.json");   // 預設 10 秒後寫出
…
trace_end();
```

記錄的事件：

> LPC function calls and efun calls、object loads and file compilation、
> `catch` evaluation、file I/O、and apply-cache lookups

每個事件帶著**持續時間、巢狀關係（呼叫者／被呼叫者）、以及物件與函式名**，
輸出成 **Chrome Trace Event Format (JSON)**——
**可以直接丟進瀏覽器開發者工具的效能面板看火焰圖。**

而它的設計取捨值得單獨記：

> The instrumentation is **always compiled in** and only records while a trace
> is active, so **no special build is required** — `trace_start()` and
> `trace_end()` are ordinary efuns available in normal builds.

**永遠編進去、只在啟動追蹤時記錄。**
這是「零成本抽象」的實際取捨：付出的是每個呼叫點一個分支判斷，
換到的是**正式環境隨時可以開始剖析，不必重編、不必重啟**。

**對照本專案**：§046 那條 11 站的鏈路，
本專案能量的只有第 ⑥ 站之後（JS 那一側）；
**第 ①–⑤ 站（LPC → VM → comm → telnet → Transport）一直是黑箱**。
`trace_start()` 加上 `-dLPC` / `-dtelnet`（§075），
**那半條鏈路是可以被量出來的**——本專案沒有用，因為不知道。

`apply-cache lookups` 那一項尤其有意思：§036 說 apply 靠字串名綁定，
而 driver 顯然為此做了快取——**而快取命中率是可以被追蹤的**。

### ② WebSocket ＋ 官方 Web 客戶端

`config.ini` 的 `external_port_N : websocket <port>` 加上 `websocket http dir`
（§039 本專案的設定裡就有 `external_port_3 : websocket 5004` 與 `www`），
**driver 本身就能發靜態網頁並接受 WebSocket 連線。**

而 `src/www/` 底下就是官方的 Web 客戶端，
終端模擬用 **xterm.js**（vendored 在 `src/www/vendor/`），
與 WASM 版的前端**共用同一份 vendor 目錄**（§040）。

**這解釋了本書第四篇那個「雙形態」架構的一個前提**：
§013 說瀏覽器版需要一個橋接程序，因為瀏覽器開不了 raw TCP。
**但如果 driver 開的是 WebSocket 埠，橋接就不需要了。**

本專案沒有走這條路，原因很實際：
**上游 mudlib 的 `config.ini` 開的是 telnet 埠，而本專案的原則是不改 mudlib。**
§045 那句「mudlib 一個位元組都不動」是整條路線的前提，
而它的代價之一就是多了一個橋接程序。

> **這是一個值得記錄的替代方案**：如果允許改一行設定
> （加一個 `external_port_N : websocket …`），
> 瀏覽器版的 `bridge/server.mjs`（165 行）就可以整個刪掉。

### ③ TLS

`external_port_N_tls : cert=cert.crt key=cert.key`——
**每個埠獨立設定憑證**。
§040 提過 WASM 版把 TLS 整個移除（頁面／瀏覽器自己負責），
而原生版有完整支援，包含 `sys_reload_tls` efun 可以熱換憑證。

### ④ 資料庫層

`db` 套件提供 SQLite3 / MySQL / PostgreSQL；
`async` 套件把查詢丟進執行緒池。

**這對 LPC 是一個結構性的補丁**：§037 說過 LPC 是單執行緒、
無執行緒的事件迴圈，所以**任何阻塞操作都會凍住整個世界**。
`async_db_exec` 這類 efun 的存在，
與 §067 那個 LDMud 的 ERQ（把會阻塞的事趕出主行程）解的是同一個問題——
**只是 LDMud 用外部程序，FluffOS 用執行緒池。**

而 §052 那棵無 schema 的屬性樹，
在有了真資料庫之後其實有了替代方案——
**但沒有任何一份老 mudlib 會改**，理由在 §056 說過（時間軸）。

**解決的痛點**：這一節解的是一個認知問題——
**FluffOS 不是一個「MUD driver」，它是一台通用的應用伺服器**，
只是它的腳本語言剛好叫 LPC。

它有：HTTP 靜態服務、WebSocket、TLS、三種資料庫、非同步 I/O、
執行緒池、效能剖析、結構化除錯日誌、跨五平台建置與 CI。

**對照 §064 講 DGD 時提過的那件事**——
1990 年代末 Yahoo 的聊天室跑在 DGD 上。
**這條血脈從來就不只是遊戲引擎**，只是它的使用者大多在做遊戲。

**踩過的坑（本專案，誠實揭露）**：本專案在 §031 寫下過一個結論——
「只要協議是原生 TCP、而執行環境是瀏覽器，中間就一定要有一個能開 socket 的程序」。
§032 記錄了那個結論如何被 WASM 推翻（前提被拿掉）。

**現在要記第二次**：那個結論還有另一個出口，而且更早就存在——
**driver 本來就能開 WebSocket 埠。**

§032 的老手視角說「任何架構結論都加一句『這個結論在什麼情況下會失效』」。
當時寫的失效條件是「協議不再是遠端 TCP」；
**漏掉的是「協議可以不是 TCP」——同一台 driver 就支援 WebSocket。**

**一個結論被同一份文件推翻兩次，這件事本身值得寫下來。**

**優點 / 罩門**：優點是這些能力全部是**可選的**——
不用就不會付出成本（tracing 只有一個分支、db 是編譯期套件）。
罩門是**能見度**，和 §075 是同一個問題：
`docs/concepts/general/tracing.md` 有 11 KB，
而本專案做了四個月才知道它存在。

**效益**：這一節給的是一份「下次可以用」的清單：

| 想知道什麼 | 用什麼 |
|-----------|--------|
| LPC 哪個函式慢 | `trace_start()` → Chrome DevTools |
| 協商到底送了什麼 | `driver -dtelnet -dconnections` |
| 某個檔編不編得過 | `lpcc`（§075） |
| 不想寫橋接 | `external_port_N : websocket` |
| 存檔資料處理 | `o2json` / `json2o`（§075） |

> 💡 君之一席話
> **一個結論被推翻兩次，第二次通常比第一次更值得記**——第一次是前提變了，第二次是你一開始就沒把選項看全。

> 🔍 老手視角──真正的門道
> 「instrumentation 永遠編進去、只在需要時記錄」這個取捨，是可觀測性設計裡最重要的一個決定。另一種做法是「特殊建置才有」（`-DPROFILE`），它的效能是零成本，代價是**你永遠無法剖析正式環境**——而效能問題幾乎只在正式環境才會出現（負載、資料量、真實使用模式）。老手幾乎一律選前者：留下永遠存在但預設關閉的探針，付出百分之一到二的常態成本，換到「隨時可以在出事的那台機器上開始量」。這個原則在 eBPF、在 Java 的 JFR、在 Chrome 的 tracing、在 OpenTelemetry 的 sampling 都是一樣的。可落地的判準：**如果你的剖析工具需要重啟服務才能啟用，那它在真正需要的時候就用不上**——因為重啟本身會清掉現場。

---

## 078　`mudlibs.fluffos.info` — 官方參考實作，與本專案的逐項對照

**標籤**：`#參考實作` `#對照` `#方法論收斂` `#規模` `#誠實揭露`
**證據等級**：🟢 上游 `fluffos/mudlibs` 的 README 與管線 ＋ 🟢 本專案自己的實測數據（附錄 F）

**起源**：本書 §045 提過一句：
「`mudlibs.fluffos.info` 是 FluffOS 社群做的中文 MUD 博物館，
本專案的 `libs/` 佈局就是參考它的」。

**那一句遠遠不夠。** 那個專案和本專案在做同一件事——
**同一台 driver、同一種 WASM 打包、同一批中文 mudlib**——
而它的規模是本專案的十倍以上。

**這一節把兩邊逐項攤開比較**，因為對照組是本書從第一篇就在強調的東西。

**技術核心**：

### 規模

| | 上游 `fluffos/mudlibs` | 本專案 `libs/` |
|---|---------------------|---------------|
| 收錄 | **199 份，158 個獨立 codebase** | **17 份** |
| 已驗證可玩 | **193 份（97%）** | 見附錄 F |
| 站上發佈 | **182 份**（可玩 180 ／ 受限 1 ／ 不可啟動 1） | 同 |
| 單一 lib 規模 | 典型 5,000–14,000 個 LPC 檔；最大約 **57,000** 檔 | 最大 29,853 檔（附錄 F.5） |
| 年代範圍 | 1990 年代中至約 2015 | 同 |
| 題材 | 武俠、仙俠為主 | 同 |
| 站點 | `mudlibs.fluffos.info`（GitHub Pages） | 本倉庫的 `site/` |

> ⚠️ **同時存在三個數字，而且都對——差別在分母。**
>
> | 數字 | 意義 |
> |------|------|
> | **199** | 倉庫 README：**已修復收錄**的總數 |
> | **193** | 倉庫 README：**已通過 WASM 端到端驗證**的數量 |
> | **182** | 站台首頁：**已發佈上站**的數量 |
>
> 引用時必須指明是哪一個。本書一律分開標示。
> 上述數字為 **2026-08-04 重新查核**（站台最新 commit 為 2026-08-03）；
> 本節初稿時的對應數字是「200 份收錄／97 個可玩」，變化見下。

### ★ 血緣判定：兩邊獨立收斂到同一個方法

**上游的做法**（README 原文）：

> Lineage determined by **diffing core files (`master.c`, `chinese.c`,
> `logind.c`)** rather than relying on similar Chinese titles alone.

**本專案的做法**（§050）：用 `mudlib.json` 的**檔名集合算 Jaccard 係數**。

兩邊解的是同一個問題（**中文標題相似不代表同源**），
而且都拒絕了「靠名字判斷」這條捷徑。

| | 上游 | 本專案 |
|---|------|--------|
| 特徵 | **三個核心檔的內容** | **全部檔名的集合** |
| 精度 | 高（看內容） | 中（只看路徑，§050 已標明此偏差） |
| 成本 | 要解開每一份封存 | **只讀一張 JSON 索引** |
| 產出 | 編號體系（001–158，衍生版用 `-1`/`-2` 後綴） | 相似度矩陣（0.012 – 0.90） |

**兩種方法互補而不衝突**：上游的精度高，本專案的成本低。
§050 那條「先挑最便宜的特徵試一次，看分佈有沒有明顯的斷層」的建議，
在這裡得到了對照——**斷層確實存在（0.90 vs 0.012），所以便宜的特徵夠用。**

### 分級：上游四級，本專案三級

| 上游 | 意義 | 現況 |
|------|------|------|
| `playable` | **完整註冊 + 端到端遊玩，且在 WASM 上驗證過** | 193 |
| `limited` | 開得起來，但有記錄在該 lib 自己 README 裡的但書 | 1 |
| **`partial (native only)`** | **政策上刻意只支援原生**（例如 Dead Souls 這類英文 lib） | 1 |
| `noboot` | 真的是 LPC mudlib，但因**與轉換無關的結構性原因**開不起來 | 4 |

本專案只有 `playable` / `limited` / `noboot` 三級（附錄 F）。

> ⚠️ **這裡有一項本節初稿之後的變動。**
> 初稿記錄上游是**五級**，其中一級是 **`native-boot verified`**（原生跑得起來、WASM 待驗）。
> 2026-08-04 重新查核時，該級**已經不存在**。
>
> 這不是分類法退步，而是**它的存在理由消失了**：
> `native-boot verified` 是一個**過渡狀態**——用來標記「已知能跑，只是還沒輪到驗」。
> 當 97% 的收錄都已通過 WASM 驗證，需要這個標籤的樣本自然歸零。
>
> **一個分級消失，往往比一個分級新增更能說明專案的進度。**

**上游剩下的四級仍然比本專案誠實**：它保留了 `partial (native only)` 這個
**「不是做不到，是刻意不做」**的類別。本專案沒有這個維度，
一份被政策排除的 lib 在本專案體系裡只能被壓成 `noboot`，
看起來像失敗，實際上是決定——**資訊有損失**。

### ★ 兩次查核之間：收集停了，修復幾乎做完了

本節初稿與 **2026-08-04** 的重新查核之間，數字的**變化形狀**比數字本身更有意思：

| 指標 | 初稿時 | 2026-08-04 | 變化 |
|------|------:|----------:|------|
| 收錄總數 | 200 | **199** | **幾乎不動** |
| 獨立 codebase | 158 | **158** | **完全不動** |
| 已驗證可玩 | **97** | **193** | **幾乎翻倍**（49% → **97%**） |

**收集階段結束了。**
總數與 codebase 數凍結，而可玩率從一半推到 97%。
這段期間的工作**全部是修復與驗證，不是再去找新的 lib**。

這個形狀本身就是一個結論：

> **中文 mudlib 的存世母體，大致就是這 158 個 codebase。**
> 能找的已經找完了；剩下的難度不在「找」，
> 而在「讓三十年前的程式碼在一台 2026 年的 driver 上跑起來」。

而這正好解釋了為什麼上游會累積出那份約 **60 類 driver 相容性 bug** 的知識庫
（見下方「其他技術細節對照」）——**那是修復階段的沉澱物，不是收集階段的副產品。**

**與本專案的數據對照，可以估出這件事的難度曲線：**

| | 樣本數 | 踩到的 bug 類別 |
|---|------:|--------------:|
| 本專案 | 17 | **8** |
| 上游 | 199 | **約 60** |
| 比值 | **×11.7** | **×7.5** |

**問題類別的增長明顯慢於樣本數。**
相容性問題有很強的長尾，但**頭部高度集中**——
這也是可玩率能被推到 97% 的原因：把頭部那幾類修掉，大部分 lib 就活了。

> 🔍 老手視角──真正的門道
> 一個長期專案的**數字變化形狀**，比任何一次快照都更能說明它處在哪個階段。
> 這裡看到的是很典型的三段式：**收集 → 修復 → 維護**。
> 當「總量停止增長、完成率逼近 100%」同時出現，代表它已經走完第二段。
> 對要不要依賴一個外部專案而言，這個判斷比「它有多少 star」有用得多——
> **一個還在第一段的收藏站，你引用的數字下個月就會過時；
> 一個走完第二段的，它的數字可以放心寫進文件。**
> 可落地的做法：引用外部專案的數據時，**記下查核日期並隔一段時間重查一次**；
> 兩次之間的差，會告訴你它的真實狀態。本節就是這樣做的——
> 而且第一次查核時的「97 個可玩」，如果沒有重查，現在就是一個錯得離譜的數字。

### 管線逐項對照

| 階段 | 上游 | 本專案 |
|------|------|--------|
| 解壓 | `extract.sh`（zip/rar/7z/tar/**SFX 執行檔**） | 手動 + `import-lib.mjs` |
| 轉碼 | `convert_lib.sh`：**GB18030 → UTF-8**，`.c` → **`.lpc`**，修正交叉引用 | `import-lib.mjs`：GBK → UTF-8（附錄 F.5，最多單一 lib 29,853 檔） |
| **靜態檢查** | **`lpcc_check.sh`——用 `lpcc` 批次編譯每一個檔** | ❌ **沒有**（只有開機路徑上的錯誤會被發現） |
| 原生煙霧測試 | `mudclient.py`（Python telnet 客戶端） | ❌（本專案不跑原生） |
| WASM 測試 | `wasm_client.js` + `wasm_boot_check.js` | **`boot-test.mjs`**（註冊→建角→進世界） |
| 打包 | `pack_lib_for_web.sh` | `pack-lib.mjs`（自訂映像格式 `format: 1`） |
| 建站 | `build_site.sh` + `gen_site_index.py` + `wasm_status.json` | `build-site.mjs` |
| 部署 | `.github/workflows/pages.yml` | 本倉庫根目錄靜態發佈 |
| 全鏈路 | — | **`verify-fullstack.mjs`（真 DOM + 真 driver + 真 HTTP，§046）** |

**兩個缺口與一個獨有：**

- ❌ **本專案缺 `lpcc` 靜態檢查**（§075 已說明後果：玩家走不到的地方，錯誤不會被發現）；
- ❌ **本專案缺原生驗證**（所以無法區分「WASM 問題」與「mudlib 問題」）；
- ✅ **本專案獨有全鏈路 DOM 驗證**——上游測的是 driver 能不能跑完登入，
  **本專案測的是「真瀏覽器 DOM ＋ 真 driver ＋ 真 HTTP」整條走完**，
  因為本專案的交付物是**客戶端**，上游的交付物是**mudlib 收藏**。

### 版控策略：唯一的重大分歧

| | 上游 | 本專案 |
|---|------|--------|
| 進版控的是 | **`work/` 檔案樹**（可在 GitHub 上直接讀 LPC） | **單一映像 `mudlib.data` + 索引** |
| 好處 | 可讀、可 diff、可 PR | 寫入快、`git status` 快 |
| 代價 | 十幾萬個小檔 | **GitHub 上看不到原始碼**，diff 只顯示「二進位檔已變更」 |

本專案的理由是量出來的（`libs/README.md`）：
跨 WSL 的 9P 檔案系統**寫入小檔約 28 檔／分**，
一個 mudlib 約一萬個檔 = **單一 lib 約 5 小時**，
16 個就是一整週。

> **這是一個環境限制導致的架構決定，而不是普遍更好的做法。**
> 上游沒有這個限制（Linux 原生檔案系統），所以選了可讀性。
> **本專案的補償是提供一行解開指令**（`unpack-lib.mjs`）。

### 其他技術細節對照

| 項目 | 上游 | 本專案 |
|------|------|--------|
| 副檔名 | **統一改成 `.lpc`** | 保留原樣（`.c` 或 `.lpc`） |
| 設定檔 | 統一 `config.fluffos` | **保留原名**（`config.ini` / `config.hell` / `config.xajh` / `config.sj`…，§039 那個坑的來源） |
| 管理帳號 | **統一 `fluffos` / `Mud@2026`**，且 `127.0.0.1` **繞過 IP ban 與連線節流** | 無（每個 lib 各自的登入流程） |
| 修復紀錄 | 每 lib 一份 `NOTES.md` | 同（**由 `boot-test.mjs` 自動產生**） |
| bug 類別知識庫 | **`AGENTS.md`：約 60 類 driver 相容性 bug**（症狀、根因、修法） | §032 的 **8 條通用修正** |

**最後一列值得停下來：本專案那 8 條，是上游那 60 類的一個子集。**

§032 說那八條「每一條修正都對應一次實測失敗，而不是預防性猜測」——
這仍然是對的，但現在知道了**完整的問題空間有多大**：
本專案 17 份封存踩到 8 類，上游 199 份踩到約 60 類。

**這是一個很好的比例參考**：樣本數 ×12，問題類別 ×7.5。
**問題類別的增長明顯慢於樣本數**——
說明相容性問題有很強的長尾，但頭部集中。

### 客戶端：兩邊最大的差異

| | 上游 | 本專案 |
|---|------|--------|
| 前端 | **xterm.js 通用終端** | **懂 ZJMUD 方言的 GUI**（§024 的 18 個元件） |
| 對不講方言的 lib | 沒問題（終端機不需要理解內容） | 需要**會話層代打**（§045 `telnetlogin.js`） |
| 對講方言的 lib | 顯示原始 `ESC003…`（當成亂碼） | **完整圖形介面** |

**這一格把兩個專案的目標差異講完了。**

上游是**保存**：目標是讓每一份 mudlib 能被跑起來、被看見。
用通用終端是正確選擇——**它對所有 lib 一視同仁**。

本專案是**客戶端實驗**：目標是驗證一套逆向出來的協議規格。
用專用 GUI 是必然選擇——**否則沒有東西可以驗證**。

**所以 §045 那筆「額外費用的帳單」，
本質上是「選擇了非通用客戶端」的代價**，而不是誰做得不夠好。

**解決的痛點**：這一節解的是本書一個潛在的誤導。

前面十二篇累積了大量關於「中文 mudlib 長什麼樣」的分析（§049–§053），
而它們**全部基於 17 份樣本**。
現在讀者知道：**上游有 199 份（158 個獨立 codebase），而且它的血緣判定方法比本專案精確。**

**本書的結論仍然成立**（斷層明顯、方法可重跑、每一條都標了偏差），
**但它的樣本代表性有一個明確的上界，而那個上界現在有數字了。**

**優點 / 罩門**：本專案相對上游的優點只有一個，而且很窄——
**它驗證的是客戶端與協議，不是 mudlib 本身。**
罩門三個：樣本少 12 倍、缺靜態檢查、缺原生對照。

**效益**：對任何要做類似事情的人，這張對照表就是一份路線圖：
**要保存 mudlib，去看上游；要做客戶端，本書第四到七篇還有用。**
兩者不是競爭關係。

> 💡 君之一席話
> **找到那個在做同一件事、而且規模比你大十倍的專案，然後逐項比對**——這是校準自己結論可信度最快的方法，也是最不舒服的方法。

> 🔍 老手視角──真正的門道
> 兩個專案在血緣判定上獨立收斂到「不要相信名字、去比對內容特徵」，這件事的意義比它看起來大：**當兩批人在不知道對方的情況下解同一個問題、走到同一個方法上，那個方法通常抓到了問題的本質。** 這是一種很便宜的驗證——比自己反覆論證有效得多。老手在做技術決策時會刻意去找「別人怎麼解同一個問題」，不是為了抄，是為了**看自己的方案是不是收斂點之一**：如果所有獨立實作都走同一條路而你走了另一條，你要嘛有一個他們沒有的約束（本專案的 9P 寫入速度就是），要嘛你錯了——**而這兩者必須分清楚**。可落地的做法：任何一個你想了很久的方案，落地前花半天找兩個獨立的同類實作比對；**收斂的地方可以放心，分歧的地方寫下你的理由**——那份理由清單，就是未來別人接手時最需要的東西。

---



# 第十四篇　地層學：ZJMUD 協議自己的歷史

> 第八到十三篇挖了 LPMud、MudOS、FluffOS、LDMud、DGD、CD 的歷史，
> 卻漏掉了本書真正的主角——**ZJMUD 這套協議自己**。
>
> 這不是疏忽，是因為它看起來沒有歷史可挖：沒有 ChangeLog、沒有版本標籤、
> 沒有 RFC、沒有作者留下任何說明。
>
> **但它有地層。** 三個方言世代（第三篇）、三個握手世代（附錄 F.2）、
> 客戶端裡的死碼（§005）、header 裡被註解掉的區塊——
> 這些都是沉積物，而沉積物是可以斷代的。
>
> 這一篇用第八到十三篇建立的方法，回頭挖本書自己的主題。

---

## 079　三份 header 的地層 — 一次斷代

**標籤**：`#地層學` `#斷代` `#可重跑量測` `#方言` `#考古`
**證據等級**：🟢 可重跑量測（17 份封存的 `include/zjmud.h` 逐份比對，方法附於 §082）

**起源**：§005 說 `include/zjmud.h` 是「這個專案最幸運的發現」——
27 個帶中文註解的巨集。
但當時只讀了**一份**（`LPMud-Name`）。

**17 份封存裡有 17 份 header。把它們並排，會看到完全不同的東西。**

**技術核心**：先看那份被讀了整本書的檔案，它的結構其實很奇怪。

```
  1–10   #include、ZJKEY、伺服器位址
 11–128  /*  ← 一個 118 行的註解區塊，裡面有 97 個 opcode 定義
    128  */
    129  //指? MUD转?协?          ← 亂碼（見 §082）
 131–133  //#define ZJKEY … （被註解掉的舊常數）
 135–     #define ESA "\x1b" … ← 25 個 opcode 定義，活躍
```

**一個檔案裡有兩套定義：97 個被註解掉的，25 個活躍的。**

直覺會說「舊版被註解掉、新版活著」。**量一下就知道不是。**

| | 註解區塊 | 活躍區塊 |
|---|--------:|--------:|
| opcode 定義數 | **97** | **25** |
| 只在這一區出現 | **71** | **0** |
| 兩區都有 | 25 | 25 |

**活躍區是註解區的嚴格子集——它沒有任何新東西。**

所以那不是「升級」，是**縮減**：有人拿到一份 97 個碼的 header，
把整份註解掉，然後在下面重新打了核心的 25 個。

### 那 97 個是誰的？

把註解區塊裡的巨集**按名稱前綴**分類：

| 前綴 | 數量 | 內容 |
|------|-----:|------|
| **`XY`** | **47** | 商城、背包、儲物袋、副本、綜合屬性、人物面板…… |
| `ZJ` | 25 | **核心**（房間、出口、描述、物件、按鈕、屬性條……） |
| `XJ` | 5 | 綜合屬性樣式一～四、體力 |
| `KILL` | 4 | 戰鬥技能、開始、結束、清場 |
| **`DMJH` / `DMZ` / `DM`** | **10** | 查看自身、判斷界面、更改界面…… |
| `SYS` | 2 | 系統 |
| 其他 | 4 | — |

**`DMJH` = 大梦江湖。**

於是有了一個可以直接驗證的假設：**去看大梦江湖自己的 header。**

### 決定性的比對

同一支腳本掃四份封存的 `include/zjmud.h`：

| 封存 | 活躍定義 | 註解掉 | 活躍區的前綴分佈 |
|------|--------:|------:|-----------------|
| **`damengjianghu`（大梦江湖 新协议版）** | **97** | **0** | **XY 47 · ZJ 25 · XJ 5 · KILL 4 · DMJH 4 · DMZ 4 · 其他 4 · SYS 2 · DM 2** |
| `LPMud-Name`（江湖论剑） | 25 | **97** | ZJ 25（＋SYS） |
| `zhijianxing`（执剑行 7.0） | 25 | 0 | ZJ 23 · SYS 2 |
| `duobaojianghu`（夺宝江湖） | 23 | 0 | ZJ 21 · SYS 2 |
| `xiaoaojianghu`（笑傲江湖） | 20 | 0 | ZJ 18 · SYS 2 |

**第一列與第二列的前綴分佈一模一樣。**

> **江湖论剑那個 118 行的註解區塊，就是大梦江湖的 `zjmud.h`。**

而其他三份封存**只有 ZJ 核心，一個擴充碼都沒有**。

### 地層的形狀

```
┌─────────────────────────────────────────────┐
│ 擴充層  XY 47 · XJ 5 · KILL 4 · DM* 10 · …   │  只有大梦江湖線
│         （帶字母的碼全在這一層）                │  ← 江湖论剑把它整層註解掉
├─────────────────────────────────────────────┤
│ 核心層  ZJ 18–25 ＋ SYS 2                     │  17 份封存全部都有
└─────────────────────────────────────────────┘
```

### 方向：誰先誰後

§050 用 Jaccard 量血緣時說過，結構相似度**說不出誰先誰後**，方向要靠別的證據。
這裡有三條，而且互相印證：

| 證據 | 指向 |
|------|------|
| **核心層在 17 份封存全部出現，擴充層只在一支** | 核心較早——**普世的東西通常是先有的** |
| **活躍區是註解區的嚴格子集（新增 0 個）** | 這是一次**縮減**，不是升級 |
| **擴充層的前綴是特定 lib 的名字**（`DMJH`、`XY`、`XJ`） | 擴充是**由特定 lib 加上去的**，不是核心規格的一部分 |

**所以江湖论剑不是「還沒升級」，是「收到了擴充版然後退回核心」**——
它是這條演化線上的一份**過渡標本**：
擴充層完整地保存在它體內，但沒有被啟用。

**解決的痛點**：這個量測解了一個本書一直含糊帶過的問題——
§005 的表寫「大梦江湖（新協議）定義 101、實際使用 81」，
但沒說**那 101 個是從哪來的、跟核心是什麼關係**。

現在知道了：**擴充層是加在核心層上面的，而且加它的人在巨集名裡簽了名。**

**踩過的坑（本專案的方法論）**：本書讀了 `zjmud.h` 整整六篇（第二、三篇），
卻**從來沒有把 17 份的同一個檔案並排比對過**。

原因很平凡：`LPMud-Name` 是唯一以檔案樹形式存在的封存（其餘都是映像），
所以它「最方便讀」，於是它變成了預設樣本。

**而預設樣本恰好是最不典型的那一份**——
17 份裡只有它有那個註解區塊。

> **教訓**：當一批樣本裡有一份特別好取用，要特別小心它是不是典型。
> §050 的老手視角說「先畫分佈再定門檻」，
> 這裡的版本是：**先看全部，再挑一份細讀。**

**優點 / 罩門**：這個方法的優點是極便宜——
一支三十行的腳本掃 17 份索引，不必解開任何映像。
罩門是它只看**巨集定義**，看不到實際發送點；
一個 lib 可以定義了卻不用（§009 那條「定義 ≠ 使用」在這裡同樣適用）。
**附錄 F.3 的實測 opcode 分佈就是這個罩門的補償**——
它量的是線路上真的出現了什麼。

**效益**：兩份證據合起來，才是完整的圖：

| | header 定義（本節） | 線路實測（附錄 F.3） |
|---|------------------|-------------------|
| 大梦江湖 | 97 個，含 XY/XJ/KILL/DM 全層 | **`111 234 517 604 605` —— 5 個擴充碼，全 17 份裡只有它送** |
| 其他 lib | 只有核心 | 只送核心碼（`000 002 003 004 005 006 012 021 022`） |

**定義與使用互相印證：擴充層確實只活在那一支上。**

> 💡 君之一席話
> **當一批樣本裡有一份特別容易讀，它往往就是被過度依賴的那一份**——而過度依賴的樣本如果剛好不典型，你的整套結論會偏，而且偏得很難察覺。

> 🔍 老手視角──真正的門道
> 這一節做的事叫**同檔跨樣本比對**——把同一個檔名在 N 份樣本裡的內容並排。它便宜到不合理，卻幾乎沒有人做，因為直覺會覺得「我已經讀懂這個檔案了」。但讀懂一份和讀懂一批是兩回事：**一份告訴你它是什麼，一批告訴你它在變什麼。** 這個技巧在很多場合都成立——比對同一個設定檔在十台機器上的差異（找出漂移）、同一個 schema 在各服務裡的版本（找出不一致）、同一份 vendored 函式庫在各專案裡的內容（找出誰改過）。可落地的做法：**接手任何有「多份相似副本」的系統時，第一天就寫一支「同名檔案跨副本 diff」的腳本**。它產出的清單就是這個系統真正的變異點地圖，而那張地圖通常沒有人畫過。

---

## 080　擴充層的內部結構 — 五個前綴就是五次擴充

**標籤**：`#擴充方言` `#命名空間` `#UI協議` `#死碼` `#演化痕跡`
**證據等級**：🟢 可重跑量測（`damengjianghu` 與 `LPMud-Name` 的 header 逐條分類）

**起源**：§079 把擴充層當成一整塊。**它不是。**
五個前綴代表至少五批不同的人、在不同時間加上去的東西，
而且它們的**設計品味明顯不同**。

**技術核心**：把擴充層按前綴拆開看。

### `XY` 層（47 個）— 一整套圖形化介面

這是最大的一批，內容是**完整的 App 式 UI**：

| 群組 | opcode | 內容 |
|------|--------|------|
| 商城 | 343–345 | 元寶數量、商城類型、商場物品 |
| 背包／儲物袋 | 346–350 | 背包物品／文字、儲物袋物品／文字 |
| 副本 | 270–272 | 副本、副本文字、副本列表 |
| 綜合屬性 | 291–293、296–297 | 標題、選項、文字 |
| 人物面板 | 417–421 | 名稱、描述、按鈕 ×3 |
| 戰鬥 | 511–515 | 加敵、加友、減敵、減友、描述 |
| 列表 | 211–215、217 | 通用列表槽 |
| 其他 | 10a、11a、130、308–311、331… | 跑馬燈、好友提示…… |

**這一層的設計哲學和核心層完全不同。**

核心層（`ZJ`）的碼是**語意性**的：房間標題、出口、描述、物件。
**`XY` 層的碼是版面性**的：`XYBEIBAO`(背包物品) / `XYBBTEXT`(背包文字) /
`XYBEIBAOT`(背包文字2) —— **同一個面板的不同槽位各佔一個 opcode。**

> 這是一個很清楚的訊號：**核心層是為「文字遊戲加標記」設計的，
> `XY` 層是為「一個有固定版面的 App」設計的。**
> 兩者中間隔著一次產品形態的改變。

### `XJ` 層（5 個）— 樣式變體

```
2k1  XJYS1  綜合屬性樣式一
2k2  XJYS2  綜合屬性樣式二
2k3  XJYS3  綜合屬性樣式三
2k4  XJYS4  綜合屬性樣式四
521  XJTILI 體力
```

**四個 opcode 只為了同一個面板的四種排版。**

這在協議設計上是很糟的做法——**版面樣式不該佔用 opcode 空間**，
它應該是 payload 裡的一個欄位。
但它揭露了一件事：**加這一層的人把 opcode 當成「函式名」在用**，
而不是當成「訊息型別」。

### `KILL` 層（4 個）＋ `XY` 的戰鬥碼（5 個）— 兩批人做同一件事

```
XY 層：  511 加敵  512 減敵  513 加友  514 減友  515 戰鬥描述
KILL 層：516 戰鬥結束  517 戰鬥技能  518 戰鬥開始  519 清理戰場
```

**同一個戰鬥面板，命名前綴卻不同、編號連續。**
最合理的解釋是：`XY` 先做了一半（實體增刪與描述），
後來另一批人補上生命週期（開始／結束／清場）與技能列，
而他們用了自己的前綴。

**編號連續但前綴不同——這是典型的「接手」痕跡。**

### `DM*` 層（10 個）— 大梦江湖自己的

```
111  DMJHLOOK    查看自身
604  DMZHUJMQH   判斷界面
605  DMZHUJIU    更改界面
…
```

**最後一批，而且直接用了 lib 自己的名字當前綴。**
§079 已經證明它是最外層——因為整份 header 就叫做大梦江湖的 header。

### ★ 兩個直接對上本書前面章節的發現

**① 帶字母的 opcode 全部在擴充層。**

```
10a  11a  （XY 層）
2k1  2k2  2k3  2k4  （XJ 層）
```

§004 記過一個坑：

> 一開始把 opcode 的規則寫成「ESC + 三個**數字**」，正則是 `/^\d{3}$/`。
> 後來在擴充方言裡撞到 `10a`、`11a`、`2k1`–`2k4` 這種**帶字母**的碼，
> 全部被當成一般訊息，把控制字元連同內容印在畫面上。

**現在知道那個坑的確切位置：這六個碼全部來自 `XY` 與 `XJ` 兩層，
也就是「App 化」之後才加的部分。**

核心層的 25 個碼**全部是三位數字**——
所以那個正則在核心層上是**完全正確**的。
**它不是寫錯了，是它的作者只看過核心層。**

> 這是本書「證據等級」規矩的一次漂亮驗證：
> 從一份樣本歸納出的規則，在那份樣本上永遠成立。

**② 唯一的「弃用」標記。**

整份 header 只有一個顯式的淘汰註記：

```c
#define XYMRX  ESA + "10a"   //跑马灯展示(弃用)
```

**擴充層自己內部也有演化**——有東西被加進來，然後被放棄，
而且放棄時**留下了註記卻沒有刪掉定義**。

這正是 §009 那條「定義 ≠ 使用」的微觀版本：
**協議的定義檔是只增不減的**，
因為刪掉一個定義的風險（可能還有人在用）永遠大於留著的成本。

**解決的痛點**：這一節解的是「擴充方言為什麼那麼亂」——
§005 抱怨過編號沒有語意分區（`045` 夾在 `024` 與 `100` 之間、
`900` 不遵循 `9xx = 反向操作`）。

**現在有了解釋：那不是一個人設計的編號空間，是五批人各自往裡面塞。**

| 前綴 | 它佔的號段 | 設計品味 |
|------|-----------|---------|
| `ZJ` | 000–023、9xx | **語意分區**（`9xx` = `0xx` 的反向操作） |
| `XY` | 10a、11a、130、2xx、3xx、4xx、5xx | **按面板分群**，但號段跳躍 |
| `XJ` | 2k1–2k4、521 | **樣式當 opcode** |
| `KILL` | 516–519 | 接在 `XY` 的 511–515 後面 |
| `DM*` | 111、604、605… | **散落**，無分區 |

**只有核心層有設計。其餘四層是累積。**

**踩過的坑**：本專案的 `dialects.js` 把擴充碼組織成 profile（§010），
每一個 opcode 標上 `kind` / `panel` / `slot`——
**那等於是替這五層做了一次事後的統一分類。**

而 §010 記錄的四個撞號，正是這種「事後統一」必然會撞上的東西（§081）。

**優點 / 罩門**：擴充層的優點是**它真的做出了一個圖形介面**——
47 個 `XY` 碼撐起商城、背包、副本、人物面板，
這在一條純文字 telnet 連線上是很可觀的成果。
罩門是**沒有任何一層知道其他層的存在**，
所以編號空間、命名慣例、設計哲學全部各自為政。

**效益**：對逆向工作，這一節提供了一個很實用的技巧：
**用命名前綴替一份沒有版本的協議斷代。**

前綴通常洩漏三件事：誰加的、什麼時候（相對順序）、以及那批人怎麼想。
一份 header 如果有五種前綴，它就至少有五層地層。

> 💡 君之一席話
> **編號空間的混亂不是設計失誤，是地層**——當你看到一份協議的編號沒有規律，先別罵設計者，先數數看它有幾種命名前綴。

> 🔍 老手視角──真正的門道
> 「同一個面板的不同槽位各佔一個 opcode」（`XYBEIBAO` / `XYBBTEXT` / `XYBEIBAOT`）這個現象，是協議設計裡一個很常見的滑坡：**當加一個 opcode 比改一個 payload 格式便宜時，人就會一直加 opcode。** 在這個生態裡加 opcode 的成本近乎零（`#define` 一行，§043 說過 driver 完全不知情），而改 payload 格式要動客戶端的解析器並考慮相容性。於是理性的個體選擇，累積成了一個 116 個碼的編號空間。老手看到這種「某個維度的擴充成本特別低」的系統時，會預期那個維度將來一定會失控——並且提早在那裡設一條規矩（號段分區、命名前綴強制、新增需要登記）。可落地的判準：**看你的系統裡哪一種擴充最便宜，那裡就是三年後最亂的地方**；在它還只有十個項目的時候定規矩，成本是一小時，之後是一次重構。

---

## 081　第三代與撞號的機制 — 兩個團隊各自從 600 開始

**標籤**：`#撞號` `#指游ZY` `#手機時代` `#命名空間` `#4字元opcode`
**證據等級**：🟡 伺服器方言表與轉換教程 ＋ 🟢 本專案 `dialects.js` 的衝突診斷

**起源**：§010 記過一件事——本專案寫了一個「列出所有 profile 中意義不同的 opcode」
的診斷函式，**本來只是想做個健康檢查，結果它第一次執行就吐出四個。**

**那四個是 602、603、604、605。**

§079–§080 把地層挖開之後，這四個撞號的成因可以完整還原了。

**技術核心**：

### 第三代：指游（`ZY*`）方言

第三個世代來自另一條線——「指游 MUD」，
本專案在封存的 `others/zjmud转zymud教程` 裡找到它的轉換說明。

它的號段是 **600–618，外加一個 1085**：

| opcode | 名稱 | 用途 |
|--------|------|------|
| 600 | `ZYRIGHTBTN` | 方向鍵右側按鈕 |
| 601 | `ZYHPTXT` | 個人狀態 |
| **602** | **`ZYCHARHP`** | 同房間其他 NPC 狀態 |
| **603** | **`ZYFIGHTBTN`** | 彈出戰鬥界面與按鈕 |
| **604** | **`ZYHANDLERBTN`** | 彈出操作面板與按鈕 |
| **605** | **`ZYCLEARSCREEN`** | 清空主畫面 |
| 606–618 | `ZYRIGHTMENU`…`ZYKJ` | 側選單、語音、進度條、重登、通知欄、網路狀態、選擇器、傷害、技能、劇情、震動、快捷鍵 |
| **1085** | **`ZYMAP`** | 地圖——**全協議唯一的 4 字元 opcode** |

### ★ 撞號怎麼發生的

把三代的 6xx 號段疊起來：

```
DMJH 層：           604 判斷界面      605 更改界面
XY   層：  602 ?    603 ?
                    ↕                 ↕
ZY   方言：602 NPC狀態  603 戰鬥界面  604 操作面板  605 清空畫面
```

**兩邊都把「界面／面板」類的 opcode 放在 6xx 號段。**

而 §043 已經解釋過根因：

> **ZJMUD 是 mudlib 層協議，driver 完全不知道它存在**，
> 所以任何人拿到 mudlib 都可以加一行 `#define`，
> **不必跟誰報備，因為根本沒有「誰」可以報備。**

**兩個團隊在互不知情的情況下，各自從一個「看起來還沒被用」的號段開始編，
而他們對「哪個號段還沒被用」的判斷，來自各自手上那份 header。**

大梦江湖那邊看到的是 000–599 大致被佔，於是往 6xx 走；
指游那邊看到的是核心層 000–023 加零星擴充，於是也往 6xx 走。

**兩邊的判斷都是合理的，而結果是必然的。**

### `1085` — 打破的第二個假設

§004 記過第一個被打破的假設（「三個數字」被 `10a`／`2k1` 打破）。
`ZYMAP = 1085` 打破的是第二個：**opcode 是三個字元**。

本專案的解析器因此改成「行首 4 或 5 個字元，視情況」——
規格書 v1.4 明確標註 **「opcode 可為 3 或 4 字元」**。

**一個沒有版本協商的協議，只能靠解析器不斷放寬來相容。**

### ★ ZY 方言洩漏了它的年代

看 606–618 這一段的內容：

| opcode | 用途 | 它假設了什麼 |
|--------|------|------------|
| 607 `ZYVOICE` | 語音（官方標註「暫未實現」） | 有麥克風 |
| **610 `ZYSTATUSINFO`** | **Android 通知欄** | **對面是一個 Android App** |
| 611 `ZYCLIENTSTATUS` | 開關網路狀態與**幀率**顯示 | 有 render loop |
| **616 `ZYVIBRATE`** | **震動（時長毫秒）** | **對面是一支手機** |
| 615 `ZYSTORYTEXT` | 逐字劇情 | 有動畫能力 |
| 609 `ZYSYSEXIT` | 幾毫秒後回登入畫面 | 有「畫面」的概念 |

**`ZYVIBRATE` 與 `ZYSTATUSINFO` 是決定性的**：
一個會讓手機震動、會寫 Android 通知欄的 opcode，
**只可能誕生在智慧型手機時代**。

而本專案的起點——那個 1.1 MB 的 APK（§001）——
封裝日期是 **2022-10-03**。

**三代的年代序因此可以排出來：**

```
第一代  核心 ZJ 層         純文字 MUD 加標記        （最早，普世）
第二代  XY/XJ/KILL/DM* 層   App 式固定版面           （PC 圖形客戶端時代）
第三代  ZY 層               震動、通知欄、幀率、語音   （智慧型手機時代，最新）
```

**規格書 §12 的判斷「這是三個方言中最新的一套」，
在這裡得到了獨立的佐證——不是從版本號看出來的，是從它假設的硬體能力。**

**解決的痛點**：§010 說「撞號的根源是協議演化時沒有中央註冊」，
這一節把那句話從**斷言**變成了**可追溯的過程**：

```
沒有中央註冊（§043）
  → 每個團隊只能從自己手上那份 header 推測哪個號段空著
    → 兩邊都推測 6xx 空著（因為兩邊看到的 header 不一樣）
      → 撞在 602–605
        → 而且雙方都不會知道，直到有人同時支援兩個方言
```

**最後一行是關鍵：撞號在協議層面是沒有症狀的。**
只有當一個客戶端試圖同時支援兩個方言時，衝突才會顯現——
而那個客戶端，就是本專案。

**踩過的坑**：§010 說發現撞號「純屬意外」。
現在回頭看，**它遲早會被發現，而且必然是被同時支援多方言的實作發現**。

這給了一個一般化的結論：

> **一套沒有中央註冊的協議，它的衝突只會在「有人試圖統一」的那一刻暴露。**
> 在那之前，每個孤島都是自洽的。

**優點 / 罩門**：ZY 方言的優點是**它把客戶端能力納入了協議**——
震動、通知欄、語音、幀率，這是三代裡唯一意識到
「對面是一個有能力的裝置，而不只是一個終端」的一代。

罩門有兩個：其一，**它假設對面是 Android**
（`ZYSTATUSINFO` 在本專案的 Web 客戶端是 `KIND.IGNORE`）；
其二，**它與第二代撞號四次**，而兩邊都不知情。

**效益**：本專案處理撞號的方式（§010 的 profile 隔離 + 診斷函式）
在這裡得到了完整的辯護：

| 做法 | 為什麼必要 |
|------|-----------|
| **分 profile** | 因為 602–605 在兩個方言裡**真的**意義不同，沒有正確的合併方式 |
| **判別機制** | 因為要先知道現在是哪個方言 |
| **診斷函式 + 測試** | 因為**撞號是無症狀的**，必須主動去找 |

**§010 說「少了第三件，你會在半年後加第四個方言時重蹈覆轍」——
這一節解釋了為什麼：因為第四個方言的作者，也會從他手上那份 header
去推測哪個號段空著。**

> 💡 君之一席話
> **沒有中央註冊的協議，它的衝突只在「有人試圖統一」的那一刻才出現**——在那之前，每一座孤島都覺得自己是對的，而且它們都沒錯。

> 🔍 老手視角──真正的門道
> 這一節最值得帶走的，是「撞號是無症狀的」這個性質。多數 bug 會叫——例外、錯誤碼、崩潰；而命名空間衝突不會，它只會讓某個功能在某些情況下做錯事。這類「安靜的正確性問題」在分散式系統裡到處都是：兩個服務對同一個欄位的單位理解不同（公尺 vs 英尺）、兩個團隊對同一個狀態碼的語意不同、兩份設定用同一個 key 表達不同的東西。它們的共同點是**只有在整合的那一刻才會顯現，而整合往往發生在上線之後**。老手的對策不是「更小心」，而是**主動製造整合時刻**：寫一個把所有命名空間攤平、列出衝突的診斷程式，並且讓它跑在 CI 上。本專案那二十行的衝突診斷函式，價值不在它找到的那四個，在於它讓第五個在被寫出來的當天就會被發現。可落地的做法：**任何有「多個來源往同一個空間註冊」的系統（路由、事件名、設定鍵、指標名、錯誤碼），都要有一支列出重複的檢查。**

---

## 082　協議的年表 — 以及它為什麼永遠是 1.0

**標籤**：`#年表` `#版本號` `#雙重轉碼` `#收束` `#證據等級`
**證據等級**：🟢 本節所有斷代證據皆可重跑（方法附於文末）

**起源**：§079–§081 把地層挖開了。這一節把它組裝成年表——
然後停在一個很不舒服的觀察上。

**技術核心**：

### 三個世代的完整對照

| | **第一代** | **第二代** | **第三代** |
|---|-----------|-----------|-----------|
| 方言代號 | `classic`（經典） | `dmjh`（擴充） | `zymud`（指游 ZY） |
| opcode 前綴 | `ZJ` `SYS` | `XY` `XJ` `KILL` `DM*` | `ZY` |
| 數量 | 18–27 | 再加約 61–72 | 20（600–618、1085） |
| **號段** | 000–023、9xx | 10a、11a、1xx–5xx、2k1–2k4 | **600–618、1085** |
| **編號規則** | **三位數字，`9xx` = `0xx` 反向** | **數字＋字母混用** | 三位數字 ＋ **一個四位數** |
| 產品形態 | 純文字 MUD 加標記 | **App 式固定版面** | **智慧型手機 App** |
| 硬體假設 | 終端機 | 有版面的圖形客戶端 | **震動、通知欄、麥克風、幀率** |
| 握手（附錄 F.2） | `ver1.0:`＋DES crypt ／ `ver1.0,`＋明文 | `ver1.0,` ＋ `$6$` SHA-512 crypt | `version 1.0 key::<crypt>` |
| 在 17 份封存裡 | **全部 17 份** | **僅 1 份**（大梦江湖）＋ 1 份保存為註解（江湖论剑） | 0 份（來自轉換教程） |
| 實測送出（附錄 F.3） | `000 002 003 004 005 006 012 021 022` | **`111 234 517 604 605`** | — |

### ★ 三個世代，三種握手格式，全部自稱 1.0

把握手那一列單獨拉出來看：

```
第一代   ver1.0:byz0rmpISExtQ                  ← 冒號分隔，13 字元 DES crypt
第一代'  ver1.0,123456789abcd                  ← 逗號分隔，★ 明文金鑰
第二代   ver1.0,$6$<16字元salt>$<86字元>         ← 逗號分隔，SHA-512 crypt
第三代   version 1.0 key::<crypt>              ← 完全不同的語法
```

**分隔符換過（`:` → `,`）、雜湊算法換過（無 → DES → SHA-512）、
連整個語法都換過（`ver1.0:` → `version 1.0 key::`）——
而版本字串從頭到尾都是 `1.0`。**

三十年、三個世代、116 個 opcode、四個撞號，
**沒有任何一次改動反映在版本號上。**

### 為什麼？§043 早就回答了

> ZJMUD 是 **mudlib 層協議**，driver 完全不知道它存在。
> 沒有 driver 支援、沒有 telnet 子協商、**沒有註冊、沒有能力協商**。

**沒有能力協商，版本號就沒有用途。**

版本號存在的理由是**讓對面據以調整行為**。
但 ZJMUD 的伺服器不會問客戶端「你懂哪個版本」（§056 第 0 代的特徵），
客戶端也無法據此要求伺服器降級——
**伺服器一律送，客戶端自己看著辦。**

在這種架構下，`ver1.0` 不是版本宣告，**是一個握手用的固定字串**。
§044 早就從原始碼證明了這一點：`jiance()` **收任何非空輸入就放行**——
**它連那行字串自己有沒有被正確解讀都不在乎。**

**所以「協議沒有版本」不是疏忽，是這個架構的必然結論。**

### 對照：其他三層都有版本機制

本書前面幾篇正好提供了三個對照組：

| 層 | 版本機制 | 出處 |
|---|---------|------|
| **driver 語言層** | `__SENSIBLE_MODIFIERS__` —— **可偵測的破壞性變更** | §059 |
| **driver 資料層** | DGD 的 LWO **建議第三格放版本號**，「以防你之後改變型別的建構方式」 | §068 |
| **本專案的映像格式** | `format: 1` —— **第一個欄位就是版本** | 附錄 F |
| **ZJMUD 協議** | **無** | 本節 |

§068 的老手視角說過：

> 任何會被寫進磁碟、送上網路、或存進資料庫的結構，第一個欄位永遠是版本——
> **即使當下只有一個版本，而且你確定不會改。**

**ZJMUD 是那句話的反例教材**：它有一個看起來像版本號的東西，
但因為沒有人會依據它做任何決定，**它三十年沒有動過，也不需要動。**

### 亂碼那一行 — 損壞本身也是史料

`LPMud-Name/world/include/zjmud.h` 第 129 行，
夾在被註解掉的擴充層與活躍的核心層之間，是一行註解：

```
//指?MUD转?协?
```

檔案整體可以解為 UTF-8，但這一行的中文字**部分是無法還原的替代字元**。
以 GBK 重新解讀會得到 `//指锟斤拷MUD转锟斤拷协锟斤拷`——
而 `锟斤拷` 是中文圈著名的**雙重轉碼殘骸**
（UTF-8 的替代字元 U+FFFD 被編成 GBK、再被讀回來的產物）。

**推測原文是「指游 MUD 轉換協議」**（🔴 推論，標明未驗證）——
它正好是這個檔案裡唯一提到第三代方言的地方，
而它**在成為證據之前就已經被兩次編碼轉換磨掉了**。

> **這一行的價值不在它說了什麼，在它證明了這份檔案經歷過什麼。**
> 附錄 D.4 說過：優先去找「系統自己寫下的東西」——
> swapfile 的檔名、intermud 名錄、檔頭署名。
> **損壞的痕跡也算一種：它記錄了這份檔案被誰、用什麼工具、搬過幾次。**

而本專案的匯入管線正是為此而存在——
附錄 F.5 記錄「GBK → UTF-8 轉檔，最多單一 lib 29,853 檔」，
**如果那次轉換也做成雙重轉碼，本書引用的每一條中文註解都會變成這個樣子。**

### 完整年表

```
1993–1996   TMI-2 mudlib 傳入中文圈（附錄 D.3）
              ↓ driver 不知道協議存在，於是誰都可以加（§043）
第一代      核心 ZJ 層成形：000–023 ＋ 9xx 反向操作
              · 唯一有設計的一層（號段有語意分區）
              · 17 份封存全部帶著它
              · 握手 ver1.0: ＋ DES crypt
              ↓ 圖形客戶端出現，需要固定版面
第二代      XY(47) → XJ(5) → KILL(4) → DM*(10) 逐層堆疊
              · 帶字母的 opcode 出現（10a / 2k1–2k4）→ 打破「三位數字」假設
              · XYMRX(10a) 被標「弃用」——擴充層自己也有內部演化
              · 江湖论剑收到整層後把它註解掉，退回核心（§079）
              · 握手改用 $6$ SHA-512；分隔符 : → ,
              ↓ 智慧型手機時代
第三代      ZY 層 600–618 ＋ 1085
              · 震動 / 通知欄 / 幀率 / 語音 → 假設對面是 Android App
              · 1085 打破「三字元 opcode」假設
              · 與第二代撞號四次（602–605），雙方都不知情（§081）
              · 握手語法整個換掉，但仍叫 version 1.0
              ↓
2022-10-03  那個 1.1 MB 的 APK 被上傳（§001）——本書的起點
2026        本專案：三個 profile、4 個撞號隔離、202 條測試（§010、附錄 F）
```

**解決的痛點**：這一篇解的是本書一個結構性的空白——
它花了十三篇挖別人的歷史，卻沒挖自己主題的。

而挖完之後，第二篇與第三篇的所有「怪異之處」都有了成因：

| 前面章節的觀察 | 這一篇給的成因 |
|--------------|--------------|
| §004 正則 `/^\d{3}$/` 被打破 | 帶字母的碼全在第二代（§080） |
| §005 編號沒有語意分區 | 只有核心層有設計，其餘四層是累積（§080） |
| §009 定義 102 個只用 16 個 | 定義檔只增不減（`弃用` 標記卻不刪，§080） |
| §010 四個撞號 | 兩批人各自從 6xx 開始（§081） |
| §011 變長 opcode | `1085` 來自第三代（§081） |
| 附錄 F.2 三種握手格式 | 三個世代各一種，而版本號從未變（本節） |

**優點 / 罩門**：本篇方法的優點是**全部可重跑**（見下方）。
罩門是**它只能重建相對順序，給不出絕對日期**——
沒有任何一份 mudlib 帶著可信的時間戳，
唯一的絕對日期是那個 APK 的 2022-10-03，以及 TMI-2 檔頭的 1993-08-15。
**中間三十年的刻度是靠地層推的，不是量的。**

**效益**：這一篇補完了本書的自我一致性。
第一篇立下的規矩是「證據等級」，而本書用它挖了六支 driver 的歷史；
**最後把同一套方法用回自己身上，是這本書該做的最後一件事。**

### 重跑本篇所有量測

```bash
# ① 三份 header 的地層（§079）
python3 - <<'EOF'
import json, re, sys
def load(slug, path='include/zjmud.h'):
    m = json.load(open(f'libs/{slug}/mudlib.json', encoding='utf-8'))
    f = next((x for x in m['files'] if x['path'] == path), None)
    if not f: return ''
    with open(f'libs/{slug}/mudlib.data', 'rb') as fh:
        fh.seek(f['at']); return fh.read(f['size']).decode('utf-8', 'replace')
for slug in ('damengjianghu', 'duobaojianghu', 'zhijianxing', 'xiaoaojianghu'):
    t = load(slug); in_c = False; live = dead = 0
    for l in t.split('\n'):
        s = l.strip()
        if s.startswith('/*'): in_c = True
        if re.match(r'#define\s+\w+\s+ESA\s*\+\s*"\w+"', s):
            dead += in_c; live += (not in_c)
        if '*/' in s: in_c = False
    print(f'{slug:18} 活躍 {live:3}  註解掉 {dead:3}')
EOF

# ② 前綴分佈（§080）—— 把上面的計數換成 collections.Counter(前綴)

# ③ 實測 opcode 分佈（附錄 F.3）
node webclient/tools/boot-test.mjs

# ④ 撞號診斷（§010、§081）
cd webclient && npm test -- --grep 撞號
```

> 💡 君之一席話
> **一個沒有人依據它做決定的版本號，三十年不會變**——不是因為東西沒改，是因為改了也沒有人在乎；版本號的價值，全部來自有人會讀它。

> 🔍 老手視角──真正的門道
> 這一篇的方法可以濃縮成一句話：**當一個系統沒有版本歷史，就去讀它的沉積物。** 命名前綴（誰加的）、號段分佈（什麼時候還空著）、被註解掉但沒刪的區塊（曾經有過什麼）、標了「弃用」卻留著的定義（放棄的成本高於保留）、硬體假設（震動 → 手機時代）、甚至編碼損壞的痕跡（被搬過幾次）——這些都是斷代證據，而且它們比任何人的回憶可靠，因為**沒有人是為了留給後人而寫下它們的**。這個技巧在接手任何長壽系統時都適用：資料庫裡沒人用的欄位、設定檔裡註解掉的區塊、程式碼裡的 `// TODO: 2014`、路由表裡的舊路徑，全部都是地層。可落地的做法：**接手一個沒有文件的系統時，先花半天把「死掉但沒被刪的東西」列一遍**——那份清單會告訴你這個系統試過什麼、放棄了什麼、以及為什麼現在長這樣。**活著的程式碼告訴你它做什麼，死掉的程式碼告訴你它經歷過什麼。**

---



# 附錄 A　opcode 總索引

> 本索引是全書的鍵。每一行都可回溯到第二篇（核心）或第三篇（擴充）的說明。

> 證據等級：核心 27 個為 🟡 伺服器 `zjmud.h` ＋ 🟢 實機；擴充為 🟡 header ∩ `.c` 使用統計。

---

## A.1　核心 27 個（14 個 mudlib 共用）

| opcode | 巨集 | 語意 | 備註 |
|--------|------|------|------|
| `ESC000` | `SYSY` | 連線／登入狀態 | 見 §008 |
| `ESC001` | `INPUTTXT` | 文字輸入面板 | `說明$zj#指令樣板` |
| `ESC002` | `ZJTITLE` | 房間標題 | **有清空副作用** |
| `ESC003` | `ZJEXIT` | 出口列表 | `dir:label[:cmd]` |
| `ESC004` | `ZJLONG` | 房間描述 | 樣式文字 |
| `ESC005` | `ZJOBIN` | 房間物件 | `label:cmd` |
| `ESC006` | `ZJBTSET` | 自訂按鈕 | `slot:label:cmd`，slot=bs/b1–b17 |
| `ESC007` | `ZJOBLONG` | 目標詳情 | **伺服器用最多，531 次** |
| `ESC008` | `ZJOBACTS` | 動作列① | 帶版面前綴 |
| `ESC009` | `ZJOBACTS2` | 動作列② | **448 次** |
| `ESC010` | `ZJYESNO` | NPC 對話框 | `$dh#` 區塊 |
| `ESC011` | `ZJMAPTXT` | 地圖 | `$br#` 換行 |
| `ESC012` | `ZJHPTXT` | 屬性條群 | `║` 分隔，色碼可能是 ARGB |
| `ESC013` | `ZJMORETXT` | 長文本分頁 | `$br#` 換行 |
| `ESC014` | `ZJFORCECMD` | **伺服器代送指令** | 98 次，不實作會斷流程 |
| `ESC015` | `ZJTMPSAY` | 頂部飄字 | 同時進系統頻道 |
| `ESC016` | `ZJFMSG` | 戰鬥訊息 | — |
| `ESC017` | `ZJFNOMSG` | 關閉戰鬥面板 | — |
| `ESC020` | `ZJPOPMENU` | 彈出選單 | **用 `$z2#` 不是 `$zj#`** |
| `ESC021` | `ZJTTMENU` | 標題列按鈕 | `label:cmd` |
| `ESC022` | `ZJCHARHP` | 物件血條更新 | `tag$zj#a:b:max` |
| `ESC023` | `ZJLONGXX` | 描述顯示開關 | `屏蔽描述` |
| `ESC024` | `—` | 傷害飄字 | 客戶端反編譯獨有 |
| `ESC045` | `—` | 開啟網頁 | 客戶端反編譯獨有 |
| `ESC100` | `ZJCHANNEL` | 聊天訊息 | — |
| `ESC900` | `—` | 換伺服器 | `ip:port` |
| `ESC903` | `ZJEXITRM` | 刪除單一出口 | `0xx` 的反向 |
| `ESC905` | `ZJOBOUT` | 刪除單一物件 | 同上 |
| `ESC913` | `ZJEXITCL` | 清空所有出口 | 無對應正向操作 |
| `ESC997` | `—` | 單行指令模式 | `\n` → `;` |
| `ESC998` | `—` | 多行指令模式 | 預設 |
| `ESC999` | `SYSEXIT` | 強制退出 | — |

> **實作優先順序**：`007`／`009`（詳情＋動作列）佔伺服器發送量絕大多數，其次是 `014`（代送指令）。先把這三個做扎實，玩家 95% 的時間都在看它們。

---

## A.2　擴充 opcode（依 profile 分組）


### 大梦江湖（新協議） — 73 個

| opcode | 巨集 | 結構 | 去處 |
|--------|------|------|------|
| `ESC030` | `ZJEXIT1` | 文字 | map |
| `ESC10a` | `XYMRX` | 文字 | escort |
| `ESC111` | `DMJHLOOK` | 文字 | self |
| `ESC11a` | `XYMRU` | 文字 | escort |
| `ESC130` | `XYTISHI` | 訊息 | sys |
| `ESC211` | `XYLIE` | 標題 | list |
| `ESC212` | `XYLIF` | 清單 | list |
| `ESC213` | `XYLIG` | 文字 | list |
| `ESC214` | `XYLIK` | 動作網格 | list |
| `ESC215` | `XYLIL` | 關閉面板 | list |
| `ESC217` | `XYJI` | 文字 | list |
| `ESC234` | `ZJHPTXT1` | 數值條 | stats2 |
| `ESC270` | `XYZFUBEN` | 標題 | instance |
| `ESC271` | `XYZFBTE` | 文字 | instance |
| `ESC272` | `XYZFBLIE` | 清單 | instance |
| `ESC286` | `DMMAP` | 文字 | map |
| `ESC291` | `XYZZHSXBT` | 標題 | attr |
| `ESC292` | `XYZZHSXXX` | 清單 | attr |
| `ESC293` | `XYZZHSXWB` | 文字 | attr |
| `ESC294` | `XYZZHSXBUT` | 動作網格 | attr |
| `ESC295` | `XYZZHSXBUT1` | 動作網格 | attr |
| `ESC296` | `XYZZHSXWB1` | 文字 | attr |
| `ESC297` | `XYZZHSXWB2` | 文字 | attr |
| `ESC2k1` | `XJYS1` | 文字 | attr |
| `ESC2k2` | `XJYS2` | 文字 | attr |
| `ESC2k3` | `XJYS3` | 文字 | attr |
| `ESC2k4` | `XJYS4` | 文字 | attr |
| `ESC308` | `XYTEXT3` | 動作網格 | item |
| `ESC309` | `XYTEXT2` | 文字 | item |
| `ESC310` | `XYTEXT1` | 標題 | item |
| `ESC311` | `XYTEXT` | 文字 | item |
| `ESC331` | `DMJHPAI` | 標題 | rank |
| `ESC332` | `DMJHPAILEI` | 清單 | rank |
| `ESC333` | `DMJHPAIMING` | 文字 | rank |
| `ESC343` | `XYSHOPJZ` | 文字 | shop |
| `ESC344` | `XYSHOPLX` | 清單 | shop |
| `ESC345` | `XYSHOP` | 動作網格 | shop |
| `ESC346` | `XYBBTEXT` | 文字 | bag |
| `ESC347` | `XYBEIBAO` | 動作網格 | bag |
| `ESC348` | `XYCWD` | 動作網格 | store |
| `ESC349` | `XYCWDT` | 文字 | store |
| `ESC350` | `XYBEIBAOT` | 文字 | bag |
| `ESC417` | `XYRWNAME` | 標題 | person |
| `ESC418` | `XYRWMIAO` | 文字 | person |
| `ESC419` | `XYRWBUT1` | 動作網格 | person |
| `ESC420` | `XYRWBUT2` | 動作網格 | person |
| `ESC421` | `XYRWBUT3` | 文字 | person |
| `ESC450` | `YJBUTTON` | 動作網格 | misc |
| `ESC491` | `XYFRIENDS1` | 動作網格 | friends |
| `ESC492` | `XYFRIENDS2` | 動作網格 | friends |
| `ESC493` | `XYFRIENDS3` | 動作網格 | friends |
| `ESC494` | `XYFRIENDS4` | 動作網格 | friends |
| `ESC495` | `XYFRIENDS5` | 文字 | friends |
| `ESC496` | `XYFRIENDS6` | 訊息 | chat |
| `ESC497` | `XYFRIENDS7` | 文字 | friends |
| `ESC498` | `XYFRIENDS8` | 文字 | friends |
| `ESC511` | `XYKILL` | 實體新增 | combat |
| `ESC512` | `XYKILLD` | 實體移除 | combat |
| `ESC513` | `XYKILLDY` | 實體新增 | combat |
| `ESC514` | `XYKILLDT` | 實體移除 | combat |
| `ESC515` | `XYKILLMIAO` | 訊息 | combat |
| `ESC516` | `KILLEND` | 關閉面板 | combat |
| `ESC517` | `KILLJN` | 動作網格 | combat |
| `ESC518` | `KILLKS` | 訊息 | combat |
| `ESC519` | `KILLQL` | 關閉面板 | combat |
| `ESC521` | `XJTILI` | 數值條 | combat |
| `ESC602` | `DMZHUYET` | 文字 | home |
| `ESC603` | `DMZHUYETY` | 文字 | home |
| `ESC604` | `DMZHUJMQH` | 關閉面板 | home |
| `ESC605` | `DMZHUJIU` | 文字 | home |
| `ESC701` | `XCGX` | 文字 | misc |
| `ESC702` | `JHYJMP` | 標題 | sect |
| `ESC703` | `JHYJMP1` | 清單 | sect |

### 指游 MUD（ZY） — 20 個

| opcode | 巨集 | 結構 | 去處 |
|--------|------|------|------|
| `ESC1085` | `ZYMAP` | 文字 | map |
| `ESC600` | `ZYRIGHTBTN` | 動作網格 | quickside |
| `ESC601` | `ZYHPTXT` | 實體新增 | status |
| `ESC602` | `ZYCHARHP` | 實體新增 | status |
| `ESC603` | `ZYFIGHTBTN` | 動作網格 | combat |
| `ESC604` | `ZYHANDLERBTN` | 動作網格 | handler |
| `ESC605` | `ZYCLEARSCREEN` | 清空主區 | - |
| `ESC606` | `ZYRIGHTMENU` | 清單 | sidemenu |
| `ESC607` | `ZYVOICE` | 明確忽略 | - |
| `ESC608` | `ZYPROGRESS` | 數值條 | status |
| `ESC609` | `ZYSYSEXIT` | 延時重登 | - |
| `ESC610` | `ZYSTATUSINFO` | 明確忽略 | - |
| `ESC611` | `ZYCLIENTSTATUS` | 網路狀態 | - |
| `ESC612` | `ZYSELECT` | 元件提示 | - |
| `ESC613` | `ZYATTACK` | 傷害飄字 | - |
| `ESC614` | `ZYSKILL` | 動作網格 | skill |
| `ESC615` | `ZYSTORYTEXT` | 逐字劇情 | - |
| `ESC616` | `ZYVIBRATE` | 震動 | - |
| `ESC617` | `ZYBTSET` | 動作網格 | quickside |
| `ESC618` | `ZYKJ` | 動作網格 | hotkey |

---

## A.3　⚠️ 跨方言撞號

| opcode | 大梦江湖 | 指游 |
|--------|----------|------|
| `602` | `DMZHUYET` 主頁文本 | `ZYCHARHP` NPC 狀態 |
| `603` | `DMZHUYETY` 主頁文本 2 | `ZYFIGHTBTN` 戰鬥按鈕 |
| `604` | `DMZHUJMQH` 判斷界面 | `ZYHANDLERBTN` 操作面板 |
| `605` | `DMZHUJIU` 更改界面 | `ZYCLEARSCREEN` 清空主畫面 |

**這四個碼證明了「所有方言合併成一張表」在結構上就是錯的**——詳見 §010。

---

## A.4　特殊格式

| 項目 | 說明 |
|------|------|
| **4 字元 opcode** | 只有 `1085`（`ZYMAP`）。消歧義規則見 §011 |
| **含字母的 opcode** | `10a`、`11a`、`2k1`–`2k4`。正則不可寫 `\d{3}` |
| **未知 opcode** | 一律降級成一般訊息並**保留完整原文**，標記 `unknownOpcode`。絕不靜默丟棄 |

---

## A.5　分隔符速查

| 標記 | 用途 |
|------|------|
| `$zj#` | 記錄分隔符（主力） |
| `$z2#` | 記錄分隔符（**僅彈出選單**） |
| `$br#` | 換行 |
| `$dh#` | 對話框區塊 |
| `$sock#` | 指令序列（一次送多條） |
| `║` (U+2551) | 屬性條記錄／登入欄位 |
| `$N` | 數量佔位符（**僅 `ESC010` 對話框**） |
| `$txt#` | 輸入佔位符（**僅 `ESC001`**）／動作列的「保持面板開啟」旗標 |
| `:` `\|` | 欄位／次級欄位 |

> ⚠️ `$N` 與 `$txt#` 是兩套不同機制，**不可混用**——這是本專案 v1.0 規格書的兩個錯誤之一。

---



# 附錄 B　檔案地圖與接手指南

> 給下一個要動這份程式碼的人。**先讀這頁，再讀程式碼。**

---

## B.1　檔案地圖

```
zjmud-rust/
├── START_SERVER.bat      只啟動 MUD 伺服器
├── START_DESKTOP.bat     伺服器 + 桌面版（Tauri）
├── START_WEB.bat         瀏覽器版橋接（手機也能連）
├── STOP_ZJMUD.bat        停止全部
├── BUILD_ZJMUD.bat       建置桌面版 release
├── LPMud-Name/world/     MUD 伺服器（driver.exe）＝ 第八篇 §043/§044 的原始碼來源
├── libs/                 ★ 17 份可在瀏覽器裡跑的 mudlib（第九篇 §050、附錄 F）
│   └── <slug>/  mud.json ＋ mudlib.data ＋ mudlib.json ＋ NOTES.md
├── docs/                 三份規格書 ＋ ZJMUD 遷移 SOP
├── designbook/           ← 你正在讀的這本
└── webclient/
    ├── src/                        前端（桌面／瀏覽器／WASM **完全共用**）
    │   ├── index.html              205 行
    │   ├── css/tokens.css          121 行　設計 token + 三套主題
    │   ├── css/app.css           1,044 行　版面與元件
    │   └── js/
    │       ├── ansi.js       233 行　樣式方言解析　**純函式**
    │       ├── protocol.js   602 行　opcode 解析　　**純函式**
    │       ├── dialects.js   262 行　方言 profile 表
    │       ├── store.js      243 行　狀態容器 + pub/sub
    │       ├── net.js        325 行　Transport 抽象（三後端）
    │       ├── telnet.js     138 行　IAC 剝除＋分行（**瀏覽器與 node 共用**）
    │       ├── ui.js         735 行　DOM 元件
    │       ├── prefs.js       61 行　本地偏好
    │       ├── main.js     1,165 行　組裝 + reducer + 登入狀態機
    │       ├── wasmdriver.js 263 行　★ WASM driver 綁定（§042 的三個陷阱都在這）
    │       ├── wasmboot.js   149 行　★ 選 mud → 下載映像 → 開機
    │       ├── mudlibimage.js 112 行　★ 映像格式（format 1）的載入端
    │       └── telnetlogin.js 309 行　★ 會話層代打，玩非 zjmud 的 lib（§045）
    ├── src-tauri/
    │   ├── capabilities/default.json   ★ 缺這個 event.listen 會被擋
    │   └── src/
    │       ├── main.rs        55 行　指令註冊
    │       ├── mud.rs       238 行　TCP（位元組層分行）
    │       └── telnet.rs    162 行　IAC 剝除
    ├── bridge/
    │   ├── server.mjs       165 行　WebSocket ↔ TCP + 靜態檔
    │   └── telnet.mjs        26 行　Buffer 轉接（退化成 telnet.js 的薄殼）
    ├── test/              2,756 行　**202 條 / 12 檔**
    ├── wasm/driver/       FluffOS v2026.0729.0（由 fetch-driver.mjs 取得，不進版控）
    └── tools/
        ├── fetch-driver.mjs      取官方 wasm release
        ├── build-site.mjs        打包 libs/ 下每個 mud + 跑開機測試
        ├── boot-test.mjs         ★ 實跑註冊→建角→進世界，決定分級（附錄 F）
        ├── verify-fullstack.mjs  ★ 全鏈路：真 DOM + 真 driver + 真 HTTP（§046）
        ├── import-lib.mjs / import-all.mjs / graft-lib.mjs   匯入與補件
        ├── pack-lib.mjs / unpack-lib.mjs / fix-image.mjs     映像工具
        └── win/                  截圖與鍵盤驅動腳本
```

**產品程式碼 6,613 行，測試 2,756 行（42%，202 條）。**

> 這份地圖與行數是**撰寫當下數出來的**。重數的方法：
> `wc -l webclient/src/js/*.js webclient/src-tauri/src/*.rs …`；
> 測試數 `cd webclient && npm test`（末三行是 `tests / pass / fail`）。

---

## B.2　從哪裡開始讀

| 你的目的 | 讀這幾個檔，照順序 |
|----------|-------------------|
| 理解協議 | `dialects.js` → `protocol.js` → `ansi.js` |
| 改 UI | `index.html` → `ui.js` → `app.css` |
| 改連線行為 | `net.js` → `src-tauri/src/mud.rs` 或 `bridge/server.mjs` |
| 加新 mudlib 支援 | `dialects.js`（加一張表就好） |
| 查為什麼這樣寫 | 每個檔案開頭的區塊註解，都寫了「為什麼」而不只是「做什麼」 |

---

## B.3　絕對不要做的事

| 別做 | 為什麼 |
|------|--------|
| ❌ 在 `protocol.js` / `ansi.js` 裡碰 DOM | 它們是純函式，一碰就失去 100 條免環境測試 |
| ❌ 把所有方言合併成一張 opcode 表 | 602–605 會撞號且**靜默覆蓋**，見 §010 |
| ❌ 用 `element.hidden` 而不確認 CSS | 作者樣式的 `display` 會蓋掉它，見 §015 |
| ❌ 從 WSL 跑 `npm install` | 會裝到 Linux 版原生 binary，見 §017 |
| ❌ 直接雙擊 debug 版 exe | 內嵌的是 dev server URL，見 §017 |
| ❌ 在 `.bat` 的 `REM` 註解裡寫中文 | cmd 預讀會誤拆成指令，見 §017 |
| ❌ 靜默丟棄未知 opcode | 新 mudlib 的內容會憑空消失且無從察覺 |
| ❌ 直接 `innerHTML` 渲染伺服器內容 | 指游方言的 HTML 標記可帶任意標籤，見 §007 |

---

## B.4　改動後的驗證流程

```bash
# 1. 全套測試（約 14 秒）
npm test

# 2. 若改了 UI，看一眼實機畫面
powershell -File tools\win\shot.ps1

# 3. 若改了協議，對真伺服器跑探針
node tools/live-login-probe.mjs <主機IP> 5001 24
```

**第 2 步不可省。** jsdom 不做完整 CSS 級聯，所有涉及顯示／隱藏或版面的改動，純 DOM 斷言都可能通過而畫面是錯的。

---

## B.5　已知未完成

| 項目 | 狀態 | 難點 |
|------|------|------|
| HTML 式行內標記（指游 web 版） | 未實作 | 需白名單標籤與屬性，不可直接 `innerHTML` |
| 語音錄製／播放 | 留介面未實作 | 依賴 AMR 與外部端點，原版 URL 是佔位值 |
| 帳號 HTTP API | 未實作 | 原版端點是 `127.0.0.1` 佔位值；且簽章 salt 明文，需伺服器端重做 |
| 屬性條（`ESC012`）實機驗證 | 未驗證 | 測試角色尚未投胎，伺服器不送這個封包 |
| 官方完整驗證 | 無法實作 | `ZJKEY` 只在伺服器端，第三方客戶端拿不到 |

---

## B.6　三個最容易再犯的錯

**① 把「編譯成功」當成「能用」。**
本專案連續四次交付不能用的東西，每一次都通過了編譯與當時的全部測試。

**② 把「已知限制」當成免責聲明。**
逐條問：「使用者能不能在沒有這個功能的情況下，完成一次完整的核心任務？」——不能的話它就是 bug，不是限制。

**③ 錯誤不可見就開始猜。**
先花二十分鐘把錯誤變得可見，再開始找 bug。本專案的 Tauri ACL 問題，是加了錯誤顯示欄之後**它自己把答案印出來的**。

---

> 💡 君之一席話
> **接手一份逆向專案時，最該先讀的不是程式碼，而是前人踩過的坑——因為程式碼只告訴你「現在長這樣」，坑才告訴你「為什麼不能改成那樣」。**

> 🔍 老手視角──真正的門道
> 這份接手指南裡最有價值的其實是 B.3 那張「絕對不要做的事」——它把八個曾經真實發生的錯誤，壓縮成八條可以在 code review 時直接引用的規則。老手交接專案時會刻意產出這種**負面清單**，因為正面文件（架構圖、API 說明）新人自己讀程式碼也能推出來，而負面知識**只存在於踩過的人腦裡**，不寫下來就永久遺失。可落地的做法：每修好一個花掉你半天以上的 bug，就在負面清單加一行，格式固定為「別做什麼 → 為什麼 → 詳見哪一節」。一年後這張表會比任何架構文件都值錢。

---



# 附錄 C　1:1 還原檢查表

> 這一頁的用途很窄：**假設有人（或某個 LLM）只拿到這本書、拿不到原始碼，要把整個客戶端重建出來。**
> 照下面的順序做，每一步都有一條可機器驗證的收斂條件。

---

## C.1　還原順序（依賴由淺到深）

| 步驟 | 產出 | 依賴 | 規格出處 |
|------|------|------|---------|
| 1 | `css/tokens.css` | 無 | §026.4 |
| 2 | `css/app.css` | tokens | §026.3 §026.5 |
| 3 | `index.html` | css | §026.1 §026.2 |
| 4 | `js/ansi.js` | 無 | §019 |
| 5 | `js/dialects.js` | 無 | §021、附錄 A |
| 6 | `js/protocol.js` | ansi、dialects | §020 |
| 7 | `js/store.js` | 無 | §022 |
| 8 | `js/prefs.js` | 無 | §025 |
| 9 | `js/net.js` | 無 | §027 |
| 10 | `js/ui.js` | ansi、store | §024 |
| 11 | `js/main.js` | 全部 | §023 |
| 12 | `src-tauri/src/telnet.rs` | 無 | §028.6 |
| 13 | `src-tauri/src/mud.rs`、`main.rs` | telnet | §028.1 §028.2 §028.5 |
| 14 | `src-tauri/tauri.conf.json`、`capabilities/default.json` | — | §028.3 §028.4 |
| 15 | `bridge/telnet.mjs`、`bridge/server.mjs` | — | §028.6 §028.7 |

**4–6 是最該先做的三個檔**：它們是純函式，不需要瀏覽器、不需要伺服器，
可以在還沒有任何 UI 的情況下就跑出 100 條以上的測試。

---

## C.2　規模對照

還原完成時，各檔行數應落在下表附近（±15% 內算正常，差一倍代表漏了東西）：

| 檔案 | 行數 | 檔案 | 行數 |
|------|------|------|------|
| `index.html` | 178 | `js/ui.js` | 680 |
| `css/tokens.css` | 121 | `js/prefs.js` | 61 |
| `css/app.css` | 926 | `js/main.js` | 847 |
| `js/ansi.js` | 233 | `src-tauri/src/main.rs` | 55 |
| `js/protocol.js` | 574 | `src-tauri/src/mud.rs` | 200 |
| `js/dialects.js` | 262 | `src-tauri/src/telnet.rs` | 162 |
| `js/store.js` | 208 | `bridge/server.mjs` | 165 |
| `js/net.js` | 242 | `bridge/telnet.mjs` | 63 |

**產品碼 6,613 行；測試 2,756 行（42%）。**

---

## C.3　測試清單（202 條，七層）

| 檔案 | 條數 | 層次 | 驗證什麼 | 對應規格 |
|------|------|------|---------|---------|
| `protocol.test.mjs` | 57 | 純函式 | opcode 解析、樣式方言、變長 opcode | §019 §020 |
| `dialects.test.mjs` | 33 | 純函式 | 三方言 profile、撞號隔離、kind 對映 | §021 |
| `ui-smoke.test.mjs` | 30 | jsdom | 掛載、按鈕綁定、疊層顯隱、登入流程 | §023 §024 §026 |
| `live-capture.test.mjs` | 14 | 實機回歸 | 53 行真實伺服器輸出（含 6 組 IAC） | §018 §028.6 |
| `bridge.test.mjs` | 12 | 整合 | WS 握手、`__state`、允許清單、目錄穿越 | §028.7 |
| `server-fixtures.test.mjs` | 11 | 交叉驗證 | 以伺服器 `.c` 原始碼推出的樣板對照 | §020 |
| `integration.test.mjs` | 8 | 端到端 | 行進入 → 事件 → 狀態 → DOM | §023 |
| `store.test.mjs` | 9 | 純函式 | 雙向 notify、裁切上限、ext API、**notify 遞迴保護** | §022 |
| `net.test.mjs` | 4 | 傳輸層 | **重連風暴**、取代事件、真斷線仍重連、手動關閉 | §027 §028.1 |

Rust 端另有 6 個 `#[test]`，全部落在 `telnet.rs`，輸入是同一份實機位元組。

### C.3.1　★ 四條「不可省」的不變式測試

還原時最容易漏掉、但漏掉就會靜默壞掉的四條：

**① 事件與 reducer 必須全覆蓋**

```javascript
const produced = new Set([...protoSrc.matchAll(/type: '([a-zA-Z.]+)'/g)].map(m => m[1])
  .filter(t => t.includes('.') && !t.endsWith('.')));
const handled  = new Set([...mainSrc.matchAll(/case '([a-zA-Z.]+)':/g)].map(m => m[1]));
assert.deepEqual([...produced].filter(t => !handled.has(t)).sort(), []);
```

> 這條測試在本專案上線前抓到 **8 個真實缺口**（`conn.relogin`、`overlay.story`、
> `overlay.storyClose`、`ui.clearMain`、`ui.hint`、`ui.ignored`、`ui.netStat`、`ui.vibrate`）——
> 全都解析成功、全都掉進 `default: console.warn`，畫面上什麼也不會發生。

**② 撞號必須恰好是那四個**

```javascript
assert.deepEqual(conflictingOpcodes(), ['602', '603', '604', '605']);
```

多出來代表新加的方言表撞到別人；少了代表 profile 被合併了。

**③ 測試替身不可比真實後端寬容**

```javascript
// 假 Tauri 的 mud_connect 必須模擬「先關掉舊連線」這個真實副作用，
// 否則重連風暴類的 bug 在測試裡永遠不會出現（2026-07-29 事故，五萬次登入）。
```

**④ 自動填入的帳密不可被當成同意**

```javascript
// 在登入框塞值但 rememberAccount 為 false → 送出的封包不得含該帳號
```

---

## C.4　逐檔收斂條件

| 檔案 | 做完的判準 |
|------|-----------|
| `ansi.js` | `parseStyled` 對 §019 的每個範例輸出完全相符；`[u:`／`[s:` 在通用「找 m」路徑**之前**處理 |
| `protocol.js` | 附錄 A 的每個 opcode 都有對應分支；變長判別對 3 碼與 4 碼各有一個測試 |
| `dialects.js` | 三個 profile 各自獨立；`lookupExtended` 撞號時回退到當前 profile |
| `store.js` | `notify` 同時往祖先與後代廣播；`msgs.*` 各有裁切上限 |
| `net.js` | 兩個後端實作同一介面；退避序列 `[1,2,4,8,16,30]` 秒、上限 8 次 |
| `ui.js` | 15 個工廠齊備；`scrollToBottom` 用 `scrollTop`；`ctx` 15 個鍵齊備 |
| `main.js` | 不變式測試 ① 通過；自動登入帳密只讀 `prefs` |
| `telnet.rs` | 實機 42 位元組輸入產出 §028.6 表列的 `data` 與 `reply` |
| `app.css` | 檔首第一條規則是 `[hidden] { display: none !important; }` |
| `index.html` | 50 個 id 與 `main.js` 取用的集合雙向差集為空 |

---

## C.5　還原完成後的三步實機驗收

**不可用「測試全綠」代替這三步。** 本專案連續四次交付不能用的東西，每一次都全綠。

```bash
# ① 全套測試（約 20 秒）
npm test                       # 期望：177 pass / 0 fail

# ② 瀏覽器版：起橋接、開頁面、確認能登入並看到房間描述
START_WEB.bat

# ③ 桌面版：build 後執行 release 版（★ 不可雙擊 debug 版 exe）
BUILD_ZJMUD.bat
```

**視覺驗收清單**（截圖比對）：

- [ ] 連線徽章由「未連線」轉為「已連線」
- [ ] 主訊息區出現房間標題與描述，且**有顏色**
- [ ] 左欄出現現場物件與方向盤，方向鍵可點且會移動
- [ ] `[u:cmds:…]` 連結渲染成可點的 `a.lnk`
- [ ] 快捷鍵長按 550 ms 會開編輯
- [ ] 切換三套主題，元件不變形
- [ ] 縮到 560px 寬，版面轉為單欄且不溢出

---

## C.6　還原時最容易掉進去的八個坑

| 坑 | 症狀 | 規格出處 |
|----|------|---------|
| 缺 `[hidden] { … !important }` | 面板永遠關不掉，像是「按鈕沒反應」 | §026.3 |
| 用了 `Element.scrollTo()` | jsdom 下 mount 中斷，連線鈕未綁定 | §024.3 |
| 缺 `capabilities/default.json` | Tauri 版 `event.listen not allowed` | §028.3 |
| 缺 `withGlobalTauri: true` | 桌面版誤判成瀏覽器環境，完全連不上 | §028.4 |
| 用 `read_line` 讀 socket | telnet IAC 位元組混進文字，首行就亂 | §028.5 |
| 三方言合併成單一 opcode 表 | 602–605 靜默覆蓋，某個 mudlib 的四個功能失效 | §021 |
| `connect()` 不擋重入 | **每秒重連一次、對伺服器狂送帳密**（實際發生：50,251 次登入） | §027 |
| Rust 為「被取代的連線」emit closed | 同上，這是風暴的源頭 | §028.2 |

---

> 💡 君之一席話
> **一份規格夠不夠格叫規格，看的不是它寫了多少，而是照著它做出來的東西，能不能用同一份測試證明自己是對的。**

> 🔍 老手視角──真正的門道
> 這份檢查表真正的設計重點在 C.3.1 那三條不變式測試——它們不驗證「功能對不對」，而驗證「**結構有沒有裂開**」。一般測試檢查已知輸入產生已知輸出；不變式測試檢查的是「所有被產生的事件都有人處理」「撞號集合恰好是這四個」這種**跨檔案的全稱命題**，它們在你新增一個 opcode 卻忘了寫 reducer 時當場失敗，而普通測試永遠不會——因為沒人會替一個自己忘了存在的功能寫測試。可落地的做法：任何「A 檔案產生的東西必須被 B 檔案消費」的關係，都值得寫一條用正規表示式掃原始碼的不變式測試。它很粗糙、會有偽陽性，但它抓的是**遺漏**，而遺漏是唯一無法靠寫更多測試補上的缺陷類型。

---



# 附錄 D　MUD 家族年表：1978 – 2026

> 第八、九篇把源流拆在各節裡講。這份附錄把它排成一條時間軸，
> 並在每一格標上**本專案手上有沒有對應的實物**。
>
> 證據等級：🟡 公開史料與上游檔頭 ＋ 🟢 本專案封存中的實物與可重跑量測。
> **凡是能用本倉庫的檔案佐證的，一律標 🟢 並附出處。**

---

## D.1　主幹：從一間大學機房到瀏覽器分頁

| 年 | 事件 | 關鍵人物／出處 | 本專案有實物嗎 |
|---|------|--------------|--------------|
| **1978** | **MUD1** — 第一個多人虛擬世界，英國 Essex 大學。**1978 為起算年；核心完成與交棒給 Bartle 約在 1980**（三個來源三個年份的辨析見附錄 I.1.3） | Roy Trubshaw、Richard Bartle | 🟡 Bartle 為 Duncan Howard《An Introduction to MUD》(1985) 所寫的序（附錄 I.1） |
| 1987 | **AberMUD** — 以 MUD 為靈感重寫，命名自 Aberystwyth | Alan Cox | — |
| **1989-04** | **LPMud** — driver／mudlib 分家、LPC 語言誕生；第一個實作叫 **Genesis**。名字裡的 `cd` 是 **Chalmers Datorförening**，Lars 所屬的電腦社 | **Lars Pensjö**（`lars@cd.chalmers.se`） | 🟢 `Credits.LPmud`（上游封存的一手史料，§033） |
| 1989 | **TinyMUD** — 砍掉戰鬥，主打玩家共建與交談；一個週末寫成 | Jim Aspnes（CMU） | — |
| 1990 | **DikuMUD** — 哥本哈根大學；「打怪—掉寶—升級」的乾淨 C 實作 | 哥本哈根團隊 | — |
| — | LPMud 官方線走到 **2.4.5**（附 Genesis mudlib），再到 3.0／3.1 | Lars Pensjö | — |
| **1991 末** | **Lars 從 Genesis 退休**；其餘管理者從他的 3.0 開發版分出 **CD gamedriver** | Johan Andersson（1992 主力）、Anders Chrigström、Jacob Hallen、Erik Gävert | 🟢 **`cd-gamedriver/doc/CREDITS`** —— 與 `Credits.LPmud` **共用同一段開頭**，然後各自往下長了十年（§065） |
| **1992-08** | **MudOS** 從 3.0.5x 分家並改名；版號自 **0.8** 重新起算 | Erik Kay、Whiplash、Truilkan、Jacques（**TMI**） | 🟢 `History.MudOS`（1992-08-28 `rec.games.mud.lp` 原始貼文，§034） |
| **1993-08** | **TMI-2 mudlib** — MudOS 的參考 mudlib | `Grendel@Tmi-2` | 🟢 **17 份 lib 中 15 份帶著它**，檔頭一字未改（§049、§050） |
| **1993-08** | **DGD 1.0.a3** 首度公開 — 極小核心、完整持久化、原子函式；**概念衍生，非程式碼衍生** | Felix “Dworkin” Croes | — |
| 1994-08 | DGD 1.0.8 首個穩定版 | 同上 | — |
| 1997 | **LDMud** — 接手 LPMud 3.2（Amylaar driver）並現代化；後來借入 DGD 的輕量物件概念 | Lars “Mateese” Düning | — |
| **2010-02** | **DGD 1.4 開源**（AGPL）——比中文 MUD 的黃金期晚了十餘年（§064） | Dworkin B.V. | — |
| 2014-04 / 2017-04 / 2023-01 | DGD 1.5（hotboot、VM v2）／1.6（C++ 重寫）／1.7（VM v2.4、JIT） | 同上 | — |
| 至今 | **FluffOS** — MudOS 的現代化延續：C++、UTF-8／ICU、64 位元、TLS／WebSocket、跨平台 CI | 社群 | 🟢 本專案用的就是它（`v2026.0729.0`） |
| **2026** | FluffOS 官方 **WebAssembly** build — 整台 driver 跑在分頁裡 | 社群 | 🟢 §032、§040–§042 |

---

## D.2　客戶端線：三十年的四件套

| 年 | 客戶端 | 作者 | 帶進來的東西 |
|---|--------|------|-------------|
| 1980s 末 | **TinyFugue** | — | 巨集語言 + 完整觸發系統；主要服務 TinyMUD 系 |
| **1992-04** | **TINTIN** | Peter Unold | 易學的腳本語言；為 DikuMUD 而生 |
| 1992-10 | TINTIN III | Peter Unold | 該線最後一版 |
| **1993-07** | **TinTin++ 1.0** | Bill Reiss | 開源、原本無授權限制——**GGMud／MudMaster／Pueblo 都以它為基礎** |
| **1995-12** | **zMUD 1.0** | Mike Potter | 搬上 Windows 圖形介面；1996-09 起轉共享軟體 |
| — | **MUSHclient** | — | 開源；內建 **MXP** 與 **MCCP** |
| — | **Mudlet** | — | 跨平台、開源；強腳本引擎 |
| **2022-10** | ZJMUD Android 客戶端（1.1 MB APK） | — | 🟢 **本書的起點**（§001–§003） |
| **2026** | 本專案的淨室 Web／桌面／WASM 客戶端 | — | 🟢 第四～七篇 |

**擴充協議世代**（§056）：

| 代 | 協議 | telnet 選項 | 資料在哪 |
|---|------|-----------|---------|
| 0 | ANSI 色碼、mudlib 行內標記、**ZJMUD** | 無 | 文字流內 |
| 1 | **MSP** / **MXP** | 90 / 91 | 文字流內，但先協商 |
| 2 | **ATCP** / **ZMP** | 200 / 93 | 子協商 |
| 3 | **GMCP** / **MSDP** | 201 / 69 | 子協商 + 結構化（JSON／型別化 KV） |

---

## D.2b　英文線：MudOS 社群（第十二篇的年表版）

| 日期 | 事件 | 本專案的關聯 |
|------|------|------------|
| **1992-02** | **TMI（The MUD Institute）開放**——一個「為了開發新 LPMud server 與 mudlib」而開的 MUD，另立章程要教 LPC | §071 |
| **1992-02-18** | **LPMud 3.1.2-A 專案改名 MudOS**（§034 那場改名的精確日期；罵戰在**半年後**才爆發） | §034 §071 |
| **1992-04-23** | **LPC socket 加入 MudOS**；TMI 據此做出第一個「很粗糙的 TCP intermud」 | 🟢 **§049 那整套 `_q`/`_a` 服務的源頭** |
| 1992-06 | Blackthorn 把 Genocide 搬上 MudOS，整個夏天當測試場（Truilkan／Jacques／Wayfarer 在 Portals 上改 code） | §071 |
| 1992-07 | Descartes 接手 Nightmare 的 mudlib，丟掉 2.4.5 改用 MudOS | §072 |
| **1992-08** | **TMI 關站**；**TMI-2 開放**（放棄教學章程） | §071 |
| **1992-12** | **Nightmare Mudlib** 發布——**第一份公開可用的 MudOS mudlib** | §072 |
| **1993-03** | **Discworld Mudlib** 發布——「當時最先進的指令解析器與 UI」 | §072 §074 |
| **1993-04** | **TMI-2 Mudlib** 發布——MudOS 至此有三份廣泛可得的 mudlib | 🟢 **本專案 15/17 份封存的祖先** |
| 1993-04 / 1993-11 | 教科書 **LPC Basics** / **Intermediate LPC** | §072 |
| **1993-09-16** | DGD 1.0.a4 —— **第一個支援 LPC→C 編譯的 driver** | §064 |
| **1993-11** | 「MudOS 正在死去」——Beek 做出 **BeekOS**（MudOS 核心 ＋ LPC→C 動態編譯，機器碼動態連結進執行中的 server） | §073 |
| 1994 初 | Genocide 轉回 LPMud；Blackthorn 停手——**MudOS 開發歸零** | §073 |
| **1994-06-26** | **Beek、Robocoder、Symmetry 接手**——結束將近九個月的無開發狀態 | §073 |
| 1994-08 | Descartes 開設 imaginary.com；Nightmare、Discworld 等陸續進駐 | §072 |
| 1995-05-15 | **Foundation II** —— 第一份為非遊戲用途設計、且可商業授權的 MudOS 函式庫 | §072 |
| 1995-06-15 | **Nightmare IV** —— 完全重寫，清掉一路追溯到 2.4.5 的遺留碼 | §072 |
| 1995-07-21 | **LIMA**（pre-alpha）—— 對標 Infocom／Zork 的指令解析 | §072 |
| 2000s | **FluffOS 從 Discworld 的 driver hacks 分出**（`ChangeLog.fluffos` 第一行：*“As MudOS is moving too slow to keep our driver hacks apart, we now call our own FluffOS :)”*；FluffOS 1.0 署名 **Wodan**，分支點是 **MudOS v22.2b11**） | 🟢 §074；**本專案今天用的就是它** |

> **中文線在 1993 年接走 TMI-2 之後，就與這張表的下半部完全斷開**——
> Foundation II、Nightmare IV、LIMA 在本專案 17 份封存裡**一個痕跡都沒有**（§072）。

---

## D.3　中文線：本專案 17 份封存的來歷

**這一段是本附錄的重點**，因為它有一個少見的性質：
**公開史料、本專案的獨立量測、以及封存檔案裡的署名，三者互相印證。**

| 年 | 事件 | 對應本專案的什麼 |
|---|------|----------------|
| **1993 年底** | **《東方故事》** 開站（交通大學伺服器）。採用 **MudOS ＋ TMI Mudlib**，並修改 MudOS 使其能處理中文。**完全捨棄 TMI mudlib 內附的遊戲內容**，自行創作武俠背景——第一個把中國武俠題材放進網路遊戲的作品 | 🟢 家族 A 的 `dongfanggushi2`（《東方故事Ⅱ》）：仍是 `std/` + `daemon/` + `.lpc` 的 TMI 佈局，設定檔叫 **`config.ES2`**（§050） |
| **1995 年底** | **《俠客行》**（XKX／XAJH）在《東方故事2》基礎上開發，金庸世界觀；**第一款簡體中文聯網遊戲** | 🟢 `Creator : ken@XAJH` 出現在 `LPMud-Name/world/adm/daemons/network/services/remote_{q,a}.c` 的檔頭——**俠客行對 TMI-2 網路層的擴充，簽名還在** |
| **1996-05** | **俠客行程式碼外流**。大量改編版本據此誕生：西遊記、風雲等分支，以及俠客行 100／俠客行 2000／**北大俠客行** | 🟢 **這就是家族 B 那 16 份**：彼此 Jaccard **0.44–0.90**、共用 `inherit/` + `feature/` + `kungfu/` 骨架（§050） |
| 1996 | 北大俠客行架設於北大物理樓 | 🟢 `pkuxkx.net 8080` 出現在 `nitan7` 的 intermud 名錄裡（見 D.4） |
| 1998 年底 | 北大俠客行因管理員畢業而搬遷，一度改名紅豆俠客行，後恢復原名 | — |
| 2000s | 商業化與分支爆炸；ZJMUD 這類**圖形化方言**在此期出現（§056 第 0 代） | 🟢 三個世代的 opcode 方言（第三篇） |
| 2026 | FluffOS 社群的中文 MUD 博物館收錄 **199 份／158 個獨立 codebase**，其中 **193 份**已驗證可在瀏覽器裡跑（2026-08-04 查核；§078 記錄了它從 97 份可玩推進到 193 份的過程） | 🟢 本專案 `libs/` 收 **17 份**（§045） |

### 三份證據的收斂

本專案是**先量測、後查史料**的——§050 那套 Jaccard 分析做完時，
還不知道 1996 年的外流事件。三者對得上，是這條血緣圖最強的支撐：

```
              史料說                    量測看到                  檔案裡寫著
─────────────────────────────────────────────────────────────────────────────
TMI mudlib    「東方故事採用      15/17 份共用 ~170 個檔      Creator : Grendel@Tmi-2
 (1993)        MudOS + TMI」      集中在 daemon/network/     93-08-15
                                  simul_efun 三處
─────────────────────────────────────────────────────────────────────────────
東方故事2     「俠客行在東方       dongfanggushi2 對其他       config.ES2
 (1995 前)      故事2 基礎上」      16 份僅 0.012 —— 另一族     es2.muds.net
─────────────────────────────────────────────────────────────────────────────
俠客行外流    「1996-05 外流 →     16 份彼此 0.44–0.90         Creator : ken@XAJH
 (1996-05)     大量改編版本」       = 同一份 code 的分身        config.xajh / xkxnpc/
─────────────────────────────────────────────────────────────────────────────
```

> **三條獨立的證據線指向同一張血緣圖。**
> 這正是本書第一篇那條規矩的實踐：**史料不能單獨採信，量測不能單獨解釋，
> 而當兩者加上第三份（檔頭署名）都同意時，結論才算釘死。**

---

## D.4　一份意外的史料：intermud 名錄快照

§049 講的那套 1993 年 TMI-2 intermud daemon，會把它見過的節點記在磁碟上。
本專案在 `nitan7` 的封存裡挖到那份目錄——

```
libs/nitan7 → data/daemon/mrtg/mudlist/    共 118 筆
```

**這是中文 MUD 網路的一份現場快照**，而且是那套 daemon 自己寫的。
挑出其中有身分的節點：

| 主機 : 埠 | 是誰 |
|-----------|------|
| `es.muds.net 8000` / **`es2.muds.net 4000`** | **東方故事 / 東方故事Ⅱ** |
| **`pkuxkx.net 8080`** | **北大俠客行** |
| **`hkxkx.com 5000`** | 香港俠客行 |
| `xh.xkx2.com 5555` | 俠客行 2000 系 |
| `china.fengyun.com 6000`、`fy.glqx.com 6666`、`fy4.vmud.com 30004`、`ylfy.91mud.com 4000` | **風雲**系（多個分站） |
| `xyj.thu.cn 6666`、`tel.xyj.cn 6666`、`xyzx.91mud.com 7000`、`xyzx.92mud.com 4000/7000/9000` | **西遊記**系 |
| `jy.mud.com.tw 6665/6666/7776`、`kk.muds.idv.tw 4000`、`220.135.174.1 6665`、`210.59.236.38 4000` | 台灣站（金庸系等） |
| `hell.52mud.com 2008`、`mud.52mud.com 8888`、`mud123.com 3000/5000/5555/7000`、`muds.cn 5000/5555` | 大陸入口／多開站 |
| 其餘 60 餘筆 | 裸 IP + 埠（`222.186.34.42 1002`、`202.100.210.107 5555`…） |

**三個可以直接讀出來的觀察：**

1. **家族樹在名錄裡是可見的**——`es`／`es2`／`xkx`／`fengyun`／`xyj` 同時在線，
   正好是 D.3 那條「東方故事 → 俠客行 → 外流分支」的下游全景。
2. **同一個主機開多個埠是常態**（`mud123.com` 四個埠、`92mud.com` 三個埠、
   `jy.mud.com.tw` 三個埠）——一台機器跑多份 mudlib，
   這與本專案 17 份封存的來源形態一致。
3. **裸 IP 佔一半以上**——沒有網域、沒有 TLS、沒有認證（§049 的罩門）。
   這份名錄本身就是那個時代網路信任模型的切片。

> 🔍 **這份快照對逆向工作的價值**：它是**由系統自己產生的**歷史紀錄，
> 不是任何人事後的整理。本書第一篇的證據等級表裡，
> 這種東西的位階很高——**它沒有敘事動機**。
> 遇到來歷不明的封存時，優先去找這類「系統自己寫下的東西」：
> 名錄、log、快取、備份檔名、swapfile 的檔名（§050 那個
> `swapfile.MudCastle.5555` 就洩漏了原站名與埠號）。

---

## D.5　一句話總結這張年表

```
1978  多人虛擬世界誕生
1989  driver / mudlib 分家 ────────┐  這一刀決定了後面 37 年
1992  MudOS 分支（TMI）            │  ——遊戲住在腳本層，
1993  TMI-2 mudlib ────┐           │    所以三十年後還讀得懂
1993  東方故事（中文化）│           │
1995  俠客行           │ 這條線     │
1996  外流 → 16 份分身 ┘ 是本專案   │
2026  FluffOS WASM ────────────────┘  整台 driver 進了瀏覽器分頁
2026  本專案：17 份封存，17 份可玩
```

**橫跨這 48 年、始終沒有變過的只有一件事：`write()` 出去的是文字。**
本書第二篇那 116 個 opcode、第九篇那四代頻外協議，
全部都是在同一個前提上想辦法——**而那個前提，是 1978 年定下的。**

---



# 附錄 E　FluffOS / LPC 技術速查

> 第八、九篇把 driver 的構造分散在各節解釋。這份附錄把**可查表的部分**集中起來：
> efun 分類、apply 全表、config 全旋鈕、telnet 選項、套件矩陣、WASM 匯出 API。
>
> 證據等級：🟡 上游 `fluffos` 倉庫（`docs/`、`src/net/telnet.cc`、`src/CMakeLists.txt`）
> ＋ 🟢 本專案對 `v2026.0729.0` 的實測。
> **數量全部是撰寫當下數出來的，不是估計。**

---

## E.1　三個呼叫方向（§036 的速查版）

| 方向 | 機制 | 誰定義 | 找不到時 |
|------|------|--------|---------|
| mudlib → driver | **efun** | driver（C++） | 編譯錯誤 |
| driver → mudlib | **apply** | mudlib（LPC） | **靜默略過** |
| mudlib → mudlib | **simul_efun** | mudlib 的單一物件 | 編譯錯誤 |

**記住這一條就夠了：apply 打錯字不會有人告訴你。**

---

## E.2　efun 目錄（451 篇文件，26 類）

| 類別 | 篇數 | 代表 efun |
|------|-----:|-----------|
| `contrib` | 64 | `abs` `base_name` `copy` `debug_message` `assemble_class` `compressedp` |
| `interactive` | 54 | `add_action` `command` `enable_commands` `exec` `ed` `act_mxp` |
| `system` | 33 | `call_out_info` `ctime` `error` `eval_cost` `function_exists` `deep_inherit_list` |
| `objects` | 30 | `clone_object` `destruct` `environment` `file_name` `find_object` `deep_inventory` |
| `general` | 29 | `filter` `explode_reversible` `distance` `compress` `id_matrix` |
| `internals` | 28 | `debug_info` `cache_stats` `check_memory` `dump_jemalloc` `destructed_objects` |
| `strings` | 26 | **`crypt`** `explode` `implode` `sprintf` `capitalize` `hash` |
| `ffi` | 18 | `ffi_load` `ffi_call` `ffi_callback` `ffi_alloc` |
| `mudlib` | 16 | `getuid` `geteuid` `export_uid` `living` `find_living` `author_stats` |
| `floats` | 15 | `sin` `cos` `log` `exp` `floor` `ceil` `floatp` |
| `sockets` | 14 | `socket_create` `socket_bind` `socket_connect` `socket_accept` `socket_write` |
| `filesystem` | 13 | `read_file` `write_file` `get_dir` `mkdir` `rm` `read_bytes` |
| `calls` | 12 | **`call_other`** **`call_out`** `catch` `previous_object` `origin` `remove_call_out` |
| `parsing` | 12 | **`parse_command`** `parse_add_rule` `parse_init` `parse_sentence` |
| `arrays` | 10 | `allocate` `member_array` `sort_array` `filter_array` `map_array` `shuffle` |
| `mappings` | 9 | `allocate_mapping` `keys` `values` `map_delete` `filter_mapping` |
| `pcre` | 8 | `pcre_match` `pcre_replace` `pcre_extract` `pcre_match_all` |
| `buffers` | 7 | `allocate_buffer` `read_buffer` `write_buffer` `buffer_transcode` `crc32` |
| `db` | 7 | `db_connect` `db_exec` `db_fetch` `db_commit` `db_rollback` |
| `async` | 4 | `async_read` `async_write` `async_getdir` `async_db_exec` |
| `functions` | 4 | `bind` `defer` `evaluate` `functionp` |
| `numbers` | 4 | `random` `secure_random` `intp` `to_float` |
| `ed` | 3 | `ed_start` `ed_cmd` `query_ed_mode` |
| **`jsbridge`** | 3 | **`js_eval` `js_call` `js_export`**（**僅 WASM**，§042） |
| `crypto` | 1 | `hash` |
| `external` | 1 | `external_start` |

> ⚠️ **`crypt()` 在 `strings` 而不是 `crypto`。** 這件事救了本專案 17 份 mudlib——
> 關掉 crypto 套件不會讓登入流程消失（§041、§044）。
> **efun 住在哪個套件，比它叫什麼名字重要。**

---

## E.3　apply 全表（69 個）

### E.3.1　object applies（11 個）— 每個物件的生命週期

| apply | 何時被呼叫 |
|-------|-----------|
| `__INIT` | 編譯器產生，初始化有初值的變數 |
| **`create`** | 物件建立時（相當於建構子；**LPC 沒有 `main()`**） |
| **`init`** | 每次有 living 物件進入時（典型用途：`add_action`） |
| **`reset`** | 週期性（預設 900 秒）——重生怪物、補貨 |
| **`heart_beat`** | 週期性（預設 1000 ms），**要 `set_heart_beat(1)` 才會跑** |
| `clean_up` | 長時間沒人碰（預設 600 秒） |
| `id` | 「這個名字指的是我嗎」 |
| `is_living` | 是不是生物 |
| `move_or_destruct` | 環境被摧毀時：跟著死還是搬家 |
| `on_destruct` | 被摧毀時的收尾 |
| `virtual_start` | virtual object 的起始 |

### E.3.2　master applies（37 個）— 全域事件、安全與編譯決策

| 分組 | apply |
|------|-------|
| **連線** | **`connect`**（§044）、`epilog`、`preload`、`crash`、`flag` |
| **編譯** | `compile_object`（virtual object）、**`include_file`**、**`inherit_program`**（熱重載相依圖，§038）、`get_include_path`、`make_path_absolute` |
| **錯誤** | `error_handler`、`log_error`、`parser_error_message` |
| **身分** | `creator_file`、`author_file`、`domain_file`、`privs_file`、`get_root_uid`、`get_bb_uid`、`object_name`、`get_save_file_name` |
| **安全（`valid_*`，14 個）** | `valid_read` `valid_write` `valid_object` `valid_shadow` `valid_override` `valid_seteuid` `valid_socket` `valid_bind` `valid_link` `valid_hide` `valid_database` `valid_ffi` `valid_save_binary` |
| **其他** | `get_mud_stats`、`retrieve_ed_setup`、`save_ed_setup` |

> 🟢 §032 那個「建角時 `Denied write permission`」的坑就在 `valid_write`：
> 找不到 SECURITY_D 就一律拒絕（fail-closed），
> 而**同一個檔案裡的 `valid_read` 是 fail-open**——兩個相反的預設。

### E.3.3　interactive applies（21 個）— 一條連線的網路事件

| 分組 | apply |
|------|-------|
| **登入／斷線** | **`logon`**（§044）、`net_dead` |
| **輸入** | `process_input`、`write_prompt`、`receive_ed` |
| **輸出** | `catch_tell`、`receive_message`、`receive_snoop`、`terminal_colour_replace` |
| **telnet 協商** | `telnet_suboption`、`terminal_type`、`window_size`、`receive_environ` |
| **擴充協議（§056）** | `gmcp` `gmcp_enable` `msdp` `msdp_enable` `msp_enable` `mxp_enable` `mxp_tag` `zmp` |

> **最後一列就是「driver 層協議」與「mudlib 層協議」的分界線**（§043）：
> GMCP／MSDP／MXP／ZMP 有 apply，driver 知情；
> **ZJMUD 沒有——driver 從頭到尾不知道它存在。**

---

## E.4　config 旋鈕（§039 的完整版）

### E.4.1　必填與核心

| 設定 | 說明 |
|------|------|
| `name` | **必填**。這台 mud 的名字 |
| `mudlib directory` | **必填**。mudlib 根目錄（**唯一不相對於 mudlib 的路徑**） |
| `log directory` | **必填**。debug.log 與統計檔的位置 |
| `include directories` | **必填**。`#include <...>` 的搜尋路徑，冒號分隔 |
| `master file` | **必填**。master 物件（弄錯就完全開不了機） |
| `simulated efun file` | simul_efun 物件 |
| `global include file` | **每個檔案自動 include** ——「這個巨集哪來的」通常在這 |
| `debug log file` | log 檔名 |
| `mud ip` | 多 IP 主機上要綁哪一個 |
| `websocket http dir` | WebSocket 埠兼靜態網頁根目錄 |

### E.4.2　時序與生命週期

| 設定 | 預設 |
|------|-----:|
| `gametick msec` | 1000 |
| `heartbeat interval msec` | 1000 |
| `time to reset` | 900 |
| `time to clean up` | 600 |
| `time to swap` | 300（0 = 停用） |

### E.4.3　限制（沙盒，§037）

| 設定 | 預設 |
|------|-----:|
| `maximum evaluation cost` | 30,000,000 |
| `maximum call depth` | 150 |
| `evaluator stack size` | 65,536 |
| `inherit chain size` | 30 |
| `maximum local variables` | 64（範圍 64–255） |
| `maximum array size` | 15,000 |
| `maximum mapping size` | 150,000 |
| `maximum string length` | 1,048,576 位元組 |
| `maximum buffer size` | 1,048,576 |
| `maximum bits in a bitfield` | 12,000 |
| `maximum byte transfer` | 262,144 |
| `maximum read file size` | 262,144 |

### E.4.4　雜湊表（調校用）

| 設定 | 預設 | 建議值 |
|------|-----:|--------|
| `hash table size` | 65,536 | 相異字串數的 **1/5**，且應為質數 |
| `object table size` | 4,096 | 物件數的 **1/4**（最小 1024） |
| `living hash table size` | 256 | **只能是 4/16/64/256/1024/4096** |

### E.4.5　語意開關（相容性層的真面目，§039）

| 設定 | 預設 | 意義 |
|------|-----:|------|
| `sane explode string` | 1 | `explode()` 最多剝一個前導分隔符 |
| `reversible explode string` | 0 | 讓 `implode(explode(x,y),y) == x`；覆蓋上一項 |
| `sane sorting` | 1 | 排序有明確且穩定的順序 |
| `old range behavior` | 0 | 負索引從尾端算（**舊行為**） |
| `warn old range behavior` | 1 | 用到舊行為就警告 |
| `this_player in call_out` | 1 | `call_out` 回呼裡 `this_player()` 可用 |
| `enable_commands call init` | 1 | `enable_commands()` 時呼叫 `init()` |
| `reverse defer` | 0 | `defer()` 反序執行 |
| `sprintf add_justified ignore ANSI colors` | 1 | 對齊時忽略 ANSI 色碼的寬度 |
| `call_out(0) nest level` | 1000 | `call_out(0)` 連鎖的巢狀上限 |
| `call other type check` / `call other warn` / `old type behavior` | 0 / 0 / 0 | `->` 的型別檢查強度 |

> **`old range behavior` 與 `old type behavior` 這種旗標的存在本身就是史料**——
> driver 知道自己以前的行為是錯的，但不敢直接改，只能給一個開關（§035）。

### E.4.6　reset 行為／診斷／玩家 I/O

| 設定 | 預設 |
|------|-----:|
| `no resets` / `lazy resets` / `randomized resets` | 0 / 0 / **1** |
| `no ansi`（把輸入裡的 ESC 換成空白） | **1** |
| `strip before process input` | 1 |
| `interactive catch tell` | 0 |
| `receive snoop` / `snoop shadowed` | 1 / 0 |
| `trace` / `trace code` | 1 / 0 |
| `display preload progress` | 1 |
| `mudlib error handler` / `trap crashes` | 1 / 1 |

### E.4.7　★ 協議開關 — 直接決定線路上的協商位元組

| 設定 | 預設 | 對應 telnet 選項 |
|------|-----:|----------------|
| `enable mssp` | **1** | 70 |
| `enable msp` | **1** | 90 |
| `enable mxp` | 0 | 91 |
| `enable gmcp` | 0 | 201 |
| `enable zmp` | 0 | 93 |
| `enable msdp` | 0 | 69 |

> 🟢 **本專案實測驗證了這張表**：WASM driver 的首包裡出現了
> `IAC WILL 70`（MSSP）與 `IAC WILL 90`（MSP）——**兩個預設為 1 的**；
> 而 MXP／GMCP／ZMP／MSDP **一個都沒出現**——**四個預設為 0 的**（§055）。
> **設定檔的預設值，在連線的第 10 個位元組上就看得見。**

### E.4.8　埠與安全

```ini
external_port_1 : telnet 5001          # 型別：telnet / websocket / ascii / binary
external_port_2 : telnet 5003
external_port_3 : websocket 5004
# external_port_2_tls : cert=cert.crt key=cert.key
```

| 安全設定 | 說明 |
|---------|------|
| `ffi allowed libraries` | `ffi_load()` 可開啟的 .so 白名單（空 = 全交給 `valid_ffi` apply） |
| `allowed os environment variables` | `get_os_env()` 可讀的環境變數白名單（**空 = 全部拒絕**） |
| `writable os environment variables` | `set_os_env()` 可寫的白名單（**空 = 全部拒絕**） |

> ⚠️ **格式陷阱（§039，本專案最貴的一次錯誤）**：
> **只有整行 `#` 註解，沒有行尾註解**——值取的是冒號後**整行**。

---

## E.5　telnet 選項速查（§055）

| 編號 | 名稱 | 用途 | FluffOS 立場 | 🟢 出現在實測首包？ |
|-----:|------|------|-------------|------------------|
| 0 | BINARY | 8-bit 透傳 | WILL / DO | — |
| 1 | ECHO | **回顯控制（密碼遮蔽）** | WILL / DO | — |
| 3 | SGA | Suppress Go Ahead——**字元模式訊號** | WILL / DO | — |
| 6 | TM | Timing Mark | WILL / DO | — |
| **24** | **TTYPE** | 終端機型別 | WONT / **DO** | ✅ `ff fd 18` |
| **31** | **NAWS** | 視窗大小（改變時重報） | WILL / DO | ✅ `ff fd 1f` |
| 34 | LINEMODE | 行模式 | WONT / DO | — |
| **39** | **NEW-ENVIRON** | 環境變數 | WONT / **DO** | ✅ `ff fd 27` |
| **42** | **CHARSET** | 字集協商 | WILL / DO | ✅ `ff fb 2a` |
| 69 | MSDP | 頻外型別化 KV | WILL / DO | ❌（預設關） |
| **70** | **MSSP** | MUD 伺服器狀態 | WILL / DO | ✅ `ff fb 46` |
| 85 | COMPRESS | MCCP v1 | — | — |
| **86** | **COMPRESS2** | **MCCP2 壓縮** | WILL / DO | ⚠️ **只有原生版**（`ff fb 56`）——WASM 版 zlib 被移除（§041） |
| **90** | **MSP** | 音效 | WILL / DO | ✅ `ff fb 5a` |
| 91 | MXP | 行內標記 | WILL / DO | ❌（預設關） |
| 93 | ZMP | Zenith MUD Protocol | WILL / DO | ❌（預設關） |
| 200 | ATCP | Achaea Telnet Client Protocol | — | — |
| 201 | GMCP | 頻外 JSON | WILL / DO | ❌（預設關） |

**協商框架**：

```
IAC WILL <opt>   我想啟用（我這邊做）      IAC WONT <opt>   我不做
IAC DO   <opt>   請你啟用（你那邊做）      IAC DONT <opt>   請你別做
IAC SB <opt> …資料… IAC SE                子協商資料通道
IAC IAC                                   跳脫的 0xFF
```

**四個性質**：對稱、雙向獨立（`WILL` 與 `DO` 各談各的）、
預設關閉、**拒絕是合法且無副作用的**——最後一條就是本專案「一律拒絕」策略成立的理由。

---

## E.6　WebAssembly 平台（§040–§042）

### E.6.1　匯出 API

| 匯出 | 簽名 | 對應原生的什麼 |
|------|------|--------------|
| `fluffos_boot` | `(string config) → int` | `main()` 讀設定、載入 master、preload |
| `fluffos_tick` | `(number now_ms) → int` | libevent 的一圈迴圈 |
| `fluffos_connect` | `() → int`（連線 id，負值 = 失敗） | accept 一條連線 |
| `fluffos_input` | `(int id, array bytes, int n)` | 一次 socket read |
| `fluffos_disconnect` | `(int id)` | 關一條連線 |
| `fluffos_shutdown` | `()` | 關機 |
| `fluffos_flag` | `(string)` | master 的 `flag()` apply |
| **回呼** `Module.fluffos.onOutput` | `(id, bytes)` | 一次 socket write |
| **回呼** `Module.fluffos.onDisconnect` | `(id)` | 對端消失 |

### E.6.2　三條必守規則（🟢 本專案各踩過一次）

| # | 規則 | 違反的症狀 |
|---|------|-----------|
| 1 | **絕不在 `onOutput` 裡呼叫 `fluffos_input`**——一律排隊、只在 tick 的堆疊上送 | 伺服器只回 2 行就卡住 |
| 2 | **不能用 `connId === null` 當「尚未連線」**——`fluffos_connect()` 會在回傳前就同步回呼 | 第一次回覆被靜默丟棄（`rawSend` 呼叫次數 0） |
| 3 | **`fluffos_tick` 要餵「經過多久」不是「現在幾點」**——開機當下記原點、之後送差值 | `call_out(…, 30)` 第一拍就到期（「您花在连线进入手续的时间太久了」） |

### E.6.3　套件矩陣

| 開著 | 關掉 | 僅 WASM |
|------|------|--------|
| core、ops、math、matrix、trim、uids、sha1、parser、contrib、develop、mudlib_stats、**pcre** | **sockets**、compress、external、async、**db**、crypto、ffi | **jsbridge** |

**第三方函式庫**：移除 libevent／libwebsockets／OpenSSL／zlib／jemalloc／backward-cpp；
**保留** libtelnet（頁面講真 telnet）、ICU、libpcre、musl crypt。

### E.6.4　已知落差

| 落差 | 症狀 |
|------|------|
| **沒有 eval limit** | LPC 的 `while(1);` **卡死整個分頁**（POSIX 計時器只支援 Linux） |
| **ICU 只剩演算法式字集** | 資料檔由 ~30 MB 裁到 ~780 KB（只留 brkitr）→ **GBK／Big5／Shift-JIS 消失**，`set_encoding("GBK")` 會 raise error |
| 沒有真 DNS | `query_ip_number()` 一律 `127.0.0.1` |
| 沒有 zlib | 壓縮 `write_file`(flag 2) 報錯；壓縮 `save_object` 退化為純文字 |
| MEMFS 不落地 | 重整即失 |
| 背景分頁 | 計時器被暫停，醒來補跑 gametick，**上限 100 拍** |

**體積**：`fluffos.wasm` ~3.6 MB 原始 / **~0.8 MB brotli**（~1.1 MB gzip）+ ~110 KB JS glue。

---

## E.7　VM 指令格式（§037）

| 指令 | 格式 | 語意 |
|------|------|------|
| `F_FUNCTION` | `b0, b1b2=函式編號, b3=引數個數` | 呼叫本物件函式；初始化 frame pointer；**自動調整引數個數**（不足補 0、過多彈掉） |
| `F_RETURN` | `b0` | 釋放整個 frame，只留堆疊頂端一值 |
| `F_CALL_OTHER` | `b1, b2=引數個數` | 呼叫其他物件（`ob->fn()`） |
| `F_AGGREGATE` | `b1, b2b3=陣列大小`（最大 0xffff） | 從堆疊頂端取 N 個組成陣列 |
| `F_CATCH` | `F_CATCH, b1b2=之後的位址` | `setjmp()` + **遞迴呼叫求值器**；`F_THROW` 做 `longjmp()` |
| `F_SSCANF` | 引數個數為單一位元組 | 前兩引數傳值，其餘用 **`T_LVALUE`** 傳參考 |

**兩個堆疊**：值堆疊（`struct svalue`，參數即區域變數，靠 frame pointer 索引）
與控制堆疊（返回位址、前一個 fp、參數／區域變數個數、`extern_call` 旗標）。

**記憶體**：沒有 `malloc`／`free`；`allocate(n)` 的單位是**元素**；回收靠**引用計數**
（沒有 GC 停頓，但**有循環引用洩漏**）。

---

## E.8　LPC 與 C 的差異速查

| 面向 | C | LPC |
|------|---|-----|
| 進入點 | `main()` | **`create()`**（apply） |
| 記憶體 | `malloc`/`free` | **無**；`allocate(元素數)`；引用計數回收 |
| 字串 | `char*` + `strcpy`/`strcat` | **內建型別**，`+` 直接串接（近 BASIC） |
| 結構 | `struct` / `union` | **`class`**（無 union）；或用 mapping |
| `->` | 結構成員 | **兩種用途**：`call_other()` **與** class 成員 |
| `sscanf("%s %s")` | 兩個「詞」 | **第一個詞 + 剩下全部**（⚠️ 最常見的移植地雷） |
| 指標 | 有 | 無（`ref`/`&` 傳參考） |
| 編譯 | 機器碼 | **位元組碼 + 解譯**，可熱重載 |
| 額外型別 | — | `object` `mapping` `mixed` `function` `buffer` |
| 現代語法糖 | — | mapping 的 `m.key`、可選鏈 `m?.key?.deep`（讀取用） |

---

## E.9　本專案用到的版本

| 項目 | 值 |
|------|---|
| FluffOS driver | **`v2026.0729.0`**（官方 wasm release，2.4 MB zip） |
| 映像格式 | 自訂（`webclient/src/js/mudlibimage.js`，`format: 1`） |
| 收錄 mudlib | **17 份**（另有 1 份匯入中） |
| 客戶端測試 | **202 條，12 個檔案，全過** |

> 取得 driver：`node webclient/tools/fetch-driver.mjs`
> 打包全部 mud：`node webclient/tools/build-site.mjs`
> 全鏈路驗證：`node webclient/tools/verify-fullstack.mjs`

---



# 附錄 F　17 份 mudlib：血緣、落差與實測數據

> 第九篇 §050 用檔名集合量出兩個家族。這份附錄把**每一份封存的實測數據**攤開，
> 並附上量測方法，讓任何人都能重跑一次。
>
> 證據等級：🟢 **全部由 `boot-test.mjs` 實跑產生**（driver `v2026.0729.0`，
> 最後測試 2026-08-01），不是人工標註。
> ⚠️ **重要限制先講**：opcode 欄位記的是**那一次開機測試中觀察到的**碼，
> 不是該 lib 能送的全部——測試只走「註冊 → 建角 → 進世界 → look」。

---

## F.1　總表

| slug | 標題 | 分級 | 映像 | 方言 | 握手 | 觀察到的 opcode |
|------|------|------|------|------|------|----------------|
| `lpmudname` | 江湖论剑 | ✅ playable | 12,051 檔 / 31.5 MB | dmjh | `ver1.0:byz0rmpISExtQ` | 000 006 021 |
| `91shujian` | 91 书剑 | ✅ | 11,389 / 29.2 MB | dmjh | `ver1.0,$6$…` | 000 002 003 004 005 006 012 014 021 |
| `damengjianghu` | 大梦江湖（新协议版） | ✅ | 9,356 / 14.7 MB | （未判定） | **不送版本挑戰** | 000 002 003 004 005 014 **111 234 517 604 605** |
| `dongfanggushi2` | **东方故事Ⅱ** | ✅ | **562 / 0.9 MB** | **telnet** | **無 zjmud 握手** | **（無）** |
| `duobaojianghu` | 夺宝江湖 | ✅ | 2,743 / 4.4 MB | dmjh | `ver1.0,$6$…` | 000 002 003 004 005 006 012 021 |
| `jianghufengyuqing` | 江湖风雨情 | ✅ | 9,086 / 14.0 MB | dmjh | `ver1.0,$6$…` | 000 002 003 004 005 006 012 021 |
| `jianjuefuyunqi` | 剑诀浮云气 | ✅ | 11,071 / 17.8 MB | dmjh | `ver1.0,$6$…` | 000 002 003 004 005 006 012 021 022 100 |
| `jianyingjianghu` | 剑影江湖 | ✅ | 10,248 / 15.9 MB | dmjh | `ver1.0,$6$…` | 000 002 003 004 005 006 014 021 022 100 |
| `nitan7` | 泥潭七（去后门） | ✅ | **29,009 / 85.0 MB** | dmjh | **`ver1.0,123456789abcd`** | 000 |
| `nt7` | nt7 | ⚠️ **limited** | 29,288 / 84.4 MB | **telnet** | 無 zjmud 握手 | （無） |
| `shuiyuzhengfeng` | 谁与争锋（原版） | ✅ | 8,344 / 12.2 MB | dmjh | `ver1.0,$6$…` | 000 002 003 004 005 006 012 021 |
| `siheyi` | 四合一修复版 | ✅ | 17,236 / 29.0 MB | dmjh | `ver1.0,$6$…` | 000 002 003 004 005 006 012 021 022 |
| `tianya2` | 天涯二 | ✅ | 9,827 / 15.6 MB | dmjh | **`ver1.0,123456789abcd`** | 000 002 003 004 005 006 012 021 022 |
| `wenzijianghu` | 文字江湖 | ✅ | 9,510 / 14.2 MB | dmjh | `ver1.0,$6$…` | 000 002 003 004 005 006 012 021 |
| `xianwuqiyuan` | 仙武奇缘 | ✅ | 10,237 / 19.0 MB | dmjh | **`ver1.0,123456789abcd`** | 000 002 003 004 005 006 014 022 |
| `xiaoaojianghu` | 笑傲江湖（海洋二） | ✅ | **30,621 / 72.2 MB** | dmjh | **`ver1.0,123456789abcd`** | 000 002 004 005 006 014 100 |
| `zhijianxing` | 执剑行 7.0 | ✅ | 8,871 / 13.3 MB | dmjh | `ver1.0,$6$…` | 000 002 003 004 005 006 012 021 022 100 |
| `zhongjidiyu` | 终极地狱 | ✅ | 13,978 / 28.8 MB | dmjh | `ver1.0,$6$…` | 000 002 003 004 005 006 012 021 022 |

**17 份收錄、17 份 playable**（`nt7` 為第 18 份，匯入中，目前 limited）。

---

## F.2　★ 握手的三個世代（本表最有價值的一欄）

把「握手」欄排一下，會看到一個**協議演化的橫切面**——
這是 §044 逐行讀 `logind.c` 時看不到的東西，**只有橫向比對 17 份封存才會浮現**：

| 世代 | 握手行的形狀 | 幾份 | 意義 |
|------|------------|-----:|------|
| **① 明文** | **`ver1.0,123456789abcd`** | **4** | **`ZJKEY` 直接送上線路，完全沒有雜湊** |
| ② 舊 DES crypt | `ver1.0:byz0rmpISExtQ`（13 字元） | 1 | `crypt(ZJKEY, salt)` 的傳統輸出 |
| ③ SHA-512 crypt | `ver1.0,$6$<16字元salt>$<86字元>` | 10 | 現代 `crypt()` 的 `$6$` 格式 |
| ④ 無 | 不送版本挑戰 / 純 telnet | 3 | `damengjianghu`、`dongfanggushi2`、`nt7` |

**三個可以直接讀出來的結論：**

1. **分隔符從 `:` 變成 `,`。** 世代 ② 用 `ver1.0:`，世代 ①③ 用 `ver1.0,`。
   本專案的客戶端因此把握手行的比對寫成 `/^ver1\.0[,:]/`——
   **那個字元集不是防禦性寫法，是量出來的**（§044、`telnetlogin.js`）。
2. **世代 ① 把金鑰明文送出。** `123456789abcd` 就是 `zjmud.h` 裡的 `ZJKEY` 常數。
   有 4 份 lib 的 `logind` 根本沒呼叫 `crypt()`，直接 `write("ver1.0,"+ZJKEY)`。
   **而這完全不影響登入**——因為 §044 證明了 `jiance()` 根本不驗證回應。
   **一個不驗證的挑戰，送明文或送雜湊沒有任何差別**，
   這也解釋了為什麼會有人把它簡化掉。
3. **`crypt()` 的輸出格式隨系統而變。** 同一行 `crypt(ZJKEY,"byname666")`，
   在舊系統上得到 13 字元的 DES 輸出，在現代 glibc 上得到 `$6$` 的 SHA-512 輸出。
   **同一份原始碼、不同的線路位元組**——
   這正是本書「證據等級」要把實機封包排在原始碼之前的理由（§01）。

> 💡 **這一節是本附錄存在的理由**：單看一份封存，握手是一個固定格式；
> 橫看 17 份，它是一條有世代、有分歧、有退化的演化線。
> **量測的價值往往不在單點，在橫切面。**

---

## F.3　opcode 的實測分佈

16 份會送 zjmud opcode 的 lib，在同一段流程（註冊 → 建角 → 進世界 → look）中觀察到的碼：

| opcode | 幾份送出 | 分佈 | 語意（附錄 A） |
|--------|--------:|------|--------------|
| `000` | **16/16** | ████████████████ | 登入／連線狀態 |
| `002` | 14/16 | ██████████████ | 房間標題（**有清空副作用**） |
| `004` | 14/16 | ██████████████ | 房間描述 |
| `005` | 14/16 | ██████████████ | 房間物件 |
| `006` | 14/16 | ██████████████ | 自訂按鈕 |
| `003` | 13/16 | █████████████ | 出口（**§052：由 `set("exits/…")` 觸發**） |
| `021` | 12/16 | ████████████ | 標題按鈕 |
| `012` | 10/16 | ██████████ | 屬性條群 |
| `022` | 7/16 | ███████ | 血條更新 |
| `014` | 5/16 | █████ | 代送指令 |
| `100` | 4/16 | ████ | 聊天 |
| `111` `234` `517` `604` `605` | 各 1/16 | █ | **全部來自 `damengjianghu` 一家**——方言擴充碼 |

**三個結論：**

1. **交集只有 `000`。** 嚴格說來，「所有 lib 都會送」的只有登入狀態一個碼。
   這不代表其他碼不通用，而是**觸發條件不同**（有些 lib 進世界時不送屬性條）。
2. **但 `002/003/004/005/006/021` 這六個在 12–14 份出現**——
   **這就是本書 §005 說的「最大公因數」在實測上的樣子**：
   房間標題、出口、描述、物件、按鈕、標題列，
   撐起主畫面的那一組碼，跨方言高度一致。
3. **長尾全部集中在單一 lib。** 5 個只出現一次的碼全來自 `damengjianghu`（新協議版）——
   §009「定義 ≠ 使用」與 §010「撞號」的現象，在這張表上是可見的：
   **擴充碼幾乎不跨 lib 流通。**

> ⚠️ **這張表的已知偏差**（誠實揭露）：
> ① 只涵蓋一段很短的流程，戰鬥、交易、對話類的碼不會出現；
> ② §052 發現有些碼由**資料寫入**觸發（`set("exits/…")` → `ESC003`），
> 這類碼的出現與否取決於該 lib 的起始房間有沒有動到那些屬性——
> **所以 `003` 的 13/16 不代表有 3 份不支援出口，只代表那次沒觸發。**

---

## F.4　WASM 落差：所有 lib 的共同缺口

| 缺口 | 影響幾份 | 缺的 efun | 後果 |
|------|--------:|-----------|------|
| **`package sockets` 關閉** | **14 份**（各 1–3 個物件載入失敗） | `socket_create`、`socket_bind`、`socket_close`、`socket_write` | **§049 那整套 intermud 不存在**（跨服聊天、mudlist、gtell） |
| **ICU 無表格式字集** | 全部 | — | 匯入時必須把整份 lib 轉成 UTF-8（見 F.5） |
| 無 eval limit | 全部 | — | LPC 無窮迴圈會卡死分頁 |
| MEMFS 不落地 | 全部 | — | 重整即失 |

**載入失敗最多的是 `lpmudname`（3 個）**：
`/adm/daemons/kuafu`（跨服）、`/adm/daemons/qqd`、`/adm/daemons/miraid`。

> **driver 照常完成開機。** preload 清單裡的項目載入失敗只會讓那個物件不存在，
> 不會中止啟動（§038、§041）——這正是 `playable` 與 `noboot` 的分界：
> **看的是「進不進得了世界」，不是「有沒有錯誤訊息」。**
>
> 這些 daemon **不打算修**：它們只在有對外網路時才有意義，
> 而 WASM 版整個沒有網路——**修了也沒有對象可連**（§049）。

---

## F.5　匯入時做了什麼（可重跑）

| 處理 | 規模 | 為什麼 |
|------|------|--------|
| **GBK → UTF-8 轉檔** | **最多單一 lib 29,853 檔**（`xiaoaojianghu`）；14 份 lib 合計逾 **17 萬檔** | WASM 版 ICU 只帶演算法式字集，GBK 會 raise error（§041） |
| `set_encoding("非UTF")` 改寫 | 逐 lib 1–2 處 | 同上 |
| **移除玩家存檔** | **`nt7` 一份就 630 個含密碼欄的 `.o`** | 原營運者的真實玩家資料，對展示站無用途，**留著只是外洩風險** |
| 憑證掃描 | 全部 | 見倉庫 `SECURITY_NOTES.md` |
| 略過檔案 | 執行檔、備份、日誌、`OBJ_DUMP` | 映像體積與安全 |

**全部由 `webclient/tools/import-lib.mjs` 產生，改動皆可重跑複現**——
每一份 `libs/<slug>/NOTES.md` 就是該次匯入的完整紀錄。

---

## F.6　怎麼重跑這些量測

```bash
# ① 血緣（§050 的 Jaccard 矩陣）——只讀索引，不必解開映像
python3 - <<'EOF'
import json, glob, itertools
S = {p.split('/')[1]: {f['path'] for f in json.load(open(p, encoding='utf-8'))['files']}
     for p in sorted(glob.glob('libs/*/mudlib.json'))}
for a, b in itertools.combinations(S, 2):
    j = len(S[a] & S[b]) / len(S[a] | S[b])
    if j >= 0.30: print(f'{j:.2f}  {a} ↔ {b}')
EOF

# ② 開機測試（分級、opcode、握手、載入失敗）
node webclient/tools/boot-test.mjs            # 會把結果寫回各 lib 的 NOTES.md

# ③ 全鏈路（真 DOM + 真 wasm driver + 真 HTTP）
node webclient/tools/verify-fullstack.mjs

# ④ 看某個 lib 的原始 LPC
node webclient/tools/unpack-lib.mjs libs/<slug> --out /tmp/<slug>
```

> 🔍 **本附錄的方法論價值**：這五張表沒有一張是人工整理的——
> 全部由腳本產生、可重跑、會隨 driver 升版自己改變。
> 本書 §041 說過的那句話在這裡是字面意義的：
> **落差表不是文件，是測試結果。**
> 一份會自己過期並自己更新的清單，比一份寫得再漂亮的靜態文件可信。

---



# 附錄 G　全書單元索引 ＋ 引用結構

> 本書有 **14 篇、82 個單元**，合併版約 600 KB。
> 這份附錄是導航用的：**一頁看完全部單元**，
> 外加一份本書自己的**引用結構量測**——哪些單元被引用最多、哪些從未被引用。
>
> 證據等級：🟢 由腳本掃描全書產生（重跑指令附於 G.4），非人工整理。

---

## G.1　全書單元一覽

### 前置

| 檔案 | 內容 |
|------|------|
| `00_序言.md` | 當一份協議只剩下它的影子 |
| `01_如何閱讀本書_三個面向.md` | **證據等級的判讀學**（先讀這篇） |

### 第一篇　逆向工程

| # | 單元 |
|---|------|
| 001 | 選對標的 — 三個 APK 裡，只有一個值得拆 |
| 002 | 字串常數 — 三十分鐘看穿一套協議的骨架 |
| 003 | 三方交叉驗證 — 這個專案能收斂的唯一原因 |

### 第二篇　協議解剖

| # | 單元 |
|---|------|
| 004 | 三層結構 — 一句話說完 ZJMUD 是什麼 |
| 005 | 行級 opcode — 33 個碼撐起整個介面 |
| 006 | telnet 那一層 — 差點讓連線在第一個位元組就死掉 |
| 007 | ANSI 擴充方言 — 那個要人命的冒號 |
| 008 | 登入握手 — 三種寫法，三種欄位數 |

### 第三篇　方言與相容性

| # | 單元 |
|---|------|
| 009 | 定義不等於使用 — 一份 header 騙走你兩天 |
| 010 | 撞號 — 為什麼不能把所有方言合併成一張表 |
| 011 | 變長 opcode — 一個 `1085` 逼出的消歧義規則 |

### 第四篇　客戶端架構

| # | 單元 |
|---|------|
| 012 | 把協議關進純函式 — 原客戶端最大的架構債 |
| 013 | 雙形態 — 同一份前端，兩條傳輸路徑 |
| 014 | 失敗隔離 — 一個 `scrollTo` 讓整個客戶端變成擺設 |

### 第五篇　工程實務

| # | 單元 |
|---|------|
| 015 | 七次事故 — 同一個症狀，七種死法 |
| 016 | 測試分層 — 為什麼 98 條全綠還是不能用 |
| 017 | 環境陷阱 — 程式碼沒錯，錯的是它跑的地方 |

### 第六篇　實作規格（可 1:1 還原）

| # | 單元 |
|---|------|
| 018 | 模組切分與依賴律 |
| 019 | `ansi.js` — 樣式方言解析 |
| 020 | `protocol.js` — opcode 解析 |
| 021 | `dialects.js` — 方言 profile 表 |
| 022 | `store.js` — 狀態容器 |
| 023 | `main.js` — reducer 與登入狀態機 |
| 024 | `ui.js` — 15 個元件工廠 |
| 025 | `prefs.js` — 本地偏好 |
| 026 | DOM 契約 — 50 個 id 一個都不能少 |
| 027 | `net.js` — Transport 抽象 |
| 028 | Rust 端與橋接 |

### 第七篇　交付

| # | 單元 |
|---|------|
| 029 | 4 GB 的中間檔 — Rust 的體積從哪裡來 |
| 030 | 交付前的清理 — 清過程，留產物 |
| 031 | WebAssembly 的邊界 — 能搬什麼，不能搬什麼 |
| 032 | **前提被拿掉的那一天 — 伺服器搬進分頁** |

### 第八篇　源流：連線的另一端是什麼

| # | 單元 |
|---|------|
| 033 | 1989 年的那次分家 — driver 與 mudlib 為什麼要拆開 |
| 034 | 1992 年的改名風波 — MudOS 是怎麼來的 |
| 035 | 四條血脈 — 從 1989 到 2026，以及相容性的價錢 |
| 036 | **三個方向的呼叫 — efun、apply 與 simul_efun** |
| 037 | 雙堆疊虛擬機 — LPC 是怎麼被執行的 |
| 038 | 物件的一生 — clone、apply 與心跳 |
| 039 | config — driver 的全部旋鈕，以及本專案在上面栽的跟頭 |
| 040 | 三個沒有瀏覽器對應物的東西 — Transport、反轉的迴圈、VFS |
| 041 | **被拿掉的東西 — 套件矩陣，與 mudlib 上看得見的形狀** |
| 042 | 五個匯出、兩個回呼，以及三個只有這裡才有的陷阱 |
| 043 | **一行 `write()` — ZJMUD 是一套 driver 不知道的協議** |
| 044 | 握手那一行 — `crypt()`、`input_to()` 與一個沒在驗的驗證 |
| 045 | 讓 zjmud 客戶端玩不是 zjmud 的 mud — 會話層代打 |
| 046 | 一個位元組的全程 — 從 LPC 的 `write()` 到瀏覽器的 DOM |

### 第九篇　引擎、mudlib 與客戶端：三層各自的架構

| # | 單元 |
|---|------|
| 047 | MudOS 加了什麼 — 一個能力面，而不是一堆功能 |
| 048 | 編譯期決定的能力，執行期才發現的缺口 |
| 049 | **用 LPC 寫一整個網路堆疊 — intermud 的架構** |
| 050 | **17 個 mud，其實是 2 份 mudlib — 用檔名集合量血緣** |
| 051 | 特徵組合，不是繼承樹 — mudlib 的物件模型 |
| 052 | 屬性樹 — 一個 mapping 撐起整個遊戲狀態 |
| 053 | 升級、門派、裝備 — 三個機制共用的同一種架構 |
| 054 | 客戶端簡史 — 三十年沒變過的四件套 |
| 055 | telnet 選項層 — 協商的架構，與 6 組實測位元組 |
| 056 | 頻外資料的四代 — 語意的定義權一路往上搬 |
| 057 | 知識該放在哪一層 — 三種客戶端架構與它們的壽命 |

### 第十篇　語言、安全與編譯

| # | 單元 |
|---|------|
| 058 | 一個可以關掉的型別系統 |
| 059 | 修飾詞 — 一個關鍵字承載兩個語意的三十年後果 |
| 060 | 繼承的真相 — 程式與資料是分開存的 |
| 061 | uid / euid — 從 Unix 借來的安全模型，以及它的兩個相反預設 |
| 062 | 三個「無中生有」的機制 — virtual object、shadow 與編譯管線 |
| 063 | CD gamedriver — 最接近原版的那一支 |
| 064 | DGD — 從頭重寫的那一支，以及它證明了什麼 |
| 065 | 五支 driver 的技術對照 — 以及一份 CREDITS 的兩個分支 |

### 第十一篇　深入各家設計

| # | 單元 |
|---|------|
| 066 | LDMud 的語言野心 — 把 LISP 放進 LPC |
| 067 | LDMud 的 driver 野心 — 把 efun 的實作交給 mudlib |
| 068 | DGD 的實作決斷 — 輕量物件其實是一個陣列 |
| 069 | CD 的節制 — 以及三份跨越三十四年沒改過的文件 |
| 070 | 四種擴充點 — 一個綜合分析 |

### 第十二篇　英文線：MudOS 的社群史

| # | 單元 |
|---|------|
| 071 | TMI — 一個為了寫 driver 而開的 MUD |
| 072 | 三份 mudlib 的競賽 — 而競賽的主軸是指令解析器 |
| 073 | MudOS 的三次死而復生 |
| 074 | FluffOS 的建國宣言 — 以及 Discworld 這條暗線 |

### 第十三篇　周邊：工具鏈、標準庫與參考實作

| # | 單元 |
|---|------|
| 075 | driver 之外的六支程式 — 被忽略的工具鏈 |
| 076 | LPC 標準庫與 TUI — 伺服器這一側的反擊 |
| 077 | 可觀測性與現代網路層 — driver 自己就是一台伺服器 |
| 078 | `mudlibs.fluffos.info` — 官方參考實作，與本專案的逐項對照 |

### 第十四篇　地層學：ZJMUD 協議自己的歷史

| # | 單元 |
|---|------|
| 079 | 三份 header 的地層 — 一次斷代 |
| 080 | 擴充層的內部結構 — 五個前綴就是五次擴充 |
| 081 | 第三代與撞號的機制 — 兩個團隊各自從 600 開始 |
| 082 | 協議的年表 — 以及它為什麼永遠是 1.0 |

### 附錄

| 附錄 | 內容 |
|------|------|
| A | opcode 總索引（116 個）＋ 分隔符速查 |
| B | 檔案地圖與接手指南 |
| C | 1:1 還原檢查表 |
| D | MUD 家族年表 1978–2026（主幹／客戶端／**英文線**／**中文線**）＋ intermud 名錄快照 |
| E | FluffOS / LPC 技術速查（efun／apply／config／telnet／WASM API／VM 指令） |
| F | 17 份 mudlib 的血緣、落差與實測數據 |
| **G** | **本頁**：全書單元索引 ＋ 引用結構 |
| **H** | **ZJMUD 協議規格與客戶端系統分析**（15 節）—— 語言中立的規範層：傳輸層／opcode 分派／逐 opcode payload／樣式方言／登入狀態機／架構與驗收矩陣／**畫面拓撲與 opcode 去處**／會話生命週期／本地偏好／**13 行實機黃金樣本**／**可驗證的伺服器環境**。**不必讀正文也能從 0 照做** |
| **I** | **外部文獻與交叉佐證** —— 兩份一手史料（Bartle 1985 年序言、**ES2 作者 Annihilator 訪談**）＋ **兩次可重跑量測**：橫向量 113 份 mudlib（兩骨架 **94.7% 互斥**）、縱向量 **ES2 七個版本**（骨架 1995 年定型、Jaccard 校準基線 **0.333–0.998**）；並補上附錄 D「1978」原本缺的出處 |

---

## G.2　引用結構 — 本書自己的量測

把全書的 `§NNN` 交叉引用掃一遍：

| 項目 | 數值 |
|------|-----:|
| 單元總數 | **82**（001–082，**無編號空缺**） |
| `§` 交叉引用總數 | **604** |
| 被引用過的相異單元 | 74 |
| **指向不存在單元的引用** | **0** |
| 平均每單元被引用 | 約 7.4 次 |

### 被引用最多的十個單元

| 單元 | 次數 | 標題 |
|------|-----:|------|
| **§032** | **36** | 前提被拿掉的那一天 — 伺服器搬進分頁 |
| §050 | 24 | 17 個 mud，其實是 2 份 mudlib |
| §041 | 24 | 被拿掉的東西 — 套件矩陣 |
| §049 | 22 | 用 LPC 寫一整個網路堆疊 — intermud |
| §043 | 20 | ZJMUD 是一套 driver 不知道的協議 |
| §036 | 20 | 三個方向的呼叫 — efun、apply 與 simul_efun |
| §034 | 19 | 1992 年的改名風波 |
| §037 | 19 | 雙堆疊虛擬機 |
| §044 | 19 | 握手那一行 |
| §028 | 18 | Rust 端與橋接 |

**這張表就是本書的骨架。**

被引用最多的 §032（36 次）是「伺服器搬進分頁」——
它是全書的轉折點：前七篇的結論在那裡被推翻，後七篇從那裡長出來。

而 §043（driver 不知道協議存在）與 §036（三個呼叫方向）
被引用 20 次，是因為**它們解釋了整套協議為什麼長成那樣**——
撞號、無版本號、逆向著力點，全部回到這兩節。

### 從未被引用的八個單元

```
002  字串常數           012  把協議關進純函式    014  失敗隔離
016  測試分層           029  4 GB 的中間檔       030  交付前的清理
070  四種擴充點         076  LPC 標準庫與 TUI
```

**這不是缺陷，而且它們分成兩類：**

| 類型 | 單元 | 為什麼沒有被引用 |
|------|------|----------------|
| **方法論類** | 002、012、014、016 | 它們講的是**通用做法**（讀字串常數、純函式化、失敗隔離、測試分層），後面的章節在**實踐**它們而不是引用它們 |
| **收束類** | 029、030、070、076 | 它們本身就是**終點**——綜合分析或一次性的交付記錄，沒有下游 |

**§070（四種擴充點）零引用特別合理**：它是第十一篇的綜合單元，
把 §066–§069 收攏起來——**綜合者不會被引用，它引用別人。**

> 🔬 **這個量測本身示範了本書的一條規矩**：
> 「零引用」聽起來像問題，但**先看分佈再下判斷**（§050）。
> 分成兩類之後，八個裡沒有一個是真正的孤兒。

---

## G.3　全書的兩個半部

| | 前七篇（001–032） | **後七篇（033–082）** |
|---|-----------------|---------------------|
| 單元數 | 32 | **50** |
| 問的問題 | **這個位元組是什麼意思？** | **它為什麼長成這樣？** |
| 主要證據 | 🟢 實機封包、🟠 反編譯 | 🟡 上游原始碼／一手史料、🟢 可重跑量測 |
| 對象 | ZJMUD 協議與本專案的客戶端 | LPMud 家族、五支 driver、mudlib、社群史、協議自己的地層 |
| 起點 | 一個 1.1 MB 的 APK | **§032：伺服器搬進了瀏覽器分頁** |

**兩個半部的接縫在 §032**——那一節記錄了 §031 的結論如何因為前提被拿掉而失效，
而「要理解新前提，就得理解那台 driver 是什麼」，於是有了第八到十四篇。

---

## G.4　重跑本附錄的量測

```bash
cd designbook

# 單元索引 + 編號完整性
python3 - <<'EOF'
import re, glob
for f in sorted(glob.glob('[0-9][0-9]_*.md')):
    t = open(f, encoding='utf-8').read()
    part = re.search(r'^#\s+(第.{1,3}篇.*)$', t, re.M)
    if part: print('\n##', part.group(1).strip())
    for n, title in re.findall(r'^##\s+(\d{3})　(.+?)$', t, re.M):
        print(f'  {n}  {title}')
EOF

# 引用完整性（找出指向不存在單元的 §NNN）
python3 - <<'EOF'
import re, glob, collections
files = sorted(glob.glob('[0-9][0-9]_*.md')) + sorted(glob.glob('附錄*.md'))
units = {m.group(1) for f in files
         for m in re.finditer(r'^##\s+(\d{3})　', open(f, encoding='utf-8').read(), re.M)}
refs, bad = collections.Counter(), []
for f in files:
    for m in re.finditer(r'§(\d{3})', open(f, encoding='utf-8').read()):
        refs[m.group(1)] += 1
        if m.group(1) not in units: bad.append((f, m.group(1)))
print(f'單元 {len(units)}  引用 {sum(refs.values())}  斷鏈 {len(bad)}')
print('零引用:', sorted(u for u in units if refs[u] == 0))
EOF
```

> 💡 **為什麼要做這件事**：本書從第一篇就主張「任何斷言都要能被重新驗證」。
> 一本有 604 條交叉引用的書，**如果沒有一支腳本檢查它們，
> 那些 `§NNN` 就只是排版**。
> 這份附錄是本書對自己套用同一條規矩的結果——
> **而它第一次執行時，斷鏈數就是 0。**

---



# 附錄 H　ZJMUD 協議規格與客戶端系統分析

> **這份附錄是全書唯一「不必讀正文也能用」的一章。**
>
> 前面十四篇回答「為什麼長這樣」，第六篇回答「本專案的 JS 客戶端精確長什麼樣」。
> 這一份回答第三個問題：**任何一個全新客戶端，不管用什麼語言、什麼 UI 框架，
> 要與既有 ZJMUD 伺服器互通，最少必須做到哪些事、每一個位元組怎麼解。**
>
> 因此本附錄刻意**與實作語言無關**：不出現 `ansi.js`、不出現 Rust、不出現 DOM id。
> 需要那一層的人請看第六篇與附錄 C；需要「照著做就能通」的人只看這一份。

---

## H.0　定位、讀法與證據等級

### H.0.1　這份文件想達成什麼

一句話：**把這份附錄交給一個沒有任何背景知識的實作者（含 LLM），
他應該能寫出一個可以登入、可以走路、可以跟 NPC 互動、畫面不會壞掉的 ZJMUD 客戶端。**

達成這件事需要十類知識，缺一不可。
**前五類是「協議」，後五類是「客戶端」——只做前五類會得到一個協議解析器，不是一個客戶端。**

| # | 類別 | 本附錄章節 | 少了它會怎樣 |
|---|------|-----------|------------|
| 1 | **怎麼把位元組變成行** | H.1 | 連線在第一個位元組就死掉 |
| 2 | **怎麼把行變成事件** | H.2、H.3 | 控制字元印在畫面上 |
| 3 | **怎麼把事件的內容拆成欄位** | H.4、H.5 | 按鈕數量不對、顏色錯、指令送不出去 |
| 4 | **怎麼從連上到進世界** | H.6 | 永遠停在版本挑戰那一行 |
| 5 | **怎麼組織這些東西才不會腐爛** | H.7、H.8 | 三個月後沒人敢動 |
| 6 | **畫面要長什麼樣、每個 opcode 畫到哪一區** | **H.11** | 解得出來，但不知道要蓋什麼；版面全靠實作者自己發明 |
| 7 | **斷線、逾時、換伺服器怎麼辦** | **H.12** | 掛機一晚回來是死的，而且不知道為什麼 |
| 8 | **哪些狀態要留在本地** | **H.13** | 每次重開都要重設一次，帳密處理也會出錯 |
| 9 | **怎麼自證做對了** | **H.14** | 沒有任何辦法驗證，只能靠「看起來對」 |
| 10 | **沒有伺服器怎麼測** | **H.15** | H.8.3 的實機驗收根本跑不了 |

### H.0.2　規範用語

本附錄採 RFC 2119 精神，但用中文表述：

| 用語 | 意義 |
|------|------|
| **必須** | 不做就無法與既有伺服器互通，或會造成連線中斷／資料遺失 |
| **應** | 不做仍能運作，但會在特定 mudlib 或特定情境下出錯 |
| **可** | 實作者自由選擇，不影響互通性 |
| **不得** | 做了會造成安全問題或協議違規 |

### H.0.3　證據等級

全書的判讀規矩在此照用，衝突時順序固定：

```
🟢 實機封包  >  🟡 伺服器原始碼  >  🟠 客戶端行為觀察  >  🔴 推論
```

| 等級 | 意義 | 本附錄中的來源 |
|------|------|--------------|
| 🟢 | 實際連線擷取到的位元組 | 53 行實機封包（已固化為回歸測試） |
| 🟡 | 伺服器端可讀的原始碼 | `world/include/zjmud.h`、`adm/daemons/logind.c`、`adm/single/master.c`，以及約 4,000 處實際發送點 |
| 🟠 | 客戶端外部可觀察的行為 | 對既有行動客戶端「輸入什麼 → 畫面出現什麼」的黑箱歸納。**不涉及其內部實作** |
| 🔴 | 由前三者推導，未被直接證實 | 明確標注，實作時**必須同時容忍其他可能形態** |

> ⚠️ **🟠 這一級的性質要說清楚。**
> 它記錄的是「送這個位元組進去，畫面上出現什麼」——是**互通性所必需的事實資訊**，
> 與內部演算法無關。凡本附錄標 🟠 者，都可以由任何人拿一台伺服器重新觀察得到相同結果。
> 標 🟠 而非 🟢，只是因為它尚未被納入那 53 行的回歸測試集。

### H.0.4　與其他章節的分工

| 想知道 | 看哪裡 |
|--------|--------|
| opcode 一覽、分隔符速查 | **附錄 A**（索引） |
| 每個 opcode 的 payload 怎麼拆 | **本附錄 H.4**（規範） |
| 為什麼會有撞號、為什麼是變長 opcode | 第三篇 §009–§011（敘事） |
| 本專案 JS 客戶端的模組簽名與逐步演算法 | 第六篇 §018–§028 |
| 照什麼順序重建、怎麼驗收 | 附錄 C |
| 協議自己的演化史 | 第十四篇 §079–§082 |

---

## H.1　傳輸層

### H.1.1　總覽

| 項目 | 值 | 等級 |
|------|-----|------|
| 協定 | 原生 TCP（**非** WebSocket、**非** HTTP） | 🟢 |
| 分幀 | 以 `\n` 分行；一行 = 一個協議單元。伺服器實際送 `\r\n` | 🟢 |
| 編碼 | **依埠而定**，見 H.1.4 | 🟡🟢 |
| 加密 | **無** | 🟢 |
| Keep-alive | 開啟 socket `SO_KEEPALIVE` | 🟠 |
| 伺服器位址 | 由帳號側通道下發（H.6.2），**不寫死** | 🟠 |

整條管線由外而內共四層，每一層都必須完整剝完才能進下一層：

```
┌────────────────────────────────────────────────────────────┐
│ ① TCP 位元組流                                              │
│   └─ ② telnet IAC 協商（必須在位元組層剝除，H.1.2）          │
│       └─ ③ 文字解碼（UTF-8 或 GBK，依埠，H.1.4）             │
│           └─ ④ 以 \n 分行 → 每行一個協議單元                 │
│               └─ ⑤ 行首 opcode：ESC + 3~4 字元（H.2）        │
│                   └─ ⑥ payload：$zj# 分記錄、: 分欄位（H.4） │
│                       └─ ⑦ 欄位文字內：樣式方言（H.5）        │
└────────────────────────────────────────────────────────────┘
```

> ⚠️ **層序不可對調。** 最常見的致命錯誤是「先做 UTF-8 解碼，再剝 IAC」——
> IAC 的 `0xFF` 不是合法 UTF-8，嚴格解碼器會在這裡直接失敗。見 H.1.3。

### H.1.2　telnet 選項協商（必須處理）🟢

連線建立後，伺服器**立刻**送出 18 個位元組，共 6 組協商：

```
ff fd 18   IAC DO   TERMINAL-TYPE (24)
ff fd 1f   IAC DO   NAWS          (31)
ff fd 27   IAC DO   NEW-ENVIRON   (39)
ff fb 56   IAC WILL MCCP2         (86)   ← 壓縮，必須拒絕
ff fb 46   IAC WILL MSSP          (70)
ff fb 2a   IAC WILL CHARSET       (42)
```

**客戶端必須實作的五條規則**

| # | 規則 |
|---|------|
| 1 | 在**位元組層**（不是字串層）剝除所有 IAC 序列，剝乾淨之後才做文字解碼 |
| 2 | 對協商**一律拒絕**：收到 `IAC WILL x` → 回 `IAC DONT x`；收到 `IAC DO x` → 回 `IAC WONT x` |
| 3 | 子協商 `IAC SB … IAC SE` **整段丟棄** |
| 4 | `IAC IAC`（`ff ff`）是「一個真正的 0xFF 資料位元組」，必須還原成單一 `0xFF` |
| 5 | 文字解碼**應**用寬容模式（壞位元組換成替換字元），不讓一行壞資料弄斷整條連線 |

**不處理的兩個後果**

| # | 後果 | 症狀 |
|---|------|------|
| 1 | `0xFF` 不是合法 UTF-8 | 嚴格逐行讀取器在第一個位元組回 `InvalidData` 並中止 —— **連線瞬間死掉，且看起來像「連不上」** |
| 2 | 誤答應 **MCCP2（選項 86）** | 伺服器之後改送 zlib 壓縮串流，整條連線變成無法解讀的二進位 —— **看起來像「協議完全錯了」** |

> ⚠️ 這兩個症狀都會把人引到錯誤的方向去查。
> 第 1 個像「伺服器沒開」，第 2 個像「opcode 表抄錯了」。
> 實作時**第一件事**就是把 IAC 剝乾淨並印出剝除後的前 200 個位元組確認。

### H.1.3　為什麼不能用「標準的逐行讀取器」🟢

多數語言的標準庫都有「從串流讀一行」的便利函式（例如 Rust `BufReader::read_line`、
Python `socket.makefile().readline()`）。**這些函式在此協議上不能直接用**，理由有二：

1. 它們多半在內部做嚴格 UTF-8 檢查 —— 撞到 IAC 的 `0xFF` 就失敗
2. 它們把「分幀」與「解碼」綁在一起，而本協議必須**先剝 IAC、再解碼、最後分幀**

**正確的讀取管線**

```
loop:
  buf ← 從 socket 讀取任意長度的位元組
  (clean, replies) ← telnet_filter(buf)      # 剝 IAC，產生要回送的拒絕封包
  若 replies 非空 → 寫回 socket
  accum ← accum + clean
  while accum 中含有 \n:
      line, accum ← accum 以第一個 \n 切開
      line ← line 去掉結尾的 \r
      text ← decode(line, encoding)          # 寬容模式
      dispatch(text)                          # 進 H.2
```

> ⚠️ `telnet_filter` **必須是有狀態的**。IAC 序列可能跨越兩次 `read` 的邊界
> （例如這次讀到 `ff fd`，下次才讀到 `18`）。無狀態的實作在網路稍有抖動時就會漏剝。

### H.1.4　埠與編碼 🟡

出處 `world/adm/single/master.c` 的 `connect(int port)`：

```c
object connect(int port)
{
    if (port == 5003){
        set_encoding("GBK");     // ← 只有 5003 是 GBK
    }
    ...
}
```

| 埠 | 編碼 | 用途 | 新客戶端 |
|----|------|------|---------|
| **5001** | **UTF-8** | 行動／Web 客戶端 | ✅ **連這個** |
| 5003 | **GBK** | 傳統 telnet 客戶端（ZMud／MushClient） | 僅相容需求時 |
| 5004 | UTF-8 | driver 內建 websocket | 視部署而定 |

> ⚠️ **連錯埠的症狀是「整頁中文亂碼」，不是「連不上」。**
> 這一點每年都會騙走一些人半天：畫面出得來、按鈕也在、就是字全花——
> 於是去查樣式解析器，其實只是埠號差 2。
> **先確認埠，再懷疑解析器。**

### H.1.5　送出方向 🟡

送出即「寫一行文字 + 換行」。指令就是 MUD 原生指令字串（`look`、`north`、`score`…）。

**多行指令模式**由伺服器以 opcode 控制：

| 模式 | 觸發 opcode | 行為 |
|------|------------|------|
| `multiline = true`（**預設**） | `ESC998` | 字串內的 `\n` 原樣送出 |
| `multiline = false` | `ESC997` | 送出前把字串內所有 `\n` 取代為 `;` |

`;` 是 LPMud 慣用的「一行多指令」分隔符。此開關讓伺服器決定客戶端要用哪種方式打包複合指令。

---

## H.2　行級分派

### H.2.1　前置樣式重置 🟢

若整行以 **`ESC[2;37;0m`**（9 個字元）開頭：

1. 清空所有目前套用中的樣式狀態（前景色、背景色、粗體、字級、連結、全形旗標）
2. 從第 10 個字元起繼續當作正常行處理

> ⚠️ **這個重置碼可能出現在 opcode 之前，也可能在之後。**
> 實機資料同時存在 `ESC[2;37;0mESC002…` 與 `ESC002ESC[2;37;0m…` 兩種順序。
> 取 opcode 的程序**必須**兩種都能處理，否則第二種會被當成無 opcode 的一般訊息，
> 房間標題就永遠不會更新。

### H.2.2　opcode 擷取（變長）🟢🟡

opcode 的形式是 **`ESC`（U+001B）+ 3 個或 4 個 ASCII 英數字元**。

**這裡有兩個陷阱，兩個都會讓畫面大面積壞掉：**

| 陷阱 | 錯誤寫法 | 後果 |
|------|---------|------|
| 以為一定是「三個**數字**」 | `/^\d{3}$/` | `10a`、`11a`、`2k1`–`2k4` 這些擴充碼被當一般訊息，**整段內容連同控制字元印在畫面上** |
| 以為一定是「**三**個字元」 | 固定 `slice(1,4)` | `ESC1085`（地圖）被解成 `108`，多出來的 `5` 混進內容開頭 |

**規範演算法**

```
parse_line(raw):
  1. raw 非字串 → { op: null, payload: "" }
  2. 若 raw 以 ESC + "[2;37;0m" 開頭 → reset_styles = true；raw ← raw 去掉前 9 字元
  3. 若 len(raw) < 4 或 raw[0] != ESC → { op: null, payload: raw }
  4. three ← raw[1:4]
     若 three 不符合 /^[0-9a-z]{3}$/i → { op: null, payload: raw }
  5. four ← raw[1:5]
     若 four 符合 /^[0-9a-z]{4}$/i
        且 is_known(four)
        且 NOT is_known(three)          ← 兩個條件缺一不可
        → { op: four, payload: raw[5:] }
  6. 否則 → { op: three, payload: raw[4:] }
```

`is_known(op)` = 該 op 存在於核心表 **或**「目前生效的方言 profile」的擴充表中（見 H.3.3）。

> ⚠️ **第 5 步的 `NOT is_known(three)` 不能省。**
> 少了它，`ESC004` 後面接「5 個人站在這裡」會被讀成 `ESC0045`（開啟網頁），
> 房間描述消失、還會試圖打開一個叫「個人站在這裡」的網址。
> 這是變長 opcode 唯一安全的消歧義規則：**三字元優先，只有在「四字元認得而三字元不認得」時才升級。**

### H.2.3　分派結果

| 情況 | 處理 |
|------|------|
| opcode 命中表 | 依該 opcode 的規則處理，payload = opcode 之後的剩餘內容 |
| opcode 未命中 | **整行**（含原始控制字元）視為一般訊息送進主訊息區，並標記 `unknownOpcode` |
| 無 opcode | 整行視為一般訊息 |

> ⚠️ **未知 opcode 絕不可靜默丟棄。**
> 丟棄的後果是「換一個 mudlib 之後，某些內容憑空消失，而且完全不會報錯」——
> 這種 bug 可以藏好幾個月。正確作法是降級顯示並打標記，讓它**吵**。

---

## H.3　opcode 全表

> 完整索引（含 116 個擴充碼與巨集名）在**附錄 A**。
> 本節只列**實作必須知道的行為與副作用**，不重複索引。

### H.3.1　核心 27 個 —— 行為與副作用 🟡🟢

| opcode | 語意 | payload 格式 | **副作用（最容易漏）** |
|--------|------|-------------|---------------------|
| `ESC000` | 連線／登入狀態 | 狀態碼或中文字串 | payload == `重连完毕` → **自動回送 `l`** 🟠 |
| `ESC001` | 文字輸入面板 | H.4.9 | 開面板、輸入框取得焦點 |
| `ESC002` | 房間標題 | 樣式文字 | ★ **清空出口區、清空物件區、隱藏方向盤、關閉戰鬥面板、關閉自訂按鈕列** |
| `ESC003` | 出口列表 | H.4.1 | — |
| `ESC004` | 房間描述 | 樣式文字 | — |
| `ESC005` | 房間物件列表 | H.4.2 | — |
| `ESC006` | 自訂指令按鈕 | H.4.3 | ★ 出現 `b1`–`b17` 時**隱藏方向盤、顯示自訂按鈕列** |
| `ESC007` | 目標詳情 | 樣式文字（`$br#` 換行） | ★ **清空兩組動作列、隱藏數量輸入** |
| `ESC008` | 互動動作列①（左） | H.4.4 | 清空後重建 |
| `ESC009` | 互動動作列②（右） | H.4.4 | 清空後重建 |
| `ESC010` | NPC 對話框 | H.4.10 | 開啟模態 |
| `ESC011` | 地圖 | 樣式文字（`$br#`） | — |
| `ESC012` | 屬性條群 | H.4.5 | 重建整組條 |
| `ESC013` | 長文本分頁 | 樣式文字（`$br#`） | — |
| `ESC014` | **伺服器代送指令** | 指令字串 | ★ **立刻把 payload 當指令回送**（見 H.3.2） |
| `ESC015` | 頂部飄字 | 樣式文字 | 顯示約 5 秒後淡出；**同時**寫入系統頻道 |
| `ESC016` | 戰鬥訊息 | 樣式文字 | 附加到戰鬥面板並顯示；**同時**寫入系統頻道 |
| `ESC017` | 關閉戰鬥面板 | （忽略） | 隱藏並清空 |
| `ESC020` | 彈出選單 | H.4.6 | 開遮罩 |
| `ESC021` | 標題列按鈕 | H.4.7 | 重建 |
| `ESC022` | 物件血條更新 | H.4.8 | 依 tag 尋找既有項目 |
| `ESC023` | 描述顯示開關 | `屏蔽描述` 或其他 | 切換鈕文字要跟著改 |
| `ESC024` | 傷害飄字 | 樣式文字 | 一次性動畫（約 1.9 秒）🟠 |
| `ESC045` | 開啟網頁 | URL | 內嵌瀏覽器 🟠 |
| `ESC100` | 聊天訊息 | 樣式文字 | 進迷你聊天列與「全部」頁 |
| `ESC900` | 換伺服器 | `<ip>:<port>` | 關閉現有連線改連新址 |
| `ESC903` | 刪除單一出口 | 方向名 | `003` 的反向 |
| `ESC905` | 刪除單一物件 | 物件 tag | `005` 的反向 |
| `ESC913` | 清空所有出口 | （忽略） | — |
| `ESC997` / `ESC998` | 指令模式開關 | （忽略） | 見 H.1.5 |
| `ESC999` | 強制退出 | （忽略） | 結束程序 |

### H.3.2　實作優先順序（依伺服器實際發送量）🟡

這是本附錄最有價值的一張表。它來自對伺服器約 4,000 處發送點的統計：

| opcode | 巨集 | 發送點數 | 意義 |
|--------|------|--------:|------|
| `ESC007` | `ZJOBLONG` | **531** | 目標詳情 |
| `ESC009` | `ZJOBACTS2` | **448** | 動作列② |
| `ESC001` | `INPUTTXT` | **147** | 文字輸入 |
| `ESC014` | `ZJFORCECMD` | **98** | 伺服器代送指令 |
| `ESC015` | `ZJTMPSAY` | 44 | 頂部飄字 |
| `ESC008` | `ZJOBACTS` | 30 | 動作列① |
| `ESC003` | `ZJEXIT` | 16 | 出口 |
| `ESC006` | `ZJBTSET` | 10 | 自訂按鈕 |
| `ESC000` / `ESC011` | `SYSY` / `ZJMAPTXT` | 各 8 | 狀態／地圖 |
| `ESC021` | `ZJTTMENU` | 5 | 標題列按鈕 |
| `ESC005` / `ESC012` / `ESC022` | | 各 4 | 物件／屬性條／血條 |
| `ESC010` | `ZJYESNO` | **0** | ← 對話框在此伺服器**完全沒用到** |
| `ESC016` / `ESC017` | `ZJFMSG` / `ZJFNOMSG` | **0** | ← 戰鬥面板未啟用 |
| `ESC913` / `ESC023` / `ESC999` | | **0** | 未使用 |

**三個直接可用的結論**

1. **先做 `007` + `009`。** 這兩個合計近 1,000 處，幾乎所有 NPC 互動都走這條路。
   做完這兩個，玩家 95% 的時間都有東西看。
2. **`014` 必須做，而且要早做。** 伺服器用了 98 次來「叫客戶端代送指令」驅動流程。
   不實作的症狀是**互動走到一半莫名其妙卡住**，而且畫面上完全沒有錯誤跡象。
3. **`010` 對話框可以最後做。** 它在這台伺服器 0 次使用；NPC 互動走的是 `007`+`009`。
   ——但別把它從表裡刪掉，其他 mudlib 會用。

> 🔍 老手視角──真正的門道
> 拿到一份新協議時，多數人按 opcode 編號從 `000` 開始往下實作。這是最慢的路徑。
> **正確的第一步是去伺服器原始碼裡數「每個 opcode 被發送幾次」**，然後照次數由大到小做。
> 上面那張表就是這麼來的：它把「33 個 opcode」壓縮成「先做 4 個就能玩」。
> 更重要的是它會告訴你哪些是**定義了但沒人用的**（`010`/`016`/`017`/`913` 全是 0）——
> 那些如果按編號順序做，你會在完全沒有回報的地方花掉好幾天。

### H.3.3　方言 profile（撞號隔離）🟡

擴充 opcode **不得**合併成單一張表。已知撞號：

| opcode | 大梦江湖 | 指游 ZY |
|--------|----------|--------|
| `602` | 主頁文本 | NPC 狀態 |
| `603` | 主頁文本 2 | 戰鬥按鈕 |
| `604` | 判斷界面 | 操作面板 |
| `605` | 更改界面 | **清空主畫面** |

合併的後果：後定義的靜默覆蓋前者，畫面上表現為「某些功能莫名其妙變成別的東西」，
**而且完全不會報錯**。特別是 `605` —— 一邊是「更改界面」、一邊是「清空主畫面」，
合併之後會出現「玩家打開某個面板，主畫面訊息全部消失」這種極難追查的現象。

**規範作法**

1. 每個方言是一個獨立的 **profile**，各自持有自己的擴充表
2. 由連線第一行的版本挑戰字串決定套用哪個 profile（見 H.6.1）
3. 查表順序：**核心表 → 目前 profile 的擴充表 → 未知**
4. `is_known()`（H.2.2）只查核心表與**目前**profile

### H.3.4　擴充 opcode 只有 8 種結構 🟡

61 個擴充 opcode 攤開來看，payload 結構只有 8 種，且全部與核心 opcode 同構：

| 結構 | 說明 | 同構的核心 opcode |
|------|------|------------------|
| `TITLE` | 面板標題（樣式文字） | — |
| `TEXT` | 內文（可含 `$br#`） | `ESC004` / `ESC007` |
| `LIST` | `label:cmd` × `$zj#` | `ESC005` / `ESC021` |
| `ACTIONS` | 帶版面前綴的按鈕網格 | `ESC008` / `ESC009` |
| `BARS` | `║` 分隔的數值條 | `ESC012` |
| `ENTITY_ADD` | `id$zj#名字$zj#a:b:max` | `ESC005` + `ESC022` |
| `ENTITY_DEL` | id | `ESC905` |
| `MESSAGE` | 進某個訊息頻道 | `ESC015` / `ESC016` / `ESC100` |

> 💡 這是整個擴充層最重要的一個發現：
> **不需要為 61 個擴充 opcode 各寫一個處理器。**
> 只要一張「opcode → 結構 + 目的面板」的對照表，再加上 8 個結構解析器即可。
> 工作量從「61 個 handler」降到「一張表 + 8 個函式」。

---

## H.4　payload 結構（逐 opcode 規範）

### H.4.0　通用分隔符 🟡

| 標記 | 用途 | 適用範圍 |
|------|------|---------|
| `$zj#` | **記錄分隔符**（主力） | 幾乎所有結構化 opcode |
| `$z2#` | 記錄分隔符 | ⚠️ **僅** `ESC020` 彈出選單 |
| `$br#` | 換行（等同 `\n`） | 樣式文字、按鈕標籤 |
| `$dh#` | 對話框區塊分隔 | 僅 `ESC010` |
| `$sock#` | 指令序列分隔（一次送多條） | 僅 `ESC010` 的確定鈕 |
| `:` | 欄位分隔符 | 通用 |
| `\|` | 次級欄位（主標題 \| 副標題） | `ESC008`/`ESC009`/`ESC020` |
| `║` (U+2551) | 屬性條記錄／登入欄位 | `ESC012`、登入握手 |
| `$N` | 數量佔位符 | ⚠️ **僅** `ESC010` |
| `$txt#` | 輸入佔位符／保持面板開啟旗標 | ⚠️ **僅** `ESC001` 與 `ESC008`/`ESC009` |

> ⚠️ **`$N` 與 `$txt#` 是兩套完全不同的機制，不可混用。**
> `$txt#` 是 `ESC001` 的「把使用者輸入的**自由文字**塞進樣板」；
> `$N` 是 `ESC010` 的「把使用者選的**數量**塞進指令」。
> 這是本專案規格書 v1.0 兩個錯誤中的一個，代價是整個文字輸入功能做錯方向。

### H.4.0.1　版面前綴（optional layout header）🟡

`ESC008` / `ESC009` / `ESC012` / `ESC020` 的 payload **可以**用 `$` 開頭帶一個版面參數區塊：

```
$<a>,<b>,<c>,<d>#<實際內容>
```

**解析規則**：`payload[0] == '$'` → 取到第一個 `#` 之間的字串，以 `,` 切成 4 個整數；
非數字的欄位用該 opcode 的預設值補；實際內容從 `#` 之後開始。

**各 opcode 的預設值（不同，必須分開記）**

| opcode | 預設值 | 四個參數的意義 |
|--------|--------|--------------|
| `008` / `009` | `[1, 3, 9, 30]` | cols、widthDiv、heightDiv、fontDiv |
| `012` | `[0, 0, 22, 35]` | cols（0=自動）、**未使用**、heightDiv、fontDiv |
| `020` | `[1, 2, 8, 25]` | cols、widthDiv、heightDiv、fontDiv |

- `widthDiv` / `heightDiv` / `fontDiv` 的語意是**除數**：按鈕寬 = 畫面寬 ÷ widthDiv，依此類推
- `012` 的 `cols` 為 0（未指定）時 → `cols = ceil(條目數 / 2)`，即預設排成兩列
- `012` 的第 2 個參數伺服器**會送**（例 `ZJMENUF(6,6,25,40)`），但客戶端**忽略**它 🟠

### H.4.1　`ESC003` 出口列表 🟢

```
<方向鍵>:<顯示文字>[:<指令>]  $zj#  <方向鍵>:<顯示文字>[:<指令>]  $zj#  …
```

| 欄 | 意義 |
|----|------|
| 1 | **方向鍵** —— 決定放在方向盤的哪個位置 |
| 2 | 顯示文字（可含樣式） |
| 3 | 點擊時送出的指令。**省略時，指令 = 第 1 欄** |

**方向鍵 → 方向盤位置**

| 位置 | 接受的方向鍵 |
|------|-------------|
| 北 | `north`, `northup`, `northdown` |
| 南 | `south`, `southup`, `southdown` |
| 東 | `east`, `eastup`, `eastdown` |
| 西 | `west`, `westup`, `westdown` |
| 西北 / 東北 | `northwest` / `northeast` |
| 西南 / 東南 | `southwest` / `southeast` |
| **其他任意字串** | 放進方向盤下方的「額外出口」清單（例 `enter`、`out`、`up`、`down`） |

> ⚠️ **第 1 欄可能含空白。** 實機出現過 `update here` 這種值。
> 因此判斷規則**必須**是「不在上表的一律歸額外出口」，
> **不得**假設它是單一英文字、也不得對它做分詞。

**範例**
```
ESC003north:北面是大門$zj#east:東$zj#enter:進入客棧:enter inn
```

### H.4.2　`ESC005` 房間物件列表 🟢

```
<顯示文字>:<指令>  $zj#  <顯示文字>:<指令>  $zj#  …
```

- 每筆建立一個清單項目，**`tag` = 指令欄**（`ESC022` 血條更新靠它尋找）
- 點擊時：記錄「目前目標」的 id（= 指令）與名稱（= 顯示文字），然後送出該指令
- payload 長度 ≤ 4 時視為**空清單**，不建立任何項目

> ⚠️ **只切第一個冒號。**
> 指令欄常是 LPC 物件 id，形如 `look /d/city/npc/wolf#1686` —— 裡面有 `/` 也有 `#`，
> 但**不會**再有第二個 `:` 之前的內容。對整串做 `split(':')` 會把指令切碎。

### H.4.3　`ESC006` 自訂指令按鈕 🟡

```
<槽位>:<顯示文字>:<指令>  $zj#  …
```

**解析前**先把 payload 中的 `$br#` 換成 `\n`（讓按鈕標籤可以兩行）。

| 槽位 | 位置 |
|------|------|
| `bs` | **房間名稱本身**（讓房間名變可點擊）。此槽**不使用**顯示文字欄 |
| `b1` – `b5` | 自訂按鈕列 第 1 排（5 格） |
| `b6` – `b10` | 自訂按鈕列 第 2 排（5 格） |
| `b11` – `b17` | 底部工具列 |

**副作用**：任何 `b1`–`b17` 出現時，**隱藏方向盤、顯示自訂按鈕列**，並把「自訂」鈕文字改成「關閉」。

### H.4.4　`ESC008` / `ESC009` 互動動作列 🟢

```
[$<cols>,<widthDiv>,<heightDiv>,<fontDiv>#]  <項目>$zj#<項目>$zj#…
```

預設版面 `[1, 3, 9, 30]`。`008` 是左欄、`009` 是右欄，兩者格式完全相同。

**單一項目格式**
```
<主標題>[|<副標題>]:<指令>
```

| 規則 | 說明 |
|------|------|
| 主標題中的 `$br#` | 先換成 `\n` |
| `\|` 分隔副標題 | 副標題常用來顯示數量、價格 |
| 指令欄含 `ESC020` | 點擊時**不送指令**，而是把其後內容當彈出選單開啟 |
| 指令欄含 `$txt#` | 點擊後**保持面板開啟**（供連續操作）；否則點完關閉面板 |
| 主標題等於 `发送语音` | 觸發錄音介面而非送指令 🟠 |

### H.4.5　`ESC012` 屬性條群 🟢

```
[$<cols>,<未用>,<heightDiv>,<fontDiv>#]  <條目>║<條目>║…
```

預設版面 `[0, 0, 22, 35]`，`cols = 0` 時取 `ceil(條目數 / 2)`。

**單一條目格式**
```
<標籤>:<數值>:<顏色>[:<指令>]
```

**`<數值>` 的三種形態決定呈現**

| 寫法 | 模式 | 呈現 |
|------|------|------|
| `<a>/<b>/<max>`（max > 0） | `dual` | 雙層條：A 層 = a/max，B 層 = b/max |
| `<a>/<max>`（max > 0） | `single` | 單層條：A 層 = a/max，B 層滿格 |
| 其他（含純數字） | `text` | **不畫條**，直接顯示 `標籤:原字串` |

- **`<顏色>`**：A 層填充色。常是 8 位 **ARGB**（如 `#99FF0000`），轉 CSS 要把 alpha 從頭搬到尾
- **`<指令>`**（第 4 欄，可省略）：有此欄時該條可點擊

> ⚠️ **標籤常含全形冒號 `：`。**
> 用 ASCII `:` 切欄位剛好安全（全形冒號不會被誤切），
> 但**不得**寫成 `[:：]` 這種「兩種冒號都切」的正則——那會把標籤本身切斷。

**範例**
```
ESC012$2,0,22,35#氣血:180/200/200:#ff0000:hp║內力:95/120/120:#0000ff:hp
```

### H.4.6　`ESC020` 彈出選單 🟡

```
[$<cols>,<widthDiv>,<heightDiv>,<fontDiv>#]  <項目>$z2#<項目>$z2#…
```

預設版面 `[1, 2, 8, 25]`。單一項目格式：`<顯示文字>|<指令>`。

點擊後依序：送出指令 → 關閉選單 → 關閉遮罩 → 關閉互動面板。

> ⚠️ **此 opcode 的記錄分隔符是 `$z2#`，不是 `$zj#`。**
> 全協議只有這一個例外。寫錯的症狀是「整個選單變成一個超長的項目」。

### H.4.7　`ESC021` 標題列按鈕 🟢

```
<顯示文字>:<指令>  $zj#  …
```

建立在房間標題右側的橫向捲動按鈕列。點擊送出指令並關閉互動面板。

### H.4.8　`ESC022` 物件血條更新 🟡

```
<物件tag>$zj#<a>:<b>:<max>
```

在物件清單中尋找 `tag` 相符者（tag = H.4.2 的指令欄），設定雙層血條：
A 層寬 = `a/max`，B 層寬 = `b/max`。

**找不到相符 tag 時應靜默忽略**——物件可能已經離開房間，這不是錯誤。

### H.4.9　`ESC001` 文字輸入面板 🟡

```
<說明文字>  $zj#  <指令樣板>
```

伺服器巨集：`#define INPUTTXT(a,b) ESA + "001" + a + ZJSEP + b`

**送出規則（點「確定」時）**

| 條件 | 送出內容 |
|------|---------|
| 樣板含 `$txt#` | 把 `$txt#` **就地替換**成使用者輸入 |
| 樣板不含 `$txt#` | `樣板` + **一個空格** + 使用者輸入 |

> ⚠️ **輸入是自由文字，不是數字。**
> 這是最容易誤判的地方：面板長得像數量輸入框，但伺服器實際拿它接
> 角色名稱、以空格分隔的四項屬性數列、聊天內容等。
> **必須**用文字輸入框，不得限制成數字型別。

**伺服器真實用例**（147 處中的 5 個）🟡

| 樣板 | 使用者輸入 | 實際送出 |
|------|-----------|---------|
| `name $txt#` | `張三 zhangsan` | `name 張三 zhangsan` |
| `edroom size $txt#` | `5` | `edroom size 5` |
| `washto $txt#` | `20 20 20 20` | `washto 20 20 20 20` |
| `chat $txt#` | `大家好` | `chat 大家好` |
| `jishou sword $txt#` | `500` | `jishou sword 500` |

看第 3 列就知道為什麼不能做成數字框：`20 20 20 20` 是四個數字加空格。

### H.4.10　`ESC010` NPC 對話框 🟡

payload 先以 **`$dh#`** 切成多個區塊，逐塊依**前 5 個字元**判斷：

| 前綴 | 意義 |
|------|------|
| `ok11.` | 「確定」鈕要送的指令。**可出現多次**，依序累積成指令序列 |
| `no11.` | 「取消」鈕要送的指令。**出現此塊才顯示取消鈕** |
| `numb.` | 顯示數量輸入框（最多 3 位數，預設值 `1`）。⚠️ **長度剛好等於 5 也要接受** |
| 其他 | **內容區塊**，再以 `$br#` 切成多行逐行處理 |

**內容行格式**

| 前綴 | 呈現 |
|------|------|
| `$exp#<文字>` | 經驗值標籤（綠） |
| `$god#<文字>` | 金錢標籤（金） |
| `$obj#<tag>,<圖檔名>,<品階>` | 物品圖示。點擊送出 `litem <tag>`；框依品階著色 |
| `#RRGGBB<文字>` | 指定顏色的文字行（**前 7 字元**是色碼） |
| 其他 | 一般文字行（走 H.5 樣式解析） |

**物品品階**：`1` 白、`2` 綠、`3` 藍、`4` 紫、`5` 金、`6` 紅、`7` 灰，其他為預設框。

**「確定」鈕行為（順序不可改）**

```
1. 若有數量輸入框且為空 → 拒絕關閉，提示「各項不能為空」
2. 關閉對話框
3. 若有數量輸入 → 把累積指令字串中的 $N 全部替換成輸入的數字
4. 以 $sock# 切開指令字串，逐條依序送出
```

**「取消」鈕**：若有 `no11.` 指令則送出，然後關閉。

**範例**
```
ESC010掌櫃笑道：「客官要幾壇女兒紅？」$br#$god#你身上有 320 兩銀子$dh#numb.$dh#ok11.buy $N wine$dh#no11.say 算了
```

### H.4.11　空記錄的處理 🟢

**記錄尾端常有多餘的分隔符**，例如 `a:1$zj#b:2$zj#`（結尾多一個 `$zj#`）。

所有以 `$zj#` / `$z2#` 切分的地方，**必須**濾掉切出來的空字串記錄，
否則畫面上會多出一個空白按鈕／空白清單項。

---

## H.5　行內樣式方言

### H.5.1　ANSI 形式（主要）🟢

payload 文字先以 **`ESC`（U+001B）切段**。每段開頭是樣式指令，其後是套用該樣式的文字。
**樣式是有狀態的**：設定後持續生效，直到被重置或覆寫。

| 指令 | 語意 |
|------|------|
| `[0m` 或任何以 `;0m` 結尾者 | **重置全部**（色、粗體、字級、連結、全形） |
| `[1m` | 粗體 |
| `[9m` | **全形模式**：後續 ASCII 轉全形（U+FF01–U+FF5E） |
| `[2J` | **清空主訊息區** |
| `[H` | 游標歸位（無視覺效果，移除此標記即可） |
| `[30m`–`[37m` | 前景色（一般） |
| `[1;30m`–`[1;37m` | 前景色（高亮） |
| `[40m`–`[47m` | 背景色（一般） |
| `[40;1m`–`[47;1m` | 背景色（高亮） |
| `[f#RRGGBBm` | 自訂前景色（擴充） |
| `[b#RRGGBBm` | 自訂背景色（擴充） |
| `[s:<n>]` | **字級**（擴充；⚠️ 結尾是 `]` 不是 `m`，參數前有冒號） |
| `[u:<url>]` | **超連結**（擴充；同上） |

**判斷順序（不可改）**

```
① 以 "[u" 開頭 → 找 "]"，取參數 → 設為連結
② 以 "[s" 開頭 → 找 "]"，取參數 → 設為字級
③ 以 "[2J" 開頭 → 清空主訊息區，切掉 3 字元
④ 以 "[H"  開頭 → 切掉 2 字元
⑤ 其餘 → 找第一個 "m"，依上表比對
```

> ⚠️ **①② 必須在 ⑤ 之前。**
> `[u:` 與 `[s:` 以 `]` 收尾。若先執行「找第一個 m」，
> `[u:cmds:look me]` 會一路找到後面某個不相干的 `m`，把整段文字吃掉。

> ⚠️ **那個冒號。**
> 伺服器巨集是 `ZJURL(w) → "[u:" + w + "]"`、`ZJSIZE(n) → "[s:" + n + "]"`，
> 參數前**有冒號**。取參數時要跳過 `[`、`u`、`:` 三個字元。
> 少跳一個的後果：URL 變成 `:cmds:…` 而點擊完全失效、字級解析失敗。
> 這兩個巨集在伺服器用了 **638 次**——解錯的話畫面會大面積失效。
> **建議兩種寫法都接受**（取出參數後若首字元是 `:` 就剝掉）。

### H.5.2　調色盤 🟠

原始客戶端不使用標準 ANSI 色值，而是自訂的一組。**必須逐值照抄**，否則顏色會整體偏掉：

```
前景（一般 [30m–[37m）
#000000  #aa3300  #00bb00  #eeee00  #0000aa  #aa00aa  #00bbbb  #aaaaaa

前景（高亮 [1;30m–[1;37m）
#000000  #ff3300  #88ff00  #ffff00  #0000ff  #ff00ff  #88ffff  #ffffff

背景（一般 [40m–[47m）
#222222  #aa0000  #00aa00  #aaaa00  #0000ff  #aa00aa  #00aaaa  #aaaaaa

背景（高亮 [40;1m–[47;1m）
#000000  #ff0000  #00ff00  #ffff00  #0000ff  #ff00ff  #00ffff  #ffffff
```

**淺底（白天）模式改寫**：索引 `{2, 3, 6, 7}`（即 `32`/`33`/`36`/`37`）一律改成索引 4（藍），
高亮版同理。理由是綠／黃／青／灰在淺色背景上幾乎看不見。

**ARGB → RGBA**：色值可能帶 alpha 前綴（如 `#99aa3300`，8 位）。
這是 **ARGB** 不是 RGBA，轉 CSS 時**必須**把 alpha 從頭搬到尾。

### H.5.3　連結 scheme 🟡🟠

`[u:<url>]文字[0m` 中的 `<url>` 依前綴決定行為：

| 前綴 | 行為 | 等級 |
|------|------|------|
| `cmds:` | **送出指令**（去前綴），並關閉互動面板 | 🟡 |
| `http://` `https://` | 開啟網頁 | 🟡 |
| `tel:` | 撥打電話 | 🟡 |
| `mailto:` | 開啟郵件程式 | 🟡 |
| `pops:` | 開啟彈出選單，參數格式同 `ESC020` | 🟠 |
| `voice:` | 播放語音訊息 | 🟠 |
| 其他 | 交給系統以外部瀏覽器開啟 | 🟠 |

> `cmds:` 讓伺服器能把訊息裡的**任意詞句**變成可點擊指令。
> 這是整個協議最靈活的一點，也是伺服器用量最大的擴充（302 處）。

### H.5.4　HTML 形式（指游 ZY 方言）🟡

指游的 web 版把樣式標記換成 HTML：

```c
#define ZYURL(cmds,msg)    "<a href=\"" + cmds + "\"><span>" + msg + "</span></a>"
#define ZYSIZE(size,msg)   "<font style=\"font-size:" + (650/size) + "rem;\">" + msg + "</font>"
#define ZYCOLOR(color,msg) color + msg + "</font>"
```

同一份 header 也保留了被註解掉的 ANSI 版，兩者同構：
`ZYSIZE(n)` 的 `650/n` rem 對應 ANSI 版 `[s:n]` 的「畫面寬 ÷ n」。

> ⚠️ **安全要求（不得違反）**
> HTML 形式代表**伺服器可以送任意標記到客戶端**。
> 實作**不得**直接把 payload 塞進 `innerHTML` 或等價的原始 HTML 插入 API。
> **必須**只白名單 `<a>` / `<font>` / `<span>` 三個標籤與其有限屬性
> （`href`、`style` 的 `font-size` / `color` / `background-color`），
> 其餘一律轉義成純文字。
> 這不是理論風險：聊天訊息會被原樣廣播給所有玩家，
> 一個玩家送出的標記會在**每一個**其他玩家的客戶端上執行。

---

## H.6　登入序列與側通道

### H.6.1　三種版本挑戰（必須全部支援）🟡

連上後伺服器送的**第一行**決定後續一切。三個來源寫法各不相同：

| 寫法 | 對應方言 |
|------|---------|
| `ver1.0:<crypt>` | 經典／大梦江湖（**冒號**） |
| `ver1.0,<crypt>` | 經典（**逗號**） |
| `version 1.0 key::<crypt>` | 指游 ZY |

**只認其中一種，另外兩種伺服器就完全連不上**（而且症狀是「連上了但什麼都沒發生」）。

客戶端**必須**以這一行**同時決定**兩件事：套用哪個方言 profile（H.3.3）、以及後續欄位數（H.6.3）。

### H.6.2　帳號與伺服器列表（HTTP 側通道）🟠

帳號系統**不走 TCP**，而是獨立的 HTTP GET API。

| 端點 | 參數 | 回傳 |
|------|------|------|
| `/mobi/vers.php` | — | `<版本號>\|<公告文字>[\|…]` |
| `/mobi/reg.php` | `id`, `pass`, `phone`, `email`, `key` | 註冊結果 |
| `/mobi/loginto.php` | `id`, `pass`, `key`, `page` | 伺服器列表（見下） |
| `/mobi/check.php` | `ip`, `port`, `key` | 伺服器可用性 |
| `/mobi/userinfo.php` | `id` | 使用者資料 |
| `/mobi/updateuser.php` | `id`, `newpwd` / `phone` / `email` | 修改資料 |

**伺服器列表格式**：`loginto.php` 成功時回傳字串以 **`$l#`** 開頭，去掉前 3 字元後：

```
<分區1>|<分區2>|…|<email>
```

每個分區再以 **`&`** 分欄：

```
<分區名>&<ip>&<port>&<欄4>&<欄5>&<驗證key>
```

- 欄 1 = 顯示名稱、欄 2 = 遊戲伺服器 IP、欄 3 = port、欄 6 = 驗證 key（TCP 登入時要送）
- ⚠️ **最後一段是使用者 email，不是分區** —— 切分後必須把最後一段取走再處理

> ⚠️ **安全立場（新實作必須改掉）**
> 原始設計的簽章是 `key = MD5(<欄位串接> + <SALT>)`，而 SALT 是一個**寫死在客戶端內的常數**。
> 這**沒有任何實質安全性**：任何人拿到客戶端就拿到 SALT，可以自行偽造任意請求。
> 新實作若要自架帳號系統，**不得**沿用此設計，**必須**改成伺服器端驗證
> （例如短期 token + HTTPS）。本附錄記錄它只是為了與既有部署互通。

### H.6.3　TCP 登入狀態機 🟢🟡

出處 `world/adm/daemons/logind.c:72-300`，並經實機連線逐步驗證。

```
Client                              Server
  │                                    │
  │◀── IAC ×6 + 版本挑戰行 ─────────────┤   H.6.1
  │                                    │
  ├── 任意非 "//" 字串 ───────────────▶│   → "版本验证成功"
  │                                    │
  ├── 帳號║密碼║密文║email ───────────▶│   欄位數見 H.6.4
  │                                    │
  │◀── ESC000 "0007" ──────────────────┤   登入成功 → 直接進世界
  │    或                               │
  │◀── ESC000 "0008" ──────────────────┤   帳號無角色 → 需建立
  │                                    │
  ├── 性別║頭像║暱稱 ─────────────────▶│
  │                                    │
  │◀── ESC000 "0007" ──────────────────┤   建立成功
  │◀── ESC002/003/004/005/… ───────────┤   進入世界
```

**`ESC000` 狀態碼**

| 碼 | 意義 | 等級 |
|----|------|------|
| `0007` | 登入成功 | 🟡 `logind.c:241,253,318` |
| `0008` | 帳號存在但**沒有角色**，需送建角資料 | 🟡 `logind.c:268` |
| `0009` | 暱稱不符合要求 | 🟡 |
| `重连完毕` | 斷線重連完成 → 客戶端**自動送 `l`** | 🟠 |

> ⚠️ **`ESC000` 的 payload 既可能是數字狀態碼、也可能是中文字串。**
> 不得假設它一定是其中一種，更不得對它做 `parseInt` 之後就當數字用。

### H.6.4　欄位數隨方言變動 🟡

伺服器端是 `sizeof(myinfo) != N` 的**嚴格檢查**，欄位數不對會被直接退回。

| 訊息 | 經典 | 官方文件版 | 指游 ZY |
|------|------|-----------|--------|
| 登入 | `帳號║密碼║密文║email`（**4** 欄） | `帳號║密碼║密文`（**3** 欄） | 4 欄 |
| 建角 | `性別║頭像║暱稱`（**3** 欄） | 3 欄 | `性別║暱稱`（**2** 欄） |

- 分隔符是 **`║`（U+2551）**，不是 ASCII 的 `|`
- 行動客戶端建角時把「頭像」欄**留空**（送 `男║║名字`）——中間那欄是有意義的頭像欄位，不是筆誤
- 暱稱需通過 `check_legal_name(name, 4)`：中文、長度上限 4

### H.6.5　登入逾時（自動化必讀）🟡

伺服器對「連上到完成登入」有時間限制。超時會送：

```
您花在连线进入手续的时间太久了，下次想好再来吧。
```

然後關閉連線。

> ⚠️ **自動化測試或腳本登入時，三個步驟必須在同一輪連續送出。**
> 不要分次、不要中間等待人工操作，否則會被踢。
> 被踢之後客戶端若有自動重連，表現為「畫面又出現版本挑戰」——
> 很容易被誤判成「握手失敗」，其實是逾時。

### H.6.6　完整驗證機制與第三方客戶端的限制 🟡

官方文件揭露了本來的設計（多數自架伺服器把它註解掉了）：

```
伺服器 → ver1.0,<crypt(ZJKEY, 0)>
客戶端 → crypt(ZJKEY, str[2..3])       ← 需由官方網關產生
伺服器 → 版本验证成功
客戶端 → 帳號║密碼║crypt(ZJKEY,帳號)+crypt(ZJKEY,密碼)
```

**`ZJKEY` 只存在於伺服器端**，客戶端拿不到——官方作法是把挑戰串丟給官方網關換回應。

因此第三方客戶端**無法通過完整驗證**，除非伺服器停用該檢查（實務上絕大多數如此）。
本附錄描述的登入流程，前提是伺服器已停用此檢查。

---

## H.7　客戶端系統架構

> 本節是**語言中立**的架構規範。第六篇 §018–§028 是本專案 JS + Rust 的具體落地，
> 兩者可以對照，但實作者不必照抄那個模組切分。

### H.7.1　分層與依賴方向

```
        ┌──────────────────────────────┐
        │  ⑤ 呈現層（UI / DOM / 原生元件）│  ← 唯一可以碰畫面的層
        └──────────────┬───────────────┘
                       │ 只讀狀態、只呼叫回呼
        ┌──────────────▼───────────────┐
        │  ④ 狀態層（單一狀態樹 + 訂閱）  │
        └──────────────┬───────────────┘
                       │ reducer 套用事件
        ┌──────────────▼───────────────┐
        │  ③ 協議層（行 → 事件，純函式）  │  ← 不得有副作用
        │     ├ opcode 分派（H.2）       │
        │     ├ payload 解析（H.4）      │
        │     ├ 樣式解析（H.5）          │
        │     └ 方言表（H.3.3）          │
        └──────────────┬───────────────┘
                       │ 收到完整的一行
        ┌──────────────▼───────────────┐
        │  ② 傳輸層（TCP + IAC + 解碼）   │
        └──────────────┬───────────────┘
        ┌──────────────▼───────────────┐
        │  ① 平台層（socket / 橋接）      │
        └──────────────────────────────┘
```

**四條強制規則**

| # | 規則 | 為什麼 |
|---|------|--------|
| 1 | ③ 協議層**不得**引用任何 UI / 平台 API | 讓它能在無 UI 環境下被單元測試 |
| 2 | ⑤ 呈現層**不得**直接呼叫 ② 傳輸層 | 否則「送指令」會散落各處，無法統一記錄與攔截 |
| 3 | ③ 的輸出是**資料**，不是副作用 | 同一行輸入永遠產生同一個事件物件，可比對、可回歸 |
| 4 | 沒有任何模組反向依賴組裝層 | 避免循環相依 |

**可測的驗收方式**：在沒有 UI 的測試環境中直接載入 ③ 協議層的所有模組，**不得**拋錯。

### H.7.2　資料流（單向）

```
socket bytes
   → [傳輸層] 剝 IAC → 解碼 → 分行
   → [協議層] parse_line → decode → Event
   → [reducer] Event + State → State'
   → [狀態層] notify 訂閱者
   → [呈現層] 重繪受影響的區塊

使用者操作
   → [呈現層] 產生 Command
   → [組裝層] 統一送出
   → [傳輸層] write socket
```

> ⚠️ **不得**讓呈現層直接改狀態樹，也**不得**讓協議層直接碰畫面。
> 這兩條一旦破一次，之後每個 bug 都要在三個地方找原因。

### H.7.3　事件契約

協議層對每一行產出一個事件物件。建議的事件族群（名稱可自訂，**分類不應變**）：

| 族群 | 事件 | 來源 opcode |
|------|------|------------|
| `conn.*` | `notice`、`relocate`、`multiline`、`quit`、`echo` | `000`、`900`、`997`/`998`、`999`、`014` |
| `room.*` | `title`、`desc`、`descToggle`、`exits`、`exitRemove`、`exitClear`、`objects`、`objectRemove`、`objectBar` | `002`、`004`、`023`、`003`、`903`、`913`、`005`、`905`、`022` |
| `stat.*` | `bars` | `012` |
| `msg.*` | `main`、`toast`、`combat`、`combatClose`、`chat`、`float` | 預設、`015`、`016`、`017`、`100`、`024` |
| `overlay.*` | `prompt`、`detail`、`actions`、`dialog`、`map`、`pagedText`、`popMenu`、`web` | `001`、`007`、`008`/`009`、`010`、`011`、`013`、`020`、`045` |
| `ui.*` | `titleButtons`、`quickButtons` | `021`、`006` |

**未知 opcode** → `msg.main`，並帶 `unknownOpcode` 標記與**完整原文**。

### H.7.4　狀態樹與清空規則

狀態樹**至少**要有這些分支：

| 分支 | 內容 |
|------|------|
| `conn` | 連線狀態、目前方言 profile、multiline 旗標 |
| `room` | 標題、描述、出口清單、物件清單（含血條） |
| `stats` | 屬性條群 + 版面參數 |
| `messages` | 各頻道的訊息佇列（含容量上限，見 H.7.5） |
| `overlay` | 目前開啟的疊層與其內容 |
| `ui` | 標題列按鈕、自訂按鈕槽位 |

**`ESC002`（房間標題）的清空副作用是整個協議唯一的「批次重置」**，
必須在 reducer 中集中實作，**不得**散在各處：

```
on room.title:
    room.title  ← 新標題
    room.exits  ← 清空
    room.objects← 清空
    ui.dirPad   ← 隱藏
    overlay.combat ← 關閉
    ui.quickButtons← 關閉
```

### H.7.5　容量上限 🟠

不設上限的後果是掛機一夜之後記憶體被訊息吃光。

| 區域 | 上限（則） | 超過時 |
|------|----------:|--------|
| 主訊息區 | 50 | 移除最舊 |
| 戰鬥面板 | 50 | 移除最舊 |
| 迷你聊天列 | 100 | 移除最舊 |
| 「全部」聊天頁 | 500 | 移除最舊 |
| 「系統」聊天頁 | 500 | 移除最舊 |

### H.7.6　客戶端自動送出的指令 🟡🟠

除了使用者手動輸入，客戶端會在下列情境**自動**送出：

| 情境 | 送出內容 |
|------|---------|
| 收到 `ESC014` | payload 原文 |
| 收到 `ESC000` 且 payload == `重连完毕` | `l` |
| TCP 連線建立、通過版本挑戰後 | `<帳號>║<密碼>║<key>║<email>` |
| 點擊出口／物件／自訂按鈕／標題列／動作列／彈出選單 | 各自的指令欄 |
| 點擊屬性條（有第 4 欄時） | 該條的指令欄 |
| 對話框「確定」 | `ok11.` 累積字串（`$N` 已替換），以 `$sock#` 拆成多條依序送出 |
| 對話框「取消」 | `no11.` 指令 |
| 對話框物品圖示 | `litem <tag>` |
| 文字中的 `cmds:` 連結 | 前綴後的字串 |

> ⚠️ **所有自動送出都應經過同一個出口函式**，並且可被記錄。
> 否則一旦出現「客戶端瘋狂送指令」這類問題，會完全查不出是哪一條路徑觸發的。
> 本書第五篇記錄過一次同類事故：客戶端每秒對伺服器登入一次，被發現時已累計五萬次。

### H.7.7　錯誤處理原則

| 情況 | 處理 | **不得** |
|------|------|---------|
| 未知 opcode | 降級為一般訊息 + 標記 | 靜默丟棄 |
| 樣式指令無法辨識 | 忽略該指令、**保留文字** | 丟棄整段 |
| 欄位數不足 | 用預設值補、記錄警告 | 拋例外中斷整行 |
| 色值格式錯誤 | 保留原值或用預設色 | 讓整個訊息不顯示 |
| 單行解析拋錯 | 捕捉、記錄、該行降級為純文字 | 讓一行壞資料弄斷連線 |
| 文字解碼失敗 | 寬容模式，壞位元組換替換字元 | 中止串流 |

> 💡 貫穿全表的一句話：**壞掉的應該是一行，不是一條連線。**

---

## H.8　驗收測試矩陣

> 這一節的目的是讓實作者能**自己判斷做完了沒有**，不必等別人驗收。
> 每一列都是可自動化的。

### H.8.1　分層測試

| 層 | 測什麼 | 最小測項 |
|----|--------|---------|
| L1 傳輸 | IAC 剝除 | 18 位元組協商完整剝除；`IAC IAC` 還原成單一 `0xFF`；序列跨 read 邊界仍正確 |
| L1 傳輸 | 拒絕策略 | 對 6 組協商各回一個拒絕；**MCCP2 必須拒絕** |
| L2 分行 | 分幀 | `\r\n` 與 `\n` 都能切；半行留在緩衝區等下一次 |
| L3 opcode | 變長消歧義 | `ESC1085` → `1085`；`ESC004` + 「5 個人…」→ `004`（**不得**變 `0045`） |
| L3 opcode | 含字母 | `10a`、`2k1` 能被辨識 |
| L3 opcode | 重置碼兩種順序 | `ESC[2;37;0mESC002…` 與 `ESC002ESC[2;37;0m…` 都要取到 `002` |
| L4 payload | 每個結構化 opcode | H.4 各節至少一個正例 + 一個邊界例 |
| L4 payload | 空記錄 | 結尾多一個 `$zj#` 不產生空項目 |
| L4 payload | 只切第一個冒號 | `look /d/x/npc#1686` 保持完整 |
| L5 樣式 | 判斷順序 | `[u:cmds:look me]` 不被「找第一個 m」吃掉 |
| L5 樣式 | 冒號兩種寫法 | `[u:x]` 與 `[ux]` 都取到 `x` |
| L5 樣式 | ARGB → RGBA | `#99aa3300` → `#aa330099` |
| L6 狀態 | `ESC002` 副作用 | 送房間標題後，出口與物件確實被清空 |
| L6 狀態 | 容量上限 | 送 60 則主訊息後只剩 50 則 |
| L7 端到端 | 登入序列 | 三種版本挑戰各跑一次，都能進到「送帳號」那一步 |

### H.8.2　四條不可省的不變式

這四條是「測試全綠但實際會壞」的最後一道防線：

| # | 不變式 | 怎麼測 |
|---|--------|--------|
| 1 | **協議層無副作用** | 同一行輸入呼叫兩次，產出的事件物件深度相等；過程中不得寫入任何外部狀態 |
| 2 | **協議層可在無 UI 環境載入** | 在無 UI 的測試 runtime 中載入所有協議層模組，不得拋錯 |
| 3 | **未知 opcode 不遺失資訊** | 送一個表中沒有的 opcode，事件的 `text` 必須包含**完整原行**（含控制字元） |
| 4 | **撞號被隔離** | 在方言 A 下送 `605`、在方言 B 下送 `605`，必須產生**不同**事件 |

### H.8.3　實機驗收（最終）

自動化測試全綠**不等於**能用——本書第五篇整篇都在講這件事。最後三步必須用真伺服器：

```
① 連上 UTF-8 埠，確認 IAC 被剝乾淨、第一行是版本挑戰
② 完成登入 → 應看到房間標題、描述、出口按鈕同時出現
③ 點一個 NPC → 應看到詳情文字（007）+ 動作列（009）
```

第 ③ 步是最有代表性的：它同時驗證了發送量最大的兩個 opcode、
`cmds:` 連結、以及互動面板的清空副作用。**這一步通了，協議層基本就是對的。**

---

## H.9　已知未驗證與風險清單

> **本節列出的項目一律不得當成既成事實實作。**
> 全書的規矩是：標了等級的可以照做，標 🔴 的必須同時容忍其他形態。

| 項目 | 狀態 | 實作建議 |
|------|------|---------|
| `ESC013` 長文本分頁 | 🟡 定義存在，伺服器僅 1 處使用 | 實作但不必優化 |
| `ESC016`/`ESC017` 戰鬥面板 | 🟡 定義存在，此伺服器 **0** 次使用 | 保留 handler，訊息先併入主訊息區 |
| `ESC010` 對話框 | 🟡 定義完整，此伺服器 **0** 次使用 | 其他 mudlib 會用，**不得**刪除 |
| `ESC913`/`ESC023`/`ESC999` | 🟡 **0** 次使用 | 低優先 |
| `ZY607` 語音 | 🟡 官方標注「暫未實現」 | 留介面不實作 |
| `ESC012` 第 2 個版面參數 | 🟠 伺服器會送、客戶端忽略 | 解析後丟棄，**不要**猜它的用途 |
| 語音訊息（AMR / 上傳端點） | 🟠 | 與核心協議無關，可最後做 |
| `ESC024` 傷害飄字的精確時長 | 🟠 約 1.9 秒 | 可自訂，不影響互通 |
| 淺底模式色彩改寫的完整規則 | 🟠 已知 4 個索引 | 其餘色值行為未觀察到 |
| 擴充 opcode 在其他 mudlib 的語意 | 🔴 | **必須**分 profile，遇到未知一律降級 |
| `ESC012` 舊世代可能只送兩欄 | 🔴 **推論** | **必須**同時容忍 2 欄與 3 欄形態 |

> 🔍 老手視角──真正的門道
> 一份規格最危險的地方不是寫錯，而是**沒有標出哪裡沒把握**。
> 讀者無從分辨「這條是實測」與「這條是作者覺得應該這樣」，於是全部照做，
> 錯的那幾條就變成再也追不回來的技術債。
> 本附錄把 🔴 與 🟠 明確標出來，就是為了讓實作者知道：
> **這幾行你要自己驗一次，而且驗完請回頭改這份文件。**

---

## H.10　最小可玩集合

如果只想先做出「能登入、能走路、能看 NPC」的版本，這是最短路徑：

| 順序 | 做什麼 | 對應章節 |
|-----:|--------|---------|
| 1 | TCP + IAC 剝除 + 拒絕協商 + UTF-8 寬容解碼 | H.1 |
| 2 | 分行 + 變長 opcode 擷取 + 未知降級 | H.2 |
| 3 | 版本挑戰 → 登入 → `ESC000` 狀態碼 | H.6 |
| 4 | `ESC002` / `ESC004`（房間標題與描述） | H.3.1 |
| 5 | `ESC003`（出口）→ **此時已經可以走路** | H.4.1 |
| 6 | `ESC005`（物件清單）+ 一般訊息 | H.4.2 |
| 7 | `ESC007` + `ESC009`（詳情 + 動作列）→ **此時已經可以跟 NPC 互動** | H.4.4 |
| 8 | `ESC014`（代送指令）→ **此時大部分流程不會卡住** | H.3.2 |
| 9 | 樣式方言（顏色 + `cmds:` 連結） | H.5 |
| 10 | `ESC012`（屬性條）+ `ESC001`（文字輸入） | H.4.5、H.4.9 |

前 8 步做完就是一個**可玩**的客戶端。第 9、10 步之後就是一個**好用**的客戶端。
其餘所有 opcode 都是錦上添花，可以按 H.3.2 的發送量統計排序慢慢補。

**但在動手之前**，先看 H.15（架一台可測的伺服器）與 H.14（黃金樣本）——
沒有這兩樣，上面每一步都無法自證做對了；
以及 H.11（畫面要長什麼樣），否則第 4 步就會卡在「這些東西該畫到哪裡」。

> 💡 君之一席話
> **一份規格是否夠用，不看它寫了多少，而看讀的人能不能在第 5 步就跑起來。**
> 上面這張表刻意把「可以走路」放在第 5 步、「可以互動」放在第 7 步——
> 因為一個三個月才會動的實作，不管規格寫得多完整，都會在中途被放棄。

---

## H.11　畫面拓撲與 opcode 去處

> 前面十節講「怎麼把位元組變成事件」。這一節講**事件要畫到哪裡**。
> 少了這一節，實作者解得出每一個欄位，卻得自己發明整個介面——
> 然後與伺服器的假設對不上（例如伺服器以為「換房間會清空物件欄」，而你沒清）。

### H.11.1　畫面盤點 🟠

整個客戶端只有 4 個畫面，其中**只有一個是複雜的**：

| 畫面 | 用途 | 佔使用時間 |
|------|------|----------|
| 登入 | 帳號登入、註冊、分區選擇、建立角色 | 少 |
| **主場景** | **遊戲主體** | **約 95%** |
| 對話框 | NPC 對話（模態，`ESC010`） | 少 |
| 使用者中心 | 改密碼、綁手機／email（純表單 + HTTP） | 極少 |

**先做主場景。** 登入畫面可以先用一個最陽春的表單，使用者中心可以完全不做。

### H.11.2　主場景版面（七個帶）🟠

原始版面是直向手機、固定不旋轉，由上到下分成七個帶：

```
┌─────────────────────────────────────────┐
│ ① 迷你聊天列（可摺疊，預設隱藏）           │  ESC100
├─────────────────────────────────────────┤
│ ② 房間標題列                              │  ESC002 + ESC021 + ESC006(bs)
│   [房間名] [標題按鈕列 →→→] [摺疊鈕]       │
├─────────────────────────────────────────┤
│ ③ 房間描述（可隱藏）                       │  ESC004 / ESC023
├──────────┬──────────────────────────────┤
│ ④ 物件欄  │ ⑤ 主訊息區                     │  ④ ESC005/905/022
│  垂直捲動 │   ★ 所有疊層都蓋在這一區上       │  ⑤ 預設訊息 + 疊層
│          │                              │
├──────────┼──────────────────────────────┤
│          │ ⑥ 方向盤 ⇄ 自訂按鈕列（互斥）    │  ESC003/903/913 ⇄ ESC006
├──────────┴──────────────────────────────┤
│ ⑦ 底部工具列                              │  ESC006 的 b12–b17
└─────────────────────────────────────────┘
```

> ⚠️ **④ 與 ⑤ 的關係是本版面唯一需要特別注意的地方。**
> 物件欄是**獨立的左直欄**，不隨主訊息區捲動、也不被疊層蓋住。
> 所有疊層（互動面板、地圖、戰鬥面板…）都只蓋 ⑤，不蓋 ④——
> 因為玩家需要在看疊層的同時還能點物件切換目標。

### H.11.3　⑤ 主訊息區的疊層（互斥）🟠

基礎是一般訊息串。以下疊層**共用同一塊空間，同一時間只顯示一個**：

| 疊層 | 由什麼開啟 | 怎麼關 |
|------|-----------|-------|
| **互動面板** | `ESC001` / `ESC007` / `ESC008` / `ESC009` | 關閉鈕；或點擊**不含** `$txt#` 的動作 |
| 戰鬥面板 | `ESC016` | `ESC017`；或 `ESC002` 換房間 |
| 地圖 | `ESC011` | 關閉鈕 |
| 長文本分頁 | `ESC013` | 關閉鈕（另有上／下頁） |
| 網頁 | `ESC045` | 關閉鈕 |
| 彈出選單 | `ESC020` / `pops:` 連結 | 點任一項；或點遮罩 |
| 傷害飄字 | `ESC024` | 動畫結束自動移除 |
| 頂部橫幅 | `ESC015` | 約 5 秒後自動淡出 |

**互動面板的內部結構**（最複雜的一個，四個區塊由三個 opcode 分別填）：

```
┌──────────────────────────────────────────┐
│ 詳情文字（ESC007）                  [關閉] │
├──────────────────────────────────────────┤
│ 輸入框 [________] [確定]   ← 只有 ESC001 顯示 │
├────────────────────┬─────────────────────┤
│ 動作列 ①（ESC008）  │ 動作列 ②（ESC009）   │
│ 網格排列            │ 網格排列             │
└────────────────────┴─────────────────────┘
```

> ⚠️ **`ESC007` 會清空兩組動作列。**
> 順序永遠是「先 `007` 填詳情並清空動作 → 再 `008`/`009` 填動作」。
> 若實作成「`007` 不清空」，上一個 NPC 的動作按鈕會殘留在下一個 NPC 的面板上，
> 玩家點下去會送出對**上一個**目標的指令——這是很難查的錯。

### H.11.4　⑥ 方向盤 ⇄ 自訂按鈕列（互斥切換）🟠

**方向盤**（預設狀態）：3×3 九宮格，中央是房間名，八方向環繞。

```
┌────┬────┬────┐
│ ↖  │ ↑  │ ↗  │      西北  北  東北
├────┼────┼────┤
│ ←  │房間名│ →  │      西  [中央]  東
├────┼────┼────┤
│ ↙  │ ↓  │ ↘  │      西南  南  東南
└────┴────┴────┘
   額外出口（up / down / enter / out…）橫排在下方
```

- **只顯示伺服器有給的方向**，其餘隱藏（不是變灰，是隱藏）
- 中央房間名可點擊開地圖
- 不在羅盤八方向的出口一律排在下方（見 H.4.1）

**自訂按鈕列**：2 排 × 5 格 = 10 個插槽（`b1`–`b10`），由 `ESC006` 或使用者長按設定。
建議預設三個：`score`（屬性）、`i`（物品）、`skills`（技能）。

**切換規則**：`ESC006` 送來任何 `b1`–`b17` 時**自動切到自訂按鈕列**（見 H.4.3 的副作用）。

### H.11.5　★ opcode → 目的面板 全表

**這是把 H.4（怎麼解）接到 H.11（畫到哪）的那張表。**
附錄 A 只對擴充 opcode 標了「去處」，核心 27 個的去處在此補齊：

| opcode | 目的區塊 | 動作 |
|--------|---------|------|
| `ESC002` | ② 標題列 | 設定房間名 ＋ **觸發批次清空**（見 H.7.4） |
| `ESC004` | ③ 房間描述 | 取代 |
| `ESC023` | ③ 房間描述 | 顯示／隱藏切換 |
| `ESC003` | ⑥ 方向盤 | 建立／更新出口鍵 |
| `ESC903` | ⑥ 方向盤 | 移除單一方向 |
| `ESC913` | ⑥ 方向盤 | 清空全部 |
| `ESC005` | ④ 物件欄 | 建立清單 |
| `ESC905` | ④ 物件欄 | 依 tag 移除 |
| `ESC022` | ④ 物件欄 | 依 tag 更新血條 |
| `ESC021` | ② 標題按鈕列 | 重建 |
| `ESC006` | ⑥ 自訂按鈕列 ＋ ⑦ 工具列 ＋ ② 房間名(`bs`) | 依槽位分派 |
| `ESC012` | 屬性條群（常置於 ② 或 ③ 之間） | 重建 |
| `ESC001` | ⑤ 互動面板（含輸入框） | 開啟 |
| `ESC007` | ⑤ 互動面板 · 詳情 | 填入 ＋ **清空動作列** |
| `ESC008` | ⑤ 互動面板 · 動作列① | 重建 |
| `ESC009` | ⑤ 互動面板 · 動作列② | 重建 |
| `ESC010` | 對話框（**獨立模態**，不在 ⑤ 內） | 開啟 |
| `ESC011` | ⑤ 地圖疊層 | 填入 |
| `ESC013` | ⑤ 長文本疊層 | 填入 |
| `ESC020` | ⑤ 彈出選單疊層 ＋ 遮罩 | 開啟 |
| `ESC045` | ⑤ 網頁疊層 | 開啟 |
| `ESC015` | 頂部橫幅 ＋ 系統頻道 | **兩個地方都要寫** |
| `ESC016` | ⑤ 戰鬥面板 ＋ 系統頻道 | **兩個地方都要寫** |
| `ESC017` | ⑤ 戰鬥面板 | 關閉並清空 |
| `ESC024` | ⑤ 之上（動畫層） | 一次性動畫 |
| `ESC100` | ① 迷你聊天列 ＋「全部」聊天頁 | **兩個地方都要寫** |
| *預設／未知* | ⑤ 主訊息串 | 附加（保留原文） |

> ⚠️ **`015`／`016`／`100` 是「一送兩寫」。**
> 它們同時進入一個即時區塊與一個歷史頻道。
> 只寫一邊的症狀是「訊息看得到但翻不到歷史」或「歷史有但當下沒感覺」，
> 兩種都會被回報成「聊天壞了」。

### H.11.6　顯示模式 🟠

三種模式，只影響配色（見 H.5.2）：

| 模式 | 特徵 |
|------|------|
| `night` | 深底，文字暖褐 `#ddbb99` |
| `day` | 淺底，**綠／黃／青／白前景一律改寫為藍**以維持可讀性 |
| `mud` | 純文字風格，文字 `#aaaaaa`，最接近傳統終端機 |

### H.11.7　共通互動規則 🟠

| 行為 | 規格 |
|------|------|
| **按鈕 → 指令** | 每個可點元件身上掛一個「指令字串」，點擊即送出。**全客戶端統一機制** |
| 點擊物件 | 送指令 ＋ 記錄「目前目標」（名稱與 id） |
| 動作含 `$txt#` | 點擊後**保持面板開啟**（供連續購買這類操作） |
| 關閉互動面板 | 同時收起軟鍵盤、清空數量輸入框 |
| 長按自訂按鈕 | 跳出輸入框，格式「名稱,指令」，存入本地偏好 |
| 自動捲動 | 聊天列、戰鬥面板、主訊息區收到新訊息一律捲到底 |

---

## H.12　會話生命週期

> H.6 講的是「一次成功的登入」。這一節講**其餘所有情況**：斷線、逾時、換伺服器、重連。
> 這是掛機型應用最常被略過、也最常出事的一塊。

### H.12.1　連線狀態機 🟠

```
      ┌──────────┐
      │   IDLE   │
      └────┬─────┘
           │ connect()
      ┌────▼───────────┐
      │  CONNECTING    │
      └────┬───────┬───┘
      成功 │       │ 失敗
      ┌────▼───┐   │
      │  OPEN  │   │
      └──┬───┬─┘   │
    斷線 │   │ 使用者主動關閉
      ┌──▼───▼─────▼────┐
      │  RECONNECTING    │──── 超過重試上限 ──▶ FAILED
      └────────┬─────────┘
               │ 成功
               └──────────▶ OPEN
```

**重連策略**：指數退避 `1s → 2s → 4s → 8s → 16s → 30s(上限)`，最多 **8** 次，
且**必須**可由使用者隨時取消。

### H.12.2　重連完成的信號 🟠

伺服器支援重連。重連成功後會送：

```
ESC000重连完毕
```

客戶端收到後**必須自動送 `l`**（look）重新取得房間狀態——
因為重連後伺服器不會主動重送房間面板，不送 `l` 的話畫面會停留在斷線前的舊狀態，
玩家會以為還在原地，實際上角色可能已經被移動過。

### H.12.3　進世界之後的「三拍 look」🟡

**這一條是實務上最容易漏、且症狀最像 bug 的一項。**

收到 `ESC000 0007`（登入成功）之後，畫面**不一定**會自動出現房間面板，原因有二：

1. 某些 mudlib 的 `enter_world()` 要**再過幾拍**才把角色移進起始房間 —— 太早 look 等於看空氣
2. 某些 mudlib 進世界後還掛著 `请敲回车键［ＲＥＴＵＲＮ］` 的輸入等待 ——
   此時所有面板 opcode 都被緩衝住，一個字都出不來

**規範作法**：客戶端在收到 `0007` 之後**分三拍送 `look`**，間隔 **500ms / 2500ms / 5000ms**。
第一次很可能被當成那個 RETURN 的答案吃掉，第二次才是真正的 look。

> ⚠️ 沒做這件事的症狀是「登入成功了，但畫面一片空白」，
> 而且**重開一次有時又好了**（時序問題）——這種間歇性症狀會被誤判成連線不穩，
> 實際上是少了這三拍。

### H.12.4　登入逾時與換伺服器

| 事件 | 觸發 | 處理 |
|------|------|------|
| 登入逾時 | 伺服器送逾時訊息後關閉連線（見 H.6.5） | 回到登入畫面，**不要**自動重連（會再逾時一次） |
| 換伺服器 | `ESC900 <ip>:<port>` | 關閉現有連線 → 清空所有狀態 → 連新位址 → **重跑完整登入流程** |
| 伺服器主動踢 | `ESC999` | 立即結束，不重連 |
| `ZY609` 延時重登 | payload = 毫秒數 | 等待指定毫秒後回登入畫面 |

> ⚠️ **`ESC900` 換伺服器時必須清空狀態樹。**
> 沒清的話新伺服器的房間會疊在舊伺服器的物件與按鈕上，
> 而這些殘留元件身上掛的指令會被送到**新**伺服器去。

---

## H.13　本地狀態與偏好

需要持久化的項目如下（鍵名可自訂，**項目與預設值應照用**）：

| 鍵 | 預設 | 說明 |
|----|------|------|
| `theme` | `night` | `night` / `day` / `mud` |
| `fontScale` | `1` | 字級倍率 |
| `descHidden` | `false` | 房間描述是否摺疊（使用者手動偏好） |
| `lastHost` | — | 上次連線位址 |
| `lastPort` | **`5001`** | ★ **UTF-8 埠**。填 5003 會整頁亂碼（H.1.4） |
| `autoConnect` | `true` | 啟動時自動連線 |
| `rememberAccount` | **`false`** | ★ 見下方立場說明 |
| `accountId` / `accountPw` / `accountEmail` | `''` | 僅在 `rememberAccount` 為真時寫入 |
| `quickSlots` | `{}` | 槽位 → `{ label, cmd }`（長按自訂的按鈕） |
| `history` | `[]` | 指令歷史 |
| `historyLimit` | `200` | 歷史上限 |

**讀取失敗時必須靜默回退到預設值，不得拋例外**——
偏好是啟動最前面被讀的東西，一拋整個畫面就起不來。

**指令歷史規則**：空字串不記；**與上一筆相同不重複記**；超過上限從頭截斷。

> ⚠️ **帳密的儲存立場（必須寫進交接文件）**
> ZJMUD 協議本身是**明文傳輸**，本地加密不會提高真實安全性，只會製造安全感。
> 因此可以明文存——但**必須設成 opt-in**：`rememberAccount` 預設 `false`，
> 使用者不主動勾選就完全不寫入。
> 這個取捨要寫下來，否則下一個維護者會either 誤以為有加密、either 把它當成疏漏而「修掉」。

---

## H.14　黃金會話樣本（可直接當測試 fixture）

> **這一節是本附錄唯一「可以直接複製進測試檔」的內容。**
> 以下每一行都是從真實伺服器（LPMud-Name，`:5001`）擷取的原始位元組，**一字未改**。
> 它們不是對協議的理解，而是**線路上真正跑的東西**——
> 任何一條在你的實作上失敗，就代表接真伺服器會出問題。
>
> 表示法：`ESC` = U+001B。實際資料中它是單一控制字元。

### H.14.1　實機擷取樣本 🟢

| # | 原始行 | 必須解出 |
|---|--------|---------|
| 1 | `ESC002ESC[2;37;0mESC[1;31m阎罗殿ESC[2;37;0mESC[2;37;0m` | `room.title`，文字 = `阎罗殿`，前景 = `#ff3300` |
| 2 | `ESC004ESC[2;37;0m这里阴深恐怖…威严肃穆。ESC[2;37;0m` | `room.desc`，**尾端重置碼不留殘字** |
| 3 | `ESC003update here:ESC[1;35m更新地图ESC[2;37;0m$zj#info here:查看本地地图资料$zj#` | `room.exits` 共 **2** 筆（尾端空記錄被濾掉）；第 1 筆 `dir="update here"`、`slot=null`、`cmd="update here"` |
| 4 | `ESC005地藏王:look /d/register/npc/dizangwang#1686` | `room.objects` 1 筆，`cmd` **完整保留** `/` 與 `#` |
| 5 | `ESC005天性:look 天性$zj#天赋:look 天赋$zj#设置邮箱:look 设置邮箱$zj#转生:look 转生` | 4 筆 |
| 6 | `ESC022look /d/register/npc/dizangwang#1686$zj#850:850:850` | `room.objectBar`，`tag` **與第 4 行的 `cmd` 完全相等** |
| 7 | `ESC006b12:常用$br#指令ESC[2;37;0m:mycmds ofen$zj#b13:…$zj#b14:…` | `ui.quickButtons` 3 筆，`$br#` 已換成換行 |
| 8 | `ESC021飞行:help ditu$zj#客栈:recall$zj#…$zj#ESC[1;32m其ESC[1;36m他ESC[2;37;0m:help qt` | `ui.titleButtons`，最後一筆標籤跨兩種顏色 |
| 9 | `ESC007姓名：某某 (ESC[1;32mNameESC[2;37;0m) …ESC[u:cmds:uplv -l]ESC[s:26]ESC[1;33m【角色等级】…$br#` | `overlay.detail`；**`[u:` 與 `[s:` 的冒號在此直接被證實** |
| 10 | `ESC009$1,1,10,28#ESC[1;32m修改密码ESC[2;37;0m:changepasswd` | `overlay.actions`，`column=2`，版面 `[1,1,10,28]` |
| 11 | `ESC0000007` | `conn.notice`，狀態碼 `0007`（登入成功） |
| 12 | `ESC0000008` | `conn.notice`，狀態碼 `0008`（需建角） |
| 13 | `ESC905look 天性` | `room.objectRemove`，`tag="look 天性"` |

### H.14.2　這 13 行各自鎖住什麼

**這不是隨便挑的 13 行**，每一行都對應本附錄的一個陷阱：

| # | 鎖住的陷阱 | 對應章節 |
|---|-----------|---------|
| 1 | **重置碼在 opcode 之後**（不是之前） | H.2.1 |
| 2 | 尾端重置碼不可留殘字 | H.5.1 |
| 3 | **方向欄含空白** ＋ **尾端空記錄** ＋ 第 3 欄省略 | H.4.1、H.4.11 |
| 4 | **只切第一個冒號**（`/` 與 `#` 不可被切） | H.4.2 |
| 6 | 血條 tag **必須等於**物件 cmd | H.4.8 |
| 7 | `$br#` → 換行 | H.4.3 |
| 8 | 一個標籤跨多段樣式 | H.5.1 |
| 9 | **`[u:` / `[s:` 的冒號** | H.5.1 |
| 10 | 版面前綴解析 ＋ `009` 是右欄 | H.4.0.1 |
| 11–12 | `ESC000` 是**狀態碼**不只是文字 | H.6.3 |
| 13 | `905` 用 tag 移除 | H.4.2 |

> 🔍 老手視角──真正的門道
> 一份規格最有價值的部分往往不是文字，而是**一組會失敗的輸入**。
> 上面 13 行的價值在於：它們全都是**真實資料**，而且每一行都精確對應一個「照直覺寫就會寫錯」的地方。
> 第 3 行尤其典型——`update here` 是個帶空白、不是羅盤方向、第 3 欄還省略的出口，
> 一行同時踩三個坑。任何實作者只要跑通這 13 行，協議層基本就不會有大問題。
> **拿到新協議時，先想辦法擷取一份真實會話，再開始寫程式碼**——
> 順序反過來的人，會花三倍時間在辯論「規格應該是什麼意思」。

### H.14.3　怎麼用這份樣本

```
1. 把 13 行原樣存成 fixture（ESC 用 ）
2. 對每一行呼叫你的 decode，比對「必須解出」那一欄
3. 這 13 條全綠 → 協議層可以交付
4. 之後每次改協議層，這 13 條都必須維持全綠（回歸測試）
```

---

## H.15　可驗證的伺服器環境

> **沒有伺服器就無法驗收。** H.8.3 的實機三步、H.14 的樣本比對，
> 都需要一台能跑的 ZJMUD 伺服器。這一節說明最小需要什麼。

### H.15.1　一台 zjmud 伺服器至少要有的三樣東西 🟡

| # | 元件 | 位置 | 職責 |
|---|------|------|------|
| 1 | **登入入口** | `adm/daemons/logind` | 握手 → 版本確認 → 帳號欄位 → 建角欄位 → 狀態碼 → 進世界（H.6.3 的全部） |
| 2 | **面板 daemon** | `adm/daemons/zjmudd` | 把 mudlib 既有資料翻成 opcode |
| 3 | **協議標頭** | `include/zjmud.h` | 提供 `ESA`、`SYSY`、`ZJTITLE`… 等巨集 |

> ⚠️ **登入入口必須注入 `logind` 本身，不能做成獨立 daemon。**
> 建立新角色要呼叫 `init_new_player()`，而它在每一家 mudlib 都是 `private` ——
> 從外部呼叫不到。

### H.15.2　面板 daemon 的三個介面 🟡

它是**轉換層**，不是原 mudlib 的一部分：原本的文字輸出完全不動，只是額外再送一份 opcode。

```lpc
void room_panel(object me, object env);   // 送 002 → 004 → 003 → 005 → 006/021/022
void status_panel(object me);             // 送 012
void item_panel(object me, object ob);    // 送 007 + 008
```

**觸發點是 look 指令**——在房間文字輸出之後掛一行：

```lpc
"/adm/daemons/zjmudd"->room_panel(me, env);
"/adm/daemons/zjmudd"->status_panel(me);
```

> 💡 這個設計解釋了一件事：**為什麼面板總是跟著 `look` 一起出現。**
> 客戶端每次點出口、點物件，實際上都在觸發伺服器重跑一次 look，
> 於是整組面板 opcode 被重送。這也是為什麼 H.12.3 的「三拍 look」有效。

### H.15.3　最小驗證流程

```
① 起伺服器，telnet 連 :5001
② 應立刻收到 6 組 IAC + 版本挑戰行
③ 送任意非 "//" 字串 → 應回「版本验证成功」
④ 送 帳號║密碼║key║email → 應收到 ESC000 0007 或 0008
⑤ 送 look → 應收到 ESC002 / ESC004 / ESC003 / ESC005 一整組
```

**第 ⑤ 步收到的東西，就是 H.14 那份樣本的來源。**
自己擷取一份，就有了屬於自己伺服器的黃金樣本。

### H.15.4　沒有伺服器時的退路

如果暫時拿不到伺服器，**仍然可以完成協議層**（H.1–H.5）：

| 能做 | 不能做 |
|------|-------|
| 用 H.14 的 13 行當 fixture 做單元測試 | 驗證登入流程（H.6） |
| 驗證所有 payload 解析 | 驗證三拍 look（H.12.3） |
| 驗證樣式與調色盤 | 驗證面板副作用的實際觀感 |
| 驗證未知 opcode 降級 | 確認方言 profile 判別正確 |

> ⚠️ **但不要以為單元測試全綠就等於能用。**
> 本書第五篇整篇都在講這件事：七次交付了不能用的東西，
> 其中好幾次都是「測試全綠、真機一按就死」。
> H.14 的 13 行能擋住協議層的錯，擋不住時序、狀態與版面的錯。

---



# 附錄 I　外部文獻與交叉佐證

> 本附錄處理三批**書外的資料**：`參考書籍`（3,102 檔 / 4.5 GB）、
> `src`（16,552 檔 / 17 GB）、`20260214MUD`（834 檔 / 12 GB）。
>
> 它存在的理由不是多列一份書目，而是回答一個問題：
> **這些外部資料，能不能證實、補強、或推翻本書已經寫下的斷言？**
>
> 結論先講：找到了**兩份一手史料**、**一份社群文件**與**一組七版原始碼序列**，
> 讓本書關於中文 mudlib 血緣的敘事從「靠檔名推斷」變成「有當事人自述 ＋ 有縱向量測」。
> 其中 I.2 那篇訪談是本附錄的核心——**ES2 的作者親口說明他為什麼要重寫一個 mudlib**，
> 而那個理由，正是本書第八篇整篇在談的東西。

---

## I.0　盤點與取捨

### I.0.1　三批資料的組成

| 分類 | 份量 | 是否採用 |
|------|------|---------|
| **一手史料**（1985 專書、Annihilator 訪談、ES2 規格文件） | 3 份 | ✅ **本附錄主體** |
| **社群技術文件**（Akuma〈一起從頭打造 mudlib〉、Mud 發展史家族樹、中文 MUD 大全） | 約 6 份 | ✅ 交叉佐證 |
| **mudlib／driver 原始碼封存**（ES2_lib 七個版本、各家 lib、FluffOS／MudOS／LDMud／Lima／Dead Souls） | 數十份 | ✅ **I.4 與 I.5 兩次量測的基礎**；其餘列為後續工作 |
| 中文 MUD 史網路文獻 | 約 5 份 | △ 僅收可交叉印證者 |
| 通用遊戲程式設計／遊戲 AI／C++ 電子書 | 約 2,900 檔、逾 4 GB | ❌ 與本書無關 |
| 工具程式（ConvertZ、MobaXterm、PCMan、Mudlet、CMUD 等） | 數十份 | ❌ 非文獻 |

> ⚠️ **排除比例約 99%，這件事本身就是結論。**
> 三個以 MUD 命名的資料夾，體積的絕大部分是通用遊戲開發電子書與工具程式。
> 這與本書第三篇 §009 講的是同一件事的另一個版本——`nt7` 的 header 定義了 102 個 opcode，
> `.c` 裡一個都沒用。
> **判斷一份資料有沒有用，要看內容，不要看它放在哪個資料夾、叫什麼名字。**

### I.0.2　使用立場

本附錄涉及的商業出版品（Duncan Howard 1985、Bartle《Designing Virtual Worlds》、
Ron Penton《MUD Game Programming》等）**著作權均屬原權利人**。

| 做 | 不做 |
|----|------|
| 記錄書誌資料（作者、出版社、年份、ISBN） | 重製內文 |
| 引用**事實**（人名、日期、事件、結構清單）——事實不受著作權保護 | 大段轉錄 |
| 短引句並標明出處 | 提供檔案本身 |
| 用它們**交叉檢驗**本書的斷言 | 取代閱讀原著 |

---

## I.1　一手史料①：《An Introduction to MUD》(1985)

### I.1.1　書誌

| 項目 | 內容 |
|------|------|
| 書名 | **An Introduction to MUD** |
| 作者 | **Duncan Howard** |
| **序言** | **Richard Bartle** ★ |
| 出版 | Century Communications Ltd, London |
| 年份 | **1985**；ISBN 0 7126 0691 2；© MUSE Ltd 1985 |

附錄 A 是**一份完整的對戰紀錄（logged game）**——1985 年的實機會話存檔。

### I.1.2　Bartle 序言中的六項一手事實 🟡

以下均為 Bartle 自述，非後人轉述：

| # | 事實 |
|---|------|
| 1 | 最初的 MUD 由 **Roy Trubshaw** 構思並寫下核心，時間是他在 **Essex 大學的最後一年** |
| 2 | Bartle 接手成為維護者，擴充地點數量與玩家可用指令 |
| 3 | 先在校內成為風潮；**PSS（Packet Switch Stream）**出現後吸引校外玩家，遠至**美國與日本** |
| 4 | MUD 太受歡迎，**拖慢了 Essex 的 DEC-10**，校方把可用時段**限制在半夜** |
| 5 | Bartle 曾把 MUD 複本借給其他有 DEC-10 的單位，**Aberdeen 與 Oslo 大學同樣祭出限制** |
| 6 | 當**凌晨三點三十人同時上線、DEC-10 每一條線都滿**時，兩人認定必須搬上**專用主機** |

> 💡 第 4、5、6 點合起來是一個很現代的故事：
> **一個內部工具因為太受歡迎而威脅到基礎設施，於是先被限流、最後被迫獨立部署。**
> 1980 年代初的 DEC-10 與今天的共用叢集，處理方式一模一樣。

### I.1.3　★ 三個來源，三個年份 —— 而且都沒錯

| 來源 | 說法 | 等級 |
|------|------|------|
| 本書附錄 D | **1978** MUD1 誕生 | 通說，**原本無出處** |
| Bartle 1985 年序言 | Trubshaw 在 **Essex 最後一年**寫下核心（即 **1980** 前後） | 🟡 一手 |
| 中文線史料 | **1979** 年 Essex 的 DEC system-10 上執行 | 🟠 二手 |

三者不衝突，因為指的是不同事件：

```
1978  Trubshaw 開始寫
1979  在 Essex 的 DEC-10 上跑起來
1980  Trubshaw 最後一年完成核心並交棒給 Bartle   ← Bartle 序言講的是這個
```

**處置**：附錄 D 的 1978 列補上本書為出處，並註明「1978 為起算年，核心完成與交棒約在 1980」。
**不改年份**——1978 作為「開始」正確，只是原本缺出處與這層區分。

> 🔍 老手視角──真正的門道
> 遇到「三份資料三個年份」時，多數人的反應是挑一個最權威的、把另外兩個丟掉。這幾乎總是錯的。
> **年份不一致，通常代表它們在講不同的事件，而不是有人記錯。**
> 正確作法是先問「這三個數字各自在標記什麼時刻」——
> 一旦問出「開始寫／跑起來／交棒」這三個里程碑，衝突就自動消失了。

---

## I.2　★ 一手史料②：Annihilator 訪談

> **這是本附錄最重要的一份文獻。**
>
> 受訪者 **Annihilator（安尼席洛特爾，本名 Minxin Zhang）**是
> **《東方故事》的開發者、《東方故事II》（ES2）的作者**——
> 而 ES2 是本書第八篇以後反覆出現、卻始終只能靠檔名間接指認的那套 mudlib。
>
> 本書寫到這裡為止，關於「ES2 是什麼、為什麼存在」，
> 用的全是**外部證據**：一個叫 `config.ES2` 的檔名、一個叫 `es2.muds.net` 的位址、
> 以及 17 份樣本互比出來的相似度。
> 這篇訪談第一次讓**當事人自己開口**。

### I.2.1　文獻性質與斷代

| 項目 | 內容 |
|------|------|
| 形式 | 問答訪談（共 14 問） |
| 受訪者 | Annihilator（Minxin Zhang），《東方故事》／《東方故事II》作者 |
| 轉載 | 部落格「喵与地下藤」，2007-06-20，發文者 Sunyanzi |
| **原訪談年代** | **約 1995–1996**（🔴 推定，理由見下） |

**斷代推理**（本書第十四篇的方法，用在文獻本身）：

| 線索 | 指向 |
|------|------|
| 受訪者說東方故事II「架構已經算是完成」但仍在修飾，**尚未正式開放測試** | 早於 ES2 公開釋出 |
| 提到「如果台灣真的走向**亞太營運中心**」 | 該政策為 1995 年前後的時事 |
| 提到東方故事「常常人滿為患」、仍在運作 | 東方故事（1993 開站）仍是主力 |

> ⚠️ **部落格的日期是 2007，訪談本身不是。**
> 這是引用網路轉載時最容易犯的錯：把轉載日當成原始日期。
> 本附錄採 1995–1996，並標 🔴，因為沒有找到原始出處與確切日期。

### I.2.2　受訪者的三段歷程 🟡

他自述的路徑很清楚，而且橫跨了兩大 MUD 家族：

```
Legend of Winds（Merc Diku mud，4040）
    ↓  ——換家族——
東方故事（LPmud，以 TMI-2 mudlib 為藍本）
    ↓  ——從頭重寫——
東方故事II ＝ ES2
```

- 大二下經由**交大資工系學長**接觸 MUD，第一次連的是國外的 MUD
- 在 Legend of Winds 時期做的是**擴充**：兼職／轉職系統、新技能、新法術
- 他特別提到 Legend of Winds 因為「敢」加變化而在當時少見，
  甚至有外國人來信要求加入國際 mudlist、探詢移植；**後來因為中文化，外國玩家漸漸少了**

> 💡 最後這一句話值得停一下。
> **中文化是中文 MUD 起飛的原因，同時也是它與國際線斷開的原因。**
> 本書附錄 D 寫過「中文線在 1993 年接走 TMI-2 之後，就與這張表的下半部完全斷開」——
> 這篇訪談給了那條斷線一個當事人的註腳：不是沒能力接軌，是**玩家自然流失**。

### I.2.3　他自己講的 LPMud 架構 🟡

訪談中有一段他向一般讀者解釋 LPmud 與 Diku 的差別。這段話的價值在於：
**它是 1990 年代中期一位實務管理者對「driver／mudlib 分家」的理解，而不是後人的整理。**

他的說法可以歸納成四點：

| # | 他的陳述 |
|---|---------|
| 1 | Diku 只要會 UNIX 上的 C，就幾乎可以做所有發展工作 |
| 2 | LPmud **把「可執行檔」跟「遊戲」分開**：driver 是翻譯並執行 LPC 的伺服器，mudlib 是**數以千計的 LPC 程式檔構成的「遊戲定義」** |
| 3 | driver 在國外有一群人持續發展改良，**可隨時經由 ftp 取得**，所以管理者通常只要發展自己的 mudlib |
| 4 | 但 mudlib 有數千個檔案、十幾萬行，他當時**沒有能力自行發展**，所以東方故事同樣找了 TMI-2 當藍本 |

他並把 TMI 展開為 **"The Mud Institution"**，
描述為「由外國人組成、專門為想做 LPmud 的人發展免費基礎 mudlib 的組織」。

> ⚠️ 他對 TMI 的展開與英文線常見的 **"The Mud Institute"** 略有出入。
> 本書採英文線一手資料的寫法（第十二篇 §071），此處僅記錄受訪者的原話，
> **不以此修改正文**——這是轉述差異，不是新事實。

他也點出 TMI-2 與 Merc Diku 的關鍵差別：
**TMI-2 只提供最基本功能與簡單的遊戲制度架構，所有區域、物品、怪物、技能、法術都得自己做。**
他認為這正是「絕大多數 LPmud 呈現的風貌差異都很大」的原因。

> 💡 這一句話解釋了本書第九篇 §050 量到的現象。
> 本書量出 17 份中文 mudlib 彼此 Jaccard 0.44–0.90、共用骨架但內容各異——
> 那不是巧合，是 TMI-2 的設計**逼出來的**：骨架給你，內容自己長。

### I.2.4　★ 他為什麼要從頭重寫 ES2 —— 四個理由

**這是整篇訪談對本書最重要的部分。** 他給出四個理由，全部是第一人稱：

| # | 理由 | 對應本書 |
|---|------|---------|
| 1 | **文化錯配**：東方故事沿用 TMI-2，而 TMI-2 的結構是以西洋 RPG 為考量；用西方觀點解釋東方事物會產生衝突 | §051、§052 |
| 2 | **效率**：東方故事常人滿為患，增加同時上線人數會使速度嚴重降低 | — |
| 3 | **TMI-2 本身的負擔**：它是「許多人集體創作的產物」，模組間有不少妥協下的結合方式，導致**記憶體與 CPU 的額外負擔**；且程式碼品質**良莠不齊** | §035 |
| 4 | **相容性斷裂**：**「新版的 driver 出爐竟然發生相容性的問題」** | ★ §035「相容性的價錢」 |

他舉的文化錯配例子非常具體：

> 西洋 RPG 中一個人的戰鬥力跟武器密不可分，但東方人的觀點比較強調人物的技能，
> 因此往往會發生**一個低等級的人物只要手中握著稀世寶刀，就可以輕易擊敗空手的武林高手**這種不合理情況。

他並說，由於東方故事已發展一段不算短的時間，**想把它拉回原來的方向已經可說是不可能了**。

> 🔍 老手視角──真正的門道
> 第 4 點是本書第八篇 §035「相容性的八類帳單」的**當事人版本**。
> 本書是從上游 ChangeLog 與套件矩陣**推導**出「相容性是要付錢的」；
> 這篇訪談則是一位管理者在 1990 年代中期親身撞上它，並且做出了最貴的那個選擇：**重寫**。
> 值得注意的是他列的四個理由裡，**只有第 2 個是效能，其餘三個都是「架構已經歪了、改不回來」**。
> 這是一個很好的提醒：**大重寫的真正動因通常不是慢，而是方向錯了而且鎖死了。**

### I.2.5　ES2 拿掉了什麼 🟡

他明確說東方故事II **取消了傳統 MUD 的 Guild、Level、Experience 制度**，
並希望用中醫的**「精、氣、神」**取代 Hit Points：

> 西洋 RPG 中常用所謂的 Hit Points 來解釋一個人「在喪失戰鬥能力之前所能承受的傷害」
> 其實是很粗糙而且籠統的，而中醫所說的「精、氣、神」不是更能符合這種數值在遊戲中的定義嗎？

他甚至說有「建立一個**東方的 D&D 系統**」的野心，希望 ES2 的制度能影響後來採用東方背景的 RPG 或 MUD。

> 💡 **這個野心實現了，而且規模遠超他當時的預期。**
> 本書第九篇 §052 量到的「屬性樹」、§053 量到的「升級／門派／裝備是同一種架構」，
> 以及本書反覆看到的 `qi`／`jing`／`neili` 欄位——
> 都是這段話的下游。附錄 H.4.5 那個 `氣血:180/200/200` 的封包範例，
> 追到底就是 1990 年代中期這一段「為什麼不用 Hit Points」的思考。

技術面他給了一個數字：ES2 **「大約有東方故事的三、四倍快」**。

### I.2.6　★ 他打算把 ES2 當 Freeware 發出去

這句話是整個中文 mudlib 生態的起點：

> 我希望東方故事II能像 TMI-2 mudlib 一樣成為一個供其他人使用的基礎 mudlib，
> 當目前的修飾告一段落之後，會把它當成 **Freeware** 供給有心想做 LPmud 的人使用。

**他做到了。** 而且後果是本書量測到的全部東西——
I.4 那 113 份封存、附錄 D.3 那條「東方故事 → 俠客行 → 外流分支」的鏈，
源頭都是這一個決定。

> 🔍 老手視角──真正的門道
> 這裡有一個對照組值得看：TMI-2 是「外國人組成的組織」有意識地做的免費基礎 mudlib，
> ES2 是一個人做完之後決定免費發出去。**兩者相隔幾年、動機不同，但產生了同一種結果**——
> 一個骨架被幾十上百個站台繼承，然後各自長成完全不同的樣子。
> 本書第十一篇 §070 談「擴充點的設計」時說過：**決定生態規模的不是功能，是有沒有人能在你的骨架上蓋自己的東西。**
> ES2 與 TMI-2 是同一條原則的兩次驗證。

### I.2.7　名字的來歷 🟡

他自述：接觸 MUD 半年左右，和同學玩一個國外的 LPmud 叫 **Kingdoms**，
扮演的角色是殺手，而該世界中殺手最強的武器是一把叫 **Annihilator** 的匕首，
「這把匕首在它的主人用它來暗殺敵人的時候會歇斯底里地大叫」。
後來在 Legend of Winds 玩 PK 時想取一個又長又難拼的字當名字，就沿用至今。

> 這段沒有技術價值，但它把 `annihilator@muds.net` 這個 email 從一個字串變回一個人。
> 本書量測了 113 份 mudlib 的目錄結構，最後追到的是**一個大學生在別人的遊戲裡撿到的一把匕首的名字**。

### I.2.8　訪談中的其他判斷

| 主題 | 他的說法 |
|------|---------|
| MUD 的樂趣 | 不在遊戲本身，而在**參與遊戲的人**；電子媒介提供「安全距離」之外的溝通 |
| MUD 的定義 | **MUD = Multi-User Domain**，字面上跟 Game 沒有絕對關係；「凡是具有『多人同時上線互動的即時系統』都應該可以歸類為 MUD」 |
| 非遊戲用途 | 國外已有人用 LPmud 的 driver 提供**電子會議**功能；他認為教育、金融、學術、資訊服務都有整合潛力 |
| 圖形化 | 加上聲光動畫「絕對可能」；但以「漫畫與小說」比喻圖形與文字介面——**小說並沒有被漫畫取代** |
| 獨立開發 vs 協作 | Diku 因為改任何一處都要重新編譯，多人同時線上寫程式會產生困擾；**LPmud 比較適合眾人共同發展**，但要視 mudlib 完成度而定 |

> 💡 「用 driver 提供電子會議」這一句，與本書第十三篇 §076–§077 記錄的
> 上游 LPC 標準庫／TUI／WebSocket／tracing 路線是同一個方向——
> **只是相隔了大約三十年。**

---

## I.3　ES2 mudlib 規格文件 (2.1.4 Draft)

### I.3.1　文件本身 🟡

| 項目 | 內容 |
|------|------|
| 標題 | ES2 mudlib 規格文件 (2.1.4 Draft) |
| 版權 | **© 2002 Minxin Zhang `<annihilator@muds.net>`** |
| 內容 | 目錄結構、simul-efun 清單、include header 清單 |

**這份規格與 I.2 的訪談相隔約七年，作者同一人。**
訪談裡他說「會寫一些說明文件說明整個 mudlib 的架構」——這份就是。

### I.3.2　十二個標準目錄 🟡

`/adm`（管理者程式與資料）、`/std`（標準物件）、`/obj`（通用物件）、
**`/feature`（物件特徵）**、`/daemon`（遊戲制度精靈）、`/d`（開放區域）、
`/cmds`（全域指令）、`/include`（系統表頭檔）、`/doc`（說明文件）、
`/data`（帳號與物件資料）、`/u`（巫師工作目錄）、`/log`（系統日誌）。

> `/feature` 值得注意：本書 §051 的結論是**「中文 mudlib 用特徵組合而非繼承樹」**，
> 那是從程式碼**歸納**出來的；而這份 2002 年的規格直接把 `/feature` 列為十二個標準目錄之一。
> 歸納得到了明文佐證。

### I.3.3　simul-efun 的三個家族 🟡

| 家族 | 代表 | 意義 |
|------|------|------|
| **中文化** | `chinese_number`、`chinese_period`、`to_chinese`、`break_chinese_string`、`currency_string`、`gender_self`、`gender_pronoun` | 為了讓 LPMud 講中文而新增的一整層 |
| **權限與路徑** | `creator_file`、`file_owner`、`wiz_level`、`wizhood`、`user_home`、`resolve_path`、`user_data`、`login_data` | uid/euid 體系的在地實作（對照第十篇 §061） |
| **覆蓋 efun 加檢查** | `destruct`、`users`、`livings`、`objects`、`exec`、`snoop` | 把 driver 的 efun 包起來再檢查一次 |

另有一組**相容層**：`tell_object`、`tell_room`、`shout`、`write`、`say`，
規格明言是「利用 `message` efun 提供**與早期 MudOS 的相容** efun」。

> ⚠️ 這組相容函式是活的地層。
> 2002 年的規格特地為「早期 MudOS」保留相容介面，代表當時已有大量既有程式碼依賴舊介面而不能改。
> 這與 I.2.4 第 4 點（新版 driver 造成相容性問題）是同一個故事的七年後續：
> **當年逼他重寫的那個問題，重寫完之後仍然要用相容層來擋。**

> 💡 `break_chinese_string`（依長度斷中文行）與 `currency_string`（「三兩銀子又十二文錢」）
> 說明了一件本書第八篇沒展開的事：
> **中文化不只是換字集，而是要在 simul-efun 層補一整套語言與文化的表現函式。**
> 排版斷行、量詞、人稱代名詞、時間與貨幣的中文表示法，全都得自己寫。

---

## I.4　★ 新量測：用 ES2 規格重新量 113 份 mudlib

> 本書 §050 的量測是「樣本**互相**比對 Jaccard 相似度」——只能看出誰跟誰像，
> 看不出**像什麼標準**。現在有了一份外部的、獨立的 2002 年規格，可以問更好的問題：
> **這些 mudlib 有多接近 ES2 規定的樣子？**

### I.4.1　方法與樣本

拿 I.3.2 那十二個標準目錄，比對姊妹倉庫封存的每一份 mudlib 的檔案索引
（`libs/*/mudlib.json` 的 `dirs`，取頂層目錄名），計算命中數。

**樣本數：113 份。**（本書正文寫 17 份，那是寫作當時的封存量，之後持續擴充。）

### I.4.2　結果一：各標準目錄的命中率

| ES2 標準目錄 | 命中份數 | 命中率 |
|-------------|--------:|------:|
| `/cmds`、`/include`、`/log` | 各 113 | **100.0%** |
| `/adm` | 111 | 98.2% |
| `/d` | 109 | 96.5% |
| `/data` | 107 | 94.7% |
| `/feature` | 106 | 93.8% |
| `/doc` | 99 | 87.6% |
| `/u` | 96 | 85.0% |
| `/obj` | 63 | 55.8% |
| **`/std`** | **48** | **42.5%** |
| **`/daemon`** | **47** | **41.6%** |

前九項幾乎普世（85% 以上，三項 100%）。一份 2002 年的規格在 113 份來歷各異的 mudlib 上
仍有這種命中率，說明它記錄的不是某一家的作法，而是**整個中文 LPMud 圈的事實標準**。

但 `/std` 與 `/daemon` 掉到約 42% —— 這個斷崖不是雜訊。

### I.4.3　★ 結果二：兩種骨架，幾乎完全互斥

`/std` + `/daemon` 是 ES2 規格的骨架；`/inherit` + `/kungfu` 是本書 §050 認定的俠客行骨架。交叉列表：

| 分類 | 份數 | 比例 |
|------|-----:|-----:|
| **只有 ES2 骨架**（`std`／`daemon`） | 45 | **39.8%** |
| **只有俠客行骨架**（`inherit`／`kungfu`） | 62 | **54.9%** |
| 兩者皆有 | 3 | 2.7% |
| 兩者皆無 | 3 | 2.7% |

**113 份裡 94.7% 乾淨落在其中一邊，只有 6 份例外。**
而且落點完全符合血緣敘事：**`dongfanggushi2`、`esI`、`es1_win` 全在 ES2 那一側。**

### I.4.4　這代表什麼

本書原本說「`dongfanggushi2` 對其他樣本的相似度僅 0.012 —— 另成一族」。
那是用相似度看出來的，只知道它不一樣，不知道它**依循什麼標準**。現在可以更精確地說：

> **兩族的分界，就是「有沒有照 ES2 規格的 `/std` + `/daemon` 佈局」。**
> 俠客行那一支在 1995 年從東方故事II 分出去之後，**把骨架換成了 `/inherit` + `/kungfu`**，
> 而且此後所有下游分支都繼承了這個換過的骨架——六十二份，一份都沒有換回去。

這讓 §050 的一項**推論**升級成**對照外部規格的可重跑量測**。

> ⚠️ **一個必須說清楚的細節。**
> I.2.4 說 ES2 是「從頭重寫」以擺脫 TMI-2，但 I.4.3 顯示 ES2 系保留了 `/std` + `/daemon`
> 這個 TMI 式佈局。兩者不矛盾——**重寫的是程式碼與遊戲制度，不是目錄骨架。**
> 這正是本書第十四篇的核心觀察在 mudlib 尺度上的重演：
> **骨架比內容保守得多。** 內容可以整個換掉，目錄名往往原封不動地傳下去，
> 於是它就成了最好的斷代標記。

### I.4.5　重跑指令

在姊妹倉庫 `zjmud-rust` 根目錄執行：

```python
import json, os
L = 'libs'
A, B = ('std','daemon'), ('inherit','kungfu')
tab = {'ES2 only':0, '俠客行 only':0, 'both':0, 'neither':0}
for name in sorted(os.listdir(L)):
    p = os.path.join(L, name, 'mudlib.json')
    if not os.path.exists(p): continue
    d = json.load(open(p, encoding='utf-8', errors='replace'))
    tops = set()
    for x in (d.get('dirs') or []):
        s = x if isinstance(x, str) else (x.get('path') or '')
        s = s.strip('/').split('/')[0]
        if s: tops.add(s)
    a = any(k in tops for k in A); b = any(k in tops for k in B)
    tab['both' if a and b else 'ES2 only' if a else '俠客行 only' if b else 'neither'] += 1
print(tab)
```

> ⚠️ 份數會隨封存擴充而變動。**比例（約 40 / 55 / 3 / 3）才是結論，絕對數不是。**
> 重跑後若比例明顯偏移，代表新收樣本來自本書尚未辨識的第三支血脈——那會是新發現。

---

## I.5　★ 第二次量測：ES2 自己的七個版本

> I.4 是**橫向**的：把 113 份不同的 mudlib 拿去比對同一份規格。
> 這一節是**縱向**的：**同一套 mudlib 的七個連續版本**，
> 從 1994 年的開發中狀態一路到 2000 年的發行版。
>
> 本書第十四篇整篇在講「怎麼替一份沒有版本歷史的東西斷代」。
> 這一節是那套方法第一次拿到**有版本歷史的對照組**——
> 於是可以反過來問：**那套方法量出來的數字，到底代表多大的差異？**

### I.5.1　材料

| 封存 | 檔案數 | 檔案 mtime 範圍 | 中位數 |
|------|------:|----------------|--------|
| `ES2_lib.v1.1c` | 1,074 | 1994-11-03 → 1995-07-22 | 1995-04-19 |
| `ES2_lib.v1.1d` | 1,145 | 1994-11-03 → 1995-08-23 | 1995-05-19 |
| `ES2_lib.v1.1e` | 1,044 | 1994-11-03 → 1995-10-05 | 1995-06-04 |
| `ES2_lib.v1.2a` | 911 | **1995-11-15** → 1996-06-16 | 1995-11-15 |
| `ES2_lib.v1.2b` | 1,028 | 1995-11-15 → 1996-10-03 | 1995-11-20 |
| `ES2_lib.v1.3a` | 1,003 | 1995-11-15 → **2006-06-26** | 2000-08-07 |
| `ES2_lib.v1.3b` | 1,001 | 1995-11-15 → 2006-06-26 | 2000-07-31 |

**三個立即可讀的事實**

| # | 事實 | 意義 |
|---|------|------|
| 1 | **1.1 系列的最早檔案都是 1994-11-03** | **ES2 的開發至遲在 1994 年 11 月已經開始**——比附錄 D 記的「1995 東方故事2」早了一年 |
| 2 | **v1.2a 把最早時間整個重置為 1995-11-15** | 整棵樹被重新產生過一次，是一道硬邊界 |
| 3 | v1.3 的 mtime 上限到 2006，中位數落在 2000 | v1.3 是 **2000 年的版本**，2006 只是後來重新打包的痕跡 |

第 1 點與 I.2 的訪談互相印證：他說接觸 MUD 是大二下，先做 Legend of Winds、再做東方故事，
然後才動手重寫 ES2——**1994 年 11 月正是那個「動手重寫」的時間點**。
而 v1.3b 的 `chinese.c` 檔頭寫著 `Copyright (C) 1994-2000 Annihilator`，
**作者自己標的起始年也是 1994。**

### I.5.2　規模曲線：總數不單調，標頭數單調

| 版本 | 總檔案數 | `include/*.h` |
|------|-------:|------------:|
| v1.1c | 1,074 | 20 |
| v1.1d | **1,145** | 24 |
| v1.1e | 1,044 | 25 |
| v1.2a | **911** | 28 |
| v1.2b | 1,028 | 28 |
| v1.3a | 1,003 | **40** |
| v1.3b | 1,001 | 40 |

**總檔案數在 v1.1d 觸頂後掉了 234 個**（v1.1d → v1.2a），
**但標頭檔數從頭到尾單調成長，20 → 40，剛好翻倍。**

> 💡 這兩條曲線的分歧就是「重構」的形狀：
> **介面（標頭）一路增加，實作（檔案）被砍掉再長回來。**
> 只看總檔案數會以為專案在萎縮；看標頭數才知道它在長。
> 量一個專案的成長時，**要挑「只增不減」的指標**——
> 標頭檔、匯出符號、設定項都算，總行數與總檔數不算。

### I.5.3　骨架什麼時候定型的

拿 I.3.2 那十二個標準目錄逐版比對：

| 版本 | `/adm` | `/std` | `/obj` | `/feature` | `/daemon` | `/d` | `/cmds` | `/include` | `/doc` | `/data` | `/u` | `/log` | 命中 |
|------|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| v1.1c | ● | ● | ● | ● | **·** | ● | ● | ● | ● | ● | ● | ● | **11/12** |
| v1.1d | ● | ● | ● | ● | **●** | ● | ● | ● | ● | ● | ● | ● | **12/12** |
| v1.1e – v1.3b | ● | ● | ● | ● | ● | ● | ● | ● | ● | ● | ● | ● | **12/12** |

**十二個標準目錄裡，十一個在 v1.1c（1995 年上半）就已經到位；
唯一缺席的 `/daemon` 在 v1.1c → v1.1d 之間補上，時間落在 1995 年 7–8 月。
從 v1.1d 起，往後六個版本、五年時間，這個骨架一格都沒有再變。**

> 🔍 老手視角──真正的門道
> 這是本附錄最能說明本書第十四篇論點的一個數字。
> **那份 2002 年的規格文件，記錄的是 1995 年 8 月就已經固定下來的結構。**
> 中間隔了七年，作者改了實作、換了程式風格、重排了目錄、發行了三個主要版本——
> **骨架一格沒動。**
>
> 這正是為什麼「目錄結構」可以拿來斷代與判血緣（I.4、§050）：
> **它是整個專案裡變化最慢的那一層。**
> 反過來說也給了一個實務提醒：**你今天隨手定下的頂層目錄名，
> 很可能會活得比你寫的每一行程式碼都久。**

v1.1c–v1.2b 另有三個**不在規格裡**的目錄：`/binaries`、`/ftp`、`/open`。
它們在 v1.3 消失，換成 `/bin` 與 `/test`——見 I.5.6。

### I.5.4　★ Jaccard 校準：同一套 lib 跨版本的距離

**這是本節最有價值的結果。**

本書 §050 用「檔名集合的 Jaccard 係數」判血緣，量出 17 份樣本落在 **0.44–0.90**，
而 `dongfanggushi2` 對其他樣本只有 **0.012**。
當時有一個沒辦法回答的問題：**0.44 到底算近還是算遠？**

現在有了對照組——**同一套 mudlib、同一個作者、連續七個版本**：

| | 1.1c | 1.1d | 1.1e | 1.2a | 1.2b | 1.3a | 1.3b |
|---|--:|--:|--:|--:|--:|--:|--:|
| **1.1c** | 1.000 | .792 | .652 | .435 | .388 | .332 | **.333** |
| **1.1d** | .792 | 1.000 | .783 | .429 | .393 | .323 | .324 |
| **1.1e** | .652 | .783 | 1.000 | .499 | .455 | .373 | .373 |
| **1.2a** | .435 | .429 | .499 | 1.000 | .845 | .661 | .663 |
| **1.2b** | .388 | .393 | .455 | .845 | 1.000 | .655 | .655 |
| **1.3a** | .332 | .323 | .373 | .661 | .655 | 1.000 | **.998** |
| **1.3b** | .333 | .324 | .373 | .663 | .655 | .998 | 1.000 |

**相鄰版本**

| 轉換 | Jaccard | 性質 |
|------|--------:|------|
| v1.3a → v1.3b | **.998** | 修補版（僅移除 2 檔、零新增） |
| v1.2a → v1.2b | .845 | 小改版 |
| v1.1c → v1.1d | .792 | 小改版 |
| v1.1d → v1.1e | .783 | 小改版 |
| v1.2b → v1.3a | .655 | **主版本跳號** |
| v1.1e → v1.2a | **.499** | **主版本跳號（最大斷裂）** |
| **v1.1c → v1.3b** | **.333** | **首版 vs 末版，相隔約五年** |

**兩個可以直接回頭修正本書理解的結論：**

| # | 結論 |
|---|------|
| 1 | **`dongfanggushi2` 的 0.012 是真的家族邊界。** 它比「同一套 lib 相隔五年」的 0.333 還要低 **27 倍**——這不是版本差異能解釋的距離 |
| 2 | ⚠️ **但 0.44–0.90 這個區間，無法區分「不同站台」與「同一套 lib 的不同版本」。** 因為同版本序列本身就橫跨 0.333–0.998，完全覆蓋了那個區間 |

> ⚠️ **這是對 §050 的一項誠實修正。**
> §050 的**主結論仍然成立**——兩族之間有巨大斷層（0.90 vs 0.012），
> 而斷層的存在正是「便宜的特徵夠用」的理由。
> **但族內那個 0.44–0.90 的分散，不能全部解讀成「站台各自演化」**，
> 其中一部分可能只是**同一份 lib 的不同版本**。
> 要分辨這兩者，Jaccard 不夠——需要 I.4 那種「對照外部規格」或上游那種「diff 核心檔」的方法。

> 🔍 老手視角──真正的門道
> 一個相似度數字本身沒有意義，**除非你知道「已知相同」與「已知不同」各是多少**。
> 本書 §050 量了三年才等到這組對照：
> **同一套 lib 的七個版本，給出了「已知相同」那一端的完整分佈（0.333–0.998）。**
> 沒有這條基線，0.44 這個數字可以被解讀成任何東西。
> 可落地的做法：**任何相似度／距離指標上線前，先找一組「你確定是同一個東西」的樣本跑一遍**——
> 那個分佈的下界，才是你的判定門檻能放到哪裡的依據。

### I.5.5　中文化 simul-efun 的出現時點

I.3.3 列了 2002 年規格中的中文化函式家族。逐版查它們的**定義**何時出現：

| 函式 | v1.1c | v1.1d | v1.1e | v1.2a | v1.2b | v1.3a | v1.3b |
|------|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| `chinese_number` | ● | ● | ● | ● | ● | ● | ● |
| `to_chinese` | ● | ● | ● | ● | ● | ● | ● |
| `gender_self` | ● | ● | ● | ● | ● | ● | ● |
| `gender_pronoun` | ● | ● | ● | ● | ● | ● | ● |
| `chinese_period` | · | · | · | · | · | **●** | **●** |
| `break_chinese_string` | · | · | · | · | · | **●** | **●** |
| `currency_string` | · | · | · | · | · | **●** | **●** |
| | **4/7** | 4/7 | 4/7 | 4/7 | **4/7** | **7/7** | **7/7** |

**1995–1996 的版本只有四個；三個在 2000 年的 v1.3 才補上。
2002 年的規格文件記錄的，正是 v1.3 的狀態。**

補上的那三個很說明問題：
`chinese_period`（中文時間表示法）、`break_chinese_string`（中文斷行）、
`currency_string`（「三兩銀子又十二文錢」）——
**全部是「排版與量詞」層次的東西，不是遊戲機制。**

> 💡 這條時間軸把 I.3.3 的觀察變得更精確。
> 中文化不是一次做完的：**先做能不能顯示（1994–95），
> 五年後才做好不好看（2000）。**
> 而「好不好看」那一批的內容——斷行、量詞、時間表示法——
> 正是本書附錄 H.5 在協議層反覆處理的同一類問題。

### I.5.6　v1.3：從「一個站台」變成「一份發行版」

v1.3 有三處與前六版不同，指向同一件事：

| 變化 | v1.1c – v1.2b | v1.3a / v1.3b |
|------|--------------|--------------|
| 根目錄名 | `es2/` | **`mudlib/`** |
| 站台執行期目錄 | 有 `/binaries`、`/ftp`、`/open` | **移除** |
| 新增 | — | **`Copyright.ES2`**、**`MANIFEST`**、`/bin`、`/test` |

`/ftp` 與 `/open` 是**一個實際運作中的站台**才需要的目錄；
`MANIFEST`（完整檔案清單）與 `Copyright.ES2`（授權聲明）則是**要發給別人**才需要的東西。

**這正是 I.2.6 那句承諾的兌現形態**——
他在訪談裡說「當目前的修飾告一段落之後，會把它當成 Freeware 供給有心想做 LPmud 的人使用」，
**v1.3 就是那個 Freeware 發行版。**

### I.5.7　授權文件裡的一條「指紋條款」

`Copyright.ES2` 標明日期 **Annihilator (03/30/2000)**。除了常見的非商業條款外，有一條值得單獨看：

> 三、使用者得於遵守以下限制之情況下，享有使用 ES2 mudlib 全部或部分之權利：
> (1) 不得使用於營利或任何以獲取金錢為目的之用途。
> **(2) 於終端使用者登入系統時，必須顯示使用 ES2 mudlib 之名稱及版本。**

**第 (2) 條是一條寫進授權條款的指紋要求。**

> 🔍 老手視角──真正的門道
> 這一條的效果，是**把「血緣可追溯」變成使用者的義務**。
> 本書花了整個 §050 與 I.4 在用技術手段判血緣——檔名集合、目錄骨架、核心檔 diff；
> 而作者在 2000 年就用**授權條款**要求每一個下游在登入畫面上自己報出身。
> **法律層的指紋比技術層的指紋便宜得多，也可靠得多**——如果大家遵守的話。
> 本書封存的樣本裡，實際遵守這一條的比例值得後續量測（列入 I.10 欠帳）。
>
> 對照本書自己的立場：本書 LICENSE 採 CC BY-NC-SA，
> 而 ES2 這份 2000 年的中文授權聲明——非商業、保留出處、要求標示——
> **精神上幾乎就是二十四年前的同一份東西。**

### I.5.8　順帶量到的兩件小事

| 觀察 | 數據 |
|------|------|
| **發行版裡留著編輯器備份檔** | `*.c~` 之類的檔案：v1.2b 有 **10 個**，v1.1d 有 6 個；到 v1.3 只剩 1 個 |
| **程式風格在 v1.3 換過** | v1.2b 是 `string chinese_number(int i)`；v1.3b 是回傳型別**獨立一行**，且縮排由 Tab 改為空格 |

第二項與 I.6 那份社群文件說的「各家差異在**目錄結構、繼承關係、乃至於函數命名風格**」對上了——
**風格差異不只出現在不同站台之間，同一個作者跨五年也會變。**

> ⚠️ **一個必須揭露的過程錯誤。**
> 本節初次量測中文化函式時，用的比對規則假設「回傳型別與函式名同一行」，
> 於是 v1.3 全部落空，一度看起來像是「`chinese_number` 在 v1.3 消失了、
> 只剩 `std/item/combined.c` 還在呼叫它」——**那會是一個很聳動但完全錯誤的結論**。
> 實際打開檔案才發現只是換了程式風格。
> 本節最終改用逐位元組比對並容忍兩種風格，才得到上表。
>
> 記在這裡是因為本書第五篇的主題就是這個：
> **一個看起來很有話題性的發現，要先懷疑是不是自己的工具有問題。**

### I.5.9　本節的限制

| 限制 | 說明 |
|------|------|
| 檔案 mtime 可能被打包工具改寫 | v1.3 的 2006 上限即為一例；本節只採**下界**（最早日期）與**中位數**作為推論依據 |
| 七個版本不必然是完整序列 | v1.1a／v1.1b 未在手上；1.1c 之前可能還有 |
| 只量檔名集合與目錄結構 | 未做內容 diff；同名檔案的內容變化未計入（這正是上游 `fluffos/mudlibs` 方法的強項，見 I.4） |
| 未驗證這七版是否能實際開機 | 本節純為靜態量測 |

**重跑方式**：解開七個 `.tar.gz`，對每一版取檔案清單
（注意根目錄名 v1.3 起由 `es2` 改為 `mudlib`，需正規化），
即可重現 I.5.2 – I.5.5 的所有數字。

---

## I.6　社群佐證：〈一起從頭打造 mudlib〉

一份中文 MUD 社群的技術長文，開頭就給出這個生態的自我認知：

> 一般來說，目前我們常見的就是 **xkx 類，fy 類，xyj 類**的 lib。
> 雖然這些 lib 最初都源自**台灣安老大的 ES2 mudlib**，但經過這麼多年的發展或者不發展，
> 各自都有很多自己的特色（不是指遊戲內容，而是**目錄結構，繼承關係，乃至於函數命名風格**等）。

**這段話同時做了兩件事：**

| 它說的 | 對應本書 |
|--------|---------|
| xkx（俠客行）、fy（風雲）、xyj（西遊記）三大類 **最初都源自 ES2** | 佐證附錄 D.3 的血緣鏈，並把源頭明確指到 ES2 而非籠統的「東方故事」 |
| 差異在**目錄結構**、繼承關係、函數命名風格，**而非遊戲內容** | ★ **正是 I.4 量測所用的軸線** |

> 🔍 老手視角──真正的門道
> 這是本附錄最令人安心的一份文獻，理由不是它說了什麼新東西，而是**它與量測獨立而一致**。
> I.4 是拿一份 2002 年的規格去比對 113 份檔案索引得到的數字；
> 這篇是一位實務者憑經驗寫下的印象。兩者**互不知情**，卻同時指向「目錄結構是分家的標記」。
> **當「數出來的」與「圈內人講的」對上時，結論才算站得住。**
> 反過來說——如果量測結果與圈內人的常識矛盾，該懷疑的通常是量測，不是常識。

---

## I.7　Mud 發展史：一份完整的家族樹

`src` 中的 `Mud發展史.txt` 是一份 ASCII 繪製的 MUD 家族譜系，從 MUD1 一路展開：

```
MUD1 ─┬─ MUD2
      └─ MIST ─┬─ Mundi ── Island ── CamMUD
               └─ AberMUD ── AberMUD3 ─┬─ AberMUD5
                                       ├─ AberMUD4 ── Dirt ─┬─ dyrt
                                       │                    └─ idirt ── FreeDIRT
                                       ├─ Diku ─┬─ Silly ── DaleMUD
                                       │        ├─ Copper ── Merc ─┬─ ROM ─┬ Oblivion / Ember / ROT / VUM
                                       │        │                  ├─ Envy ── Mythran
                                       │        │                  ├─ Ack / The_Isles / Smaug
                                       │        └─ Sequent / Pirate / Vie / Circle / DikuII
                                       └─ LP ─┬─ DGD
                                              └─ LPMud_2.4.5 ── LPMud_3.0 ─┬─ CD
                                                                           ├─ LPMud_3.1.2 ── LPMud_3.2 (Amylaar)
                                                                           └─ LPMud_3.1.2-DR ── MudOS
```

**與本書的關係**

| 項目 | 比對結果 |
|------|---------|
| `LP → DGD` 與 `LP → LPMud_2.4.5` 並列 | ✅ 與第十篇 §064 一致（DGD 是獨立實作而非 LPMud 分支） |
| `LPMud_3.0 → CD` | ✅ 與第十篇 §063、§069 一致（CD 從 3.0 分出，1991） |
| `LPMud_3.1.2 → 3.2 (Amylaar)` | ✅ Amylaar 線即後來的 LDMud（第十一篇 §066–§067） |
| `LPMud_3.1.2-DR → MudOS` | ✅ 與第八篇 §034 的改名風波背景一致 |
| **Merc Diku** 一支 | ★ 佐證 I.2.2：Annihilator 的第一個 MUD（Legend of Winds）正是 Merc Diku |

> 💡 這張樹最有價值的地方是**它把 Diku 線畫得跟 LP 線一樣詳細**。
> 本書因為主題是 LPMud 家族，Diku 那一支幾乎沒有著墨；
> 而 I.2 的訪談提醒我們：**中文 MUD 的第一批開發者是先玩 Diku 再轉 LP 的。**
> 兩條線在中文線的起點交會過一次。

---

## I.8　Bartle 的玩家分類

原文 *Hearts, Clubs, Diamonds, Spades: Players Who Suit MUDs*（1996），中譯〈牌上的花色〉。

| 類型 | 花色 | 主要動機 |
|------|------|---------|
| **成就型** Achievers | ♦ 方片 | 累積點數與升級是主要目標（「總是在尋找財寶」） |
| **探索型** Explorers | ♠ 黑桃 | 揭露隱蔽機制，嘗試深奧操作（「掘地三尺尋找線索」） |
| **社交型** Socialisers | ♥ 紅桃 | 樂趣源自與人相處；遊戲只是背景（「樂於助人」） |
| **殺手型** Killers | ♣ 梅花 | 將自身能力施加於他人 |

**它解釋了本書多處觀察的成因：**

| 本書的觀察 | 玩家分類給出的解釋 |
|-----------|------------------|
| 伺服器把 `ESC007`+`ESC009`（詳情＋動作列）用了近 1,000 次（附錄 H.3.2） | **成就型**要的是「看得到數值、按得到按鈕」——使用量最大的介面 |
| `cmds:` 連結被用了 302 次，把任意詞句變成可點指令（H.5.3） | 降低**探索型**的操作成本：看到什麼就能點什麼 |
| `ESC100` 聊天要同時寫進即時列與歷史頁（H.11.5） | **社交型**要的是「不能漏訊息」——這就是為什麼一送要兩寫 |
| 中文 mudlib 普遍有 `/kungfu` 而非通用戰鬥模組（§051） | **殺手型**與成就型在武俠題材下的交集：門派與武功本身就是成就階梯 |

> 💡 這張表與 I.2.5 可以連起來讀。
> Annihilator 說他要拿掉 Guild／Level／Experience、改用精氣神——
> 那是**針對成就型玩家的制度改造**。
> 而本書在協議層量到的使用量分布，顯示成就型介面仍然佔絕對多數。
> **協議的形狀，是玩家動機的化石。**

---

## I.9　中文線的補充事實

| 年份 | 事件 | 等級 |
|------|------|------|
| 1979 | Essex 大學 DEC system-10 上執行 Bartle 的 MUD | 🟠（見 I.1.3 的三年份辨析） |
| 1990 年代初 | 中文 MUD 由**在美華人學生**先架設於學校主機 | 🟠 補上附錄 D.3「1993 東方故事開站」的前史 |
| **1995-03-14** | **《東方故事2》Admin「Annihilator」寫下中文 MUD 史上第一份系統性的管理者指南** | 🟠 ★ 與 I.2／I.3 同一人，證據鏈完整 |
| 1996 | 文字 MUD 吸引中國第一代網民；聯眾創辦人之一簡晶即為 MUD 玩家 | 🟠 說明 MUD 是中國早期網路遊戲的共同前身 |
| 1999-04 | 第一款簡易圖形文字 MUD《笑傲江湖之精忠報國》 | 🟠 |
| 1999-07 | UO 民間模擬伺服器出現，600 MB 客戶端 | 🟠 文字 MUD 時代結束的外部壓力 |

> ⚠️ **本節全部標 🟠。**
> 中文 MUD 史的網路文獻彼此轉載嚴重、年份常有出入，多數沒有列出處。
> 本節只收錄**能與本書封存樣本或既有 🟢/🟡 證據相互印證**的項目；無法印證的一律不收。

---

## I.10　尚未處理的一手材料

以下已確認存在、且**對本書有明確價值**，但本次未展開。列出來是為了讓後續接手的人知道還有什麼可挖：

| 材料 | 為什麼值得做 |
|------|------------|
| ~~**`ES2_lib` 七個版本封存**~~ | ✅ **已完成，見 I.5**（本清單第一項已結清） |
| **授權第三條 (2) 的遵守率**（I.5.7） | ES2 要求下游在登入畫面顯示「ES2 mudlib 名稱及版本」。**封存的 113 份裡有多少真的照做？** 這會是一次「法律層指紋 vs 技術層指紋」的直接比對 |
| **同名檔案的內容 diff**（而非只比檔名） | I.5 只量了檔名集合。上游 `fluffos/mudlibs` 的方法是 diff `master.c`／`chinese.c`／`logind.c`——把它套到 ES2 七版上，可得到比 Jaccard 更細的演化軌跡 |
| `es-master` / `es2-master` / `es2-master-GBK` / `ES2-big5-master` / `ES2-utf8-master` | 同一份 lib 的字集分支，可量測中文化在檔案層留下的痕跡 |
| `fy2` / `fy3` / `fy4` / `jy` / `nitan3` / `mhxy2002` 等原始碼 | 擴大 I.4 的樣本並驗證兩骨架比例是否穩定 |
| MudOS v22.2pre11、FluffOS 2017/2019、LDMud、DGD、Lima、Dead Souls、Discworld 封存 | 第十一篇 §065 五支對照的原始碼佐證 |
| `LPC函數手冊`、`LPC詳細教程`、`巫師入門教程`、`cdoc-v22`、`wiztxt` | 中文 LPC 教學文件，可對照附錄 E 的 FluffOS 速查 |

> ⚠️ 這一節是**誠實的欠帳清單**，不是「未來展望」。
> 這些材料現在就在手上，只是本次沒有做。
> 本書第五篇講過「交付了不能用的東西」七次——其中一種形式就是**假裝沒看到還沒做的部分**。

---

## I.11　書目

### I.11.1　採用

| 文獻 | 性質 | 用在 |
|------|------|------|
| Duncan Howard, **An Introduction to MUD**, Century Communications, 1985, ISBN 0 7126 0691 2（序：Richard Bartle） | 🟡 一手 | I.1、附錄 D 1978 列的出處 |
| **Annihilator（Minxin Zhang）訪談**（約 1995–96；2007-06-20 轉載於部落格「喵与地下藤」） | 🟡 **一手** | ★ **I.2** |
| Minxin Zhang, **ES2 mudlib 規格文件 (2.1.4 Draft)**, © 2002 | 🟡 一手 | I.3、I.4 |
| 〈一起從頭打造 mudlib〉（中文 MUD 社群技術長文） | 🟠 社群 | I.6 |
| `Mud發展史.txt`（MUD 家族樹） | 🟠 社群 | I.7 |
| Richard Bartle, **Hearts, Clubs, Diamonds, Spades: Players Who Suit MUDs**, 1996（中譯〈牌上的花色〉） | 🟡 | I.8 |
| 中文 MUD 與中國早期網路遊戲史料 | 🟠 二手 | I.9 |
| 姊妹倉庫 `libs/*/mudlib.json`（113 份封存索引） | 🟢 **可重跑** | **I.4 的量測** |

### I.11.2　列名但未引用

| 文獻 | 未引用的原因 |
|------|------------|
| Richard Bartle, **Designing Virtual Worlds** | 商業出版品；與本書協議／引擎主軸只在 I.8 交會，該處已直接引 1996 原論文 |
| Ron Penton, **MUD Game Programming**, Premier Press | 商業出版品；示範 C++ 從零寫 MUD，與 LPMud 的 driver／mudlib 分家屬不同路線 |
| *From MUDs to MMORPGs: The History of Virtual Worlds* | 綜述論文，史實均可回溯至已採用的一手來源 |
| 《Cybertext》讀書筆記系列（七篇） | 文學理論取向 |
| *Language Learning and MUDs*、*International Handbook of Internet Research* | 社會科學取向 |
| 通用遊戲程式設計、遊戲 AI、C++ 教材（逾 2,900 檔） | 與本書無關，見 I.0.1 |

---

## I.12　本附錄改變了本書什麼

誠實結算，一共九項：

| # | 變更 | 位置 |
|---|------|------|
| 1 | **附錄 D 的 1978 列補上一手出處**，並註明「1978 為起算年、核心完成與交棒約在 1980」 | 附錄 D |
| 2 | ES2 從「檔名 + DNS 的間接推斷」升級為**有當事人訪談、有規格文件、有作者、有日期的證據鏈** | I.2、I.3 |
| 3 | §035「相容性的價錢」取得**當事人版本**：新版 driver 的相容性斷裂是他重寫 mudlib 的四個理由之一 | I.2.4 |
| 4 | §050 的「兩族」從**相似度推論**升級為**對照外部規格的可重跑量測**，並指出分界就是骨架被換掉 | I.4 |
| 5 | §051「特徵組合而非繼承樹」由歸納取得**明文規格佐證**（`/feature` 是標準目錄） | I.3.2 |
| 6 | §052 的屬性樹追到**設計動機**：作者明言要用「精、氣、神」取代 Hit Points | I.2.5 |
| 7 | ★ **§050 的 Jaccard 首次有了校準基線**：同一套 lib 跨版本落在 **0.333–0.998** | I.5.4 |
| 8 | ★ **第十四篇的「骨架比內容保守」得到縱向實證**：ES2 的十二目錄骨架在 **1995 年 8 月**定型，此後五年一格未動，2002 年的規格只是把它寫下來 | I.5.3 |
| 9 | **ES2 的開發起點前推到 1994-11**（附錄 D 原記 1995），有檔案 mtime 與作者自標的 `Copyright (C) 1994-2000` 兩項佐證 | I.5.1 |

### ★ 唯一一條被修正的

第 7 項不只是補強，它**修正了一項理解**：

> §050 用 Jaccard 0.44–0.90 描述族內分散，並隱含「這個分散代表各站台自行演化」。
> I.5.4 顯示**同一套 lib 的七個版本就橫跨 0.333–0.998**，完全覆蓋那個區間——
> 因此**族內的 0.44–0.90 無法區分「不同站台」與「同一份 lib 的不同版本」。**

§050 的**主結論不受影響**（0.90 vs 0.012 的斷層是真的，而且 0.012 比「同 lib 相隔五年」還低 27 倍），
**但族內數字的解讀必須放寬。** 這一條已寫進 I.5.4。

---

除第 7 項外，**沒有其他書中斷言被推翻。**
這既是好消息也值得警惕——外部資料幾乎只證實不推翻時，
要先懷疑是不是挑資料時只挑了合意的。
本附錄的防線是三條：
I.9 的收錄規則（**無法互相印證的一律不收**）、
I.10 的欠帳清單（**還沒做的要寫出來**）、
以及 I.5.8 那個揭露（**自己的量測工具出錯，也要寫出來**）。

---
